name: Temporary PR394 reconcile

on:
  push:
    branches:
      - design/ux-f4-validation-setup

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: design/ux-f4-validation-setup
          fetch-depth: 0
      - name: Reconcile main and review fixes
        run: python3 .github/temp-pr394-reconcile.py
