# Open Issues

> **Created:** July 31, 2026
> **Purpose:** Live blockers, partially-landed work, and decisions awaiting an owner.
> Resolved entries are archived in `open-issues-resolved.md`.
> Split out of `CLAUDE.md` on July 31, 2026. Content was **verbatim** at the split — entries were moved, never edited, reordered, or deduplicated.
> **Amended August 17, 2026:** a live entry MAY be edited in place to record an owner decision on it — a dated `✅ OWNER DECISION` block appended, and a title amended to lead with what is now open. The body is otherwise left as published, and superseded text is annotated rather than deleted. Seven entries were edited that way on August 17, 2026.

**Keep this section current.** When resolving an issue, move its entry to
`open-issues-resolved.md` in the same commit. Each entry carries a "since" date so
staleness is visible.

**Classification note.** Entries were filed by whether their title announces a
terminal status. The bias is deliberately conservative: an entry that is ambiguous
stays here, because a stale active entry is visible and gets re-filed, whereas one
wrongly archived is forgotten. Several entries below are partially landed with a
genuine open remainder — those belong here. A few are near-duplicates retained for
history by the original convention; they were not merged.

---

- **DisciplineConfig restructure — DEFERRED by owner (Fable escalation) ruling, August 16, 2026, from the #44 adversarial-review rounds 8-9 M2 finding.** Scope, verbatim: DisciplineConfig readonly-struct validated at construction, injected into DisciplineRules; retires CommitWithExplicitConfig, both RequireCommittableConfig forms, the driver pre-flight call and the internal guard seams; requires the #44 §2.3 F6 and #30 §3.4 ERR-030-039-block spec edits; execute at (or after) the GameplayConfigHolder.Bind composition-root pass, as the tree-wide pattern, not a #44 one-off. Landed instead, minimally, at round 8: `DisciplineConfigCompletenessTests.cs` reflects every public static readonly `[GT]` field of `DisciplineConstants.cs` (so a future non-int constant fails until classified), the four pre-flight guards were verified isolating by deletion, and five pointer comments name the call sites the restructure would retire. Owner-decision-pending; no spec or code change beyond the minimal fix until the `GameplayConfigHolder.Bind` pass is scheduled. **✅ OWNER DECISION, August 17, 2026: DEFER, confirmed — execute at the `GameplayConfigHolder.Bind` composition-root pass, tree-wide, and never as a #44 one-off.** Three reasons recorded so this is not re-litigated at the next #44 pass: the defect is **future-conditional** (it bites when a non-`int` `[GT]` is added to `DisciplineConstants`, which nothing schedules); the **tripwire already catches it loudly** — `DisciplineConfigCompletenessTests` reflects every public static readonly `[GT]` field and fails until a new one is classified, so the hazard is a red test, not silent behaviour; and restructuring #44 alone would create exactly the **parallel-surface asymmetry** this repo has filed repeatedly — one subsystem taking a validated-readonly-struct config while every sibling keeps the guard-and-pre-flight shape, which is a second surface to keep in agreement by convention. **The release condition, stated checkably (added August 17, 2026 after review):** "the `GameplayConfigHolder.Bind` composition-root pass" is **not itself a tracked item** — it appears on no roadmap, in no backlog, and in no open-issues entry of its own; `GameplayConfigHolder.Bind()` exists as an API, but "the Bind pass" is an unscheduled intention. So this hold does NOT name a measured event the way the W2-gated holds do, and it should not be counted among them. What IS checkable: **this hold releases when `DisciplineConfigCompletenessTests` goes red**, i.e. when a non-`int` `[GT]` is added to `DisciplineConstants` — the tripwire that converts the future-conditional defect from silent to loud. Whoever schedules the `Bind` pass should file it as its own item then and point this entry at it. Status stays: no spec or code change beyond the round-8 minimal fix until then. The five pointer comments naming the call sites the restructure would retire stay as the hand-off.

- **Depleted-squad cover — ERR-044-003 STAGE 1 LANDED August 15, 2026 (an extremis appearance no longer serves the ban it was fielded through). WHAT REMAINS: stages 2 and 3, both blocked — youth call-ups need #42 to have a `src/` assembly, and generated "greyed-out" cover needs the packed `PlayerId` space widened.** *Opened August 15, 2026 (owner decision, replacing the "recorded, not fixed" half of `ERR-044-003`).* The #44 C1/C2 landing recorded, twice and explicitly as an owner call, that #30 §2.3 F9's depleted-squad back-fill can press a **suspended** player onto the pitch when a club would otherwise be unable to field a formation, and that `OnClubFixturePlayed` then decremented that same player's ban for the fixture he had just played — so the appearance was **strictly free**, and a two-match red cost a mass-suspension club nothing at all. **Stage 1 is the decision on that half: exempt the appearance.** `DisciplineRules.OnClubFixturePlayed` takes a required `int[] fieldedPlayerIds` and skips anyone in it, because the rule is not "the club played" but "the club played **without** him". Fixed at the *serving* site rather than in `AvailabilityComposition`, since the football rule is about who PLAYED — so the reinstatement tier order is untouched; the eleven is the array `ERR-041-010(b)`'s appearance record already derived at the filter+configure site, and `SeasonLoop.FieldedXi` widens that derivation's gate from `_career` to the union of its two consumers. **Behaviour-identical on every fixture that does not reach the extremis tier, by construction** — the filter removes every suspended player before selection, so nothing but the back-fill can put a banned id in the eleven. **The open remainder is the better answer, agreed and not built.** The Football Manager posture is a tier ladder: when a club cannot field a side, promote **youth** players, and below them field **generated low-attribute cover** — under which a banned man never takes the field at all and the suspended reinstatement tier becomes *unreachable* rather than merely expensive, letting #30's liveness invariant and the Laws of the Game both hold instead of trading one against the other. Neither tier is buildable today. **Stage 2 (youth) is blocked on #42 Youth having no `src/` assembly** — it is one of the 19 APPROVED-but-unimplemented specs, so there is nothing to draw from. **Stage 3 (generated cover) is blocked on the id space**: `PlayerId = clubId × CLUB_SQUAD_SIZE + local` with `CLUB_SQUAD_SIZE = 25` is fully packed, so a 26th player for club N collides with club N+1's first — widening it touches #27 FR-SQ-010 (as amended by ERR-027-004), every save file, and the ERR-041-019 global-uniqueness guard, which makes it a design pass with an owner decision in it rather than a follow-up commit. Until both land, stage 1 is the whole answer: a banned player can still be fielded in extremis, but it now costs the ban it was fielded through. Full account: `spec-error-log.md` v2.25, `ERR-044-003`. **✅ OWNER DECISION, August 17, 2026, on both open sub-questions — landed in the owning spec (#30 §3.4 v2.12), not only here.** **(a) The trigger stays the eighteen-player selection walk; the short-bench posture is NOT built** — the honest fix for a depleted club is cover, not a shorter bench, so stages 2–3 of this very entry retire the question rather than answer it, and a short-bench mechanism built now would be thrown away when the ladder lands; it is also engine surgery (`ConfigureSquads` and every `LineupPlan` consumer assume a full bench), and narrowing the probe without narrowing selection reproduces the divergence `LineupSelector.TrySelect` was collapsed to one walk to prevent. `ERR-030-044`'s open half is CLOSED. **(b) The beyond-cap branch keeps degrading to greedy; refusing the fixture is REJECTED** — refusal breaks the twice-affirmed "the composed filter can never leave a club worse off than having no filter at all" invariant in order to buy a corner (more than twelve *concurrently suspended* players in one squad) that is unreachable at measured card rates. Both decisions are recorded inline at the rules they govern so a reader meets them where the question was, and both name what would reopen them.
- **#44 Discipline & Suspensions — C1 (T0+T1) and C2 (T2) LANDED August 13, 2026: `src/discipline/` is the 35th production assembly, suspensions are LIVE end to end, and the last gap in the season spine for PM-2 is closed. WHAT REMAINS: #44 T3 — the #30-owned quick-sim card synthesis, without which suspensions are live for ONE club in twenty.** — *opened and landed August 13, 2026 (branch `claude/discipline-c1-c2-1vmq4g`).* The subsystem is a third consumer of two proven seams rather than new plumbing, exactly as the roadmap predicted: the #37-class per-tick ledger tap (B3) feeds a `CardLedgerFold`, and the ERR-030-009 resolve→filter→configure seam takes a third contributor. **Draw-free by construction** — no RNG stream, no domain tag, no `SubsystemOrdinals` entry, no `SNAPSHOT_SCHEMA_VERSION` bump (still 21). `SEASON_SAVE_FORMAT_VERSION` **5 → 6** for the seventh mandatory sub-blob. **The pre-implementation council changed the design on two of five forks, and the authority it cited was one neither the roadmap nor #44 had named: `season-competition-loop/section-3.md` §3.4 (APPROVED, and LATER than #44), which overrides #44's own text twice.** **(1) FR-DC-010 scoped the filter to "the engine-resolved fixture"** while FR-DC-011 one row below serves bans on both paths — a self-contradiction whose narrower half would have made a ban cost nothing, since nearly every fixture of a career is quick-simmed (`ERR-044-002`). **(2) #44 §2.3 F5 required the filter to fail loud below eighteen players**, while #30 §2.3 F9 settles the same event by back-filling and states outright that "#44/#36 contribute removals only and inherit the rule unchanged" (`ERR-044-003`). The second is the landing's real structural cost: `PlayerCareerStates.SelectAvailable` filtered AND back-filled in one method, which **no second contributor can compose with** — chained after, the back-fill runs before #44's removals are known; chained before, #41's back-fill presses a suspended player onto the pitch without knowing he is suspended. Split into `PlayerCareerStates.MarkUnavailable` (a removal SET) + the new `AvailabilityComposition` (one intersection, one back-fill), which makes #30 §3.4's "order-independent BECAUSE both are removals" property structural rather than conventional. **RECORDED, NOT FIXED — an owner decision:** preserving #30's stated invariant ("the composed filter can never leave a club worse off than having no filter at all") means a suspended player IS reinstatable in extremis, which the Laws do not allow; implemented as a **stricter reinstatement tier** (every injured player is pressed back before any suspended one, and — per `ERR-044-019`/`ERR-030-045`/`ERR-030-046` — a reinstated suspended player is benched whenever any completing choice permits it — the extremis tier runs an exhaustive clean-completion search (capped at 12 candidates) whose composed XI carries the minimum achievable number of reinstated-suspended starters — and he starts only in a probe-verified forced start, where every completing choice within the search bound starts at least as many; only then does the stage-1 exemption stall his ban. Residual: forced starts, and the beyond-cap corner (>12 concurrent suspended candidates, unreachable at measured card rates), where the choice degrades to greedy with no minimality claim), with #44 §7.2's deferral queue as the designed alternative if the owner would rather refuse the fixture. **`ERR-044-001` is the fourth instance of a defect this project has now filed four times**: Appendix B specified the sub-blob version-first with no magic, and at seven identically-shaped `byte[]` payloads in one `Encode` signature a transposition decodes silently as the wrong subsystem — `DISCIPLINE_SAVE_MAGIC` ('DISC') plus the typed `DisciplineBlock`. Its second half is worth more: **#44 §7.1 T2's own verification obligation ("the `Incoming`-id semantics verified against the live engine") FAILED.** Appendix C's worked example puts a bench player at "slot 19" and hedges "(absorbed either way)"; `SQUAD_SIZE = 22`, so 19 is an ON-PITCH slot, and `SubstitutePlayer` derives `Incoming` as the synthetic `SQUAD_SIZE + teamId × SUBSTITUTES_PER_TEAM + benchIndex` ∈ [22, 36). Implemented as written, every post-substitution card would have been misattributed and F1 would never have fired — the spec's hedge was the spec declining to verify a fact. **The occupancy seed comes OUT of the engine** (`MatchEngine.PlayerIdsByAgentId`, written one statement from the `_canonicalAttrs` assignment at all four sites that move a player) rather than being re-derived by a second `LineupSelector` walk in `season-save` — the parallel-surface trap `SquadRating` exists to prevent and that #29/#41's T2 AR pass 2 filed. Boot-constant snapshot exclusion, `_canonicalAttrs`'s own proof unchanged. **Tests: `Discipline.Tests` 81/81 (⚠️ CORRECTED August 16, 2026: this landing-time count was never advanced in this file — re-measured today by direct execution at **143/143, 0 failed**, over AR rounds 8-9; itself now superseded — re-measured August 16, 2026, later, by direct execution at **146/146, 0 failed**, over AR rounds 10-12; see the new head bullet and `docs/tracking/CHANGELOG.md`) with four mutants killed** (residual-vs-reset, the FR-DC-017 drop, the magic check, the substitution swap — and the suite's author caught his own first residual test being tautological, because `AddYellow` only ever advances by one so a crossing reached by repeated single cards cannot distinguish `-= threshold` from `= 0`); **`SeasonLoopDisciplineTests` +14 wiring locks**, every one failing if its wiring point is reverted, including a real 90-minute engine fixture whose observer-neutrality assertion is PAIRED with a positive control — #44 §5's T-DC-NEU-001 as written is green if the fold never runs at all, which is ERR-030-014's shape one layer up. **THE THING TO KNOW BEFORE READING A LEAGUE TABLE:** in `ManagedThroughEngine` only the managed fixture runs the engine, and only engine fixtures generate cards, so at the August-13 measured rate the managed club accrues ~95 yellows and ~19 reds a season while every rival accrues ~5 and ~1. **#44 does not make suspensions live; it makes them live for one club in twenty, and that club is the human's.** #44 §1.1 concedes the generation asymmetry but not its competitive consequence; T3's #30-owned quick-sim card synthesis is the named answer, and until it lands a suspension count is a fact about the resolution mode, not about the football. Full account: `docs/tracking/spec-error-log.md` v2.17. **GATE (August 16, 2026, 051c25a, whole tree): meta-integrity OK; build 0 errors; 34 suites, quarantine empty; MatchEngine.Tests 461/1/11 — the one failure is the inherited owner-held-red sim_match_engine_close_chance, failing at the pre-branch baseline too, so the branch adds no new failure; Discipline.Tests 143/143; SeasonSave.Tests 441/0/3; MatchAnalytics.Tests 58/58.** **Adversarial-review ROUNDS 10-12 landed August 16, 2026, later** (round 10 `a4a672b` M/L only; round 11 `21bde36` 1 High — `ERR-030-046`, the tier-2 reinstatement rule escalated to a capped exhaustive clean-completion search, guarantee restated as a theorem; round 12 `d0f534a` 0 High / 1 Medium / 5 Low, all fixed, the search's monotonicity lemma stated and proven and the `ERR-044-022` ordering claim genuinely locked via a new `CardLedgerFold.OccupancyAt` seam) — **GATE (August 16, 2026, `d0f534a`, whole tree): meta-integrity OK; build 0 errors; 34 suites, quarantine empty; MatchEngine.Tests 461/1/11 (no new failure); Discipline.Tests 146/146; SeasonSave.Tests 447/0/3; MatchAnalytics.Tests 59/59.** See `docs/tracking/CHANGELOG.md`, `docs/tracking/spec-error-log.md` v2.42.

- **A4a round-resolution calibration — the corpus is captured, the three `[GT]`s are fitted, and the MEAN bar now PASSES after `ERR-030-033` was re-specified (August 12, 2026). What remains open is `ERR-030-034`: the model's distribution family** — *opened August 12, 2026 (the A4a run).* **✅ OWNER DECISION, August 17, 2026: HOLD — neither adopt nor reject; decide after the W2 calibration capture, exactly as KD-7a's own tripwire (S7) says.** Recorded at the design supplement as **S9** (`league-bootstrap-design.md` v1.5). The corpus that would determine `α` predates tackle wiring, so S7's condition 4 fails on its own terms, and conditions 1–3 would in any case be re-tested against a corpus KD-8's re-capture trigger replaces. **S7's condition 4 was also corrected in place**: it was written on August 12 as "no player has ever made a tackle", which W2's landing that same day made false about the *code* while leaving it true about a *shipped match* (`TackleContactRadiusM = 0`); it now reads as **post-arming**, or the tripwire would fire against a corpus statistically identical to the one it exists to reject.
  The run itself is complete and its outputs are committed: KD-8 **Step 0 PASSED** on the current tree
  (strong-at-home mean margin **+4.000**, strong-away **−3.500**, upsets present), the corpus is **198
  real 90-minute `MatchEngine` matches** over 11 `dSquad` buckets at 18 each (~90 s/match, four
  processes, ~1.4 h — inside C1a's ~9 h budget), and the least-squares fit moved
  **`QuickSimBaseGoals` 1.35 → 1.2325, `QuickSimGoalRatingSlope` 0.35 → 0.2162,
  `QuickSimHomeAdvantageRating` 0.30 → 0.4996**. Those three constants had carried a
  "provisional, not fitted" warning at their own declaration since #30 T2; they no longer do.
  `RoundResolutionFitLockTests` pins them, the per-bucket table over a fixed sweep, grid-wide
  monotonicity, and home advantage isolated at `dSquad = 0`.

  **What is open is the acceptance verdict, and it is open in two independent ways.**

  **(1) `ERR-030-033` — KD-8's ±0.25 per-bucket bar cannot be met at the depth KD-8 itself specifies.**
  A bucket's mean is an estimate, and at ~18 matches its standard error runs 0.135–0.633; **15 of the
  22 bucket-sides have a standard error larger than the entire bar**. A perfectly correct model
  re-scored against a re-run of this same corpus would fail it too, so the bar is not currently a
  statement about the model at all. Reaching ±0.25 as a *resolvable* bar needs n ≈ 770/bucket — about
  210 h of engine time against a budgeted ~9 h. **✅ RESOLVED August 12, 2026 — the bar was re-specified
  against the corpus's measured precision, and the same fit now reads mean-agreement PASS.** Deliberately
  NOT closed by widening the tolerance to whatever this run achieved — that stops it being a bar, and
  there is a standing owner ruling against exactly that move (`close-chance-creation-design.md` §10.9
  item 6, August 11, 2026). KD-8 now carries **A1** a per-cell `|Δ| ≤ max(0.25, 2·se)` screen with
  **±0.25 retained as a FLOOR**, so a corpus deep enough that `2·se < 0.25` automatically restores the
  original requirement rather than abandoning it; **A2** at most `1 + round(0.0455·cells)` exceedances,
  none over `max(0.40, 3·se)`; **A3** a pooled `χ² ≤ χ²₀.₉₅(cells − 3)`, which is where the statistical
  power actually lives, since A1/A2 are per-cell screens blind to systematic misfit every individual
  cell passes; **A4** an 18/bucket scoreability floor, without which the se-relative form is gameable by
  shrinking n; and **A5** the unchanged ±5 pp W/D/L bar plus a pinned n ≥ 250 for a resolvable *pass*.
  Measured: worst |z| = 2.06, one exceedance of an allowed two, pooled **χ² = 16.0 on 19 dof** against a
  30.1 threshold. **The verdict is now reported in two parts — mean agreement PASS, distribution shape
  FAIL** — because they fail for unrelated reasons, and the single flat verdict had the practical effect
  of making ERR-030-034 read as a fit failure.

  **(2) `ERR-030-034` — the model's distribution family cannot express the engine's spread.** KD-7
  resolves a fixture as two Poisson draws, and Poisson fixes variance = mean. The engine is
  **over-dispersed**: mean `var/mean` **1.395** across 22 bucket-sides, 19 of 22 above 1, pooled
  `chi2 = 521.7` on 374 dof, **z = +5.40**. The visible consequence is draws: at `dSquad ≈ 0` the
  corpus draws **19.2%** against the fitted model's **26.8%** — the whole of the 7.6 pp W/D/L miss **⚠️ CORRECTED August 12, 2026 (Fable advisory review, independently reproduced): the causal sentence above is WRONG and would misdirect the fix.** Marginal over-dispersion fattens BOTH tails — more blowouts *and more 0–0s, which are draws* — so it barely moves the draw share. Computed at the fitted bucket-0 lambdas: independent negative-binomial at the measured dispersion gives **26.3%** draws against Poisson's 26.8%, i.e. it closes ~0.5 pp of the 7.6 pp gap. **Dispersion and the draw deficit are substantially INDEPENDENT findings.** The only mixed-Poisson mechanism that cuts draws materially is a shared antithetic swing, and it necessarily implies negative home/away correlation, which this corpus refutes: pooled within-bucket correlation is **+0.004 ± 0.052** (n=378), ~4σ from the ≈ −0.20 such a family predicts. So the draw deficit's mechanism is NOT established and is not expressible by any mixed-Poisson consistent with the measured correlation. Over-dispersion is separately confirmed real and NOT a pooling artifact (within-bucket `dSquad` spread contributes ≤ 0.005 of the ~0.4 excess), and is better specified as `var = μ(1+αμ)`, α ≈ 0.15–0.25, than as a constant 1.395 ratio., measured at n = 198.
  No value of three mean-shaping parameters closes a second-moment gap. **This is the surviving half of
  roadmap risk row 1** — a quick-sim league table will show systematically more draws and fewer decisive
  results than the same fixtures played through the engine. **Owner decision: whether to change the
  family** (negative-binomial / Poisson-gamma, or a draw-inflated bivariate form — each keeps the keyed
  one-uniform-per-side discipline but needs its quantile pinned by name for KD-7's own stated reason,
  and the scoreline is persisted through `LeagueTable`/`SeasonStateCodec`, so this moves save state).
  **The corpus is committed, so a re-fit against a new family costs seconds, not the run.** **The successor is now PRE-DECIDED and gated (August 12, 2026): `league-bootstrap-design.md` **KD-7a**** — NB2 (`var = μ(1+αμ)`), `NegativeBinomialInverseCdf` pinned by name and by algorithm, one uniform per side with the existing sub-streams unchanged, and a `[GT] QuickSimDispersion` whose zero case routes to today's `PoissonInverseCdf` verbatim so `α = 0` is bit-identical rather than identical-in-the-limit. **It is deliberately NOT adopted**, on four measured gates: **α is not determined by this corpus** (0.0773 weighted vs 0.1552 unweighted, one 18-sample cell carrying 36% of the weighted fit); **NB2 does not fix the draw deficit** (26.5% vs Poisson's 26.8%, engine 19.2% — ~0.3 pp of a 7.6 pp gap); the draw deficit's own mechanism is unestablished (the shared-swing family that would cut draws implies negative home/away correlation, and the corpus measures **+0.044 ± 0.073**); and the capture predates the defensive wiring (**no player has ever made a tackle**) that moves the second moment. The tripwire is stated so the next capture decides rather than re-litigates.

  **Also recorded, not a blocker: the corpus's grid-weighted goal rate is 3.09/match.** **⚠️ CORRECTED August 12, 2026 by the goal-rate match-realism pass:** 3.09 is the **grid-weighted** mean, and the grid samples `dSquad` −5…+5 uniformly while a real season clusters near 0 and mismatches score more. Re-measured: **balanced fixtures (`dSquad ≈ 0`, n=198) give 2.70 ± 0.13 vs football's ~2.7 — 0.02σ**; league-weighted 2.93 ± 0.15 (+1.47σ, not significant). **The engine did NOT overshoot football's rate; no defect, no `[GT]` moved.** The error was reading a calibration grid as a league; the fitter now emits all three figures so it cannot recur. It remains a re-capture trigger for this corpus by KD-8's own rule the moment the engine's scoring moves again.

- **Football-judgment proxy review — 32 itemized findings still open across 24 specs; the §6 doctrine governs every fix; ERR-008-020 (template), ERR-008-019 (the founding long-shot cliff) and ERR-008-021 (the shot-lane follow-up) LANDED** — *opened August 4, 2026 (review + doctrine + template landing all same day); ERR-008-019 and ERR-008-021 landed August 5, 2026.*
  **ERR-008-021 LANDED August 5, 2026 — the §6.4 shot-lane deferral, discharged.** The geometry the
  template fix deliberately left behind turned out to carry **both** of the pass lane's defects, and
  the containment one is the more damaging. #8 §3.2.3.2 step 4 counted an opponent's occlusion only
  when his angular *centre* lay inside the goal arc, and then counted his **entire** angular width:
  a defender whose centre sat a hair the wrong side of the post direction contributed **exactly
  nothing** — the shooter read a **fully open goal** with a man standing squarely across his near
  post — while one a centimetre the other side contributed a full width, half of it behind the post
  and blocking nothing. On the fixture the suite now uses (shooter 15 m out on the centre line, one
  blocker 5 m in front), **4 cm of lateral defender position stepped `GoalOpeningScore` from 0.595
  to 1.000**. That score prices the SHOOT candidate (§3.2.3.1), *gates its existence* (§3.1.4.1) and
  drives `PowerIntent` (§3.5.3), so the discontinuity reached shot selection, shot value and shot
  speed alike. Second defect, §2's pattern (a) transposed to the goal: the blocking width was
  `2·atan(radius/distance)` — body radius alone — so a defender who neither reads the shot nor gets
  his body into its line shut the goal off exactly as hard as one who does. **Fixed** to the true
  angular **overlap** of the blocking disc with the goal arc, which unlike -019 and -020 needed **no
  ramp constant, no half-width `[GT]` and no tolerance epsilon** — an interval intersection is
  continuous by construction (P1) *and* is the geometrically honest answer, so the over-blocking and
  the under-blocking fall out with the cliff; the 0.01° epsilon the containment test required is
  deleted. Overlap × the blocker's **Anticipation + Positioning** ability
  (`SHOT_BLOCKER_ABILITY_MIN/MAX` = 0.6/1.4 `[GT]`, league-average exactly 1.0) read through the
  **shooter's Vision** as discrimination fidelity (P2), reusing `LANE_VISION_FIDELITY_FLOOR` rather
  than declaring a second — fidelity belongs to the assessor, so a duplicate would be a parallel
  surface, not a parameter. **The goalkeeper is exempt from the ability term** and occludes on
  geometry alone (P3): #11 §3.5's save model and §3.7.0's rush — which *sets* the geometry this
  function measures — own his shot-stopping, so pricing it here too would charge the shooter twice
  for one keeper; locked by a test that moves the keeper between the attribute extremes and asserts
  the score does not move. **P5 pivot:** over a uniformly-placed blocker the old rule
  integrates a rectangle of area `4h·halfArc` and the overlap a trapezoid of the same area, so the
  fix redistributes occlusion from a step to a slope and from anonymous bodies to identified ones
  without opening or closing the goal on average. *(This originally read "for every disc width and
  arc" — **corrected at ERR-008-022**: it is false above `h = halfArc`, where the old model's
  per-opponent clamp saturates and the trapezoid does not, up to **2×**; that regime is any blocker
  within ~2.7 m of a 20 m shooter. It was the stated reason no recalibration was needed; the reason
  is withdrawn and the residual left for the balance pass.)* No
  schema / RNG / domain-tag / draw-site / draw-order change. **Digest invariance is NOT claimed and
  is false** — the -019 lesson applied while authoring rather than at review: this model is live on
  every SHOOT candidate the generator produces and moves for any blocker who is not both exactly
  average and wholly inside the arc, so a tick-window or rate-band instrument that trips at the
  first gate run should be checked against this before being read as a regression. 9
  `OptionGeneratorTests` locks; a reference implementation of both models, run over all ten locks, confirms that **5 of the 9 evaluable against the old model FAIL on it** — continuity (step 0.405 against the asserted < 0.05), the straddling blocker (1.000, not < 1.0), home discrimination, the low-Vision separation (pre-fix gap exactly zero) and the away mirror; the two P5 pivot rows and null-view neutrality pass pre-fix by construction, and the MIN/MAX-midpoint invariant cannot be evaluated pre-fix because the constants are new. **Gate NOT run — no .NET SDK in the authoring environment.** **Recorded, not fixed:**
  (a) `IsInShotPath`'s corridor end-bounds — **FIXED at ERR-008-022 (August 6, 2026), and they were
  the larger defect in this section.** The deferral reasoning above was wrong on its own terms: the
  far bound was not a judgment collapsed into a threshold, it was **the wrong plane**. `proj <
  distToGoal` describes a plane through the goal *centre*, which for any off-centre shooter cuts
  diagonally across the goal mouth — it discarded the **far-post** blocker on **20,213 of 20,213**
  sampled in-range off-centre shooters and dropped a keeper on his line at goal centre for *every*
  shooter position (shooter (95,20) read a **completely open goal**), while admitting an opponent
  standing *behind* the goal line at the keeper's radius. The near bound was a 0.95 step (1.000 →
  0.050 across 1 cm, taking the SHOOT option with it), more than twice the cliff -021 removed. See
  ERR-008-022. (b) **§3.2.10's constant catalogue has now been left behind by five
  consecutive #8 landings** (-016, -017, -018, -019, -020 and this one all defined their constants
  in their own §3 subsection tables), so its "Total constants: 58" summary is wrong by at least
  nine; it wants one reconciliation pass of its own rather than a sixth half-correction.
- **Football-judgment proxy review — 32 findings open across 24 specs; the §6 doctrine governs every fix; ERR-008-020 (template), ERR-008-019 (the founding long-shot cliff, August 5) and ERR-008-021 (the deferred shot-lane follow-up, August 6) LANDED** — *opened August 4, 2026 (review + doctrine + template landing all same day); ERR-008-019 landed August 5, 2026; ERR-008-021 landed August 6, 2026.*
  **ERR-008-021 + AR-1 are GATE-VERIFIED (PR #305, CI run 404, head `3f207ee`, August 7):** build
  0 errors, `DecisionTree.Tests` **120 passed / 0 failed / 4 skipped / 124** carrying all 7
  `ShotLane_*` locks, whole-tree gate PASSED with the quarantine empty, and `MatchEngine.Tests`
  **420/430 unchanged** — the intended digest movement tripped no goal-rate band or tick-window
  scenario, so the blast-radius caution below resolves as "checked by execution, nothing moved".
  The "gate NOT run" caveats below are retired; CI on push remains the only compiler for this work.
  **ERR-008-021 AR-1 (same day): 1 High, 7 Medium, 5 Low — all fixed.** The High: the landed P3
  exemption keyed on the whole 6 m GK band, so every near-goal defender escaped the weighting —
  inert exactly where shots are blocked — and all six original fixtures sat outside the band, so
  no lock registered it. Now a single **GK candidate** (goal-line-nearest in band) is exempt;
  every other blocker is weighted; radius stays per-band. Also corrected: the P5 "today's arcs
  bit-for-bit" overclaim (exact only at ability midpoint raw 10/11 or null view; the all-default
  10/10 squad reads ≈ 0.979 — the ERR-008-019 overclaim shape, one day later), margin-less and
  vacuously-passable locks, the duplicated fidelity expression (hoisted), away mirrors running a
  post assignment production never builds, the inverted Known-limitation direction, and the
  §3.1.4.1 gate boundary (strict ">" per spec). `OptionGenerator.cs` v1.8,
  `OptionGeneratorTests.cs` v1.8 (7 locks), `UtilityWeights.cs` v1.11 (doc), `section-3-1.md`
  v1.5, `section-3-2.md` v1.13, `spec-error-log.md` v1.68. Gate still not runnable; CI on push.
  **ERR-008-021 LANDED August 6, 2026 — the shot-lane follow-up ERR-008-020 deferred, closed.**
  #8 §3.1.4.3/§3.2.3.2's goal-occlusion sum was attribute-blind: every outfield blocker in the
  shot path occluded the same geometric arc whoever he was (a Pace/Anticipation 1/1 defender
  walled off the goal exactly as hard as a 20/20 one), and no shooter attribute entered the read
  — pattern (a), with no P1 cliff (occlusion is already continuous in position). Fixed as
  §3.2.3.2 **step 3a** (spec + code same commit): each OUTFIELD blocker's `blockedAngle` ×
  §3.1.3.3's `perceived_ability` (Anticipation/Pace → `INTERCEPTOR_ABILITY_MIN..MAX`, read
  through the SHOOTER's Vision fidelity — doctrine P2). **No new constants** — the ERR-008-020
  `[GT]`s reused verbatim, so one lever calibrates the pass-lane and shot-lane reads together at
  the eventual KD-W1 balance pass. The **goalkeeper's arc stays purely geometric** (doctrine P3:
  keeper shot-stopping quality is priced once, at the #11 save — weighting his occlusion would
  double-count it; `GK_BLOCKER_RADIUS` is an abstraction of coverage, not a body). League-average
  / null-attribute-view ability = 1.0 reproduces today's arcs — and therefore today's SHOOT
  generation set — exactly (P5 pivot). `section-3-1.md` v1.4, `section-3-2.md` v1.12,
  `OptionGenerator.cs` v1.7, 6 new `OptionGeneratorTests` locks (computed-average pivot =
  null-view arc exactly, Vision-20 vs Vision-1 discrimination, null-view neutrality, GK-arc
  invariance under keeper attributes, away mirror). Adjacent defect recorded-not-fixed:
  §3.2.3.2's numerical example is in a legacy centre-origin frame (the ERR-008-001 class,
  surviving in an example) and its blocker classifies as GK under the section's own step-3
  heuristic yet uses the outfield 0.5 m radius — annotated in place, left standing because its
  0.757 feeds the approved §3.2.3.3 chain. No schema / RNG / draw-order change; digests move
  where a generated SHOOT has a non-neutral outfield blocker in the path, as intended. **Gate NOT
  run — no .NET SDK in the authoring environment; CI on push is the gate.**
  **ERR-008-019 LANDED August 5, 2026; owner-revised to the FULL-RANGE ramp later the same day** —
  the review's founding finding, under the id soft-reserved for it (re-verified free at landing):
  #8 §3.2.3.1's midfield `ZoneModifier_SHOOT` hard threshold (0.55 strictly above shifted-LongShots
  0.75, 0.05 at/below — an 11× jump across one raw point) is now a linear ramp centred on the old
  threshold with new `[GT] LONG_SHOT_RAMP_HALF_WIDTH`, owner-revised from the initial 0.05 to
  **0.25, its maximum valid value**: the ramp spans the whole attribute — raw 1 exactly 0.05,
  raw 20 exactly 0.55, every raw point between moves the modifier ≈ 0.026, **no plateau anywhere**
  (doctrine P1; midpoint at the old cliff and uniform-population mean 0.30 preserved — the P5
  pivot, locked by test; P2/P3 deliberately out of scope — execution capability, not a recognition
  judgment; spec §3.2.3.1/§3.2.3.4 + Case B recomputed 0.200 → 0.162 + `UtilityScorer`/
  `UtilityWeights` same commits; 4 `UtilityScorerTests` locks: shifted-form at raw 10, no-cliff,
  exact midpoint, endpoints-exact + strictly-monotone). **Digest invariance was RETRACTED at the
  same-day adversarial review over that landing** (documentation only — no formula, constant or
  test changed): the recorded argument assumed the shooter sat within Ball Physics #1 §3.1.11.1's
  **0.5 m** `ControlRadius`, but the engine's production possession-granting paths are
  `MatchEngine.RunLooseBallPickup` (§5.Z Phase H, KD-H3 — `LooseBallPickupRadiusM` = **1.0 m**,
  and the ball is **left where it lies**) and the 1.0 m first-touch path, and after the grant
  nothing re-anchors the ball to the holder or drops possession on separation (the executors check
  only the possession id). So a MIDFIELD ball at x → 70⁻ with the holder 1.0 m goal-side reaches
  just above **34.0 m** — inside raw 19's range gate (20 + (18/19) × 15 = 34.21 m), where the
  full-range ramp gives ≈ **0.524** against the old step's 0.55. A generated option **can** score
  differently, so "no digest moves on any seed" no longer holds for the full-range form and a
  scenario instrument that trips at the first gate run should be checked against this before being
  read as a regression. The behaviour change is **owner-intended**; P5, the four locks and the
  worked examples stand, and the superseded narrow ramp's disjoint-bands argument survives
  (29.0 m vs > 34.0 m). Recorded with the correction (Low): `LONG_SHOT_RAMP_HALF_WIDTH`'s XML doc
  advertised a (0, 0.25] range the suite forbids below 0.25 — the formula's validity domain, not a
  free dial. **Gate NOT run — no .NET SDK in the authoring
  environment.** `football-judgment-proxy-review.md` swept all 53 APPROVED specs for one defect shape: a continuous football judgment implemented as a hard threshold, bare geometry, or a raw-attribute comparison (the ERR-008-019 long-shot-cliff class). 34 findings across 24 specs; 29 specs clean. The owner-converged **§6 remediation doctrine** is binding on every fix — P1 continuous-never-cliff; P2 skill as *discrimination fidelity* (`perceived = neutral + fidelity × (true − neutral)`, low skill degrades to today's attribute-blind engine); P3 the attribute ownership ledger (Vision owns on-ball recognition, Anticipation off-ball/predictive; **no new "play recognition" attribute** — owner call); P4 intent as a first-class object (pass-to-space, run-intent signals, set-piece routine targets — mechanism-class, design supplement first); P5 calibrate the chain end-to-end, pivot on today's baseline, `[GT]`s first-guess per KD-W1. **ERR-008-020 landed August 4** as the owner-selected template (#8 §3.1.3.3: continuous falloff × Anticipation/Pace ability through the passer's Vision fidelity; spec v1.3 + 6 code files + 9 test locks across two suites — incl. the away mirror, the computed-path P5 pivot, the MIN/MAX midpoint invariant, and the engine construction wiring lock, the last three added at the same-day adversarial review (2 M, 1 L fixed; pass 2 clean); **gate NOT run — no .NET SDK in the authoring environment**; shot lane §3.1.4.3 deliberately deferred). **Record correction made at that landing:** the review originally carried ERR-008-019 as "FIXED … gate green" — verified false against both branches (no log entry, cliff live in `UtilityWeights.cs`/`UtilityScorer.cs`/`section-3-2-3-to-3-2-9.md`, no branch has a fix); the finding is re-opened and the id soft-reserved for its real landing, which must re-verify the id free. **Remaining:** the formula-patch findings (per §6.3, ERR + spec + code same commit each); the mechanism-class findings need design supplements before code; #46 is a governance call (its spec *defends* the pattern); the pairwise playing-familiarity gap (§6.5) is a candidate supplement. Blast-radius note for whoever lands the next fix: ERR-008-020 moves digests on effectively every seed, so tick-window/rate-band scenario instruments may need re-anchoring at the first gate run. **That note cashed out August 7, 2026 (main run 419, the chain's first full run on main): two acceptance bands tripped and were rebaselined by owner call** — keeper-contact deep dive-early `== 0` → `<= 1` (one episode 616.7 ms early, *inside* the pre-fix class — a real recurrence, not an instrument artifact) and close-chance cosine −0.10 → −0.16 (pooled −0.119; one seed's entire ERR-008-018 gain returned). Both regressions are queued for the P5/KD-W1 calibration pass; details in `gk-contact-rate-design.md` v1.4 (AR-6) and `close-chance-creation-design.md` v1.1 (Acceptance-3). Also from that diagnosis: **the full dotnet gate now runs in Claude remote sessions** (Ubuntu-archive `dotnet-sdk-8.0`; `tools/dotnet-ci/README.md` v1.2) — the standing "gate NOT run — no .NET SDK" caveats in this entry were true of those sessions' hosts, but the blocked installer was `dot.net`, and the Ubuntu archive is reachable.

- **#28 Player Progression — T1 + T2a LANDED August 8, 2026 (roadmap D1, part one): ERR-029-006 CLOSED, #30's KD-2 slot 1 is LIVE, and the career roster has MOVED OFF THE WORLD SEED. Four adversarial-review passes since (August 9–10) found the landing genuinely broken in production and fixed it; pass 4 returned 0 High. REMAINDER — still open: the season boundary (retiree removal + 1:1 regen) and with it the `player-progression.regen` stream, plus T3's deep growth curve.** — *opened August 8, 2026 (T1 + T2a landing); AR passes 1–4 closed August 9–10, 2026.* `ProgressionEngine` is the KD-7 sole writer; `SeasonLoop` gathers #29's `ComputeTrainingInput` batch through the new `PlayerCareerStates.GatherTrainingInputs` and hands it to the FR-PG-021 batch `AdvanceDay`. **The load-bearing decision is KD-4**: `Squad` is immutable and `League` is rebuilt from the world seed at load, so a player's evolving `[1,20]` attributes had *nowhere to live and nowhere to persist* — growth could not be represented at all. #28's block is now the serialized roster and `ProgressionSquads` is the single provider every consumer reads through, so there is one authority at every moment rather than two reconciled at save/load (the shape #29/#41 T2's H2 filed). **This retires roadmap A3's property that a career is reopenable from the world seed alone**; `SavedWorldSeed_RebuildsTheSameLeague` asserted exactly the retired rule and was NARROWED to the half that survives (generation is still seed-pure), not deleted. `SEASON_SAVE_FORMAT_VERSION` **4 → 5**. **The landing has NO draw site at all** — no stream registration, no `0x20` promotion, no `DETERMINISM_DIGEST_VERSION` or `SNAPSHOT_SCHEMA_VERSION` question — because new-game `PotentialAbility` is seeded deterministically. Four ERRs filed AND resolved in the landing commit: **ERR-028-003** (new-game PA had no derivation in spec or code; owner's call is that it is **#47 authored data**, seeded by a deterministic `[GT]` placeholder; recorded-not-fixed that a whole youth career raises CA by only ~421 of `ABILITY_MAX`=10,000, so the PA ceiling is decorative regardless of PA's source — a growth-RATE property Stage-3 `curveEnabled` owns); **ERR-028-004** (§3.5 specified the save block version-first with the RNG domain tag as its identifier — the ERR-029-005/ERR-041-009 MUST arriving in a third spec; now magic-led, `ProgressionBlock` typed); **ERR-028-005** (§5.2's keystone T-PG-DET-002 was unsatisfiable as worded and §3.1 carried no per-day cursor while `RunCareerDaySteps` runs a fixture day's slots TWICE, ERR-030-027 — a wired #28 would have double-accrued growth every fixture day; fixed with `LastAdvancedWorldDay`, sentinel `uint.MaxValue`, idempotent-per-day and gap-complete); **ERR-030-030** (five stale #28-null-seam sites + the v4 frame description). Landing suites: `PlayerProgression.Tests` 26 → 41, `SeasonSave.Tests` 356/0/3.

  **AR pass 1 (August 9): 4 High, 7 Medium, 8 Low, all fixed — the landing was broken and had already been pushed, every High demonstrated by executing probes against the built assemblies.** **ERR-028-006:** a new world starts on day **0**, so the `uint` `BirthWorldDay` anchor underflowed for every player with a non-zero generated age and was clamped to 0, making derived age `worldDay/365` — the **entire league read as age 0** after one advance (`26,22,30,26,28,30` → `0,0,0,0,0,0`; bands `growth=100 stable=0 decline=0`), retirement unreachable, and `Age=0` reaching `LineupSelector`/`SquadRating`/`ConfigureSquads` through the KD-4 projection. Both #28 fixtures used `BaseDay=100000` specifically to keep the anchor non-negative — a fixture written around the defect, the ERR-030-014 shape again. Fixed: `BirthWorldDay` `uint` → `long`, both clamps deleted (re-measured bands `growth=42 stable=26 decline=32`); the landing's own headline lock (`days × players == |Σ cursors|`) was complicit — satisfiable only when every player sits in one band — and was rewritten against each player's independently-sourced bootstrap age. **ERR-028-007:** the fourth persisted per-player cursor (`LastAdvancedWorldDay`) was checked at none of the three boundaries the #29/#41 loop spent passes 5/6/9 establishing; a cursor 9,999 days ahead of the clock was accepted at composition, Save and Load, silently freezing growth. Fixed with a shared `PlayerCareerStates.RequireProgressionCursorWithinClock`, all three boundaries delegating. **ERR-028-008:** `Save`'s `?? ProgressionEngine.Empty` let a resume that dropped the store overwrite a populated 4-club roster with a zero-club one (measured: `clubs=4 cursorSum=600` → `clubs=0 cursorSum=0`), every other gate green. The reviewer's first-choice fix (refuse `career != null && progression == null`) was narrowed after it broke four legitimate pre-#28 suites; fixed at the destination instead — `Save` refuses only an empty store overwriting a populated block. **ERR-028-010:** a progression-wired `SeasonLoop` could not play a round through **any public API** — the constructor's provider was private and the public `AdvanceAndPlayNextRound(ISquadProvider)` demanded reference-equality nothing exposed, so the configuration the whole landing exists to enable could advance days and save and nothing else; fixed with a parameterless `AdvanceAndPlayNextRound()` resolving through the loop's own store, both overloads sharing one extracted `PlayNextRound`. Plus **ERR-028-009** (Medium: no F8 sentinel guard — `AdvanceDay(uint.MaxValue)` stored, breaking idempotency, and a gap-replay loop never terminated; fixed, refused up front) and **ERR-028-011/-012** (Medium×4/Low×8: cross-club duplicate ids `Encode` wrote and `Restore` refused; `FromBlocks` accepting a stale id cursor, defeating FR-PG-011; no coherence check across the four career blocks; no range gates on decoded attributes/weak-foot/age; and this landing's own records had been wrong — six changed files carried no version row and `SeasonLoop.cs`'s claimed 1.6→1.7 version chain never existed, the file being at 1.15).

  **AR pass 2 (August 9): ERR-028-013 (High+Medium) and ERR-028-014 (Medium), plus a 33-guard mutation audit that found 15 fixes with no test able to fail if reverted — all 15 now locked.** **ERR-028-013:** the `SeasonLoop` constructor conflated "a progression store was supplied" with "#28 is the roster authority" — an empty store (the honest pre-#28 composition `SeasonSaveManager.Save` itself documents and its own ERR-028-008 message advises resuming from) could be composed beside a caller's provider but not resumed through any documented path, because **nothing anywhere reconstructed a `SeasonLoop` from `SeasonSaveManager.Load` output** — all 28 `Load` call sites assert on the contents and stop. Fixed with one `progressionIsRoster = store != null && store.ClubCount > 0` predicate driving every gate; the old career/provider pairing rule split so a populated store no longer requires #29/#41 career state beside it. **ERR-028-014** is the diagnosis worth keeping: the never-advanced cursor sentinel was exempt from the cursor-vs-clock rule at all three boundaries — a **sibling-copy error**, since "never advanced is coherent at any clock" is true for #29/#41 (fresh states carry no clock-anchored quantity) and false for #28, the only one of the four whose fresh state (age, derived from `BirthWorldDay`) means something different at every clock value. A day-0-seeded store composed against a day-3650 clock banked **one day of growth for a decade**, silently, every gate green. Fixed by deleting the special case — `SeedFrom` anchors the cursor at the seed day — rather than adding a gate; two tests that had been locking the defect as intended behaviour were **inverted**, not adjusted (mutation-verified: reverting the anchor fails 9 of 85). Also fixed `tools/recurring-defect-lint.py`, whose `TODAY` constant was hardcoded to its own authoring date and had been producing false ERRORs every day since.

  **AR pass 3 (August 9–10): ERR-030-031 (High, doc sweep) and ERR-028-015 (High×2, code — both introduced by pass 2).** **ERR-030-031:** the ERR-028-014 sweep had stopped at its own spec folder — this repo's **fifth** recurrence of the grep-boundary-widening class — leaving #30 §2.3's F8 row and Appendix B.1 asserting "all three" persisted cursors and a blanket sentinel exemption ERR-028-014 had made false the same day, and #28 §5.1 still documenting the retired behaviour for a test that had been renamed and inverted; a reader following §5 would have rebuilt the defect. Fixed repo-wide, both sections now carrying the *reason* (why the exemption is sound for #29/#41 and wrong for #28) rather than just the corrected rule. **ERR-028-015:** anchoring the cursor at the seed day made `AdvanceDay(seedDay, …)` a total no-op — and mutation testing, not the static audit that found it, proved three locks had gone **silently unguarded**: deleting the idempotency guard outright left **all 469 tests across both suites green**; deleting the retirement-age comparison left 85/85 green. Separately, the ERR-028-013 relaxation had reopened the ERR-028-010 gate by breaking the biconditional its key relied on (`_career` vs `_careerSquads`) — a progression-only loop could skip the two-provider refusal entirely, verbatim the ERR-028-010 shape, in the fix that cites it. Both rekeyed/relocked.

  **AR pass 4 (August 10): ERR-028-016, 0 High, Medium×4 — the loop's first pass with no High.** The headline corrects **pass 3's own fix**: pass 3 attributed a guard's necessity to backward-call cursor regression, when the `if` condition already prevents that regression regardless; the `return;` actually prevents the §3.4 retirement evaluation from running on a call that advanced nothing (not inert — it could stamp a `RetirementDay` earlier than the player's own cursor), and pass 3's rewrite had discarded the original comment's correct half while fixing a different overclaim. Plus three decode range checks tested only one side of a two-sided OR (the same half-guard shape pass 2's mutation sweep found) and five further guards with no isolating test at all. Suite: `PlayerProgression.Tests` 89 → **100**.

  **Still open — this loop fixed defects in what landed, it did not extend the landing's scope:** the season boundary (retiree removal + 1:1 regen) and the `player-progression.regen` stream it needs, plus T3's deep growth curve. **Verified 2026-08-10 at branch HEAD:** whole tree builds 0 errors; `PlayerProgression.Tests` 100/0/0; `SeasonSave.Tests` 385/0/3; `tools/recurring-defect-lint.py --repo .` reports 0 ERROR tree-wide. Full record: `CHANGELOG.md`, `spec-error-log.md`.

- **Match-engine wiring backlog — W1, C1 and W2 BUILT-BUT-DISABLED; 8 built subsystems (W3–W10) still have no production caller, and the `[GT]` freeze (KD-W1) that follows** — *opened August 4, 2026; W1 landed the same day; W2 landed August 12, 2026.* A three-pass audit (comment sweep → whole-tree production-caller count over every `public` method in the 18 assemblies the engine composes → manual triage in source) of what is built, tested, reachable, and **never runs**. Full inventory, evidence and wire-order: `docs/tracking/match-engine-wiring-backlog.md` v1.0. **The two largest were invisible to this file until now.** (1) **The keeper never comes off his line** — `GoalkeeperMechanics.CommitRushIntent` has zero production callers; the engine calls `CommitSaveIntent` and only that. Everything downstream is built and works (`GoalkeeperRushDispatch.UpdateRushFrame` advances the keeper and writes back to the movement array; `Rushing → OneOnOne → Smothered` with abort reasons, 1v1 and smother radii, telemetry; the `RushIntent` is serialized into the snapshot) — **only the trigger condition is missing**, so every one-on-one in the game is a stationary keeper waiting to dive. Cheapest large realism lever available. (2) **No player has ever made a tackle** *(as filed August 4, 2026; ✅ RESOLVED August 12, 2026 — see the W2 landing account at the end of this entry. Left as originally written below, since it is the audit finding that opened this item.)* — three dormant links in one chain (a fourth was found at W2's investigation step, see below): `DefensiveAITick.GetTackleIntentRequests` is populated every tick and read by nobody (class doc: *"all output surfaces are populated at Stage 0 but integration with the match orchestrator and Decision Tree #8 occurs at Stage 1 (KD-16)"*); `GetAndClearTackleFlag` is hardcoded `=> false` in **both** engine collision adapters (`MatchEngine.cs:6721`, `:6789` — **stale line numbers, corrected at W2's investigation to the PASS and SHOT adapters**); so `PassExecutor.cs:393`'s §3.8.5 tackle-interrupt branch and its `CancelReason.TackleInterrupt` outcome are **unreachable code**. Possession can be lost to interception and to collision, never to a tackle — which bears directly on the possession-churn and turnover figures earlier passes reasoned from. No comment anywhere records this; only the call-graph pass found it. **Also dormant:** cross claims (`ResolveHandContactDuel` intentionally not called — same missing multi-agent contact feed as the AGENT_BALL fan-out already filed under the GK/Heading entry below, so one dependency, not two); the keeper's vision (`GkHeadingIntentSource.SaveArmed` is four lines of pure geometry, while a tested `OcclusionFilter` runs for every outfielder — traffic in front of goal costs the keeper nothing and a deflection does not restart his reaction window); the #13 BackwardPass press trigger (`PassEventRing.Push` has no producer, so the per-team ring the engine builds is permanently empty); `BallStateType.Controlled` (no producer — possession is a flag, never a kinematic constraint; the same root cause as §5.Z.23 item (c) below); #26's kickoff preset selection (`ManagerAdaptation.ApplyKickoff` uncalled — an AI-managed team starts every match on the human baseline, though the mid-match ladder IS wired); and goalkeeper distribution (`CommitDistributeIntent` uncalled — the engine's six-second rule substitutes, so this is fidelity, not missing behaviour). **Stated blind spot:** the method sees *method-level* dormancy and is structurally unable to see *gate-level* dormancy — a call site that executes but whose condition is almost never true. One is already measured (#12 commits `InPoss` on **9.5%** of final-third samples, starving every phase-gated mechanism in #13/#14/#15 — see the close-chance entry below), found by runtime instrumentation and by no static analysis. **The backlog is therefore a floor, not a ceiling**, and books a gate-firing instrument as its own item before any calibration. **KD-W1 — the `[GT]` freeze:** do not land a constant governing an unwired subsystem; the hazard is diagnostic, not arithmetic (measured ~18% conversion against football's ~11% reads as "the shot model is too generous" when part of it is "no keeper has ever narrowed an angle and no defender has ever tackled"). Defect fixes, instruments and measurement continue freely. **KD-W2** scopes this to the match engine; the 22 approved specs with no assembly remain `path-to-playable-roadmap.md`'s problem. **Consequence for the entry below:** the §5.Z.23 `pointQuality` owner decision is **parked, not resolved** — the rush trigger changes the contact geometry that decision turns on, so deciding it now risks paying for a fix to a problem about to change shape. **Next:** wire in the order proposed in §5 of the backlog (rush trigger → the `InPoss` gate → tackles → keeper perception → the gate instrument → the small items → cross claims), then one calibration pass. The `match-realism-pass` skill needs a gate at its top — *is the subsystem this touches fully wired? If not, this is a wiring task, not a realism pass.* **W1 LANDED August 4, 2026** (`docs/tracking/gk-rush-trigger-design.md`): `MatchEngine.TryCommitRushIntents` gives `CommitRushIntent` its first production caller, over a pure `GkHeadingIntentSource.RushArmed` built from one sentence — **a keeper comes out to reduce the shooting angle**. So the only thing that keeps him home is a team-mate already **goal-side** of the ball inside the shot corridor; a defender merely *chasing* the carrier narrows nothing and does not stop him (the v1.0 predicate used a last-man test and was corrected at owner review, before any measurement — it kept the keeper home in exactly the situation he exists for). **How far** he comes out is #11 §3.7.0's attribute-driven `ComputeRushCommitDistanceM` (`OneVsOne` / `Composure` / fatigue), not an engine range: ~9 m for a timid keeper, ~16 m for an aggressive one. An **intercept-race solve** picks the locked target for a loose ball, because KD-15 locks it at commit and the ball's current position is where it *was*. Skipped whenever `SaveArmed` holds for the same keeper — a ball driving at the goal is a save, not a rush, and without that exclusion a shot would send the keeper charging out while the ERR-011-007 lead gate still held the dive. Deliberately **not** a DT action: `ActionType.SAVE = 7` is the last ordinal fitting the 3-bit composure-noise field, so RUSH would force the same rebaseline that defers W9. **No new engine state** — #11's own serialized `_rushIntentActive` is the latch, read through new accessors — so **no schema bump**. The wiring surfaced **two** spec defects. **`ERR-011-010`**: §3.7 delegated the rush DECISION to Decision Tree #8, which has no goalkeeper model and structurally cannot acquire one (`ActionType.SAVE = 7` is the last ordinal fitting the 3-bit composure-noise field), so the condition belonged to nobody — which is why the method sat uncalled for ten weeks — and, because the "when" was delegated, the spec never said what a keeper is *deciding* either; new §3.7.0 takes the decision back and states the model normatively. **`ERR-011-009`**: a rush that REACHED its locked target had **no §3.1.1 exit at all** (for a loose ball the 1v1 and smother triggers are false by construction and F-08 needs a possessor, while the per-frame update stops dead at the target), so a swept ball would have stranded the keeper in `Rushing` for the rest of the match; fixed spec-and-code in the same commit with two new §3.1.1 rows, a completion rather than an abort, so FR-GK-018 / KD-15 are untouched. **REMAINDER — and it is the honest headline: NOTHING IN THIS LANDING HAS BEEN EXECUTED.** The authoring environment has no .NET SDK and the agent proxy denies the installer, so `tools/dotnet-ci/run-gate.sh` did not run and the new `GkRushDiagnosticTests` instrument is written-and-unrun: there are **no pre/post numbers**, the eleven new (un-calibrated) `[GT]`s have no measured basis, and no compile or test claim here may be cited until the GitHub `dotnet-compile-test` job reports. Next in the backlog sequence: **C1**, the `InPoss` gate. **C1 LANDED August 8, 2026 (`ERR-012-011`).** #12 §3.0.2 classified phase from the ON-BALL CARRIER, which the engine clears at every `ApplyKick` and restores only on physical receipt — so for the entire flight of every pass the snapshot read "loose ball" and the ball-velocity branch called a passing team **in transition**. Phase now classifies from TEAM possession, composed by the orchestrator as the carrier's team ∪ the intended receiver of a pass in flight, over a new `_passInFlightReceiverId` latch. The latch expires by reusing `RunFirstTouch`'s own receding predicate (hoisted to one shared `BallApproaching`), so there is **no new `[GT]` and no timeout** — it stays inside the KD-W1 freeze. Snapshot fields were **added, not redefined**: redefining `PossessionOwnerEntityId` would have excluded the intended RECEIVER from #23's dismark nudge for the whole flight of every pass. `SNAPSHOT_SCHEMA_VERSION` **19 → 20**; no RNG stream / domain tag / draw site / draw-order change. **Measured, 6 seeds × 90 min, pre → post:** final-third `InPoss` **7.5% → 40.8%**, possession-phase share (InPoss+OutOfPoss) **24.2% → 96.8%**, and the away mirror is clean (41/56/3/0 against 56/41/0/3). Two facts measured for the first time: the engine holds **no on-ball possessor on 86.0%** of final-third samples, and a pass is in flight on **77.0%**. **THE HEADLINE IS NOT THE GATE, THOUGH — IT IS THAT THIS ENTRY'S OWN RATIONALE WAS WRONG, AND THE FOOTBALL GOT SLIGHTLY WORSE.** The pre-implementation council refuted "unblocks phase-gated behaviour across #13/#14/#15" before a line was written: `TacticalContext.HasAttackIntent` is written by the engine and **read by no production code anywhere**, so #15 is inert independent of its gate (filed as new Class-A **C5**); #13's `PressDirective.PrimaryTargetPosition` / `PressAssignment.TargetPosition` have no consumer outside `pressing-ai`, so more-correct pressing still steers nobody. The one large behavioural lever C1 does pull is #12's own `PullFactor` table, whose `InPoss` column is **less** advanced than the `TransToAtk` column it replaces for every attacking role — and the predicted regression **measured**: deepest composed slot **23.0 → 25.7 m** from goal, mean attackers in the box **0.04 → 0.02**, slot-in-box 3% → 1%, dribble cosine −0.03 → **−0.180**. Shots 16 → 19/match and goals 2.83 → 3.33/match. C1's real value is a **correct phase label** plus an `InPoss` `PullFactor` column that is exercisable for the first time — a precondition for the KD-W1 calibration pass, which now inherits a differently-shaped table. **The named creation lever remains C4** (#8 cannot pass to a place, only to a player). Acceptance: `match-engine-inposs-gate` (#19 ScenarioRunner, Tier B, 2 seeds × 90 min), **proven to FAIL at the pre-fix commit `ba4e194` by execution in a worktree** — both predicates read 0.256 against a 0.70 bound. It pins **no** goal, shot, box or dribble figure, deliberately: every shape metric moved the wrong way and pinning one would encode a regression as a contract. **Recorded, not fixed:** deleting `ClearPassInFlight()` from the GK/heading adapter or from `ApplyRestart` leaves the whole suite green. The restart one is benign (the placed ball is at rest, so the receding rule clears it anyway); the GK/heading one is a real gap, and the reason is new Class-A **C6** — **no test in the tree drives `GkHeadingWorldAdapter.ApplyKick` at all** (pure-function tests build no engine; the `MatchEngineGkHeading*` tests and scenarios stop at *intent committed*; the only paths that plausibly reach a strike are env-gated and end in `Assert.Pass(…)`). **GATE: FAILED — executed locally, whole tree, and the failure is C1's own doing.** Build 0 errors, quarantine empty, every suite green except `MatchEngine.Tests` **446 passed / 2 failed / 10 skipped** (53 m 26 s; 446 → 458 total with this landing's 12 locks plus the new scenario, which PASSED). `sim_match_engine_close_chance` fails both its ERR-008-018 predicates (`meanCosine` **-0.165** vs the -0.16 bound; `goalwardShare` **0.407** vs 0.42) and `sim_match_engine_shot_outcomes` fails `fast-balls-deflect-off-bodies` (`totalDeflections > 0`, read **0**). **Both PASS at the pre-fix commit `ba4e194`, verified by executing them in a worktree - so C1 caused both, and that is measured, not inferred.** **Neither bound has been moved.** The close-chance pair misses by 0.005 and 0.013 on a band already rebaselined twice this fortnight, and the council's standing note applies: a predicate rebaselined a third time has stopped being a lock. The shot-outcomes one is not a band at all - it is ERR-003-007's REACHABILITY lock, and C1 drove it to exactly zero, meaning no fast ball found a body anywhere in the corpus. That is consistent with the measured shape compression (deepest slot 23.0 -> 25.7 m) and is a new fact about the engine, not a threshold to retune. Both were filed for the owner with the mechanism named. **THE BRANCH IS RED BY DESIGN.** **`sim_match_engine_close_chance` is RESOLVED by owner call, August 11, 2026: hold red, do NOT rebaseline a third time** — a predicate already moved once (§9 Acceptance-3, owner call August 7, 2026) and already deleted once under the same rule (§9 Acceptance-1) does not get a second rebaseline to pass; the failure is queued for the KD-W1 calibration pass rather than fitted around piecemeal today. No `[GT]` moved, no code changed. Formal record: `close-chance-creation-design.md` §10.9 item 6 (v2.2). **`sim_match_engine_shot_outcomes`'s `fast-balls-deflect-off-bodies` reachability predicate is a separate lock and remains open, awaiting its own owner call.** **W2 MEASURED before being built (August 12, 2026):** the census (`TackleIntentDiagnosticTests`, 3 seeds × 90 min, both defending teams separately) found, per defending team per match, **681.7 defending episodes, 310.2 with an outfielder within 3 m of the carrier, 97.2 with an intent naming him, 65.3 with a COMMIT naming him** — against football's ~15–17 tackle attempts per team per 90, so the gate supplied **~4× what was needed**: W2 was a RESOLUTION problem, not a producer problem and not dead upstream, the opposite of the C1 trap. Two of the investigation's own predictions were refuted: the FR-DA-010 presser exclusion was not the bound (`poolElig` 310.0 vs the 3 m population's 310.2, presser 0.5 — though a #13 press-role holder being almost never within 3 m of the carrier is itself a gate-level dormancy signal for #13, filed as **C9**), and `MarkAssigner`'s ball-blindness was a fidelity gap, not a precondition (31% of eligible episodes still yielded an intent). Also filed: **C10** (`DefensiveAgentSnapshot.HasBall` written every stride, read by nothing — the C5 class in a second assembly). **W2 LANDED the same day (`fc8f81f2`), resolving `ERR-014-006`.** New #14 §3.6.5 "Tackle Outcome Resolution" takes the tackle outcome decision back into the spec that owns the players, on the W1 precedent, because neither #8 (its `ActionType` ordinals are exhausted by the 3-bit composure-noise field) nor #3 (defers slide tackles to Stage 2) can accept the KD-6 delegation. A Stage-0 tackle is an abstract attribute duel producing one of **four** outcomes — `MISSED` / `BALL_WON` / **`BALL_LOOSE`** / `FOUL` — `BALL_LOOSE` at owner direction, since a won/missed-only model has no way to express the ball going somewhere neither player controls. New `src/defensive-ai/TackleOutcome.cs`, `TackleDuelInputs.cs`, `TackleOutcomeResolver.cs` (ten new `[GT]` + one `[FIXED]` ceiling, all un-calibrated per KD-W1). `MatchEngine.cs` wires the resolver at the COMMIT contact gate, re-derived (not fitted) from 1.5 → 2.5 m on what COMMIT means (a lunge, #3 §7.2.1's extended-leg case); a tackle foul enters the **existing single foul-candidate slot** rather than a second authority; `ContactType.SLIDE_TACKLE` gets its first producer. `SNAPSHOT_SCHEMA_VERSION` **20 → 21** (per-agent tackle flag + challenge cooldown). `DOMAIN_TAG_DEFENSIVE_AI` (0x1A) gets its **first draw site anywhere in `src/`**, un-blocking #14's own T-DA-DET-005; the `match-flow.card-severity` draw order **moves by design** (the foul branch now draws on ticks that previously had none) — **no digest invariance is claimed anywhere in this landing.** `Tackling` gains its first consumer anywhere in the tree; `Marking` still has none. FM-08's CONTACT-time possession-loss log corrected from `LogError`("Race condition") to `LogWarning` (a tackle makes it an ordinary event). Tests: 12 pure resolver locks + 7 composed engine locks, all green at the landing commit. **GATE PASSED for W2 (August 12, 2026):** whole-tree build 0 errors / 0 warnings, quarantine empty, 32 suites; `MatchEngine.Tests` **461 passed / 1 failed / 11 skipped** (38 m 2 s). The single failure is `sim_match_engine_close_chance`, the inherited owner-held-red predicate that also fails at the pre-change baseline `4b9271c` — so the branch is at its baseline red state and W2 adds no new failure. Baseline was 451/1/10; the +10 passed are W2 locks and the +1 skipped is the env-gated census instrument. — a whole-tree gate was in flight at the time this entry was written; ⟨PLACEHOLDER — operator to fill in build/suite counts, quarantine state, and PASS/FAIL once the run completes⟩. Full account: `docs/tracking/tackle-wiring-design.md`, #14 `docs/specs/defensive-ai/section-3.md` §3.6.5, `docs/tracking/spec-error-log.md` `ERR-014-006`, `docs/tracking/match-engine-wiring-backlog.md` v1.8. **Next in sequence: W4** (keeper perception), **then W12** (the gate-firing instrument).

- **Conversion at contact — the CLAIM defect is FIXED (ERR-011-008); REMAINDER: the `pointQuality` lottery is blocked on a design decision, and parry placement is unfixed-but-currently-costless** — *opened August 3, 2026 (§5.Z.23).* §5.Z.22 recorded the goal-rate residual as *"marginal, end-of-envelope touches whose parries and spills keep the ball alive"* and named two levers. **The premise was refuted by measurement**: per-contact ball speed before → after is parried 10.8 → 0.0, deflected 10.3 → 4.2, spilled 13.9 → 9.0, missed 9.5 → 9.5 — and **caught 11.1 → 10.8** (one tick of drag). The parries and spills work; the CATCH did nothing, and 7 of 10 catches were followed by a goal within 5 s. **That half is closed** (ERR-011-008 — `ParkBall()` at both claim sites; goals 5.0 → 3.7/match). **What remains open:** (a) the Stage-0 **`pointQuality` lottery**, now quantified — quality 0.559 / 0.564 / 0.590 across *rising* contact marginality with catch rate 43% / 38% / **50%**, i.e. blind AND inverted, and `HandlingPointErrorSigmaM` provably cancels out of its own formula (σ·z ÷ σ), a `[GT]` dial whose value cannot matter at any setting. The geometry-aware form was **implemented and measured, then reverted**: it fixes the direction (0.261 / 0.255 / 0.150) and collapses the level to **zero catches and zero parries**, goals 3.7 → 4.3/match, because mean contact marginality is 0.68 and no `[GT]` inside #11 §3.4.5's ranges lifts the blend back over `CatchThreshold`'s 0.65 floor. **The ladder refuses, so the next action is an owner decision — improve contact geometry upstream (the §5.Z.22 commit gate's prediction error is what puts the ball at the envelope edge), or re-scale the divisor and widen the `[GT]` ranges — not a calibration run.** (b) **Parry placement**: `ComputeParryVelocity` reverses the incoming velocity and rotates it by a *deterministic* ≤ 0.20 rad, so a parry goes back out along the shot line toward the attackers; real keepers parry wide. It produced **zero goals** in both the pre- and post-fix corpora, so it is a realism defect with no current goal-rate cost — but it becomes load-bearing the moment (a) lands and pushes contacts out of the catch band. (c) A claimed ball is not held at hand height and the keeper cannot carry it (the parked ball settles under gravity); a genuine hand-carry needs the `BallStateType.Controlled` question answered first — that state has **zero production call sites** and no exit path. Full detail and tables: `docs/tracking/gk-conversion-at-contact-design.md` §4 and §7. **✅ OWNER DECISION, August 17, 2026: STAY PARKED until W1's rush geometry is MEASURED.** Recorded at the design supplement as **KD-CC6a** (`gk-conversion-at-contact-design.md` v1.1). The §4 ladder's refusal — no `[GT]` inside #11's own ranges recovers the catches — was measured against a keeper who never left his line; W1 gives him his first production rush and moves the contact geometry the whole ladder is a function of, so re-fitting now is KD-W1's mistake with the names swapped. **The unparking condition is a measurement, not a landing:** specifically the close-range **conversion** comparison on identical seeds that `gk-rush-trigger-design.md` §6 names as still owed — NOT W1's rush anatomy, which WAS measured August 12, 2026: 23–46 rush intents committed per match against a pre-W1 baseline of exactly 0 by construction, keepers reaching 9.1–14.1 m off their own goal line, no `ERR-011-009` re-stall (§6 / v1.5; `match-engine-wiring-backlog.md` §5 row 1 records the discharge). Those are anatomy numbers, not an outcome comparison, and no pre/post conversion pair exists on identical seeds. *(Corrected August 17, 2026, same day, per adversarial-review finding H6 — as first written this clause said W1 "has never been executed", which had been false since August 12; see `gk-conversion-at-contact-design.md` v1.2.)* Until the conversion pair exists the recorded refusal is a fact about the pre-rush keeper and must not be quoted as a fact about the mechanism.

- **Close-chance creation — the DRIBBLE-direction defect is FIXED (ERR-008-018); REMAINDER: the ball still does not enter the penalty area, and the reason is now specific** — *re-filed August 4, 2026 (§5.Z.24); supersedes the August 3 entry, which superseded §5.Z.21's "possession churn" framing.* **Two premises checked; the first survived** — re-counted with a 1 s exit dwell over six full matches, final-third entries are **311 episodes against 312 raw crossings**, so the ~3×-football entry rate is real and the 6.5% denominator was never chatter. The second premise located the stage without naming a mechanism, and the instrument found two. **(a) Nobody is in the box:** mean attacking outfielders inside the penalty area while the ball is in the final third is **0.11**, with 92% of samples at zero — and the deepest *composed target slot* is **22.8 m** from goal against a deepest *attacker* at 22.2 m, so the players are within 0.6 m of where they are told to be and are simply **never asked into the box**. **(b) The carrier walks the ball out:** DRIBBLE is the modal attacking-third action at **40%** of heartbeat decisions with a mean cosine to the opponent goal of **−0.302** and only 31% pointing goalward. **That half is closed** (ERR-008-018 — #8 §3.1.5.2 delegated the directional-to-goal modifier to "the scoring stage (§3.2.2)", which is the **PASS** formula, so §3.2.4.1 never received it; `DirectionQuality_DRIBBLE` added. Measured cosine **−0.302 → +0.006**, goalward **31% → 49%**, moving on all six seeds with no overlap). **What remains open:** (1) **the funnel did not move and is not claimed** — box occupancy 0.11 → 0.10, ball into the box 6% → 5% of episodes, passes into the box 1% → 0%, shots 19.3 → 19.5, goals 3.67 → 3.50; the residual shot-count gap is **NOT** closed. (2) **The real bound is that #8 cannot pass to a place, only to a player** — §3.1.3 generates one PASS candidate per visible teammate *at that teammate's current position*, so there is no pass-into-space, no through-ball-to-a-run and no cross-to-an-arriving-header; passes into the box measured 1% at **every** rung of the ladder, including rungs where players did reach the box. That is the next lever and it is a §3.1.3 generator change, not a `[GT]`. (3) **`DRIBBLE_GOAL_DIR_MIN_MODIFIER` is pinned at 0.80 by a defect in a different action** — HOLD has no timeout, so suppressing the dribble pushes the carrier onto it (share 20% → 23% at 0.80, → 31% at 0.50) and at floors 0.65 and 0.50 one seed in six stalled outright (mean final-third episode 5.1 s → 17.5 s / 28.6 s). (4) **#15 §4.5.2's run overlay was implemented, measured and REFUSED**: it moves the committed RUNNER's target 80.9 m → 14.7 m from the attacked goal and box occupancy **down**, 0.11 → 0.08, because a RUNNER's target is `carrier + 12 m` and the carrier is usually still in midfield. (5) **#12 commits `InPoss` on only 9.5% of final-third samples** (`TransToAtk` 58.3%) because `PossessionOwnerEntityId >= 0` is false for the whole flight of every pass — every phase-gated mechanism in #13/#14/#15 is gated behind a state the engine rarely occupies. (6) #15's TRANSITION branch never republishes per-agent intents, so `GetIntent` serves stale ones; and `RunParameters.RunTriggerTick` is inert because run params are regenerated every heartbeat. Full detail, per-seed tables and the ladder: `docs/tracking/close-chance-creation-design.md`. **Amended August 7, 2026 (Acceptance-3, design v1.1): the ERR-008-021/-022/-023 shot-lane chain partially undid the headline fix on the acceptance seeds** — pooled dribble cosine +0.08 → **−0.119**, and per-seed it is one seed entirely (0x0F1E…78 held +0.078; 0xD1A6D05E returned its whole gain, −0.232 vs −0.221 pre-fix). The scenario band was rebaselined −0.10 → −0.16 by owner call so main is green against today's intended baseline; **the regression itself is now part of this entry's remainder**, owned by the KD-W1 calibration pass alongside items (1)–(6).


- **Foul/card rate — the "~7 red cards per 9 minutes" claim this entry carried for months is STALE and is corrected here, and the re-measurement it prompted found a real drift.** — *opened July 26, 2026 (§5.Z Phase H landing); corrected August 13, 2026 (#44 pre-implementation council).* The 7-reds figure was **RESOLVED July 26, 2026** by the §5.Z.9 foul-discipline balance pass (`foul-discipline-balance-design.md`; `match-engine-design.md:1003` records the resolution verbatim), which took fouls 480 → 21.0, yellows 147 → 3.0 and reds 75 → 1.0 per 90. This entry simply outlived its own fix — flagged by the pre-implementation council at the #44 landing, which refused to size a suspension subsystem against a number nobody had re-measured. **Re-measured August 13, 2026** on the committed instrument (`FoulRateDiagnosticTests`, `TD_FOUL_DIAGNOSTIC=1`, 6 seeds × 54 000 ticks = one 90-minute equivalent): **fouls 35.0 / yellows 5.0 / reds 1.00 per 90** against football's ~22 / ~3.5 / ~0.25. **Fouls and yellows are both ~67% above their own post-fix figures** — the balance design's stated tripwire ("`FoulCallProbability` is a rate knob calibrated against *that* contact stream; if it changes, re-measure") fired and nobody was watching: C1 phase classification (Aug 8) moved possession-phase share 24.2% → 96.8% and final-third `InPoss` 7.5% → 40.8%, which is directly upstream of the cross-team contact stream. **Reds remain 4× football**, unmoved since July. **The acceptance bands cannot see any of this** — `MatchEngineDisciplineScenarios` caps fouls at 90, yellows at 20 and reds at 5, so reds could quintuple and eleven consecutive whole-tree PASSES would still read green; that is deliberate as a plausibility floor and worthless as a calibration signal. **Not fixed here, by KD-W1**: `defensive-ai`'s tackle challenge ships at `TackleContactRadiusM = 0` (W2), so today's foul population is pre-tackle and arming W2 routes ~47 challenges per team per 90 into the same single foul-candidate slot. This is now the most load-bearing open realism item — #44 turns cards into suspensions, so a 4× red rate is a 4× suspension rate. **SEQUENCING DECIDED August 15, 2026 (owner call), RATIFIED August 17, 2026 and now recorded in the owning design supplement as `foul-discipline-balance-design.md` §7 item 3 (v1.2): arm W2 FIRST, then calibrate ONCE — do not fit `FoulCallProbability` to today's numbers.** Today's 35.0 / 5.0 / 1.00 is a measurement of a *pre-tackle* contact stream, and arming W2 changes the population the knob acts on, so a fit landed now would be re-fitted immediately and its intermediate value would sit in the tree looking calibrated. That is KD-W1 applied literally — the rule exists for exactly this shape, and the July-26 pass is the counter-example: it fitted against the contact stream of its day, C1 moved that stream in August, and the fit silently drifted 67%. **The consequence to accept while this waits:** the drift stays live, so the card rate — and therefore the suspension rate #44 now derives from it — is knowingly wrong until the W2 calibration pass runs. The acceptance bands will keep reading green throughout, since they are a plausibility floor, not a calibration signal.

- **`EnvironmentFingerprint.floatModelHash` — hasher + §4.8.3 Mono mapping LANDED (Option A); §4.8.2 runtime MXCSR gate code LANDED (July 21, 2026); compiled plugin + certified live read LANDED July 22, 2026 (ERR-016-006) — REMAINDER: `SaveManager` still writes `Fingerprint = null`; load-bearing only where a real cert run reads a `SaveManager`-written save — no longer host-blocked (the certification host block cleared July 19, 2026 and the MXCSR plugin host block cleared July 22, 2026); the gap is unimplemented code, not host access** *Re-filed August 2, 2026: stays ACTIVE. Every landing the title announces is real, but the entry's own closing sentence records a live remainder — the fingerprint is written as `null` and is load-bearing only at a real cert run reading a `SaveManager`-written save. **Correction (August 3, 2026):** this note previously said `certification-platform.md` "still marks ⏳ RECERT REQUIRED" — stale; the file has been v1.4 ✅ PINNED since July 19, 2026 (see the July-19 UPDATE below), so the certification host block is not what keeps this remainder open. Title amended so the index shows what is open rather than what is done.* — *opened July 19, 2026; Option A landed same day; MXCSR gate July 21, 2026.* The `SessionManifest` requires an `EnvironmentFingerprint` (#16 §4.8), whose `floatModelHash` (§4.8.3) is `SHA-256(SerializeCanonical(0x14 ‖ floatFlagTuple))` over an 11-field compiler/runtime float-mode tuple. **Original gap:** no code computed that hash for a live host (the field was a plain constructor string; the only factory, `CreateStage0Dev()`, stamped a `STAGE0_DEV_PLACEHOLDER` sentinel — `ComputeDigest()` hashes the *outer* 6-field fingerprint, a different digest), and the tuple's fields 1–4 (`compilerToolchain`/`compilerVersion`/`targetTriple`/`il2cppVersion`) were IL2CPP-shaped while §5.5 row 0 / §4.8.3 field 4 required IL2CPP and rejected `"MONO"` — contradicting the **Mono** Stage-0 backend pinned in `certification-platform.md` v1.3 (the spec text May 3–4 predates the June-7 pin). **Resolved via Option A (owner sign-off July 19, 2026; `docs/tracking/env-fingerprint-float-model-hash-mono-mapping.md` v0.2 §7):** the §4.8.3 tuple is mapped onto the Mono backend, keeping the 11-field shape. **Spec (`section-4.md` v1.1 / `section-5.md` v1.1):** field 1 gains `"Mono"`; field 4 flips so Stage-0 certification ACCEPTS `"MONO"` (reject-MONO / IL2CPP-required → Stage 5+); a "Stage-0 Mono backend mapping" paragraph pins fields 1–4 (toolchain `"Mono"`, compilerVersion = host-supplied Mono version, targetTriple RID `"win-x64"`, il2cpp `"MONO"`); §5.5 row 0 backend → Mono + §5.5.1 Mono note. **Code:** new `src/deterministic-sim/FloatFlagTuple.cs` (`ComputeHash()` = the live-host hasher) + `EnvironmentFingerprint.CreateStage0MonoCertified(monoRuntimeVersion)` (v1.3) — a genuine, non-placeholder fingerprint from the Option-A fields + the §4.8.3 Required Stage-0 flag values; `CreateStage0Dev()` fixed `"SSE2"` → `"SSE4.2"` (v1.2) + named `FloatModelHashDevPlaceholder` sentinel + `IsDevPlaceholder` gate. Golden vector (`89f50a31…f343e7`, Python-mirror-verified) + determinism/sensitivity tests in `DeterministicSimTests`. **`monoRuntimeVersion` (field 2) is host-supplied — deliberately not synthesised.** **UPDATE July 19, 2026 — (b) the full platform-determinism certification is now DONE:** the determinism-KAT run executed on the pinned Windows 11 / Unity 6000.4.9f1 / DX11 / Mono host (commit `819f9d1`, `TacticalDirector.DeterministicSim.Tests` under Unity Test Framework EditMode) — all three golden-vector corpora (#16 §9.5 #4 a/b/c) + the §5 determinism-tier locks pass byte-exact (44 passed / 0 failed / 4 Stage-0+1-deferred skips); `certification-platform.md` v1.4 → **✅ PINNED**, `FR-DS-009-GATE` closed. Run record + raw NUnit evidence: `docs/specs/deterministic-sim/cert-runs/determinism-cert-2026-07-19.md` (+ `determinism-results-2026-07-19.xml`). **Remaining host-blocked / consumer-blocked:** (a) the §4.8.2 **runtime MXCSR validation** — the *code* LANDED July 21, 2026 (`src/deterministic-sim/native/mxcsr_query.c` STMXCSR shim + `MxcsrNative.cs` P/Invoke + `MxcsrValidator.cs` decode/gate, called at `MatchEngine` boot AND `RestoreFromSnapshot` step 0 — the snapshot-deserialize/restore path that gives it its consumer now exists; full dotnet gate PASSED). It *enforces* the now-certified pin as defense-in-depth (a no-op off the pinned host / without the compiled plugin), and is not part of *proving* the bits exact (the KAT run is that proof). The compiled `td_mxcsr` plugin build + the certified live golden read on the pinned host LANDED July 22, 2026 (live MXCSR `0x1FBF`; mode fields DAZ/FTZ/RC match the Stage-0 pin; `ValidateStage0FloatMode()` → `Validated`) — evidence `docs/specs/deterministic-sim/cert-runs/mxcsr-live-mode-cert-2026-07-22.md`, plugin `src/deterministic-sim/native/td_mxcsr.dll` (see `src/deterministic-sim/native/README.md`). No host-block remains on the MXCSR gate. **UPDATE July 19, 2026 — the FR-PO-052 perf-baseline certified capture is DONE:** the pinned-host 100-run capture (p50=0.4768 / p99=2.5669 ms/tick) was promoted PENDING → CERTIFIED (`docs/specs/performance-optimization/baselines/match-engine/kickoff-multi-second.cert.md` v1.2; code entry `CertifiedPerfBaselineTests.KickoffCertified()` swapped `Pending()` → `Certified()`), using `CreateStage0MonoCertified("mono-bundled-unity6000.4.9f1")` for the host-supplied Mono version (§4.8.3 field 2 — Unity versions its forked Mono runtime by editor release) ⇒ `floatModelHash 73c47ad5…b6bb756a` (golden-vector-validated Python mirror); Platform Certification owner sign-off recorded via the PR merge. The recorded tuple uses the pinned Stage-0 flag values, which is exactly what (a) validates against. **Latent, not live:** `SaveManager` still writes the header `Fingerprint = null`; the fingerprint is load-bearing only at a real cert run. Distinct from ERR-016-005's follow-up (the *outer* envFp golden vector).

- **Goalkeeper Mechanics (#11) / Heading Mechanics (#10) engine integration — Phase 1 (opt-in) LANDED; the GK/Heading attribute projections now have a live consumer — REMAINDER: `CollisionConsumer` AGENT_BALL duel fan-out, DT-emitted HEADER (ordinal 8 → composure-noise rebaseline), attribute-modulated save commit** *Re-filed August 2, 2026: stays ACTIVE. Phase 1 landed; the entry's "Still deferred" list is the open work. Title amended to name it, since the index previously showed only the landing.* — *opened + Phase 1 landed July 22, 2026.* The `ToGoalkeeper`/`ToHeading` projections `player-attribute-projection-design.md` deferred under KD-P8 (as phantom consumers — `MatchEngine` built neither struct) are now non-phantom: #10/#11 are wired into the engine, and the projections seed the intents. Governed by the new converged supplement `docs/tracking/gk-heading-engine-integration-design.md` (AR-1 1M+2L → AR-2 CONVERGENCE → AR-3 scope revision to opt-in phasing; code self-AR folded CS0118 fully-qualification, `_gkAgentIds` refresh across `ConfigureSquads`/subs, and guard placement). **Phase 1 (this landing):** `MatchEngine.cs` v1.44 constructs both sealed orchestrators + four stateless ball/RNG adapters at boot and registers `heading.mechanics` + `goalkeeper.mechanics` RNG streams (the card-severity precedent, KD-1; `entityId: -1`, `SubsystemOrdinals.HeadingMechanics`=6 / `GoalkeeperMechanics`=7). A new public `EnableGkHeading()` opts in (KD-11, default off): while off the engine is **byte-identical to pre-wiring** (no `SNAPSHOT_SCHEMA_VERSION` change — verified by the unchanged 279-test existing snapshot/determinism/restore suite); while on, `DriveGkHeadingTactical` (10 Hz, in `RunAiPhase`) + `DriveGkHeadingPhysics` (60 Hz, in `RunPhysicsPhase` before the Resolve goal check) drive both, and conservative Stage-0 world-state triggers (§4 — the `MatchFlowCollisionConsumer` heuristic-foul precedent) commit a `SaveIntent` seeded from `ToGoalkeeper` (loose on-target ball near the defended goal) and a `HeaderIntent` seeded from `ToHeading` (nearest agent to a loose airborne ball) — the projections' live consumer. The two RNG adapters draw from the single per-subsystem stream, accept-and-ignoring the draw-site id per the Stage-0 stub posture (KD-3). A flag-on engine is deterministic FORWARD but not yet snapshot-safe, so `CaptureDurableHeader`/`CaptureDurablePayload` **fail loud** (`NotSupportedException`) when on (KD-11 / §6) — the per-tick digest `SerializeWorldState` is untouched, so a flag-on engine still ticks. New `src/match-engine/PlayerAttributeProjection.cs` v1.2 (`ToGoalkeeper` int→float widen of the ten GK fields; `ToHeading` raw copy of Heading/Strength/Balance; runtime TeamId/Fatigue per KD-P4), `MatchEngineConstants.cs` v1.25 (+6 `[GT]` trigger constants). New `MatchEngineGkHeadingTests` (8 — flag semantics, flag-off default determinism + commits-nothing, save/header commit the projection, distinct-squad roster GK Pace flows through, flag-on forward determinism, durable-capture fails-loud-on / succeeds-off) + `PlayerAttributeProjectionTests` +2 (ToHeading/ToGoalkeeper field-scale). **Full dotnet gate: PASSED, 0 failures (whole tree green; 290 match-engine tests; SDK 8.0.129 via apt).** **Phase 2 cross-tick state serialization LANDED July 23, 2026 (`MatchEngine.cs` v1.46 — a flag-on engine is now snapshot-safe):** the GK (#11) / Heading (#10) cross-tick state is serialized at **`SNAPSHOT_SCHEMA_VERSION` 17 → 18**, so `EnableGkHeading()`-on save/restore round-trips deterministically and the Phase-1 `RequireGkHeadingSnapshotSafe` fail-loud guard (+ its two call sites) is removed. New typed value carriers `GoalkeeperTickState` (`src/goalkeeper-mechanics/`) + `HeadingTickState` (`src/heading-mechanics/`) with `CaptureState()`/`RestoreState(in …)` seams on both orchestrators (the `PressingTickState` Option-B convention — the orchestrator returns a typed carrier over its live arrays, `MatchEngine` owns 100% of the byte layout). The v18 block (`MatchEngine.WriteGoalkeeperState`/`ReadGoalkeeperState` + `WriteHeadingState`/`ReadHeadingState`, written UNCONDITIONALLY after the v17 card cursor) = the opt-in flag itself (so a restore reproduces the engine's mode) + the two subsystem RNG-stream cursors (`heading.mechanics` / `goalkeeper.mechanics`, RngCursor + ActionOrdinal, the card-severity precedent) + the two §4 trigger latches (`_saveCommittedForGk` / `_headerCommittedThisEpisode` — a completeness finding surfaced during implementation: engine-level cross-tick state that GATES trigger re-commits, so a restore that omitted them would re-fire a suppressed trigger and diverge) + both orchestrators' full in-flight arrays via the seams. Forward-determinism completeness proof: both orchestrators hold ONLY `readonly` deps + `readonly` arrays (no scalar mutable field), so the array inventory is provably the complete cross-tick surface; the excluded `_lastCommitted*Attrs` are write-only TestOnly observation and `_gkAgentIds` is rebuilt each drive (`RefreshGkAgentIds`). Flag-off is a deterministic no-op through the block (the version bump moves the digest; comparative round-trip contract, no absolute-golden rebaseline). New `MatchEngineSnapshotRestoreTests` flag-on round-trips (neutral + with a committed save advancing the GK RNG cursor / latches / `GkContactState` arrays — the omit-a-field-and-diverge completeness lock), `MatchEngineSnapshotSchemaTests` pin 17 → 18 + `GkHeadingState_FeedsSnapshotDigest` probe, `MatchEngineGkHeadingTests` durable-capture-succeeds (replacing the Phase-1 fails-loud lock). **Full dotnet gate: PASSED, 0 failures (whole tree green; 304 match-engine tests; SDK 8.0.129 via apt).** **The closed-loop `#19 ScenarioRunner` scenario LANDED July 23, 2026** (governed by the new converged supplement `docs/tracking/gk-heading-scenario-design.md` v1.0; AR-1 outline 3M+2L → AR-2 plan 3L → AR-3 code 3L, all CONVERGED): new `src/match-engine/tests/MatchEngineGkHeadingScenarios.cs` + `MatchEngineGkHeadingScenarioTests.cs` register `gk-heading-flag-on-composition` (Tier B; owning specs `{2,10,11,16,19}`; path under `SCENARIO_PATH_CROSS_SPEC_PREFIX`) — the Simulation-layer composition the unit suite does not exercise. Predicates: flag-on free-play forward determinism (two same-seed runs, byte-identical digest chains) + **flag-on restore-through-the-harness** (save@180 → `MatchSaveManager.Encode`/`Restore` in-memory blob → tick to 300 == uninterrupted flag-on run, which transitively locks Phase-2 v18 flag-on snapshot completeness) + structural on-pitch/finite invariants across a 5 s composed run (Agent Movement #2 bound) + the two projections firing as a **live consumer through the natural `RunTick` AI phase** (the scenario scripts only the world stimulus via `TestOnly_ForceBallLoose`, then lets `RunAiPhase → DriveGkHeadingTactical` commit — NOT the `TestOnly_Drive` seam) + a same-stimulus flag-off no-commit contrast (non-vacuous). No production change; no `SNAPSHOT_SCHEMA_VERSION` change. **Full dotnet gate: PASSED, 0 failures (whole tree green; 305 match-engine tests; SDK 8.0.129 via apt).** **The DT-driven SAVE producer LANDED July 23, 2026 (ERR-008-013)** — the goalkeeper save is now a first-class Decision Tree (#8) action (`ActionType.SAVE = 7`, the DT-emitted save the #11 `SaveIntent` doc always anticipated), superseding the engine-side heuristic save trigger. Governed by the new converged supplement `docs/tracking/gk-heading-dt-producer-design.md` (Option-B outline AR-1 killed as unsound — the DT off-ball PRESS/INTERCEPT model run-to-the-ball, inverting the save; owner chose Option A; outline AR-2/AR-3, plan AR-4 (a real missed-save defect: `ScoreSave=ceiling` isn't robust — `INTERCEPT` can reach the clamp ceiling via `MentalityRiskMult`×`RoleWeightModifiers` and win the lower-ordinal tiebreak → fixed by emitting SAVE as the **sole** off-ball option)/AR-5, impl AR-6 all CONVERGED). New `TacticalContext.SaveAvailable` (set only for the threatened keeper under `EnableGkHeading`, from `GkHeadingIntentSource.SaveArmed`); `OptionGenerator` short-circuits the off-ball branch to SAVE alone; `UtilityScorer.ScoreSave` + a SAVE exemption from `PlayerTacticActionMultiplier` (its 7-wide #21 tables would OOB at ordinal 7); new `IDtSaveDispatch` seam + `ActionDispatcher` SAVE case + `DecisionTree` ctor param; `MatchEngine.HostSaveDispatch` maps agent→GK slot, applies the v18 latch, projects `ToGoalkeeper`, commits the same Stage-0 `SaveIntent`; `DriveGkHeadingTactical` drops `TryCommitSaveIntents` (header stays heuristic). SAVE is continuous for free (no state-machine edit); **no `SNAPSHOT_SCHEMA_VERSION` change** (SAVE reuses `AgentAction.Type`/`TargetPosition`); flag-off byte-identical. New `OptionGeneratorTests` sole-option lock + `UtilityScorerTests` crash-guard lock + `MatchEngineGkHeadingTests` DT-path save-commit + `SaveDecision_SurvivesAdversarialTactic`. Spec back-prop `ERR-008-013` (spec-error-log.md v1.34) + `decision-tree/section-3-1.md`/`section-3-2.md` anchor notes. **Full dotnet gate: PASSED, 0 failures (whole tree green; DecisionTree 84, MatchEngine 306; SDK 8.0.129 via apt).** **`EnableGkHeading` default flipped to ON July 27, 2026** (§5.Z.15, as part of the match-realism pass below — `DisableGkHeading()` added for hosts/tests that want the old path), and the keeper given locomotion via the existing per-agent `AgentMovementSystem.Update` seam (no new locomotion model needed); flipping the default surfaced a real wiring gap (the engine booted the Pass/Shot/DecisionTree `EventBusRegistrar`s but never the Heading/Goalkeeper ones — fixed same commit). **Still deferred:** the `CollisionConsumer` AGENT_BALL duel fan-out (for contested headers), a DT-emitted HEADER (ordinal 8 overflows the 3-bit composure-noise field → rebaseline), and attribute-modulated / hesitant save commit — the save quality itself (not just its existence) is now the recorded next lever on the goal rate, see the match-realism OPEN ISSUES entry below.

- **Advanced positional behaviors + game-model/AI-manager tactics — design supplements OPENED (candidate specs #23–#26) — all four promoted to specs and landed; REMAINDER: #26 §9.2 own-`[GT]` balance review** *Re-filed August 2, 2026: stays ACTIVE, but only for the tail. The four candidates scoped here are now specs #23–#26, all promoted and implemented (#23–#25 in `src/positioning-ai/`, #26 in `src/tactical-instructions/`), and the entry's own tail records the #26 §9.1 engine-substrate gates as CLOSED. What is left is the §9.2 `[GT]` balance review. Title amended so the index stops advertising an opened design supplement that has since become four approved specs.* — *opened July 7, 2026, later same day.* Follow-up to the entry above: the four items assessed there as "medium-large / genuinely new capability, not cheap seam-reuse" (dismarking, scripted build-up-phase structures, positional rotations, and the game-model/AI-manager preset-selection layer) are now scoped in two new design-supplement documents, at the same pre-promotion DESIGN SUPPLEMENT stage `tactical-instruction-layer-design.md` and `living-world-system-design.md` occupied before becoming Specs #21/#22 — **no code, no section files, not yet reviewed or approved.** **(1) `docs/tracking/advanced-positional-behaviors-design.md` v0.1 (→ v0.3 after AR-1 + implementation plan below)** — joint scoping note for dismarking (candidate **#23**), scripted build-up structures (candidate **#24**), and positional rotations (candidate **#25**); all three extend #7 Perception System / #8 Decision Tree / #12 Positioning AI. Candidate KD-2: dismarking must derive its marking-pressure signal from `PerceptionSystem.FilteredView` (proximity + dwell), never from reading the opposing team's internal `MarkAssignment.TargetEntityId` directly — an explicit **perception-boundary invariant** (candidate KD-5) that applies to all three items and should be cited verbatim in each eventual spec's §1. Candidate KD-4 flags positional rotations (dynamic `FormationSlotRecord`/`SlotIndex` reassignment mid-match — nothing today reassigns a slot after `SeedFromFormation`) as the largest and riskiest of the three. **(2) `docs/tracking/game-model-ai-manager-design.md` v0.1 (→ v0.4 after AR-1/AR-2 + implementation plan below)** — scoping note for a tactical-preset library over the existing `TeamTacticConfig`/`PlayerTacticConfig` substrate (Spec #21; already parser-swap-ready and default-identity-safe) plus AI-manager selection/adaptation logic, staged as candidate **#26** (one spec, T0–T4 phases, mirroring #21's own T-phase staging — not two specs, since both halves share one consumer path through the existing `SetTeamTactic`/`SetPlayerTactic` appliers). Candidate KD-2: manager decisions run on a new, coarser cadence (kickoff / half-time / fixed interval) — never every 10 Hz AI stride. Candidate KD-4 (mirrors positional-behaviors KD-5): a manager AI may not read an opponent's private `PlayerTactic` state directly; opponent-aware adaptation is explicitly flagged as a candidate deferral to avoid building a consumer for canon that doesn't exist yet (the same FR-LW-031 phantom-interface reasoning `BackgroundTierSim` cites). **Candidate spec numbers #23–#26 are reserved in both design notes' own §5 tables to prevent a future renumbering collision, but are deliberately NOT added to `SPEC_INDEX.md`'s registry table yet** — per that file's own "Before creating a new spec folder, add the entry here first" rule and the #21/#22 precedent (registry row added only at promotion to section files, not at design-note stage); `SPEC_INDEX.md` instead gains a small NOTES-section reservation line so the numbers cannot be silently reused. **Adversarial review of the design-supplement text: AR-1 (0H+0M+2L, resolved same day; findings
verified against actual source files, not narrated)** — L-1: `advanced-positional-behaviors-design.md`
KD-3 originally cited a generic `TacticalInstructions #21 TransitionPlan` field as the
build-up-structure gating dial; grepping `src/tactical-instructions/TeamTactic.cs` shows the
actual fields are the pair `TransitionWon`/`TransitionLost` (§3.2/FR-TI-020, one enum type
`TransitionPlan` backing two distinct properties) — corrected to cite `TeamTactic.TransitionWon`
specifically (the "plan on winning the ball" dial, the one conceptually tied to
build-up-after-regain, fixed in both KD-3 and the parallel §1 table cell), and confirmed by grep
that it is serialized (`MatchEngine.WriteTeamTactic`) but has no AI-side consumer anywhere yet, so
the seam claim holds. L-2: `game-model-ai-manager-design.md` KD-1 stated a preset is applied via
`TeamTacticConfigApplier.Apply`/`PlayerTacticConfigApplier.Apply` unconditionally; reading
`TeamTacticConfigApplier.cs`'s own doc comment shows `Apply` is scoped to **"before kickoff"**
only — the mid-match AI-manager-adaptation case (T4) must call
`MatchEngine.SetTeamTactic`/`SetPlayerTactic` directly instead; KD-1 and the T-phase table (§3)
corrected to state this boot-vs-mid-match distinction explicitly. **Then both notes' §6
Implementation Plan sections were authored** (spec-promotion pipeline + T-phase code-landing
sequence for each candidate) — self-review while drafting the FR-ID-prefix assignment step caught
a real collision: the proposed `FR-PR-*` prefix for Positional Rotations (#25) is already
Pressing AI's (#13) prefix (grep-verified against `docs/specs/**/*.md`'s existing `FR-[A-Z]+-`
prefixes: AT/CS/DA/DS/EVT/GK/HE/LW/PA/PO/PR/TI/TS); corrected to `FR-RO-*` before it could
propagate into an eventual spec. **AR-2 (0H+0M+1L, resolved same day)** — a further fresh-eyes
pass over `game-model-ai-manager-design.md` KD-2 found its event-triggered-re-evaluation claim
overstated: it read as if goal-scored re-evaluation were available today with only
red-card/substitution deferred, but grepping `src/match-engine/MatchEngine.cs` shows **zero**
producers for `GoalAwardedEvent`, `CardIssuedEvent`, or `SubstitutionEvent` (only
`PossessionChangedEvent` is wired) — corrected to defer all three event-triggered points
uniformly. **AR-3 (0H+0M+0L) — CONVERGENCE**: full re-read of both finished documents end to end;
re-checked KD numbering has no collision with the closed cheap-item entry's KD usage (both design
notes use locally-scoped KD-N numbering per file — KDs are never globally numbered across
documents); re-checked the reserved-number table in both notes cites the sibling note correctly
(no duplicate or skipped candidate number across #23–#26, and no remaining `FR-PR-*` reference
anywhere in either file); re-checked neither note's file header claims an approval status
stronger than DESIGN SUPPLEMENT; re-verified via grep that `AgentHysteresisState`,
`RestDefenseEvaluator.cs`, `ShapeAnalyzer.cs`, `MarkAssignment.TargetEntityId`,
`AgentPositioningData.SlotIndex`, `IsAiStrideTick`, and the `TeamTacticConfigApplier`/
`PlayerTacticConfigApplier`/`SetTeamTactic`/`SetPlayerTactic` call shapes (all cited as existing
precedent/hazard anchors) exist with the claimed shape; found nothing further. Both notes now sit
at `advanced-positional-behaviors-design.md` v0.3 and `game-model-ai-manager-design.md` v0.4.
**Next step:** none outstanding for this entry's own scope — the detailed implementation plans
requested are the §6 sections just landed; further work is each candidate's own spec-promotion
pipeline (§6 step 1 onward in each note), not tracked here. **PROMOTED July 8, 2026:** all four
candidates authored as full 11-file section-file sets at `IN REVIEW` (§6 steps 1–3 complete) —
`dismarking-ai/` #23 (FR-DM: `MarkingPressure` = perception-derived proximity×dwell; #12
`SlotComposer` dismark offset stage + #8 awareness-scaled marked-pass-target penalty; KD-1 cites
the perception-boundary invariant verbatim), `build-up-structures/` #24 (FR-BU: 3-zone
team-relative classifier + hysteresis; per-structure `[GT]` offset overlay; **KD-3 deliberately
refines the supplement** — opt-in `BuildUpStructure` dial + `TransitionWon` post-regain
suppression window, because `Balanced` already sets a non-`CounterAttack` `TransitionWon` and a
pure `TransitionWon` gate would break default-neutrality), `positional-rotations/` #25 (FR-RO:
static per-family adjacency table, both-sided organic-exchange trigger, two-sided hysteresis with
the `ROTATION_HOLD_TICKS ≥ line-dwell` `[DERIVED]` non-interference bound, atomic pairwise
`SlotIndex` swap + partner lock, permutation-validated restore; the #12 "`SlotIndex` immutable
after `SeedFromFormation`" contract amendment is an explicit pending back-prop), and
`tactical-presets/` #26 (FR-TP: preset = named point in #21's pinned parameter space; decision
gate (not a clock file) at kickoff/half-time/5-min intervals on the stride boundary; own-state
scoring + one-rung ladder with hold intervals; boot via existing appliers, mid-match via
`SetTeamTactic` — the supplement's AR-1/AR-2 corrections carried into FRs). All four: zero-value
dial = identity (`Off`/`None`/`Human`), byte-identity determinism tests specified, away-mirror
tests where geometry is team-relative (#24/#25), no RNG/domain tags, `[GT]` magnitudes
illustrative pending balance pass (#21 G2 precedent). **PASS-1 adversarial reviews run July 8,
2026 (later same day), all findings resolved in same-day v0.2 fix passes:** #23 0H+1M+3L (dwell
update pinned to the per-agent perception pass, one-stride-stale #12 consumption — the
RestDefense same-tick analogy does not transfer to `FilteredView`-derived signals); #24 0H+3M+2L
(team-level-regain arming — the per-agent `PossessionChangedEvent` fires on teammate receptions;
committed-zone-expansion hysteresis; lane-key corrections); #25 1H+1M+3L + PASS-2 clean at H/M
(serialized `LastComposedTarget` cache — `AgentPositioningData` has no composed-target field and a
restore re-seed broke byte-identity; phase-exit freeze-vs-reset contradiction fixed;
`LINE_DWELL_TICKS = 5` verified); #26 0H+1M+2L (explicit engine-substrate gates for score/halves
state + `MATCH_TICKS_TOTAL` `[CROSS-PENDING]`; Appendix E sensitivity values re-derived). **Open
gates per spec §9.3 (updated July 10, 2026 — closable gates closed):** §8 citation rows CLOSED
for #23 (both VERIFIED — Wilson ISBN 978-0-7528-8995-5; Low et al. DOI
10.1007/s40279-019-01194-7), for #24 (Wilson verified; Spielverlagerung reclassified informal
background), and for #25 (Wilson verified; Memmert & Raabe REPLACED with the verified Low et
al. 2020 review per the OI-003 precedent); for #26 partially closed (Wilson verified; the
Bradley score-line row remains pending with a recorded environment-blocked July-10 attempt).
The #25 Appendix A is COMPLETED (A.2 4-3-3 + A.3 4-2-3-1 authored against the verified
`Family433`/`Family4231` rosters, F1 hand-audited). The #26 A.1 member names are PINNED against
the #21 enums (PASS-1 L-2). **ALL FOUR SPECS APPROVED July 10, 2026 (later same day):**
R-01..R-05 sign-off granted (checklists v0.4); the #26 Bradley citation VERIFIED (Bradley &
Noakes 2013, *J Sports Sci* 31(15):1627–1638, DOI 10.1080/02640414.2013.796062, PMID 23808376);
the seven back-prop ERRs filed and landed atomically (`spec-error-log.md` v1.30:
ERR-021-005/006/007 `TeamTactic` field + Appendix B appends in pinned order #23 → #24 → #25;
ERR-012-007/008/009 `positioning-ai/section-3.md` v0.6 §3.7.1 pipeline amendments incl. the
`SlotIndex` single-writer contract; ERR-008-012 `decision-tree/section-3-2.md` v1.5 §3.2.2.1
marked-pass-target anchor). `SPEC_INDEX.md`: 26 APPROVED / 0 IN REVIEW. Remaining follow-ups are
post-APPROVED and non-blocking: the `[GT]` balance passes (#21 G2 pattern), the #26
engine-substrate gates (upstream-owned), and implementation per each spec's §6 T-phase plan.
**T0 scaffolding LANDED July 10, 2026 (latest same day), behaviour-neutral:** the #21 back-prop
code side (`TeamTactic` gains the three dials via defaulted ctor params in pinned Appendix B
order; NOT serialized — `WriteTeamTactic` + schema bump land at each spec's wiring;
`TeamTacticFileLoader` gains the three keys, omitted ⇒ identity); #23 `MarkingDwellState` +
`MarkingPressureEvaluator` (pure FM-DM-01/02 + dwell machine; primitive-span inputs — Mechanics
cannot import the AI-layer `FilteredView`, provenance enforced at the future match-engine call
seam per #23 §4.4); #24 `BuildUpZone(State)/Classifier/OverlayCatalogue` — implementation surfaced
**ERR-024-001 (H, filed + resolved same commit, spec-error-log.md v1.31)**: the Appendix A v0.2
row keys matched NO slot in any family table (fullbacks are recorded `DefaultLane` LH/RH, not wide
L/R — PASS-1 M-3 checked lane geometry, not the recorded key values), making the whole catalogue a
structural no-op; re-keyed to the recorded values (magnitudes unchanged), Appendix A v0.3 + §3.2
v0.3 patched, per-family regression test added; #25 `RotationPair(State)` +
`RotationAdjacencyCatalogue` (all three Appendix A v0.3 tables + F1 invariant tests + the
FR-RO-007 bound lock); #26 `TacticPreset` + `TacticPresetLibrary` (the five A.1 presets in the
pinned ladder order; KD-7 identity discipline locked; #26 §2.2.2's stale pre-ladder ordinal list
fixed, v0.3). New constants in `PositioningAIConstants` v1.2. 43 new tests across 4 suites. The
**full dotnet gate ran locally for the first time (SDK installed via apt): PASSED, 0 failures,
quarantine empty.** Next per the T-phase plans: #23/#24 SlotComposer stage insertions + #25
`RotationController` + serialization schema bumps + match-engine Phase-D writers; #26 T1
preset→config projection, T2 decision gate, T3 kickoff scoring, T4 adaptation. **T0 AR-1 fix pass
(July 10, 2026, latest same day): 0H+1M+3L, all resolved** — findings confined to the #26 preset
surface + one header defect: M-1 `TacticPreset` ctor snapshot-copies `Players` (the v1.0 ctor
retained the caller's LIVE array, so post-construction mutation bypassed the FR-TP-014 gate — the
living-world slice-2 AR-1 M-1 / match-viewer AR-3 M-1 defect class; latent at Stage 0, closed
before T1 builds the preset→config projection on it; regression-locked); L-1 the four composition
tests gain inherited-dial == Balanced locks (the `Compose`-defaults coherence was claimed but
unlocked for dials some presets touch); L-2 the FR-TP-014 docs re-anchored to the consuming
applier seam (no library-side validation call exists and the bottom-of-graph assembly cannot know
roster size — vacuously satisfied at Stage 0, test-locked); L-3 `TeamTacticFileLoader.cs` missing
`// Modified:` header (FR-CS-056). Everything else verified spec-exact with no change (#23/#24
worked examples incl. NaN-gate semantics, #25 Appendix A/D row-for-row, #26 A.1, the ERR-024-001
regression). Gate re-run: PASSED, 0 failures. See src/CLAUDE.md v2.12.
**#23/#24/#25 WIRING LANDED July 11, 2026** (the T-phase step after T0; all
default-behaviour-neutral — Balanced ⇒ Off/None/Off = the exact identities, byte-identical
default match): the #24 build-up overlay (Step 3b) + #23 dismark offset (Step 4b) `SlotComposer`
stage insertions per ERR-012-007/008; the #25 `RotationController` wired into `PositioningAITick`
per §4.2/ERR-012-009 (serialized `LastComposedTarget` cache per PASS-1 H-1; sole post-seed
`SlotIndex` writer; identity binding never rewrites a row); the #23 §3.4 marked-pass-target
penalty in the #8 `UtilityScorer` (awareness-scaled, Off ⇒ exact ×1.0); and the match-engine
Phase-D writers + **`SNAPSHOT_SCHEMA_VERSION` 11 → 12** (per-agent dwell / per-team zone+
settledTeam / rotation binding+cache+pairs serialized; the three dials appended to
`WriteTeamTactic` in pinned #21 Appendix B order; FM-BU-03 team-level regain arming in
`OnPossessionChanged`; 9 TestOnly seams). 28 new/extended tests across 5 suites. **Full dotnet
gate: PASSED, 0 failures.** See the July-11 Last-Updated header entry + src/CLAUDE.md v2.13.
**Next per the T-phase plans: #26 T1 preset→config projection, T2 decision gate, T3 kickoff
scoring, T4 adaptation** (T2/T4 remain gated on the upstream engine-substrate deliverables —
halves/`MATCH_TICKS_TOTAL`, goal detection — recorded in #26 §9.3).
**#26 T1–T4 WIRING LANDED July 11, 2026 (later same day)** — see the July-11 later-same-day
Last-Updated header entry + src/CLAUDE.md v2.14 for the full description. Default-behaviour-neutral
(Human zero-init identity, KD-4); the shipped decision gate fires kickoff + fixed interval only
(half-time gated on the engine halves model) and the live ladder call runs at the engine-TRUE
goalDiff = 0, so adaptation switches are unreachable until goal detection + `MATCH_TICKS_TOTAL`
land (the §1.6/§3.4 PASS-1 M-1 gates, honoured with one code path). `SNAPSHOT_SCHEMA_VERSION`
12 → 13 (per-team `ManagerState`, Appendix C order). Full dotnet gate: PASSED, 0 failures.
**With this, every §6 T-phase plan from the July-7 supplements (#23/#24/#25/#26) is implemented**;
what remains on #26 is upstream-owned (goal detection, halves/match-length model) plus the KD-6
on-disk preset format parser swap and the post-APPROVED `[GT]` balance passes.
**ENGINE SUBSTRATE LANDED July 11, 2026 (latest same day)** — see the latest Last-Updated header
entry: the match engine gained goal detection (v14 score state + `GoalAwardedEvent` + centre-spot
restart) and the match-length/halves model (`MATCH_TICKS_TOTAL` promoted `[CROSS]`); the #26
half-time trigger and live goalDiff/clock ladder inputs are ACTIVE (#26 §9.1 engine-substrate
gates CLOSED, checklist v0.5). Remaining #26 follow-up: only the §9.2 own-`[GT]` balance review.
The KD-6 on-disk preset format is NOT a doable follow-up at this stage — FR-TP-002/FR-TP-017 pin
"no disk format / no disk-loader interface at Stage 0+1" as MUSTs, so it waits for Stage 1 (unlike
the #21 tactic-file loaders, whose spec carried no such prohibition).
- **Living World (#22) season/world loop — slices 1–7 LANDED (incl. the KD-10 season composition root + the InteractionTextGenerator wired into it + deep-memory auto-cite + the opt-in arc-trigger evaluator / `world.arcs` sub-stream); upstream-gated services open** — *opened July 2, 2026; arc-triggers July 24, 2026.* The first KD-10 prerequisite ("persistent world store + season-calendar loop", #22 §7.1) is implemented in `src/living-world/`: `WorldClock` (KD-4/FR-LW-019 — one `worldTick` = one calendar day; distinct from `MatchClock`, never advanced by the 10/60 Hz match loops), `WorldLoop` (§4.2 per-tick phase order; phase 3 salience decay live, phases 1/2/4/5/6 documented seams — their producers (structured match-outcome events, vol-2/vol-3 human-systems, ArcEngine, BackgroundTierSim, the platform-tuned SAVE_SIZE_BUDGET) don't exist yet and FR-LW-031 forbids phantom interfaces), `MemoryStore` (canonical (FromId,ToId)-order edge store per FR-LW-021; §3.2 evict-before-append — the incoming episode is always remembered, matching the §3.2 worked example; FR-LW-018 arc pins with F1 dangling-id fail-loud + all-pinned transient growth shrunk on unpin; §3.1 owned-layer `ApplyEvent` with FR-LW-004 PlayerEdge refusal and F6 inactive-layer refusal), `ColdStore` (§3.5 `Compress`/`Rehydrate` pure transforms; **Residue-A v1 compression schema recorded** — NetRelationship = mean of active owned layers, PlayerEdge never summarised; FR-LW-009 episodeId high-water resume). New `Tests/SeasonWorldLoopTests.cs` (25 tests): T-LW-U-011..018, T-LW-I-011..014 incl. the F5 retained-fields round-trip (T-LW-FAIL-005), T-LW-DET-002/006/007 subsets, two-run field-identity determinism. **AR-1 (July 2, 2026, same day): 0H+2M+4L, all resolved** — M-1: pins were single-entry idempotent, so two arcs pinning the same episode lost protection when the FIRST resolved (the F1 dangling-episode class FR-LW-018 exists to prevent); pins are now reference-counted, exempt until the LAST arc unpins. M-2: `RemoveEdge` would remove an edge holding arc-pinned episodes and strand orphan entries in the pin table (a stale pin surviving a remove→compress→rehydrate cycle dangles or pins an unrelated retained episode); removal now refuses pinned edges per the §3.5 demotion-skip rule — also giving `EdgeHasPinnedEpisode` its production consumer. L: `GetOrCreateEdge` conflicting-mask refusal (was silently ignored); `InsertEdge` F6 gate (active layers finite in [0,1], NaN-gated); `ColdStore.Add` coherence gates (NetRelationship NaN-gate; retained id < NextEpisodeId or rehydration breaks FR-LW-009); `ValidatePair` names the offending parameter. +5 regression tests. **AR-2 (July 2, 2026, same day): full-surface sweep over all 17 `src/living-world/` files, 0H+1M+2L, all resolved** — M-1: `ApplyEvent`'s else-branch routed ANY out-of-roster `RelationshipLayer` cast to the Trust write, and the caller-supplied `ActiveLayers` byte was never validated, so an undefined mask bit made `IsLayerActive` pass for the junk layer — a silent write on a digest-relevant path; now an explicit owned-layer allowlist + `DefinedLayersMask` refusal at both edge-entry seams. L-1: the FR-LW-009 coherence gate was one-sided — `InsertEdge` accepted non-ascending/duplicate ids or ids ≥ `NextEpisodeId` (next `RecordEpisode` re-allocates a live id, breaking the durable-handle contract pins depend on); both seams now gate ascending-strict + high-water. L-2: episode salience NaN-gated to [0,1] at both seams. +3 regression tests (28 total). **AR-3 (July 2, 2026, same day): 0H+0M+3L, all resolved — CONVERGENCE (doc-only round), AR cycle closed** — L: `ApplyDecay`/`LAYER_*` declared-but-unconsumed doc-notes (production callers defer with the KD-10 phase-1/phase-2 wiring); `ColdStore.TryTake` destructive-removal ordering contract (verify-live-edge-first or a post-take `InsertEdge` throw strands the summary, FR-LW-025); `UnpinEpisode` guard documented defensive-only (every pin references a live edge). See src/CLAUDE.md v1.90–v1.93. **Slice 2 (July 2, 2026, same day): ArcEngine + ActiveSetMembership LANDED** — `ArcEngine` (§3.4 spawn steps 1–3 with SpawnCause captured inline per FR-LW-016/KD-8 and **atomic FR-LW-018 pinning** — an F1 dangling-id spawn rolls back every hold already taken; AdvanceState APPEND-only per FR-LW-028; ResolveArc unpin/remove with shared-pin refcount integration; §6.2 expiry sweep in deterministic spawn order). **Trigger evaluation + its `world.arcs` RNG draws remain the documented KD-10 seam** (FR-LW-020/031 — the triggers read vol-2/vol-3 canon that does not exist; a registered sub-stream with zero draw sites would be a phantom surface, so none is registered). `ActiveSetMembership` (§3.5, FR-LW-023/025): entry on first interaction; cold-history promotion honouring the ColdStore.TryTake verify-live-edge-first ordering (the AR-3 L-2 contract's first consumer); deterministic LRU demotion at ACTIVE_SET_EXTERNAL_CONTACTS_MAX (max episode worldTick, ties → lowest EntityId, arc-pinned edges skipped — all-pinned ⇒ transient over-cap); own-club at-club exemption with `Depart` demoting via the external path (pinned departure defers as external until the arc resolves). `WorldLoop` v1.1 wires phase 4 (arc expiry) + phase 6 (cap enforcement), null-injectable. New `Tests/ArcMembershipTests.cs` (19 tests). See src/CLAUDE.md v1.94. **Slice-2 AR-1 (July 2, 2026, same day): 0H+2M+4L, all resolved** — M-1: `SpawnArc` retained the caller's LIVE pin array, so post-spawn mutation desynchronised resolve/expiry unpinning from the holds actually taken (stale-hold leak = the F1 class, or a fail-loud on a never-pinned key); now snapshot-copied before any hold. M-2: the promotion path silently ignored the caller's `activeLayers` while the doc claimed a consistency check; the check is now real and runs against a new `ColdStore.TryPeek` BEFORE the destructive `TryTake` (post-take throw would strand the summary — the FR-LW-025 ordering contract). L: spawnTick+lifetime uint-overflow refusal (wrap made an arc instantly expired); stale-worldTick entrant can be its own LRU victim (doc, spec-conformant); single manager→contact edge scope (doc). +3 regression locks (22 tests). See src/CLAUDE.md v1.95. **Slice-2 AR-2 (July 2, 2026, same day): full-surface sweep over all 19 files, 0H+1M+2L, all resolved** — M-1: two residual strand-the-summary vectors closed (the class AR-1 M-2's peek-check does not cover — incoherent cold state): `ColdStore.Add` gains the ActiveLayers mask coherence gate (via `MemoryStore.DefinedLayersMask`, private→internal) and `RecordInteraction` validates entityId up front (negative / == managerId), both refusing BEFORE any destructive take (FR-LW-025); defensive take-result guard added. L: `SALIENCE_REF_THRESHOLD`/`CLIQUE_THRESHOLD` unconsumed doc-notes (§3.3 text-generator / vol-2 clique consumers, KD-10-deferred); single-manager cold-store scope documented (ColdSummary keyed by EntityId alone). +2 regression locks (24 tests). See src/CLAUDE.md v1.96. **Slice-2 AR-3 (July 2, 2026, same day): 0H+0M+2L, all resolved — CONVERGENCE (doc-only round), slice-2 AR cycle closed** — L: `ColdStore.TryTake`'s ordering doc still said a "(follow-up slice)" owns the verify-before-take sequencing (stale — `ActiveSetMembership.RecordInteraction` is the live consumer, now named); `WorldLoop` Spec header citations aligned to the wired phases (+FR-LW-014/023). See src/CLAUDE.md v1.97. **Slice 3 (July 2, 2026, same day): InteractionTextGenerator (§3.3) + §4.6 snapshot serializer LANDED** — `InteractionTextGenerator` (§3.3/KD-6): deterministic template selection off the **new aperiodic `world.text` sub-stream** (the first #22 draw site — one reserved draw per interaction; ERR-022-001 `DOMAIN_TAG_LIVING_WORLD = 0x1E` + `SubsystemOrdinals.LivingWorld = 80` allocated in #16), separate from the tick-driven `world.arcs` stream so player-triggered text never perturbs the arc cursor (FR-LW-011/020, T-LW-DET-003/004); slot expansion from match-engine facts + an optional §3.2 cited episode gated at `SALIENCE_REF_THRESHOLD` (FR-LW-013); all validation runs BEFORE the draw so a refused call consumes no cursor (replay parity across fail-loud paths); no runtime model inference (FR-LW-012). Stage-0 in-code authored corpus (`InteractionTextCorpus`, APPEND-only template order) — the §7.2 AI-assisted corpus is a pure data swap. `WorldStateSerializer` (§4.6): canonical living-world block (clock + edges/episodes + arcs + cold summaries) in the Appendix B pinned field order via the #16 `CanonicalSerializer` (bitwise round-trip); deserialization rebuilds through the validating store seams — `SpawnArc` re-takes every pin so FR-LW-018 refcounts are reconstructed exactly — with fail-loud version/tag/trailing-byte/roster gates. New `Tests/WorldTextSnapshotTests.cs` (~20 tests). `living-world.asmdef` now references `TacticalDirector.DeterministicSim`. See src/CLAUDE.md v1.98. **Slice-3 AR-1 (July 3, 2026): 0H+0M+3L, all resolved** — L-1: `WorldStateSerializer` length prefixes (edge/arc/input/pin/episode/cold counts) gated through a new `ReadCount` helper — a corrupt count now fails loud with `ArgumentException` (bound `0 ≤ count ≤ remaining bytes`) instead of desyncing the read offset or throwing `OverflowException`/OOM on the allocation; L-2: `SpawnCause.Input.Value` (the one float in the §4.6 digest block with no store-seam gate) finiteness-gated at the serialize seam; L-3 (doc): the unreachable `Generate` draw-failure branch documented as an internal-invariant corruption abort (its cursor advance is deliberate, not a normal pre-draw refusal). +2 regression locks. See src/CLAUDE.md v1.99. **Slice 4 — KD-10 season composition root LANDED (July 3, 2026):** `src/living-world/WorldStore.cs` (sealed) is the persistent world store + season-calendar loop — the one KD-10 prerequisite §7.1 assigns to this spec ("persistent world store + season-calendar loop") and the composition root every prior slice deferred to. It constructs/wires the six existing services (`WorldClock`+`MemoryStore`+`ColdStore`+`ArcEngine`+`ActiveSetMembership`+`WorldLoop`) for one manager, drives one calendar day per `AdvanceDay` (the §4.2 phases with producers run — 3 decay, 4 arc expiry, 6 external-cap LRU; phases 1 match-outcome ingest / 2 vol-2 read / 5 background-tier stay the WorldLoop's documented null seams, FR-LW-031 — no phantom invented), and `RecordInteraction` stamps the episode with the store's current day. `Snapshot()`/`Restore()` round-trip the whole store as one canonical payload: a fail-loud composite header (`WORLD_STORE_FORMAT_VERSION` + `DomainTagLivingWorld` + managerId) + the existing §4.6 four-store block (`WorldStateSerializer`, bitwise via #16 CanonicalSerializer) + the FR-LW-022 active-set membership roster (entityId + isOwnClub per member — the one piece the §4.6 block does not carry; its snapshot entry was flagged "a follow-up", this is it), with fail-loud gates on version/tag/managerId/length-count-prefix (a `ReadCount` parallel to the serializer's)/membership-flag/trailing bytes; Restore rebuilds through the validating store seams and re-wires the loop. `ActiveSetMembership` v1.3 gains the roster seam (`MemberCount`/`GetMemberAt`/`RestoreMember`, the last validated against the rebuilt `MemoryStore` for the membership↔edge live-edge invariant); `LivingWorldConstants` gains `[FIXED] WORLD_STORE_FORMAT_VERSION`. New `Tests/WorldStoreTests.cs` (14 tests): wiring, AdvanceDay decay+arc-expiry across the lifetime boundary, current-tick stamping, idempotent+field-identical + empty-store round-trip, two-run determinism, six fail-loud restore gates. Adversarial review converged 0H+0M on the first pass (residual low: managerId not cross-checked against edge FromIds on an empty-roster corrupted save — corruption-only, non-crashing, caught whenever the roster is non-empty, consistent with WorldStateSerializer's own no-managerId scope). See src/CLAUDE.md v2.0. **Slice 5 — InteractionTextGenerator wired into the WorldStore (July 3, 2026):** the `world.text` cursor is now folded into the composite save. `WorldStore` v1.1 owns a `DeterministicRngService` (constructed from a `worldSeed` — a new `WorldStore(int managerId, ulong worldSeed)` overload; the `WorldStore(int managerId)` ctor seeds deterministically from `managerId` so the same manager always resumes the same text stream and two managers never share a cursor) plus the slice-3 `InteractionTextGenerator` that registers the aperiodic `world.text` sub-stream; a public `GenerateInteractionText(intent, in slots)` exposes it (player-triggered, off the tick loop). `Snapshot()`/`Restore()` fold a world.text RNG block (world seed + stream `RngCursor` + `ActionOrdinal`) into the composite between the §4.6 four-store block and the membership roster — the draw keys on the seed-derived `k0/k1`+`streamKey`+`ActionOrdinal` (#16 §3.2.5), so Restore re-derives the service from the persisted seed and resumes the exact stream position (generation continues byte-for-byte across the save boundary). `WORLD_STORE_FORMAT_VERSION` 1 → 2 (composite field-order change; the constant's own doc mandates the bump). `Tests/WorldStoreTests.cs` +3 (slot expansion; save-boundary generation determinism; seed-varies-the-sequence) and `PopulatedStore` now advances the cursor so every round-trip/determinism test exercises a non-zero stream position. Note the generator's cursor lives in the WorldStore's own composite save, NOT yet in the unified match/season save (that fold stays blocked on FR-LW-003 + the season save-file root). **Slice-5 AR-1 (July 3, 2026): 0H+0M+3L, all resolved** — L-1: `Restore` now fails loud on a non-zero `RestoreStream` code (was silently resuming the text stream at cursor 0 under a future registration-order change); L-2: documented the atomic-`Generate` invariant that lets the block omit the reservation fields; L-3: header byte-order corrected. Core resume logic verified sound by hand + closed-loop-locked. See src/CLAUDE.md v2.1–v2.2. **Slice 6 — deep-memory→text auto-cite (July 3, 2026):** a new `WorldStore.GenerateInteractionText(intent, entityId, subject, opponent, homeGoals, awayGoals)` overload pulls the contact's most-salient **citable** §3.2 episode from the store's own `MemoryStore` (the manager→entityId edge) and cites it — the §3.3 "cite a §3.2 episode" path (FR-LW-013) that until now had no composition-root consumer (the slots overload left the caller to hand-build the cited episode). Selection scans the edge's live `Memory` buffer for the highest-salience episode with a citable kind (not `EventKind.None`) clearing `SALIENCE_REF_THRESHOLD` (negated-compare fail-closed on the NaN the store seams already reject); deterministic tiebreak salience→worldTick→episodeId; no qualifier ⇒ no citation. Exactly one `world.text` draw either way, so the selection is a pure function of the serialized memory and resumes byte-identically across `Snapshot`/`Restore` — **no new serialized state, no `WORLD_STORE_FORMAT_VERSION` bump** (the episode is read from the already-serialized MemoryStore). `Tests/WorldStoreTests.cs` +4 (fresh-episode cite; full-tie episodeId tiebreak; no-edge omits citation; save-boundary auto-cite determinism). See src/CLAUDE.md v2.3. **Slice 7 — arc-trigger evaluator + `world.arcs` sub-stream LANDED (July 24, 2026; Slice 1/E1 + Slice 2/E2):** the KD-10 "arc trigger evaluators + first draw site" the slice-2 note deferred, per the converged supplement `docs/tracking/living-world-arc-triggers-design.md` (v0.5, Path B — opt-in, default-off, byte-neutral). Rather than the phantom-interface surface FR-LW-031 forbade at slice 2 (a `world.arcs` stream with zero draw sites reading vol-2/vol-3 canon that does not exist), the trigger signals now arrive through a concrete, nullable, caller-supplied `ArcCanonSource` (KD-1/KD-2 — a single concrete class + nested `Builder`, NOT an `IArcCanonSource` interface): a flag-off `WorldStore` never constructs one and is byte-identical to pre-slice-7, while a caller that attaches one opts the world into arc spawning. New `src/living-world/` files: `ArcCanonSource.cs`, `ArcTrigger.cs` / `ArcTriggerCatalogue.cs` (the APPEND-only Stage-0 table — two entity-scoped rows (WonderkidVsVeteran ego-clash thr 0.75/45d; MediaVendetta media-hostility thr 0.70/60d) + two board rows in ArcKind ordinal order (DressingRoomSplit pulse-divergence thr 0.65/30d; BoardPatienceCollapse board-impatience thr 0.80/90d)), `ArcTriggerEvaluator.cs` (registers `world.arcs` under a **distinct** entity sentinel `WORLD_STREAM_ENTITY_ARCS = -2` so `ComputeStreamKey` differs from `world.text`'s -1 — KD-4, the collision hazard; walks the catalogue in FR-LW-017/021 order applying the **KD-7 rising-edge armed-off latch** — a sustained signal fires exactly once and drop-below re-arms; one `world.arcs` draw per rising edge; resolves a minimal FR-LW-018 pin (entity: most-salient episode on the manager→entity edge; board/edge-less: none) then `ArcEngine.SpawnArc`). `WorldLoop` gains a 5th nullable seam + `RunWorldTick(ArcCanonSource canon = null)` (KD-5, per-tick argument, no stale-canon field); `WorldStore` owns the evaluator + a nullable `_canon` (`SetArcCanon` / a canon ctor overload / `Restore(payload, canon)` thread it, never persisted — the `ISquadProvider` Load-time-parameter precedent); `season-save/SeasonSaveManager.Load` gains an optional `ArcCanonSource canon` threaded into `WorldStore.Restore` (§8.9(a)) so a flag-on world keeps evaluating through the season file. **Slice 1/E1:** a flag-on run is deterministic FORWARD but `Snapshot()` fails loud (not yet snapshot-safe); no `WORLD_STORE_FORMAT_VERSION` change (default path byte-identical). **Slice 2/E2:** `Snapshot`/`Restore` serialize the `world.arcs` cursor + the KD-7 latch (a canonical-order block — `EnumerateLatchedCanonical` sorts by scopeKey then triggerId — with a strict-order/no-dup `RestoreLatched` fail-loud gate) between the `world.text` block and the membership roster, **`WORLD_STORE_FORMAT_VERSION` 2 → 3** (v2 payloads rejected fail-loud, no migration); the E1 fail-loud is dropped, so a flag-on `save@N → Restore(canon) → advance to N+K` is byte-identical to the uninterrupted run AND a still-latched trigger does NOT re-fire on restore (the re-fire-after-restore bug the latch serialization exists to prevent). New `Tests/ArcTriggerTests.cs` (design §9 tests 1–4/4b/4c/6–11 + a latch enumerate/restore seam test) + `SeasonSaveManagerTests` +1 (flag-on world resumes evaluating through the season file, with a null-canon negative control asserting divergence). **Full dotnet gate: PASSED, 0 failures (SDK 8.0.129 via apt; whole tree green — 118 living-world tests, 20 season-save). Slice-1 default path byte-identical (no version change); Slice-2 comparative round-trip, no absolute rebaseline.** See src/CLAUDE.md v2.34. **Still open:** **BackgroundTierSim summary updates (§3.5) — remains the documented WorldLoop phase-5 seam, deliberately NOT built:** it summarises club-AI/transfer/sacking outcomes that do not exist yet (building a consumer is the phantom-interface class FR-LW-031 forbids — the same reasoning that keeps `world.arcs` unregistered in this same task), and §3.5 specifies no background-tier update formula to implement against; the composite `SNAPSHOT_SCHEMA_VERSION` bump that folds the §4.6 block into the *match/season* snapshot (still deferred — the WorldStore now owns the living-world composite save, but folding it into the unified match/season save is blocked because FR-LW-003 bars the match engine from referencing this assembly and the unified season save-file root does not exist yet — note the `world.text` cursor now lives in the WorldStore's *own* composite save per Slice 5, so only the fold into the *unified* match/season save remains); and the KD-10 upstreams owned elsewhere (vol-2/vol-3 human-systems; structured match-outcome events).

- **UI / Client Framework (#38) — T0 substrate LANDED; Wave-7 screens + the UGUI binding remain open** — *opened and landed July 25, 2026.* The approved-but-unbuilt §3.1–§3.3 contracts now exist in a new host-free presentation assembly `src/ui-framework/` (`TacticalDirector.UiFramework`), governed by `docs/tracking/ui-framework-t0-implementation-plan.md` (v0.3; plan AR-1 3M+3L → code AR-1 1H+3M+4L → code AR-2 1M+2L, all resolved). Landed: the `IViewModelSource<T>` projection contract, the `NavigationShell` deterministic stack machine, `ICommandDispatcher` + `ManagerIntent` + the one concrete `MatchTacticsDispatcher` (routing ONLY to the three verified public seams `SetTeamTactic` / `SetPlayerTactic` / `SubstitutePlayer`), and the one concrete match projection (`MatchFrameView` + `MatchViewModelSource` over a new `ILiveFrameSource` seam). 39 tests incl. the two locks that keep the layer honest — observer neutrality (a 240-tick digest chain is byte-identical whether or not the UI projects) and a mechanical reverse-reference scan over every `src/**/*.asmdef` (FR-UI-001). **Full dotnet gate PASSED, whole tree green.** No `SNAPSHOT_SCHEMA_VERSION` change; no RNG stream / domain tag / ordinal (FR-UI-022). **Three deliberate deviations from the approved spec text, each back-propped** (`spec-error-log.md`): **ERR-038-001** — live-match marshaling reuses the shipped `ManagerCommandQueue` + pre-tick drain rather than §3.3/§4.1's proposed `LiveMatchStreamer.EnqueueIntent`, which would have given the SHARED streamer a mutation surface and regressed the browser viewer's playback-only invariant (the interactive-Unity-client AR-1 H-2 finding, settled one day after #38's approval); FR-UI-023 is satisfied identically. **ERR-038-002** — §4.1's "generic substrate references nothing sim-side" cannot hold literally, since §2.2's own `ManagerIntent` carries `TeamTactic`/`PlayerTactic`/`SubstitutionReason`; the load-bearing invariants are FR-UI-001 + FR-UI-003, both preserved. **ERR-038-003** — `Register` refuses a duplicate `ScreenId` instead of §3.2's silent overwrite. **Still open:** the **UGUI rendering binding** (§4.3/§7.2 — Unity-host-gated when this entry was filed; the July 19, 2026 determinism cert (`certification-platform.md` v1.4) cleared that host gate, so the binding is now buildable against the certified Unity 6000.4.9f1/DX11 pin rather than blocked on host access) and every **Wave-7 screen** (§7.1, each gated on its data spec existing). Of the screens the July-25 mockups cover, only **Tactics (S1)** and **Squad** have built data behind them today (#21 + #27); transfers / training / scouting / finances / staff are gated on #31 / #29 / #32 / #40 / #34, none of which have any `src/` — a screen against them would be the phantom-consumer trap KD-2 forbids. Each screen also needs its own Wave-7 spec before code, per #38 §7.1. **UPDATE August 7, 2026:** the P5a landing surfaced, and the owner resolved the same day, the layering question this entry never carried — where a concrete screen catalogue lives, given FR-UI-010 forbids the framework hard-coding one. Answer: the new `src/client-app/` assembly above `ui-framework` (`TacticalDirector.ClientApp`, roadmap B9c; `interactive-unity-client-design.md` §5-P5a resolution / v0.17) — the four screens' `[FIXED]` `ScreenId` catalogue + the `ClientScreenFlow` navigation graph, 15 tests — **gate-run green later the same day (15/15 on first execution; the FR-UI-001 scan fired on the new assembly exactly as designed and `client-app` is now sanctioned, `UiFramework.Tests` 50/50)** after the SDK-via-apt discovery recorded in the August 7 CHANGELOG entry. Wave-7 screens and their per-screen specs remain open and register into that assembly when built.

- **Presentation layer — minimal match viewer LANDED; interactive Unity client remains open** — *opened July 2, 2026.* `src/match-viewer/` (`TacticalDirector.MatchViewer`; presentation tooling, not a numbered spec — the UI layer proper stays Stage 1+ per the src/CLAUDE.md layer taxonomy) records a real `MatchEngine` run and exports a **self-contained HTML canvas replay**: ball + 22 agents with team/GK/possession/ball-height cues, play/pause/scrub/speed controls, pitch markings from an IFAB `[FIXED]` catalogue (`MatchViewerConstants.cs`). Enabled by the `MatchEngine.cs` v1.24 public observation surface (read-only value-type copies; `MatchViewerTests` digest-locks observer neutrality — recording a run is byte-identical to not observing it — plus bitwise two-run determinism, on-pitch finiteness, cadence, and fail-loud guards incl. the NaN-gate on export). The HTML output is explicitly NOT a determinism-pinned wire format (rounded display coordinates only; same contract class as the Stage-0 tactic text grammar). **AR-1 (July 2, 2026, same day): 0H+2M+3L, all resolved** — M-1 `MatchReplay` incoherent-frame refusal (canvas silently no-opped short/long data rows); M-2 observation-surface roster-index guards (`MatchEngine.cs` v1.25); L: exposed-array hand-over doc, non-uniform final-interval playback timing, `0:60.0` clock rounding + contiguous-roster jersey assumption (see src/CLAUDE.md v1.86). **AR-2 (July 2, 2026, later same day): full-surface sweep, 0H+3M+3L, all resolved** — M: empty-replay refusal (exported player crashed at first render), non-monotonic-tick refusal (per-interval timing needs dt > 0), `MatchViewerConstants` [GT] rows migrated onto `GameplayConfig` (June-30 catalogue-migration conformity; recorder default-stride params → explicit overloads); L: `MatchReplay` roster-index guards (the AR-1 M-2 fix had not reached the sibling surface), ball-marker radius clamp (negative-z ⇒ `arc()` IndexSizeError killed all rendering), metadata NaN-gates (see src/CLAUDE.md v1.87). **AR-3 (July 2, 2026, latest same day): 0H+1M+2L, all resolved** — M: the frames `ReadOnlyCollection` wrapped the caller's LIVE list (a view), so post-construction `frames.Add(...)` bypassed every ctor guard; now snapshot-copied before validation. L: `PossessingAgentId` range-validated; empty roster refused (src/CLAUDE.md v1.88). Convergence: AR-3 found no High and nothing in the recorder/exporter/JS/engine-surface — findings confined to the public `MatchReplay` ctor validation set. **AR-4 (July 2, 2026): 0H+0M+3L, all resolved — CONVERGENCE (L-only round)** — Space-key toggle no longer hijacks focused controls; `Export` gates the config-resolved [GT] presentation values (post-AR-2-M-3 a malformed config could emit a silently broken document); doc-contract fixes (`Tick` pre-run engines, `SampleStride` provenance-only). AR cycle closed per the project convention (src/CLAUDE.md v1.89). **Still open:** the interactive Unity client's HOST half (live rendering, input, match presentation) — the viewer is replay-only. **UPDATE August 3, 2026 — P4a LANDED, and P4 is split.** The client's whole host-free stack is now built: P0/P2 (July 24), P1/P3 (July 27), the head-less half of P6 (August 3), and **P4a, the render model** (August 3) — `PitchViewProjection` (the one documented corner-origin ⇄ centre-origin adapter), `PitchMarkings` (the IFAB catalogue as shapes, off the existing `[FIXED]` values), `MatchRoster`, and `MatchRenderProjection` → `AgentRenderModel`/`BallRenderModel`, all in gate-compiled `match-client-core`. The split follows the standing "keep logic out of `MonoBehaviour`s" rule: **P4a is every render decision, P4b is the binding.** P4a also surfaced **KD-P4a-1** — `LiveMatchStreamer` cached goalkeeper flags as immutable roster metadata, but `MatchEngine.SubstitutePlayer` rewrites them, so a keeper substitution had been drawing the keeper ring on the wrong player in the browser viewer since P1; the flag now rides `LiveAgentCue` per tick and `MatchRoster` deliberately holds none. **What remains is P4b, P5 and P6's on-host half** (roadmap rows B8–B10): the Unity binding, the UGUI shell, scene boot, 60 FPS, live tactical input through a screen, and the FR-PO-052-class render-loop perf capture — all needing the pinned host, with a cert-host run budgeted per landing rather than one at the end. This entry originally named the first cert run on the pinned Windows/Unity tuple (`certification-platform.md` v1.2, then ⏳ Recert required) as the recommended precursor before investing in it; **UPDATE August 3, 2026:** that precursor is now satisfied — the platform-determinism KAT run completed July 19, 2026 against the v1.4 Unity 6000.4.9f1/DX11 pin (44 passed / 0 failed / 4 documented Stage-0+1 deferral skips; `docs/specs/deterministic-sim/cert-runs/determinism-cert-2026-07-19.md`), so the interactive Unity client is no longer cert-gated. It remains genuinely open and unbuilt — `src/match-client-unity/` holds only an asmdef and a README. **UI mockups landed July 25, 2026** (`docs/design/ui-mockups/` — a design-system page + 11 screen mockups + shared assets, imported as the visual reference for UI / Client Framework #38 and the Wave-7 screen specs it defers to). They are **non-normative**: static HTML/CSS/JS with hardcoded illustrative data, not on any build path, not read by the sim, not in any snapshot/digest — the spec wins on any disagreement. Two visual directions ship side by side in `tokens.css`; **`touchline` (analyst tool) was CHOSEN July 25, 2026** — `stadium` is retained as the rejected alternative behind the mockups' switcher, and `assets/app.js`'s default was corrected from `stadium` to `touchline` (every page declared `data-direction="touchline"` in its markup while the script overrode it on load, so the mockups had been rendering a direction they did not claim). The `touchline` token block is what the eventual client theme derives from; nothing in it is `[GT]`-tagged or read by the sim.

- **Approval tags — NOT "created locally, awaiting push": verified August 17, 2026 that the eight original tag objects no longer existed anywhere, and the tag-push 403 is still live. The eight were RE-CREATED locally that session against resolved, ancestry-verified targets and do NOT survive the container. What remains is the PRIVILEGED PUSH — and, in any fresh container, re-running the recipe below first** — *April 26–27, 2026; precondition check added May 6, 2026; tag 8 added May 13, 2026:* Annotated tags `spec-ball-physics-v1.0-approved`, `spec-agent-movement-v1.0-approved`, `spec-collision-system-v1.0-approved`, `spec-first-touch-v1.0-approved`, `spec-shot-mechanics-v1.0-approved`, `spec-perception-system-v1.0-approved`, `spec-decision-tree-v1.0-approved`, `spec-event-system-v1.0-approved` created on their respective branches. Remote returns HTTP 403 on tag push (branch-protection). Push after merge to main with privileged credentials. **Precondition check (run BEFORE `git push --tags`):** (1) After merge to main, run `git tag -v <tagname>` for each of the 7 tags and confirm the SHA each tag points to is still reachable from `origin/main` (`git merge-base --is-ancestor <tag-sha> origin/main`). (2) If main uses **squash-merge** or **rebase-merge**, the original branch commit SHAs are orphaned and the tags point to dead commits — in that case **delete and recreate** each tag against the corresponding squashed/rebased commit on main (use `git log --grep` or commit-message search to identify the new commit), do NOT just `--force` push. (3) If main uses true merge commits, normal `git push origin --tags` is correct. Document which path was taken in this OPEN ISSUES entry when resolving. **✅ OWNER DECISION, August 17, 2026: push them. ATTEMPTED — and the attempt found the entry's own premise false. RE-TITLED accordingly; the entry stays OPEN on different, verified grounds.** **(1) The tags do not exist anywhere.** `git tag` in a fresh clone returns nothing and `git ls-remote --tags origin` returns nothing: the eight annotated tag objects were created in the April/May 2026 authoring containers, were never pushed, and are unrecoverable — their messages (which recorded each original sign-off date and §9 checklist status) are gone with them. Every session since has read "created locally, not yet pushed" as meaning they were sitting somewhere ready to push. They were not. **(2) The precondition check as written cannot run**, because all three of its steps take a tag object as input. What it was really asking is answerable without one, and was answered: `origin/main` uses **true merge commits**, so branch commits are preserved, and each candidate target below is confirmed an ancestor of `origin/main` by `git merge-base --is-ancestor` — path (3), not the squash-merge path (2). **(3) The 403 is still live**, verified today from a Claude Code Remote session with the same message the April 26 entry recorded, on both `git push origin --tags` and a single-tag push. The proxy is healthy; the refusal is GitHub-side, and this session's credentials push branches but not tags. **(4) Re-creation is possible and the targets are resolved** — done and verified locally this session, though the local tags die with the container, which is why the recipe is recorded here rather than the outcome. Targets, each the earliest commit on `origin/main` at which `SPEC_INDEX.md` records that spec APPROVED:

  ```
  # #1  Ball Physics       APPROVED Feb  8, 2026 -> caaf5cf0   #3  Collision System APPROVED Feb 19, 2026 -> caaf5cf0
  # #4  First Touch        APPROVED Feb 22, 2026 -> caaf5cf0   #7  Perception       APPROVED Apr 22, 2026 -> a88dba03
  # #2  Agent Movement     APPROVED Apr 27, 2026 -> bcf94199   #6  Shot Mechanics   APPROVED Apr 27, 2026 -> bcf94199
  # #8  Decision Tree      APPROVED Apr 27, 2026 -> bcf94199   #17 Event System     APPROVED May 13, 2026 -> 7dbcf121
  ```

  `bcf94199` is "Sign-off pass: Agent Movement #2, Shot Mechanics #6, Decision Tree #8"; `a88dba03` is "Record lead developer sign-off: Perception System #7 APPROVED"; `7dbcf121` is "Approve Event System Specification #17". **`caaf5cf0` ("Fully uploaded spec docs", April 10, 2026) is a weaker claim and must be tagged honestly:** #1/#3/#4 were signed off in February, before any spec text existed in git, so **no commit represents the moment of their approval** — `caaf5cf0` is merely the earliest commit that *contains* them at APPROVED. Note this differs from what the originals did, which was to point all four April-26 tags at one branch HEAD ("Created annotated tags on this branch's HEAD"), a target with no per-spec meaning at all. **⚠️ Note on this entry's own history, August 17, 2026:** the finding above — "the tags do not exist anywhere" — was true of the container **before** this session and false of it **after**, because the session that wrote it re-created all eight sixteen minutes earlier (verified: `git tag` lists them, annotated, at the four targets below). The commit message that published the finding says `git tag` returns nothing, which was not true of the repository it was committed to; corrected here rather than left to mislead. The substantive finding is unaffected — the ORIGINAL eight objects, with their recorded sign-off dates and §9 checklist status, are gone for good, and container-local tags die with the container. **NEXT STEP (owner, privileged credentials, one paste):** create the eight annotated tags against the SHAs above with messages stating the approval date and *why that commit* — including, for the three February specs, that the commit predates nothing and is a containment claim, not an approval claim — then `git push origin --tags`. If the 403 persists with privileged credentials, the block is a repository ruleset on `refs/tags/*` rather than a credential scope, and the entry should be closed as "tags refused by policy" rather than left open indefinitely.
