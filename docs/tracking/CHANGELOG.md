# CLAUDE.md — Change Log

> **Created:** July 31, 2026
> **Purpose:** The `**Last Updated:**` entry chain formerly carried in the header of
> `CLAUDE.md`. Newest first; each entry records what landed, what was measured, and
> what was deliberately not done.
> Split out of `CLAUDE.md` on July 31, 2026. Content is **verbatim** — entries were moved, never edited, reordered, or deduplicated.

**Appending a new entry:** add it at the top of the chain below and re-label the
previous newest entry `**Last Updated (prior):**`. The chain is the record — do not
break it, and do not edit historical entries.

---

> **Last Updated:** September 11, 2026 — **UX F4 validation protocol packet lands (PR #394). Documentation only; no `src/` file, `.asmdef`, spec status or roadmap row touched. F4 is deliberately NOT closed.**
>
> New: `docs/design/ux-validation-protocol.md` **v0.5** — the repeatable operating packet `ux-detailed-plan.md` v1.4 F4 requires, so that someone other than the UX author can run the same S0/S1 validation and produce comparable evidence. It carries the four-layer sequence (§2), seven outcome-oriented S0 tasks plus eight S1 tasks mapped from §7.6 (§3), the F4 human-assignment table with S0 and S1 participant slots (§4), the scripted walkthrough matrix (§5), the neutral moderator script and stop conditions (§6), journey-specific session/task evidence templates and stable finding IDs (§7), severity/disposition rules with the closed §12 vocabulary (§8), a journey-specific Gate-G decision record (§9), and Gate-J continuity (§10).
>
> **The packet defines no new rule.** §1 states that where it and `ux-detailed-plan.md` disagree, the detailed plan wins and the packet is the defect. Verified against the plan rather than taken on report: the S0/S1 two-participant floor, the removed provisional bypass to Gate H/I, the one-round cap and its two exceptions, the four severity bands, and §12's five-value disposition vocabulary all match the detailed plan in substance.
>
> **F4 remains OPEN, and the packet says so in its own §11.** Three F4-exit human-assignment facts are unfilled and none is invented here: the `ux-detailed-plan.md` §10.1 UX-workstream accountable owner, S0-P1, and S0-P2. `open-issues.md`'s September 9 owner-acceptance record already states those three are F4 outputs and still unassigned; this landing does not move them, does not advance the detailed plan's status line, and does not claim Gate E, F or G. S1-P1/S1-P2 are defined because F4.2 applies to S1, but they are journey-level prerequisites before S1 reaches Gate F/G, not additional F4-exit blockers.
>
> **Twelve review findings were raised across six rounds and all twelve are fixed within the unmerged v0.5 protocol.** (1) The §5 matrix silently dropped two of F4.6's nineteen binding test-data profiles — `no save` and `save/load failure where relevant` — behind a generic error row, while §11 claimed the walkthrough READY; §6.3's Gate-A audit names the current save/new-game limitation explicitly, so the gap was not hypothetical. §5 now covers all nineteen, re-ordered to F4.6's own order, with the audio row kept and labelled as a Gate-E-only addition beyond the minimum. (2) No task exercised the Tactics Setup cancel/back edge that §6.4 names and Gate F requires prototyping; **S0-T7** now does, and is wired into §7.2, the §9 Gate-G record and the keyboard-only/mouse-only rows. (3) The file declared `Version: 0.1` with no version history; §12 now carries one. (4) S0-T1 assumed a working New Game path and is narrowed so the current start limitation is recorded as a dependency rather than scored as participant failure. **(5) The v0.2 fix for (1) introduced a narrowing of its own**, caught in the follow-up review: it *replaced* the original generic error row with `Save/load failure where relevant` instead of adding alongside it, so §5 no longer covered F4.3's wider `disabled/error states` condition, of which a save/load failure is one class. v0.3 restores a general `Error/failure state` row, states the F4.3 floor in §5's preamble, marks the audio row as the one Gate-E-only addition beyond that floor, and makes the non-substitution note symmetric in both directions. **(6) Codex found that color-independent meaning was coupled to the `Many status indicators` stress row, so marking that density profile N/A could silently waive Gate E's color-independent-meaning requirement. v0.4 splits color-independent meaning into its own Gate-E condition that remains binding wherever semantic state or action meaning uses color; the many-indicators row now tests density/scannability only.** **(7) Codex found that the `honestly testable` qualifier could let Gate G pass after a prescribed `FUTURE-BLOCKED` task was omitted, bypassing Gate F's complete-task requirement; v0.5 makes any untestable prescribed task a Gate-F/G failure and sends the prototype back to Gate F.** **(8) Codex found that the Gate-E matrix had no per-condition outcome/evidence record; v0.5 adds `PASS`/`FAIL`/justified-`N/A`, prototype/version, evidence, and finding/reason fields for every condition and makes blank rows gate-blocking.** **(9) Codex found that color-independent meaning was not the same as contrast and F3-001 explicitly requires Gate E to prove contrast; v0.5 adds a standalone contrast verification/evidence row and rejects mockup token values as proof.** **(10) Codex found that §7.3 omitted required §12 finding-ledger fields; v0.5 now records journey, gate, evidence, severity, owner, disposition, release condition and retest result explicitly, retaining affected task/state as extra context.** **(11) Follow-up review found that merely adding the `Disposition` field still left its binding vocabulary unstated; §8 now carries §12's exact closed set: `FIX NOW`, `ACCEPT FOR CURRENT GATE`, `DEFER TO P2/P3`, `BLOCKED BY DOMAIN/CLIENT IMPLEMENTATION`, `INVALID / NOT REPRODUCED`, and forbids free-text alternatives.** **(12) Codex found that the packet promised repeatable S0/S1 validation while S1 still had only an `S1 equivalent` placeholder; v0.5 now defines S1-P1/S1-P2, maps all eight §7.6 outcomes to S1-T1–T8 with completion evidence, provides an S1 task-evidence table, and makes Gate G journey-specific.**
>
> **A pre-existing manifest gap is corrected in the same PR rather than widened.** `file-manifest.md` carried **zero** `docs/design/ux-*.md` rows, although `open-issues.md`'s September 7 updates twice state that `ux-baseline-evidence.md` and `ux-experience-architecture.md` are "inventoried in `file-manifest.md`". They were not, and neither were the high-level plan, the detailed plan, the shared system, or the superseded foundation stub. Landing a sixth uninventoried UX document under a claim that the fifth was already inventoried is the drift `9af9626` exists to clean up, so a **UX Workstream Documents** section now lists all six with their current versions. Those five rows record files that landed earlier; only `ux-validation-protocol.md` is new here.
>
> **Deliberately not done.** `open-issues.md` and the `project-reference.md` **23 active / 54 archived** count are untouched — this landing opens no blocker and closes none, and the UX entry's status (F0–F3 complete, F4 next) is unchanged by it. `src/CLAUDE.md` and `CHANGELOG-src.md` are untouched: no `src/` file changed. `README.md`'s status snapshot is untouched — no production-assembly count, assembly-less figure, Unity target or governance stage moved. No `SPEC_INDEX.md` row, no `ERR` filed (the packet contradicts no APPROVED spec; it is a design document under `docs/design/`, not a spec).
>
> **Determinism:** no `SNAPSHOT_SCHEMA_VERSION` change, no new RNG stream / domain tag / draw site, no draw-order change, no `[GT]` constant, no new assembly. **Blast radius:** none — no scenario tick window, per-90 rate band, A4a round-resolution fit or `FR-PO-052` perf baseline is perturbed by a Markdown file. Checked, nothing moved.
>
> **GATE: not owed and not run.** No `src/` file, `.asmdef` or test changed, so `dotnet-gate` has nothing to measure for this landing and a previous landing's result is not restated as this one's. The PR's documentation checks are the applicable evidence — `Markdown lint`, `Markdown link check`, `Spec hygiene checks`, `File manifest sanity` and `YAML lint`; the current-head GitHub run is authoritative and must be green before merge.

> **Last Updated (prior):** September 10, 2026 — **A wrong sentence in the wiring backlog is corrected, and a dispatched measurement lane lands — after adversarial review found the lane's first cut could report a green measurement that never happened. Documentation + CI tooling only; no `src/` code changed.**
>
> **The correction is the load-bearing half.** `match-engine-wiring-backlog.md` **v1.13 → v1.14**. Since v1.0 the W4 entry had closed *"The keeper is simply not on that path."* Direct source read refutes it: `MatchEngine.cs:3074` runs `_perception.OnHeartbeat` over the full `SQUAD_SIZE` including both keepers (keeper index == team id, `:922`), and `BallPerceptionEvaluator` already computes an occluded ball visibility for each of them every heartbeat. **Nothing needs putting on the perception path.** W4 is a *downstream-consumption and tick-order* problem, and the old framing pointed an implementer at a seam that does not exist. Three constraints recorded, two of which make the naive fix a regression: `FilteredView.BallVisible` bundles **FoV** with occlusion while keeper facing is `AUTO_ALIGN` (so gating the save on it would blind keepers to **unobstructed** shots — frequency UNMEASURED); `SaveArmed` has **two** callers (`:3311`, `:4461`) and both precede the `:3074` heartbeat (100 ms stale at 10 Hz); and `:4457-4460` requires the two sites to share one predicate. **No ERR** — the backlog is an AUDIT document, not an APPROVED spec. §5 wire-order untouched.
>
> **The defect review caught, recorded because it is the same class the backlog audits.** The lane's first cut took an env switch and a free-text filter as *independent* inputs and treated exit 0 as success — so a zero-match filter (VSTest returns success by default) or a mismatched pair (the instrument `Assert.Ignore`s itself) produced a green run and an artifact with **no measurement**. `run-gate.sh:268`'s `Gate PASSED` is an unconditional `echo` with no postcondition. Closed by making **evidence**, not exit status, the success criterion: canonical instrument id → one validated tuple; `TreatNoTestsAsError` scoped to the owning project only; TRX + detailed console; `verify-run` requires a `Passed` outcome **and** the catalogued sentinel.
>
> **Verified:** whole-tree generate+build 0 errors; three real-path controls — positive `verify-run` **PASS**, env-gate-absent **rejected (exit 2)** though `dotnet test` itself exits 0, sentinel-stripped **rejected (exit 2)**; **270 tooling tests + 32 subtests** (+9); `measurement_catalog.py validate` **11 instruments valid** against tracked sources, `python3 tools/doc-consistency-check.py --repo .` **PASS**, `recurring-defect-lint.py` clean, `yamllint` clean. **Full CI not yet run** — `ci.yml` triggers on `pull_request` to `main` only. No `SNAPSHOT_SCHEMA_VERSION` change, no new RNG stream / domain tag / draw site, no draw-order change, no `[GT]`, no new assembly.

> **Last Updated (prior):** September 10, 2026 — **PR #391 documentation/governance close-out for the #40 T1b review correction (`ERR-030-050`).**
> **Correction landed.** The functional head already preserves populated finance state through `Load -> SeasonLoop.Restore -> Save(SeasonLoop, ...)` and enforces one shared current-season coherence rule: empty is legal pre-T2; a non-empty finance set exactly matches `SeasonState.ClubIds`. The approved #30/#40 surfaces now match it. `ERR-030-049` is retained verbatim as history, while its incorrect producer-wired narrowing is expressly superseded. The N→0 destination guard remains separate from partial/wrong-set coherence.
> **Phase boundary.** T1b owns persistence composition, the loop carrier/resume seam, and coherence. T2 owns `CreateInitial`, `SettleFinances`, actual producer wiring, and consumer exposure. New source/test files from the correction are registered in the manifest; no C# changes are made by this close-out.