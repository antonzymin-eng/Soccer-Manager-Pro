name: PR439 SeasonSave focused proof

on:
  push:
    branches:
      - evidence/pr439-seasonsave-focused

permissions:
  contents: read

jobs:
  focused:
    name: SeasonSave focused post-fix proof
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: f40f0853909cc2a42190023fea1da72d25409b12
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Run the two previously log-sensitive discipline tests
        shell: bash
        run: |
          set -euo pipefail
          FILTER='Name=ARealEngineFixtureFoldsItsCardsOntoPlayerRecordsAndChangesNothingElse|Name=ANewBanEarnedThisFixtureIsNotServedByThisSameFixture'
          bash tools/dotnet-ci/run-gate.sh --fast --test-filter "$FILTER"
