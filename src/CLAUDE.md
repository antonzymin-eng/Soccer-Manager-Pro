# src/CLAUDE.md — Tactical Director Coding Guide

> **Created:** May 19, 2026
> **Last Updated:** July 25, 2026, latest same day (v2.37 — **Season & Competition Loop #30 T1 LANDED — the
> season save/restore path** (`docs/tracking/path-to-playable-roadmap.md` item A2, the second landing on the
> path-to-playable track). New `src/season-save/SeasonStateCodec.cs`: a pure byte codec for the season-state
> sub-blob over the #30 Appendix B layout (version gate first; seed / seasonNumber / managedClubId; club set;
> the CONCRETE schedule per KD-5; the calendar cursor per KD-4; the table in ClubId order; the board), with
> the `MatchSaveCodec`/`WorldStateSerializer` fail-loud posture — overflow-safe length bounds, a trailing-byte
> guard, and **decode-through-the-validating-constructors** so a corrupt blob throws rather than materializing
> a structurally impossible season (`Fixture` / `LeagueTableRow.Create` / `SeasonCalendar.Create` /
> `BoardState` / the `SeasonState` cross-field gates each catch their own class). `SeasonSaveCodec` v1.1 /
> `SeasonSaveBlobs` v1.1: the frame gains a THIRD opaque sub-blob between the world and match blocks
> (`version → matchPresent → [len]world → [len]season → ([len]match iff present)`), **`SEASON_SAVE_FORMAT_VERSION`
> 1 → 2** (FR-SN-020 — the world and match blobs are byte-untouched; only the frame around them moved, and a
> v1 file is rejected fail-loud). `SeasonSaveManager` v1.2 / `SeasonSaveContents` v1.1: `Save(world, season,
> matchOrNull, path)` / `Load(...) → { World, Season, Match }` per FR-SN-021, with the season blob captured
> before the file is opened alongside the other two. Unlike the match, the season is **never optional** — a
> null fails loud. **Two code self-AR findings fixed:** (1) the per-array length bound is now expressed in
> ELEMENTS (`remaining / bytesPerElement`) rather than as a `count * width` byte product, which can overflow
> int for a large blob and a crafted count and wrap back to a small positive value that slips past a byte-wise
> guard — dividing cannot overflow, so the bound is provably exact and the per-array `Require` it replaces is
> gone; (2) an encode/decode asymmetry at the T0 `SeasonState` constructor — an EMPTY schedule with a
> `default(SeasonCalendar)` was constructible (the calendar-coverage check is vacuous at `maxRound = -1`) and
> `Encode` would write a zero-round calendar that `Decode` cannot rebuild, so the constructor now requires a
> calendar mapping at least one round (`SeasonState.cs` v1.3, regression-locked). **Implementation surfaced
> ERR-030-011** (filed + patched same commit, spec-error-log v1.42): §3.6's `EncodeSeason` pseudocode omitted
> `ManagedClubId` — which Appendix B row 3a lists and `SeasonState` requires, so a codec written to §3.6
> verbatim emits a blob no season can be reconstructed from — and Appendix B row 11 left job security as
> `f32/u8`, neither matching the integer per-mille `BoardState` carries. **Appendix B is the byte-layout
> authority**; §3.6 gains the missing line + a correction note, row 11 is pinned `jobSecurityPerMille i32`
> (ratifying the convention #30 T0 adopted and flagged as a back-prop candidate — the #41 AR-1
> float→integer-per-mille precedent). No `SEASON_STATE_FORMAT_VERSION` change: T1 is that version's first
> use, so the correction lands before any file exists. New `tests/SeasonStateCodecTests.cs` (round-trip field
> identity for fresh / mid-season / **completed** seasons, per-column and scalar locks, encode determinism +
> a non-vacuity control, a pinned-offset layout lock guarding field order, and every FR-SN-023 fail-loud gate
> incl. the serialized-vs-derived goal-difference coherence check); `SeasonSaveManagerTests` v1.3 (a
> `MidSeasonState` fixture; the no-match and with-match round-trips assert the season resumes field-identical
> per FR-SN-022; a v1-frame rejection lock; `Save_NullSeason_Throws`). **Full dotnet gate: PASSED, 0 failures
> (whole tree green; season-save 112 → 135 tests; SDK 8.0.129 via apt).** **Adversarial review over the landing (3 passes, converged): 1M+4L / 1M+2L / 0H+0M+3L.** Pass 1: the T1 self-AR's zero-round calendar guard was MOVED, not resolved — `BeginNextSeason` carries the identical vacuous coverage check unguarded (its `maxRound >= RoundCount` is false at `maxRound = -1`) and the ctor still took an empty fixture array, so a roll could install a state `Encode` writes and `Decode` refuses (reproduced by an executed probe); fixed at the root (the ctor now refuses an EMPTY schedule) and mirrored onto the roll. Pass 2: **FR-SN-011 (MUST) / F4 were unimplemented** — `SatisfiesCursorInvariant` had ZERO production callers while its own doc claimed `SeasonLoop.Restore` invoked it, so a save whose world clock had passed the pending round loaded silently and would surface at T2 as a stuck or skipped round; `SeasonSaveManager.Load` now enforces it (the one cross-blob coherence rule, checkable only at this root, which is the layering argument for the root existing), with the completed-season vacuous case locked so the gate cannot become a spurious refusal. Pass 3: three `Modified` headers stale against their history rows (FR-CS-056). L also: `<exception>` docs on the `Decode` seam, an outer-frame pinned-order lock, a mis-naming test rename, offset-helper widths named + a coherence guard, and two docs naming a T2 type / an already-closed back-prop. **Gate re-run: PASSED, 0 failures (season-save 135 → 141).** Remaining #30: T2 the day-advance
> loop + round resolution, T3 the boundary roll.)
> **Last Updated (prior):** July 25, 2026 (v2.36 — **#30 T0 adversarial-review pass 3: 2M + 1L, all fixed.**
> Pass 2 had returned clean; pass 3 deliberately re-attacked that verdict and found two real defects the
> earlier passes missed. **M-1 (FR-CS-044):** `FixtureScheduler.NextUInt64` performed the SplitMix64 add +
> two 64-bit multiplications with no `unchecked { }` block and no `// Spec #16 §3.4.4` citation — the rule
> applies "regardless of which assembly the code lives in", and all four pre-existing SplitMix64 copies
> in-tree (collision-system / heading-mechanics / pass-mechanics / decision-tree) plus the canonical
> `DeterministicRngService` comply; this one was the sole outlier, and would throw `OverflowException` on the
> first wrap under any future overflow-checked build config. **M-2 (parallel-surface drift):** `SeasonState`
> had no instance-field-count coupling guard, so a field added at T1 but omitted from BOTH the codec and
> `FieldsEqual` would make the FR-SN-022 round-trip pass vacuously while the save silently dropped it —
> invisible in-process, surfacing only as a corrupted season after a reload. Added the reflection lock
> (8 fields), the `PassExecutorStateTests.cs:348` / `ShotExecutorStateTests.cs:376` precedent. **L:** the
> single-threaded-access contract is now recorded on `SeasonState` along with the T2 marshaling obligation
> (#38 already runs a sim thread beside a UI thread — FR-UI-023/F6). One candidate finding was WITHDRAWN
> rather than reported: SplitMix64 duplication is the project's established norm (4 pre-existing copies,
> no public helper on `deterministic-sim`), not a defect introduced here. `FixtureScheduler.cs` v1.1,
> `SeasonState.cs` v1.2, `SeasonStateTests.cs` v1.1. **Full dotnet gate: PASSED, 0 failures (whole tree
> green; season-save 106 → 107 tests); meta integrity clean.** **Pass 4 then found the pass-3 M-2 fix
> INCOMPLETE (1M, fixed):** `SeasonState.FieldsEqual` does not compare `SeasonState`'s own fields
> structurally — it reaches INTO the aggregates (`Board.JobSecurityPerMille`,
> `Board.Objective.TargetPositionOrBetter`, the four `Fixture` columns inline) and delegates to
> `SeasonCalendar.FieldsEqual` + `LeagueTable.FieldsEqual` (which hand-enumerates nine `LeagueTableRow`
> columns), so a column added to ANY aggregate leaves `SeasonState`'s own count at 8, the new guard stays
> green, and the FR-SN-022 round-trip still passes vacuously — the exact failure mode the guard exists to
> catch, open on five of six surfaces. The same sibling-drift class as PM AR-7 M-1 ("gating drift fixed
> across sibling files"). Extended to `LeagueTableRow` (9) / `Fixture` (4) / `SeasonCalendar` (2) /
> `BoardState` (2) / `BoardObjective` (1); `SeasonStateTests.cs` v1.2, season-save 107 → 112, gate green.
> **Pass 5 (1M, fixed):** `SeasonLoopConstants`' four `[GT]` rows were `public const` ALL_CAPS under a
> `#region GameplayTuned` — the exact pattern the June-30 FR-CS-019 migration removed tree-wide, in a
> catalogue authored after it landed. Three conventions at once: ALL_CAPS is `[FIXED]`-only (these are
> explicitly tunable — the file's own doc says "a rules variant is a config change, not a code change",
> which the declaration form made false), `[GT]` must be `static readonly` (the v1.84 attacking-ai /
> defensive-ai defect), and the region is named `GT`. The cost is expandability: a `const` cannot take
> `Config.GetInt`, so these four were structurally locked out of the migration 17 other catalogues
> completed, and `const` inlines into consumers so a points-scheme change would need a full recompile.
> Renamed `WinPoints`/`DrawPoints`/`LossPoints`/`JobSecurityScale`, converted to `static readonly` off
> `GameplayConfig` (literals as fallbacks ⇒ behaviour-neutral), region renamed, `season-save.asmdef`
> gains the `ProjectConstants` ref. `SeasonLoopConstants.cs` v1.1; gate green, 112 tests.)
> **Last Updated (prior):** July 25, 2026, later same day (v2.34 — **#30 T0 adversarial-review fix pass: 1H + 1M + 6L,
> all fixed; re-review pass 2 clean.** **H-1 (structural):** `SeasonState.Table` was a PUBLIC property
> returning a mutable `LeagueTable`, so any consumer could call `ApplyResult` and rewrite season state
> outside the command API — defeating the KD-7 / FR-SN-032 single-writer contract that every other
> mutator on the type honours (the tests themselves exploited it at 5 call sites). The constructor also
> stored the caller's table by reference while the arrays 20 lines above were snapshot-copied with a
> comment explaining why. Fixed: `Table` narrowed to `internal` (the visibility IS the enforcement —
> `SeasonLoop` at T2 is the only production writer), public read-only projections added
> (`TableOrdered` / `TableRowsInClubIdOrder` / `TableRow` / `PositionOf`, all value copies), an
> `internal ApplyResult(in MatchResult)` forwarder added for the §3.4 round loop, and the ctor now
> `table.Clone()`s. Verified no public mutation path remains: every public member returns a value copy
> or a `ReadOnlyCollection`, and `SeasonCalendar` never exposes its day array. **M-1:**
> `LeagueTableRow.Create` had no validation, and it is #30 T1's decode seam — a corrupt save would have
> produced a table with negative counts instead of failing loud (F3 / FR-SN-023); now rejects negative
> counts and requires `won + drawn + lost == played`. Points are checked for non-negativity only,
> deliberately NOT against `3W+D`, since the scheme is a `[GT]` value. **L:** `default(SeasonViewModel)`
> null-dereference documented; `ApplyResult(in MatchResult)` + `FromRows` failure paths given tests
> (both previously uncovered, both T1/T2 entry points); §3.1 pseudocode binds `ring := ids` (used but
> never defined); roadmap engine-test count 306 → 321; roadmap C1 relabelled a **lower bound** (it
> multiplies p50 by the tick count, but wall-clock tracks the mean and p99 = 2.5669 ms — the true
> figure is higher, which only strengthens the infeasibility conclusion). `section-3.md` v0.9,
> `path-to-playable-roadmap.md` v0.3. **Full dotnet gate: PASSED, 0 failures (whole tree green;
> season-save 97 → 106 tests).**)
> **Last Updated (prior):** July 25, 2026 (v2.33 — **Season & Competition Loop #30 T0 LANDED — the first code on the
> path-to-playable track** (`docs/tracking/path-to-playable-roadmap.md` item A1). Eleven new files in the
> EXISTING `TacticalDirector.SeasonSave` assembly (#30 §4.1 — no new assembly; the root already sits above both
> `match-engine` and `living-world`): `SeasonLoopConstants` (Appendix A points + `SEASON_STATE_FORMAT_VERSION` +
> `IDENTITY_PERMUTATION_SEED`), `Fixture`, `FixtureScheduler`, `LeagueTableRow`, `LeagueTable`, `SeasonCalendar`,
> `BoardObjective`, `BoardState`, `MatchResult`, `SeasonState`, `SeasonViewModel`, + `AssemblyInfo.cs`
> (`InternalsVisibleTo` — the KD-7 single-writer enforcement: every `SeasonState` mutator is `internal`, so the
> only production writer will be `SeasonLoop` at T2). **Behaviour-neutral by construction** — nothing wires
> `MatchEngine` or `WorldStore` yet (T2), and no codec touches the save frame yet (T1), so
> `SEASON_SAVE_FORMAT_VERSION` stays 1 and every existing blob is untouched. **The [CROSS] determinism rows are
> deliberately ABSENT** — ERR-030-001 is spec-text-first and pins `DOMAIN_TAG_SEASON_LOOP`/ordinal 84 to T2's
> first draw site (a zero-draw stream is the FR-LW-031 phantom class; the `world.arcs` precedent).
> **Implementation found a real spec defect — ERR-030-010** (filed + patched same commit, spec-error-log v1.41):
> §3.1's generation pseudocode venues the first leg by round parity, but the worked schedules in §3.7 and
> Appendix C were hand-derived without it (rounds 1/4 inverted) and §5.2's T-SN-FIX-001 pinned those tables.
> The pseudocode wins — measured at 20 clubs the unparried form gives the pinned club **all 19** first-leg
> fixtures at home, the parity form gives every club 8..10 of an ideal 9..10; both satisfy FR-SN-002/003 at
> N = 2,3,4,5,6,19,20 and no FR constrains venues, so §3.1's own "for a balanced first leg" intent decides.
> §3.7 (v0.9) / Appendix C (v0.3) corrected, T-SN-FIX-001 re-anchored, new **T-SN-FIX-008** venue-balance lock.
> Two self-review findings folded in before commit: the `SeasonState` ctor now rejects a calendar shorter than
> the schedule (which would strand the cursor mid-season, and also catches a `default(SeasonCalendar)`), and
> `BeginNextSeason` now applies the ctor's club-membership + calendar-coverage gates (the seam #43's
> promotion/relegation transform will use). Copy-then-validate throughout (the recurring live-array aliasing
> class), and `ApplyResult` resolves BOTH rows before writing EITHER so an unknown away club cannot leave the
> home row half-updated (F2 "table unchanged"). **Full dotnet gate: PASSED, 0 failures (whole tree green;
> season-save 20 → 97 tests; SDK 8.0.129 via apt).** Remaining #30: T1 codec + `SEASON_SAVE_FORMAT_VERSION`
> 1 → 2, T2 the day-advance loop + round resolution, T3 the boundary roll.)
> **Last Updated (prior):** July 22, 2026 (v2.32 — **Goalkeeper #11 + Heading #10 engine integration, Phase 1
> (opt-in)** — the GK/Heading attribute projections landed with a live consumer, per the new converged
> supplement `docs/tracking/gk-heading-engine-integration-design.md`. `MatchEngine.cs` v1.44 constructs
> both sealed orchestrators + four stateless nested adapters (`HeadingBallWorldAdapter` /
> `GoalkeeperBallWorldAdapter` / `HeadingRngWorldAdapter` / `GoalkeeperRngWorldAdapter`, the
> `PassWorldAdapter`/`ShotWorldAdapter` pattern) at boot and registers `heading.mechanics` +
> `goalkeeper.mechanics` RNG streams. A new public `EnableGkHeading()` opts in (default off = byte-identical
> to pre-wiring, no schema change); when on, `DriveGkHeadingTactical` (10 Hz) + `DriveGkHeadingPhysics`
> (60 Hz, before the Resolve goal check) drive both, and conservative §4 world-state triggers commit a
> `SaveIntent` seeded from `PlayerAttributeProjection.ToGoalkeeper` + a `HeaderIntent` from `ToHeading`.
> The durable-capture seams (`CaptureDurableHeader`/`CaptureDurablePayload`) fail loud
> (`NotSupportedException`) when on (Phase 1 not snapshot-safe); the per-tick `SerializeWorldState` is
> untouched, so a flag-on engine ticks forward-deterministically. `PlayerAttributeProjection.cs` v1.2 adds
> `ToGoalkeeper` (int→float widen of the ten GK fields) + `ToHeading` (raw Heading/Strength/Balance copy);
> `MatchEngineConstants.cs` v1.25 adds 6 `[GT]` trigger constants. `match-engine(.tests).asmdef` gain the
> HeadingMechanics + GoalkeeperMechanics refs. The orchestrator CLASS names collide with their own
> NAMESPACES (CS0118), so `_heading`/`_goalkeeper` are fully qualified. Code self-AR folded: CS0118 fix,
> `RefreshGkAgentIds` (ConfigureSquads reassigns `_isGoalkeeper`, so the boot keeper roster would go
> stale), and the guard placement. New `tests/MatchEngineGkHeadingTests.cs` (8) +
> `PlayerAttributeProjectionTests` v1.1 (+2). **Full dotnet gate run locally (SDK 8.0.129 via apt):
> PASSED, 0 failures (whole tree green; 290 match-engine tests). Default engine byte-identical — no
> `SNAPSHOT_SCHEMA_VERSION` change.** Phase 2 (deferred): serialize GK/Heading cross-tick state at v18 +
> flip default on + digest rebaseline; DT-driven producer; the closed-loop scenario.)
> **Last Updated (prior):** July 22, 2026 (v2.31 — **Unified season save landed** — the snapshot-deserialize N2 /
> match-engine Phase G-Phase 3 season save-file root, per the new converged supplement
> `docs/tracking/unified-season-save-design.md` v0.5 (AR-1 2M+3L → AR-2 CONVERGENCE; code AR 0H+0M+2L, fixed).
> New `src/season-save/` assembly `TacticalDirector.SeasonSave` (references `MatchEngine` + `LivingWorld`
> + `DeterministicSim`) — the composition/persistence layer **above both**, the only assembly that may
> see both blobs, which is how **FR-LW-003** is honoured (neither `match-engine` nor `living-world`
> references the other; the root sits above, like `match-viewer` over `match-engine`). Files:
> `SeasonSaveConstants` (`[FIXED] SEASON_SAVE_FORMAT_VERSION = 1` — a FOURTH format version, distinct from
> the two snapshot schema versions + `MATCH_SAVE_FORMAT_VERSION` + `WORLD_STORE_FORMAT_VERSION`),
> `SeasonSaveBlobs` (deframe result — two opaque byte sub-blobs), `SeasonSaveCodec` (pure static:
> `byte[] Encode(worldBlob, matchBlobOrNull)` / `SeasonSaveBlobs Decode(byte[])` — a version-gated frame
> around a `matchPresent` flag + two length-prefixed opaque sub-blobs, each keeping its own version gate;
> overflow-safe `Require` bound + fail-loud on bad format version / bad flag / out-of-bounds length /
> trailing bytes — the `MatchSaveCodec` posture), `SeasonSaveContents` (reconstructed `WorldStore` +
> nullable `MatchEngine`), `SeasonSaveManager` (static: `Save(world, matchOrNull, path)` = capture both →
> `Encode` → atomic temp→fsync→rename; `Load(path, ISquadProvider squads = null) → SeasonSaveContents` =
> read → `Decode` → `WorldStore.Restore` +, when present, `MatchSaveManager.Restore`). **KD-2:** the file
> is a thin frame over two SELF-CONTAINED blobs — `WorldStore.Snapshot()` (always) + the match save blob
> (only when a match is in progress, a `matchPresent` flag byte, KD-3) — the codec never parses either's
> internals, so all four inner versions are untouched (no `SNAPSHOT_SCHEMA_VERSION` /
> `WORLD_STORE_FORMAT_VERSION` / `MATCH_SAVE_FORMAT_VERSION` change; the season file only adds the fourth
> frame version). **KD-5:** `MatchSaveManager` gains public `Encode(engine) → byte[]` +
> `Restore(blob, squads) → MatchEngine` (the "match save as a value" blob API — the capture seams stay
> internal); `Save`/`Load` refactored to delegate (behaviour-identical — all 279 match-engine tests still
> green). **KD-6:** the `ISquadProvider` is a Load-time parameter, never persisted, threaded into the
> match restore only when a match blob is present; the match restore's fingerprint + MXCSR float-mode
> gates run on season `Load` unchanged. New `season-save/tests/SeasonSaveManagerTests` (19): disk
> round-trip determinism for a no-match season (world field-identical via re-`Snapshot()` byte-equality +
> `world.text` resumes) and a season with a neutral / distinct-squad match (match digest chain
> byte-identical AND world field-identical, both through one file — the distinct-squad case exercises the
> provider threading through season `Load`), `SeasonSaveCodec` round-trip (with/without match, empty
> world) + all fail-loud gates, and `SeasonSaveManager` missing/corrupt/no-provider/null-world/overwrite.
> The booking-cursor (KD-8) case is a nested-match-blob property already locked by `MatchSaveManagerTests`
> (needs a match-test-internal seam), not re-driven at the season level — the season frame provably cannot
> alter it (KD-2). **Full dotnet gate run locally (SDK via apt): PASSED, 0 failures (whole tree green; 18
> new season-save tests). This closes snapshot-deserialize Phase 3 / the unified season save.** A fresh
> code adversarial-review pass over the shipped diff (0H+0M+2L, both fixed): L-1 restored
> `MatchSaveManager.Save`'s engine-before-path argument-guard order (the `Encode` delegation had flipped
> it — kept exactly behaviour-identical, `MatchSaveManager.cs` v1.2); L-2 added
> `Load_NoMatchSeason_WithProvider_IgnoresProvider` (locks R4, 18 → 19 tests). Gate re-run PASSED.)
> **Last Updated (prior):** July 21, 2026 (v2.30 — **On-disk match save format landed** — snapshot-deserialize
> Phase 3 `SaveManager` fold (N1), per the new converged supplement
> `docs/tracking/match-save-file-design.md` v0.3. Three new `src/match-engine/` files: `MatchSaveCodec`
> (pure static: `byte[] Encode(matchSeed, header, payload)` / `MatchSaveContents Decode(byte[])` — a
> single `MATCH_SAVE_FORMAT_VERSION`-gated blob = the KD-7 boot-`matchSeed` boot-header + the
> `SnapshotHeader` incl. its `EnvironmentFingerprint` + digest chain + the `SnapshotPayload`, via
> `CanonicalSerializer`; fail-loud decode on version mismatch / out-of-bounds length prefix / trailing
> bytes, with an overflow-safe `Require` bound guard), `MatchSaveContents` (the decode result struct),
> and `MatchSaveManager` (static: atomic `Save(engine, path)` — capture → Encode → temp→fsync→rename,
> the §4.6.1.1 contract re-implemented since `SaveManager` exposes no raw-blob write — and
> `Load(path, ISquadProvider squads = null) → MatchEngine` via `RestoreFromSnapshot`). `MatchEngine.cs`
> v1.43 gains a public `MatchSeed` property (the boot seed the payload does not carry) and the two
> durable-capture seams promoted `TestOnly_CaptureDurableHeader/Payload` → production internal
> `CaptureDurableHeader/Payload` (now consumed by `MatchSaveManager`; `MatchEngineSnapshotRestoreTests`
> repointed to the production names). `MatchEngineConstants.MATCH_SAVE_FORMAT_VERSION` [FIXED] = 1 (a
> third version, distinct from the two snapshot schema versions it frames). No `SNAPSHOT_SCHEMA_VERSION`
> change (a file frame around the unchanged Phase-1/2 reader/writer). New `MatchSaveManagerTests` (16):
> disk round-trip determinism (neutral / booking-before-save / distinct-squad via `ISquadProvider`),
> `MatchSaveCodec` round-trip + fail-loud gates (bad format version, oversize payload length, trailing
> bytes, truncation, null blob, tampered fingerprint → restore refusal), and `MatchSaveManager`
> missing-file / corrupt-file / no-provider / null-engine / overwrite-atomically paths. Code
> self-adversarial review folded one hardening: the `Require` decode bound guard rewritten overflow-safe
> (a corrupt near-`int.MaxValue` length prefix could wrap `offset + need` negative and slip the guard;
> now compares against `total − offset`), locked by `Codec_OversizePayloadLength_FailsLoud`. **The KD-6
> `EnvironmentFingerprint` gate now runs end-to-end through disk** — a save under `CreateStage0Dev`
> validates on load; a tampered/foreign fingerprint is rejected (closing snapshot-deserialize O3 for the
> on-disk path). No asmdef change (DeterministicSim + PlayerDatabase already referenced). **Full dotnet
> gate run locally (SDK via apt): PASSED, 0 failures (279 match-engine tests; whole tree green).**
> Remaining Phase 3: the native MXCSR live-mode query (host-blocked) + the N2 unified season save
> (FR-LW-003 + season save-file root).)
> **Last Updated (prior):** July 20, 2026 (v2.29 — **Snapshot-deserialize Phase 2 landed** — distinct-squad
> restore re-projection (#27 T3 / KD-3), per `docs/tracking/snapshot-deserialize-design.md` v0.8. New
> public `ISquadProvider` interface (`src/match-engine/ISquadProvider.cs`) — the caller-supplied
> `int ClubId → Squad` resolver — threaded into `MatchEngine.RestoreFromSnapshot(in SnapshotHeader,
> SnapshotPayload, ulong matchSeed, ISquadProvider squads = null)` (`MatchEngine.cs` v1.42). New private
> `ReprojectDistinctSquads` replaces the Phase-1 distinct-squad fail-loud: neutral fast-path returns
> immediately; each team with a non-sentinel `_rosterClubId` resolves its roster (ClubId-check +
> `ValidateSquadSize` + `ValidateSelectedRecords`, both teams before any apply), re-runs
> `LineupSelector.Select` + `PlayerAttributeProjection` for the base lineup (`ReprojectBaseLineup` — writes
> `_canonicalAttrs`/`_attrs`/`_dtAttrs`/`_perceptionAttrs`/`_benchCanonicalAttrs`/`_benchAttrs` **and** the
> bench GK flags `_benchIsGoalkeeper`, a boot-constant NOT serialized — but deliberately NOT the on-pitch
> `_isGoalkeeper`, which is serialized and already restored, so a substituted slot keeps its restored
> bench-GK flag), then replays the substitutions the serialized `_activeBenchSlot` records
> (`ReprojectSubstitutions`, the attribute half of `SubstitutePlayer`). Fail-loud on absent provider
> (`NotSupportedException`), unresolvable ClubId (`NotSupportedException`), or a provider returning a
> mismatched ClubId (`InvalidOperationException`). New `TestOnly_BenchIsGoalkeeper` seam;
> `MatchEngineSnapshotRestoreTests` v1.1 gains distinct-squad G3 round-trip (base / mid-match sub /
> post-restore sub / post-restore keeper-for-keeper sub) + the three provider fail-loud gates. No
> `SNAPSHOT_SCHEMA_VERSION` change. **Full dotnet gate run locally (SDK via apt): PASSED, 0 failures (263
> match-engine tests; whole tree green).** Implementation finding: `_benchIsGoalkeeper` is NOT serialized
> (only on-pitch `_isGoalkeeper` is), so it must be re-projected for a post-restore keeper substitution to
> set the flag correctly — added, locked by the keeper-for-keeper test. Out-of-scope discovery (a Phase-1
> snapshot-completeness follow-up, recorded in the root `CLAUDE.md` OPEN ISSUES): a post-restore
> substitution that FLIPS a slot's GK status (keeper onto an outfield slot — never done in realistic play)
> diverges via a Positioning-AI (#12) formation-slot interaction with the flag flip. Remaining: Phase 3
> (MXCSR / on-disk fold, host-gated).)
> **Last Updated (prior):** July 20, 2026 (v2.27 — **Snapshot-deserialize Phase 1 reader landed** — the read
> half of the save/load/replay path, per `docs/tracking/snapshot-deserialize-design.md` v0.7 (Phase 1
> complete). New `MatchEngine.DeserializeWorldState` (`MatchEngine.cs` v1.41) — the symmetric line-for-line
> mirror of `SerializeWorldState` + per-block `Read*` helpers, reconstructing each subsystem's cross-tick
> state through its `RestoreState` seam (KD-1/KD-2), with a version-gate first-field check and an
> event-ledger-boundary trailing guard (R1). New static `MatchEngine.RestoreFromSnapshot(in SnapshotHeader,
> SnapshotPayload, ulong matchSeed)` factory (KD-4/KD-6: `EnvironmentFingerprint.ValidateAgainst` gate step 0
> against `CreateStage0Dev` → `new MatchEngine` boot + `EventBus.ResetForNewMatch` → `DeserializeWorldState`
> → KD-3 non-sentinel-roster fail-loud → `SnapshotCodec.CommitLoadedDigest` + `MatchClock.RestoreFromSnapshot`,
> KD-5). New `RestoreState` counterparts: `PressingAITick`/`DefensiveAITick`/`AttackingAITick`
> `RestoreState(in XxxTickState)`, `PerceptionSystem.RestoreState(in PerceptionTickState)`,
> `PositioningAITick.RestoreState(HysteresisState)`, `MovementCommand.ReconstructFromSnapshot` (v1.5) — the
> RotationController `Restore*` / executor / DecisionTree / OscillationGuard / clock / RNG restore seams
> pre-existed. The reader reconstructs `_possessingAgentId`/`_prevPossessingAgentId` from the restored
> `MatchContext.PossessingAgentId` (they are excluded per the writer's exclusion proof but read at the next
> tick's start; the `_prev == _poss == MatchContext.PossessingAgentId` snapshot-time invariant makes one
> field sufficient), and validates the world-state read ends at the appended event-ledger boundary rather
> than restoring the ledger (replayed forward; baked into the header digest). New `TestOnly_CaptureDurable-
> Header/Payload` seams + `MatchEngineSnapshotRestoreTests` (G3 round-trip determinism: neutral / mid-match-
> tactics / KD-8 booking-cursor + version-gate/trailing-byte/distinct-squad fail-loud). No `SNAPSHOT_SCHEMA_
> VERSION` change. **Full dotnet gate run locally (SDK via apt): PASSED, 0 failures (257 match-engine tests;
> whole tree green).** Remaining: Phase 2 (#27 T3 distinct-squad re-projection) + Phase 3 (MXCSR/on-disk).)
> **Last Updated (prior):** July 18, 2026 (v2.26 — **Squad/Player Data Layer #27 T3 landed** — the snapshot
> roster-reference field, per the converged `docs/tracking/squad-roster-reference-design.md` (v0.2,
> AR-1..AR-2). New per-team `MatchEngine._rosterClubId[TEAM_COUNT]` (the loaded `Squad.ClubId`, or
> `[FIXED] NO_ROSTER_CLUB_ID = -1`; `MatchEngineConstants.cs` v1.23), set by `ConfigureSquads` after
> both squads validate-and-apply, serialized at **`SNAPSHOT_SCHEMA_VERSION` 15 → 16** (`MatchEngine.cs`
> v1.39). Boot-constant identity (the `_teamIds`/`_isGoalkeeper` class — non-phantom despite no restore
> consumer): a save records which squad each team loaded (the identity half of restore fidelity); the
> per-slot attribute VALUES stay excluded, re-projectable from the roster keyed by the serialized
> `_activeBenchSlot`. KD-T3-2: a configured squad is digest-distinguishable from unconfigured by design
> (identity, not attributes) — supersedes the T1 KD-P7 all-default byte-identity lock; behaviour stays
> neutral (a config-default run diverges from unconfigured at tick 1 = the roster field alone). KD-T3-3:
> the restore re-projection is future (no snapshot-deserialize path exists in `MatchEngine.cs` —
> verified). New `TestOnly_RosterClubId` seam; exclusion-proof + `ConfigureSquads`/substitution
> restore-scope docs updated. Tests: `MatchEngineSnapshotSchemaTests` v1.13 (pin 15 → 16 +
> `RosterReference_FeedsSnapshotDigest` probe), `MatchEngineSquadTests` v1.2 (T1 neutrality lock
> replaced with KD-T3-2 identity-capture + same-config-determinism + distinct-ClubId + sentinel-seam
> locks). **Post-landing code AR (fresh-eyes over the shipped diff): 0H+0M+1L — replacing the T1
> byte-identity lock dropped the direct match-level proof that a config-default match is BEHAVIOURALLY
> identical to unconfigured; added `ConfiguredDefaultSquad_IsBehaviourNeutral_ObservableStateMatches-
> Unconfigured` (ball + every agent position match tick-for-tick — the observable signal a digest can
> no longer isolate from the roster field). Re-verified: field appended last, no payload-offset
> decoder exists.** **Full dotnet gate re-run locally (SDK via apt after `apt-get update`): PASSED, 0
> failures (237 match-engine tests).** See the root `CLAUDE.md` July-18 Last-Updated header entry.)
> **Last Updated (prior):** July 17, 2026, latest same day (v2.25 — **Repeat adversarial review of the T1/T2
> landing (AR-4): 1 M + 3 L, all doc-only, fixed; AR-5 sweep 0H+0M+1L (doc) — CONVERGENCE, cycle
> CLOSED.** Full-surface re-walk verified clean: writer completeness of every projected array,
> the 3-site FirstTouchAbility inventory, no Perception-side `_perceptionAttrs` mutation, DT's
> internal `(raw−1)/19` normalization correctly untouched (KD-P2), and the #13 WeakReceiver
> trigger's T1-activated distinct-squad path (default unchanged). M-1: `AttackingAgentSnapshot.
> Pace/Dribbling` docs claimed the `(raw−1)/19` convention while the T1 writer populates ÷20
> (KD-P3) — post-T1 that misdescribed LIVE data; aligned (v1.1), math switch stays deferred. L:
> three stale "neutral placeholder" comments in `MatchEngine.cs` (v1.38, doc-only); the
> `STAGE0_NEUTRAL_*` stale ERR-007 TODOs retired — production-unconsumed since T1, kept as the
> KD-P7 neutral-equivalence references (`MatchEngineConstants.cs` v1.22); `ConfigureSquads` doc
> notes players beyond the consumed 18 are ignored (AR-5). `PlayerAttributeProjection.cs` v1.1
> (ToNormalized note updated). Full dotnet gate re-run: PASSED, 0 failures.)
> **Last Updated (prior):** July 17, 2026, latest same day (v2.24 — **Squad/Player Data Layer T1/T2 landed** —
> `MatchEngine` attribute seeding sourced from canonical player records per the converged
> `docs/tracking/player-attribute-projection-design.md` (v0.3). New
> `src/match-engine/PlayerAttributeProjection.cs` (pure static, one method per LIVE target — KD-P8
> keeps GK/Heading out as phantom consumers): `ToAgentMovement`/`ToDecisionTree`/`ToPerception` raw
> copies with caller-supplied runtime TeamId/IsHalfTurned (KD-P4); `ToPass`/`ToShot` with the KD-P1
> derived KickPower (`(Passing+Technique)×.5` float / `Mathf.RoundToInt((Finishing+LongShots)×.5)` —
> the ERR-007 proxies now computed from real attributes); `FirstTouchAbility` for the three #13/#14/#4
> sites (KD-P9); `ToNormalized` = `÷ATTRIBUTE_MAX` for the sole pre-normalized target (KD-P3, neutral
> ⇒ 0.5 exactly). `MatchEngine.cs` v1.37: `_canonicalAttrs`/`_benchCanonicalAttrs` (fully-qualified
> per KD-P6 — CS0104 vs `AgentMovement.PlayerAttributes`; default `CreateDefault()`; NOT serialized,
> B3 exclusion proof extended with the KD-P10 distinct-squad restore scope), every seeding site
> converted (zero production `STAGE0_NEUTRAL_*` consumers remain — incl. `FirstTouchContext.Technique`,
> an implementation-time inventory addition recorded in the projection doc v0.4), public
> `ConfigureSquads(homeSquad, awaySquad)` (pre-kickoff gate on `CurrentTick == 0`; Stage-0
> roster-order lineup — players 0..10 → pitch slots (0 = GK slot), 11..17 → bench; [1,20]/[1,5]
> bounds gates), `SubstitutePlayer` copies the canonical bench record + re-projects
> `_dtAttrs`/`_perceptionAttrs` (the v2.20 hazard's on-pitch half), +6 `TestOnly_*` attribute seams.
> `match-engine(.tests).asmdef` gain the `PlayerDatabase` ref. **Self-AR of the landing: AR-1 1 M
> fixed** (per-team validate-then-apply left the home squad landed when the away squad was refused —
> `ValidateSquad` hoisted for both teams before any write, + `ConfigureSquads_InvalidAwaySquad_
> LeavesHomeUnapplied` lock); AR-2/AR-3 sweeps clean. New suites: `PlayerAttributeProjectionTests`
> (per-field scale locks with distinct inputs, KickPower incl. the pinned half-to-even case,
> WeakFoot [1,5] round-trip, KD-P7 neutral-equivalence against every pre-T1 seed) +
> `MatchEngineSquadTests` (all-default-squad digest neutrality, distinct-squad divergence +
> two-run determinism, substitution canonical swap, all fail-loud gates). Doc corrections landed
> with it: #27 doc v0.5 (FirstTouchAbility reserved→consumed, KD-P9) + `PlayerAttributes.cs` v1.1.
> **Full dotnet gate: PASSED, 0 failures (232 match-engine tests).** Remaining #27: T3 roster
> reference (distinct-squad restore), lineup selection, Stage-1+ persistence.)
> **Last Updated (prior):** July 17, 2026, later same day (v2.23 — **Fourth repeat adversarial review (AR-4 of the
> July-16 cycle): 0H+0M+1L (doc-only), fixed — CONVERGENCE, cycle CLOSED** per the L-only-round
> convention (match-viewer AR-4 precedent). The pass walked the complete sent-off participation
> matrix (dispatch skip / 4 snapshot fills / forced-stop / offside line / AR-8 receiver scan /
> AR-9 foul interpretation / substitution refusal / half+full-time one-shots) plus the in-flight
> compositions the prior rounds never checked together: a card's `ApplyRestart` clears possession
> BEFORE the executor advance and the adapters' `IsBallPossessedBy` reads the live value, so a
> just-sent-off agent's mid-windup pass/shot self-cancels at CONTACT (FM-08/FM-05) — no leak
> through in-flight executors. L: `_lastHolderAgentId` writer comment overclaimed the
> `GoalAwardedEvent` credit ("names the agent whose kick scored") — a deflection-chain goal
> credits the last SETTLED holder (deflections never update the tracker, the RestartResolver-seam
> approximation), possibly not the kicker and possibly sent off since; aligned, doc-only
> (`MatchEngine.cs` v1.36). Full dotnet gate re-run: PASSED, 0 failures.)
> **Last Updated (prior):** July 17, 2026 (v2.22 — **Third repeat adversarial review (AR-3 of the July-16 cycle):
> 1 M, fixed.** M-1: `ApplyFoulIfCaptured` never checked `_isSentOff` on either foul participant —
> the foul/card/restart interpretation is itself a participation surface, and sent-off agents
> remain collision bodies (the physics forced-stop decelerates them, then they stand frozen in the
> path of play), so a red-carded agent repeatedly "won" free kicks (`ApplyRestart` teleported the
> ball to their feet) and drew cards against opponents who ran into their back, for the rest of
> the match. Fixed at the application site (`MatchEngine.cs` v1.35 — timing-equivalent to the
> capture site since capture and application share a tick and cards are issued only there; covers
> the `TestOnly_InjectFoulCandidate` seam; a single gate avoids the PM-AR-7-M-1 sibling-drift
> class); no event/cooldown/restart on a discarded candidate; physical collision response
> unchanged. +2 locks (`MatchEngineFoulCardTests` v1.1 — sent-off victim in the exact positive
> free-kick geometry, and sent-off offender). Verified clean this pass: every card path clears
> possession via `ApplyRestart` (no sent-off-possessor deadlock vector), the Interception case
> maps the Stage-0 unresolved interceptor to NO_POSSESSION, the gate-4 tie-break matches its
> doc, and all six AR-7/AR-8 fixes hold. Full dotnet gate re-run: PASSED, 0 failures.)
> **Last Updated (prior):** July 16, 2026, later same day (v2.21 — **Repeat adversarial review (AR-2), 1 M + 1 L,
> both fixed.** M-1: sent-off agents excluded from `RunFirstTouch`'s gate-4 receiver scan — the ONE
> participation surface missing the `_isSentOff` exclusion (AI dispatch / four snapshot `IsActive`
> fills / physics forced-stop / offside line all have it); pre-fix a ball rolling past a frozen
> red-carded agent handed them possession they could never release (no AI dispatch ⇒ no kick),
> deadlocking play until the next half/full-time reset (`MatchEngine.cs` v1.34 +
> `MatchEngineFirstTouchTests` v1.1 lock — the CONTROLLED-receive geometry with the agent sent off
> stays loose; physical presence deliberately unchanged). L (doc): `AttrIdx` "Technical (8)" group
> comment lists 7 members (`AttrIdx.cs` v1.1; ordinals/totals were correct). Also re-verified the
> v2.20 fixes hold and swept LiveMatchFrame / NameCatalogue / the four previously-unread test
> suites / RunPhysicsPhase — clean. Full dotnet gate re-run: PASSED, 0 failures.)
> **Last Updated (prior):** July 16, 2026 (v2.20 — **Adversarial-review fix pass over the last three landings**
> (match-flow completion v2.17 / interactive match view v2.18 / squad-player data layer v2.19; a
> fresh-eyes cross-cutting review, run at the user's request, of work all three of which had
> already passed their own AR cycles). Findings: 2 M + 4 L, all fixed. **M-1 (match-flow):**
> `SubstitutePlayer` never reset `_yellowCards[outSlotIndex]` — discipline was slot-keyed, so a
> substitute replacing a booked player was sent off on their own first yellow via the
> second-yellow promotion; the count now resets at substitution (`MatchEngine.cs` v1.33; no schema
> change — v15 already serializes the count) + regression locks. **M-2 (player-database):**
> `SquadFileLoader` bounded every numeric key EXCEPT `age` (int.MinValue/MaxValue), contradicting
> its own "out-of-range int all throw" remarks and the generator's [AgeMin, AgeMax] model — now
> range-checked (+2 locks). **L:** post-full-time `SubstitutePlayer` refused (it mutated a frozen
> match while its queued SubstitutionEvent could never flush — RunResolvePhase returns before
> PublishPendingSubstitutions once `_matchEnded` is set); `RestartResolver`'s `lastTouchTeam` doc
> claimed "touched last" while the caller passes the last settled HOLDER (docs aligned at both
> ends of the seam); the live viewer's clock reintroduced the exact `m:60` rounding bug
> `HtmlReplayExporter`'s AR-1 had already fixed (rounds before the minute split now; verified with
> node); `LiveMatchServer.Stop()` joins only the accept thread, so straggling connection threads
> could still drive /control after Stop() returned — a volatile shutdown flag now answers 503;
> `RosterGenerator.DrawBounded`'s modulo bias recorded as deliberate (doc-only). Also flagged
> forward (root `CLAUDE.md` OPEN ISSUES, squad/player entry): substitution swaps `_attrs[outSlot]`
> but `_attrs` are not serialized — byte-neutral today only because bench attrs == starter attrs
> == `CreateDefault()`; the player-database T1 wiring must either serialize attrs or re-derive
> them from the serialized `_activeBenchSlot` on restore. Full dotnet gate not runnable in this
> environment — verified by manual review; CI runs on push.)
> **Last Updated (prior):** July 15, 2026, later same day (v2.19 — **Squad/Player Data Layer T0 landed** — new
> `src/player-database/` assembly (references only DeterministicSim). See the root `CLAUDE.md`
> Last-Updated header entry for the full description; canonical `PlayerAttributes` (31 `[1,20]`
> fields reconciling all 7 existing per-spec attribute structs + `WeakFootRating` `[1,5]`),
> `PlayerRecord`/`Squad` (club-scoped roster), `RosterGenerator` (deterministic; new
> `DOMAIN_TAG_PLAYER_DATABASE`=0x1F + `SubsystemOrdinals.PlayerDatabase`=81 back-propped into
> `deterministic-sim`), `SquadFileLoader` (Stage-0 text import, mirrors `TeamTacticFileLoader`).
> Design doc self-adversarial-reviewed to convergence (2 rounds); code adversarial-reviewed over 2
> passes, catching 3 real defects before landing (`PlayerRecord.Position` had no RNG draw at all;
> `WeakFootRating`'s jitter reused the far-wider attribute spread, clamping most draws to its
> boundary; `SquadFileLoader`'s identity default computed `PlayerId` from the raw local index
> instead of the club-scoped formula `RosterGenerator` uses — caught by a round-trip test that
> would have failed against the bug). Not wired into `MatchEngine` (T1, deliberately deferred — see
> design doc §4/§5). Full dotnet gate not runnable in this environment (mirror 404s) — verified by
> exhaustive manual review in place of `dotnet test`.)
> **Last Updated (prior):** July 15, 2026 (v2.18 — **Interactive match view landed** — a live-updating
> browser viewer watched during a real match, replacing "watch after it ends" with "watch it
> happen"; new `src/match-viewer/LiveMatchFrame.cs`/`LiveMatchStreamer.cs`/`LiveMatchServer.cs` +
> a 3-property `MatchEngine.cs` v1.32 observation-surface extension. See the v2.18 Version History
> row for the full description, including two real concurrency bugs caught and fixed during the
> code-adversarial-review pass (`docs/tracking/interactive-match-view-design.md`).)
> **Last Updated (prior):** July 14, 2026 (v2.17 — **Match-flow completion landed** — throw-ins, corners,
> goal kicks, fouls/cards, offside, substitutions, half-time break, full-time end (see the root
> `CLAUDE.md` v2026-07-14 Last Updated entry for the full description and
> `docs/tracking/match-flow-completion-design.md` for the plan + adversarial-review history). New:
> `src/match-engine/RestartResolver.cs`, `OffsideEvaluator.cs`, `SubstitutionReason.cs`;
> `src/event-system/OffsideCalledEvent.cs` (0x18) / `RestartAwardedEvent.cs` (0x19) /
> `MatchPhaseChangedEvent.cs` (0x1A), registered in `EventRegistry.cs` v1.8. `MatchEngine.cs` v1.31:
> `CheckRestartAndApply` (renamed/extended from `CheckGoalAndRestart`) + `ApplyRestart` shared
> primitive; `MatchFlowCollisionConsumer` (replaces the former no-op `NullCollisionEventConsumer`)
> captures at most one foul candidate/tick against a new `match-flow.card-severity` RNG stream;
> `_yellowCards`/`_isSentOff` discipline state feeds a Physics-phase forced-stop + `IsActive = false`
> in all four Mechanics-AI snapshot fill sites (#12/#13/#14/#15); `EvaluateAndApplyOffside` hooked
> into `RunFirstTouch`'s Controlled case; public `SubstitutePlayer` (bench swap, cap-enforced,
> queued `SubstitutionEvent` publish — the queue exists because `SubstitutePlayer` may be called
> between ticks when `EventBus.CurrentPhase` is not a valid producer phase); `CheckMatchFlowTransitions`
> (every Input phase) fires half-time (ball reset only — no ends-swap; `team 0 attacks +X` is
> hardcoded across goal detection/offside/Mechanics-AI, so a real ends-swap is a documented Stage-1+
> deferral) and full-time (`_matchEnded` freezes AI/Physics/Resolve) transitions, each once.
> **`SNAPSHOT_SCHEMA_VERSION` 14 → 15** (discipline + substitution + match-flow-clock state, all
> cross-tick and digest-load-bearing). New tests: `MatchEngineRestartTests` / `MatchEngineOffsideTests`
> / `MatchEngineFoulCardTests` / `MatchEngineSubstitutionTests` / `MatchEngineMatchFlowTests` (pure-
> function locks + integration + two-run determinism each); `MatchEngineSnapshotSchemaTests` v1.12
> (pin 15 + 2 new probes). Per the user's process instructions, the design doc and the code each went
> through repeated adversarial-review rounds until a full pass found nothing new — the design-review
> cycle caught (among other things) that a full half-time ends-swap would break goal/offside
> detection for the second half, and the code-review cycle caught an `OffsideEvaluator` bug where
> fewer than two active defenders left the accumulator at `+Infinity`/`-Infinity` instead of `NaN`,
> making `IsOffside` return true for every finite attacker position (backwards from the intended
> rule) — fixed via an explicit active-defender count gating the `NaN` return. Full dotnet gate not
> runnable in this environment (no SDK access); verified by exhaustive manual review of the entire
> touched surface (not just the diff) in place of `dotnet test`.)
> **Last Updated (prior):** July 13, 2026 (v2.16 — **Unity engine version bump referenced (documentation
> only): 2022.3.62f1 → Unity 6000.4.9f1, DX11.** Root `CLAUDE.md`'s coding-convention line ("C# with
> Unity 2022 LTS conventions") updated to Unity 6; see the root `CLAUDE.md` v2026-07-13 Last Updated
> entry and its new OPEN ISSUES entry for the full change (`certification-platform.md` v1.3,
> `ProjectVersion.txt`, `cert-run-runbook.md` v1.1). **This file's own technical claims about
> Unity's C#/BCL surface are DELIBERATELY UNCHANGED and now flagged stale-pending-verification**:
> the "BUILD AND TEST COMMANDS" section below and the `tools/dotnet-ci` description still state
> production compiles against `netstandard2.1` because that is "Unity 2022.3's BCL surface" — under
> Unity 6000.4.9f1 that claim has not been re-verified (Unity 6's default scripting API level / BCL
> target may differ) and is out of scope for this documentation-only pass; see root `CLAUDE.md`
> OPEN ISSUES item 4. No source or build-config file changed in this update.)
> **Last Updated (prior):** July 11, 2026, latest same day (v2.15 — **Engine substrate landed: goal detection +
> score state + match-length/halves model — the #26 §9.3 upstream deliverables; #26 half-time
> trigger + live ladder inputs ACTIVATED.** (a) `MatchEngineConstants` v1.20: `[FIXED]
> MATCH_LENGTH_MINUTES` (90) + `[DERIVED] MATCH_TICKS_TOTAL` (= 90×60×`PHYSICS_TICK_HZ` = 324 000
> — the #26 §3.5 `[CROSS-PENDING]` allocation, promoted `[CROSS]` in the spec, §3.5 v0.3) +
> `[DERIVED] HALF_TIME_BOUNDARY_TICK` (162 000 — the FR-TP-019 Stage-0 halves model: boundary
> only, no break / end-swap / match-end; `ticksRemaining` clamps at 0 past full time). (b)
> `MatchEngine.cs` v1.30: NEW Resolve-phase `CheckGoalAndRestart` between the executor advance and
> first touch — `BallCollision.CheckBoundaries` ⇒ `RestartType.KickOff` = goal (the z-gate /
> corner-precedence are that predicate's own documented Stage-0 scope); the scoring TEAM is
> classified by exit half-space geometry (own goals credit the right side); per-team `_goals`++,
> the FIRST-EVER Tier A `GoalAwardedEvent` (0x07; Scorer = the new `_lastHolderAgentId` tracker,
> Assister −1) enters the digest-load-bearing ledger, and the ball restarts at the centre spot
> (minimal restart — agents keep positions; possession cleared). Non-goal exits untouched (no
> throw-in/corner model — pre-substrate behaviour preserved exactly). **`SNAPSHOT_SCHEMA_VERSION`
> 13 → 14** (goals ×TEAM_COUNT + last-holder serialized). (c) **#26 activation** (§3.4 PASS-1 M-1
> gates CLOSED): the RunAiPhase manager block → `RunManagerDecisionPoints`, passing LIVE goalDiff
> (v14 score) + `ticksRemaining`/`MATCH_TICKS_TOTAL`; `ManagerDecisionGate` v1.1 activates the
> half-time trigger (fires once at the first stride evaluation at/after the boundary, regardless
> of interval position; no clock state beyond `LastDecisionTick`). New seams: `TestOnly_Goals` /
> `TestOnly_SetGoals` / `TestOnly_LastHolderAgentId` / `TestOnly_RunManagerDecisionPoints`
> (late-match ladder arithmetic testable without ~270k real ticks). Tests: new
> `MatchEngineGoalTests` (6) + `ManagerAITests` v1.1 (+4 half-time/live-ladder) +
> `MatchEngineSnapshotSchemaTests` v1.11 (pin 14 + ScoreState probe). Spec docs: #26
> section-1/2/3/9 gate-closure + `[CROSS]` promotion rows; `match-engine-design.md` v1.4.
> **Full dotnet gate: PASSED, 0 failures.** Remaining #26 follow-up: only the §9.2 own-`[GT]`
> balance review (the KD-6 on-disk preset format stays deferred BY SPEC — FR-TP-002/017 pin
> "no disk format at Stage 0+1"). NOT built here (Stage-1+ restart model): throw-ins / corners /
> goal kicks, the half-time break / end swap, and a match-end model.)
> **Last Updated (prior):** July 11, 2026, later same day (v2.14 — **#26 T1–T4 manager-AI wiring landed**
> (the last body of work the July-10 T-phase plans named), default-behaviour-neutral —
> `ManagerMode.Human = 0` is the zero-init identity (KD-4): no gate fire, no adaptation, no engine
> calls, a default match byte-identical to pre-#26. **T1 (FM-TP-01):** new
> `tactical-instructions/TacticalPresetsConstants.cs` (§3.5 scalars via `GameplayConfig`; the A.2
> archetype + A.3 affinity `[GT]` tables literal per the array-table carve-out;
> `MATCH_TICKS_TOTAL` deliberately absent — still `[CROSS-PENDING]`) +
> `match-engine/TacticPresetProjection.cs` (pure preset → `TeamTacticConfig`/`PlayerTacticConfig`;
> managed team only, other team keeps its own value; FR-TP-014 roster gate at this consuming seam).
> **T2 (FM-TP-02):** `match-engine/ManagerDecisionGate.cs` — pure tick predicate (KD-3), evaluated
> ONLY inside RunAiPhase's stride branch BEFORE the FR-TI-027 commit (FR-TP-018; off-stride firing
> impossible, F5); ships kickoff (first-ever evaluation, `LastDecisionTick < 0`) + fixed interval —
> the half-time trigger stays gated on the engine halves model (§1.6 PASS-1 M-1). **T3
> (FM-TP-03):** `ManagerProfile` (F4 NaN-gated; A.2 archetype factory) +
> `ManagerAdaptation.KickoffScore/SelectKickoffPreset` (argmax, tie → lowest ordinal per KD-8;
> Appendix B.1 exact — Aggressive → Gegenpress 0.66, Pragmatic → Balanced 0.50) +
> `ManagerAdaptation.ApplyKickoff` (the FR-TP-004 boot path through the EXISTING appliers;
> generalizes FM-TP-01 to both teams; seeds `LastDecisionTick = 0` so the first stride gate does
> not double-fire). **T4 (FM-TP-04):** `ManagerAdaptation.StepToward/EvaluateLadder` (one rung,
> saturating; `URGENCY_DIFF_CAP`; B.2 exact — 0.622 steps / 0.233 holds) +
> `RunDecisionPoint` (FR-TP-005 mid-match path via SetTeamTactic/SetPlayerTactic — never the
> appliers, F3; decrement-then-check hold per the B.2 70′→80′ cadence, the #25 hold precedent).
> The live engine call passes goalDiff = 0 — engine-TRUE (no goal producer exists), making both
> ladder terms identically zero for any clock inputs, so the T4 prerequisite gate is honoured
> without a second code path; the ladder body is unit-exercised through explicit parameters.
> `MatchEngine.cs` v1.29: `_managerStates[TEAM_COUNT]` (zero-init inert), public
> `ConfigureManager` (F2-gated), internal `GetManagerState`/`SeedManagerKickoff` +
> `TestOnly_ManagerState` (§4.3); **`SNAPSHOT_SCHEMA_VERSION` 12 → 13** — per-team `ManagerState`
> serialized in the pinned Appendix C order (`MatchEngineConstants` v1.19). Tests: new
> `ManagerAITests` (21 — B.1/B.2 worked examples exact, gate arithmetic incl. kickoff-fires-once,
> exact-float tie → lowest ordinal, projection + F1/F2/F4 gates, hold cadence + Patience
> multiplier, boot/mid-match routing through a real engine, Human-identity digest lock, two-AI
> two-run bitwise determinism); `MatchEngineSnapshotSchemaTests` v1.10 (pin 13 +
> `ManagerState_FeedsSnapshotDigest`, isolated via the Pragmatic → Balanced identity selection).
> **Full dotnet gate: PASSED, 0 failures.** Remaining #26 follow-ups are the spec's own
> engine-substrate gates (half-time trigger; live goalDiff/`MATCH_TICKS_TOTAL` — upstream
> match-engine deliverables per §9.3) + the on-disk preset format (KD-6 parser swap).)
> **Last Updated (prior):** July 11, 2026 (v2.13 — **Specs #23/#24/#25 wiring landed** (the T-phase step after
> the July-10 T0 scaffolding; #26's T1 preset→config projection onward is the next body of work),
> all default-behaviour-neutral (Balanced ⇒ Off/None/Off = the exact identities). **(a) SlotComposer
> stage insertions (ERR-012-007/008, the #24 §4.2 combined order):** Step 3b build-up overlay
> (FM-BU-02 — after ContextModifier, before spacing; gated on dial ≠ None + InPoss + committed zone
> ∈ {Own, Middle} + suppression closed) and Step 4b dismark offset (FM-DM-02 — after spacing,
> before the pitch clamp; gated on dial ≠ Off + InPoss + off-ball outfielder; the FR-DM-006 phase
> gate lives at the stage with the tick's committed phase, so the snapshot carries UNGATED
> proximity × dwell pressure). `PositioningPerceptionSnapshot` v1.1 gains the routing dials + the
> per-agent pressure/marker carriers (all zero defaults = identities). **(b) #25
> `RotationController`** (new `positioning-ai/RotationController.cs`): FM-RO-01 predicate on the
> controller-owned SERIALIZED `LastComposedTarget` cache (PASS-1 H-1 — restore loads it verbatim
> via a validating seam; no re-seed), FM-RO-02 dwell/commit + hold/revert (revert evaluable the
> same heartbeat the hold reaches 0, per the §3.2 worked example), §3.3 atomic pairwise SlotIndex
> swap + partner lock (slot-sharing rows skipped with dwell reset), §3.4 phase-exit freeze
> (dwell frozen, hold continues, committed rotations persist), FR-RO-009 per-tick commit cap,
> F2/F5/F6 validating seams (non-permutation / GK rebind / undefined dial / incoherent pair state /
> non-finite cache all refused). Wired into `PositioningAITick` v1.3 per §4.2 (after phase
> classification, before compose — the ERR-012-009 sole-post-seed-writer contract; identity binding
> leaves snapshot rows untouched, so every pre-#25 fill is byte-identical; entityId→slot lookup
> refreshed from the rebound rows; post-compose cache write-back; `CaptureRotationState()` seam).
> **(c) #23 §3.4 marked-pass-target penalty in #8** (`UtilityScorer` v1.10):
> `Lerp(1.0, TargetMarkedUtilityMult, targetProx01 × awareness01)` on PASS options before the
> clamp — both terms from the PASSER's own FilteredView (FR-DM-011), awareness-scaled per
> FR-DM-010 (an unaware passer plays the marked pass anyway; mirrors the rest-defense design);
> `TacticalContext` v1.7 gains the `DismarkIntensity` routing field (zero = Off = identity);
> `TacticalWeights` v1.5 gains `TargetMarkedUtilityMult` [GT] + `MarkedPassRadiusM` [CROSS] mirror
> of `PositioningAIConstants.MARKING_RADIUS_M` (#23 Appendix D one-definition-of-tight;
> `decision-tree(.Tests).asmdef` re-gain the `PositioningAI` ref — a valid AI→Mechanics direction).
> **(d) Match-engine wiring** (`MatchEngine.cs` v1.28, `SNAPSHOT_SCHEMA_VERSION` 11 → 12):
> `FillPositioningSnapshot` is the sole Phase-D populator of the three dials + the per-agent
> dismark carriers (computed from the PREVIOUS stride's FilteredView + current dwell — positioning
> runs before the per-agent perception pass, the §3.2 M-1 one-stride contract; carriers are NOT
> stored cross-tick, so dwell is the only new #23 serialized surface, matching #23 Appendix B);
> the per-agent perception pass updates `_markingDwell` (FR-DM-003, ascending index, runs
> regardless of dial so a mid-match flip starts warm); RunMechanicsAI runs the #24 per-team
> pre-pass (classify → gate-read → decrement, check-then-decrement per the §3.3 worked example);
> `OnPossessionChanged` arms the FM-BU-03 window on a TEAM-LEVEL regain only (settledTeam diff;
> first-ever settle arms nothing; the regaining team's own TransitionWon decides — Balanced
> carries HoldShape so a default match never opens a window); v12 serializes per-agent dwell,
> per-team zone state + the settledTeam tracker, and per-team rotation binding/cache/pair state
> (#25 Appendix B order) + the three dials appended to `WriteTeamTactic` in pinned #21 Appendix B
> order. Nine new TestOnly seams. **Tests:** new `SlotComposerStageTests` (7 — exact catalogue Δ /
> FM-DM-02 offset, bitwise identities, gates, post-overlay spacing invariant, clamp bounds) +
> `RotationControllerTests` (12 — trigger/dwell/commit, Conservative bar, hold+mirrored-dwell
> revert, partner lock, commit cap, phase freeze, Off identity, F2/F5/F6 gates);
> `UtilityScorerTests` v1.5 (+4: exact 0.832 worked-example ratio, Off bitwise identity,
> zero-awareness + out-of-radius no-penalty); `MatchEngineTacticTests` v1.5 (+5 incl. FM-BU-03
> arming/decrement + marking-dwell coherence + non-identity-dial determinism);
> `MatchEngineSnapshotSchemaTests` v1.9 (pin 12 + two new preimage probes). **Full dotnet gate run
> locally (SDK via apt): PASSED, 0 failures.** Remaining per the T-phase plans: #26 T1
> preset→config projection, T2 decision gate, T3 kickoff scoring, T4 adaptation; the `[GT]`
> balance passes stay post-APPROVED follow-ups.)
> **Last Updated (prior):** July 10, 2026, latest same day (v2.12 — **T0 AR-1 fix pass: 0H+1M+3L, all
> resolved**; fresh-eyes sweep over the v2.11 landing (14 src files + 4 suites), findings confined
> to the #26 preset surface + one header defect. M-1: `TacticPreset` ctor now snapshot-copies
> `Players` — the v1.0 ctor retained the caller's LIVE array, so post-construction mutation
> bypassed the FR-TP-014 gate (the living-world slice-2 AR-1 M-1 / match-viewer AR-3 M-1 defect
> class; latent at Stage 0 since every catalogue preset carries null, closed before T1 builds the
> preset→config projection on it); getter hand-over convention documented; new
> `Preset_SnapshotsPlayersAtConstruction` regression lock. L-1: `TacticPresetLibrary.Compose`'s
> doc claimed its Balanced-identity defaults were "locked by the T0 suite's identity test", but
> the NonListedDials test covers only globally-untouched dials — dials some presets touch
> (Tempo/Passing/Width/Pressing/LineOfEngagement/TimeWasting/TransitionWon/TransitionLost) were
> unlocked for the presets inheriting them; the four composition tests now assert every
> non-listed dial equals its Balanced value, and the doc points at the real lock. L-2:
> `TacticPreset`'s docs claimed `ValidatePlayers` is "called at library construction" — no such
> call exists (and this bottom-of-graph assembly cannot know roster size); re-anchored to the
> consuming applier seam, with FR-TP-014's library-construction mandate recorded as vacuously
> satisfied at Stage 0 (null-Players catalogue, test-locked). L-3: `TeamTacticFileLoader.cs`
> header was missing its `// Modified:` field (FR-CS-056). Verified clean with no change: #23
> §3.1/§3.2/§3.3 + #24 §3.1/§3.2 worked examples spec-exact (incl. Unity `Clamp01`-NaN gate
> semantics); #25 Appendix A tables row-for-row + the Appendix D FR-RO-007 bound; #26 A.1
> composition-for-composition; overlay row keys hit real slots in all three families (the
> ERR-024-001 regression holds); `TeamTactic` pinned append order + loader keys + enum ordinal
> locks. Files: TacticPreset.cs v1.1, TacticPresetLibrary.cs v1.1, Tests/TacticPresetLibraryTests.cs
> v1.1 (9→10 tests), TeamTacticFileLoader.cs (header only). Full dotnet gate re-run locally:
> PASSED, 0 failures.)
> **Last Updated (prior):** July 10, 2026 (v2.11 — **Specs #23–#26 T0 scaffolding landed**, all
> behaviour-neutral (every new dial's zero value is the exact identity; nothing is wired into any
> orchestrator or the match engine — wiring is each spec's later T-phase). **#21 back-prop code
> side (ERR-021-005/006/007):** `TeamTactic` gains `DismarkIntensity`/`BuildUpStructure`/
> `RotationFreedom` via defaulted ctor params in pinned Appendix B order after `MarkingOrientation`
> (new enums `tactical-instructions/DismarkIntensity.cs`/`BuildUpStructure.cs`/`RotationFreedom.cs`;
> `TeamTactic.cs` v1.3; NOT serialized — `WriteTeamTactic` + schema bump land at each spec's
> wiring); `TeamTacticFileLoader` v1.2 gains the three keys (omitted ⇒ identity, preserving the
> "every field has a key" contract). **#23:** `positioning-ai/MarkingDwellState.cs` +
> `MarkingPressureEvaluator.cs` — pure static FM-DM-01 pressure, §3.2 dwell machine, FM-DM-02
> offset with F1/F3/F4 gates; primitive-span signatures (LAYERING: `FilteredView` is AI-layer,
> positioning-ai is Mechanics — the sanctioned match-engine caller extracts perceived
> positions/ids per #23 §4.4, keeping FR-DM-001 auditable at the call seam). **#24:**
> `BuildUpZone.cs` + `BuildUpZoneState.cs` + `BuildUpZoneClassifier.cs` (FM-BU-01
> committed-zone-expansion hysteresis + FM-BU-03 suppression arithmetic) +
> `BuildUpOverlayCatalogue.cs` — which surfaced **ERR-024-001 (H)**: the spec's PASS-1 M-3 row
> keys matched NO slot (fullbacks are recorded `DefaultLane` LH/RH in every family table, not wide
> L/R) so the whole catalogue was a structural no-op; re-keyed to the recorded values (magnitudes
> unchanged), Appendix A v0.3 + §3.2 v0.3 patched same commit, regression test locks per-family
> coverage. **#25:** `RotationPair.cs` (normalized SlotA<SlotB, GK-refusing ctor) +
> `RotationPairState.cs` + `RotationAdjacencyCatalogue.cs` (all three Appendix A v0.3 family
> tables; ReadOnlyCollection surface). **#26:** `tactical-instructions/TacticPreset.cs`
> (FR-TP-001/014) + `TacticPresetLibrary.cs` (the five A.1 presets in the pinned defensive →
> attacking ladder order; `Compose` helper whose defaults ARE the Balanced identity values, KD-7)
> — also fixed #26 §2.2.2's stale pre-ladder ordinal list (L, spec v0.3). Constants:
> `PositioningAIConstants.cs` v1.2 (+2 Fixed, +2 Derived, +13 GT incl. the
> `DISMARK_INTENSITY_SCALAR` table; positioning-ai keeps its no-config-loader carve-out). Tests:
> new `MarkingPressureEvaluatorTests` (16) + `BuildUpStructureTests` (11) +
> `RotationCatalogueTests` (7) + `TacticPresetLibraryTests` (9); `EnumOrdinalStabilityTests` v1.3 /
> `FactoryIdentityTests` v1.3 extended. See the v2.11 Version History row.)
> **Last Updated (prior):** July 7, 2026, later same day (v2.10 — **no code change; two design
> supplements opened, AR cycle converged, §6 Implementation Plans added** at
> `docs/tracking/advanced-positional-behaviors-design.md` v0.3 (dismarking, scripted build-up
> structures, positional rotations — candidate specs #23–#25) and
> `docs/tracking/game-model-ai-manager-design.md` v0.4 (tactical preset library + AI-manager
> selection/adaptation — candidate #26). AR-1 (0H+0M+2L) + AR-2 (0H+0M+1L) + AR-3 (clean,
> CONVERGENCE) — all findings were citation/prefix-collision corrections in the design-note text
> itself, verified against real `src/` source; no code exists to change yet. Both are pre-promotion
> design notes with no section files and no implementation; nothing in this coding guide changes
> until one is promoted and reaches its own T0 scaffolding milestone (the #21/#22 precedent). See
> root `CLAUDE.md` OPEN ISSUES for the full entry.)
> **Last Updated:** July 7, 2026, later same day (v2.10 — **Tactical-theory cross-reference items (2)/(3)/(4) corrected/reverted after user review.** The v2.9 designs for the rest-defense dampener, half-spaces PASS bonus, and curving-press bias were architecturally wrong, not buggy: (2) the rest-defense dampener is now gated by the ball carrier's own tactical awareness (`Mathf.Lerp(1.0f, TacticalWeights.RestDefenseRiskMult, (A_Decisions + A_Anticipation) * 0.5f)` in `UtilityScorer.ComputeUtility`) — an unaware carrier is never silently corrected for a manager-facing setup flaw. (3) the half-spaces PASS bonus is REVERTED entirely — `TacticalContext.AgentLane`, `TacticalWeights.LaneMult`, and `decision-tree.asmdef`'s `PositioningAI` reference are all removed; `UtilityScorer.ScorePass` no longer reads a lane. (4) the curving-press mechanic is redesigned from a flat "blind-side approach" bias into a cover-shadow curve: `pressing-ai/BlindSideApproach.cs` DELETED, replaced by `pressing-ai/CoverShadowCurve.cs` — bends the primary presser's raw interception target toward the shadow-lane point between the ball carrier and the nearest eligible opponent receiver (the same lane concept `CoverShadowSelector` uses), blended by `PressingAIConstants.CoverCurveBlendWeightMax` scaled by the presser's own attribute average (new `PressingAgentSnapshot.DefensivePositioningAttribute`/`PhysicalEffortAttribute` (avg WorkRate/Pace/Stamina)/`MentalSharpnessAttribute` (avg Decisions/Anticipation), sourced by `MatchEngine.FillPressingSnapshot` from the same `_dtAttrs` the Decision Tree reads) — a poor, low-effort defender curves almost none. Removed: `MatchEngine.TestOnly_AgentLane` seam + its `MatchEngineTacticTests`/`UtilityScorerTests` cases; `decision-tree-tests.asmdef`'s `PositioningAI` reference. New: `CoverShadowCurveTests.cs` (7 tests); `UtilityScorerTests.RestDefenseInsufficient_ObliviousCarrier_TakesNoDampening`. Spec docs updated: `decision-tree/section-7.md` §7.7 (redesigned) + §7.8 (marked REVERTED, not deleted, per project convention) + `pressing-ai/section-7.md` §7.12 (redesigned). See the v2.10 Version History row.)
> **Last Updated (prior):** July 7, 2026 (v2.9 — **Tactical-theory research cross-reference: four cheap-item additions landed**, all default-behaviour-neutral. (1) `MarkingOrientation` dial (new `tactical-instructions/MarkingOrientation.cs`; `TeamTactic` v1.2 appends the field, `SNAPSHOT_SCHEMA_VERSION` 10 → 11) scales the #14 MAN_MARK candidate radius via new `defensive-ai/TacticTranslation.MarkRadiusScalar` + `DefensiveSnapshot.MarkingOrientation` (ctor-seeded Balanced) + `MarkAssigner` consumption. (2) Positioning AI #12 rest-defense coverage check (new `positioning-ai/RestDefenseEvaluator.cs` — counts outfield agents behind `REST_DEFENSE_DEPTH_M` while `IN_POSSESSION`; `PositioningAITick.GetRestDefenseSufficient()`) dampens PASS/SHOOT/DRIBBLE via new `TacticalContext.RestDefenseSufficient` (Stage0Default seeds `true`) + `TacticalWeights.RestDefenseRiskMult` in `UtilityScorer`. (3) Half-spaces PASS bonus: `TacticalContext.AgentLane` routes each agent's EXISTING Positioning AI `LaneId` (already team-relative — no new axis-mirroring risk) into the Decision Tree; `decision-tree.asmdef` gains the `PositioningAI` reference (first AI→Mechanics reference beyond `TacticalInstructions`); `TacticalWeights.LaneMult[5]` gives LH/RH lanes a PASS bonus, C/LW/RW stay ×1.0. (4) Curving-press blind-side bias: new `pressing-ai/BlindSideApproach.cs` nudges the primary presser's approach target (post-selection only — who presses is unaffected) toward the ball carrier's blind side (opposite `PressingAgentSnapshot.Facing`) by `PressingAIConstants.BlindSideApproachBiasM`; wired into `PressingAITick` Step 3. New `MatchEngine.TestOnly_MarkingOrientation`/`_RestDefenseSufficient`/`_AgentLane` seams + `MatchEngineTacticTests` cases + `MatchEngineSnapshotSchemaTests.MarkingOrientation_FeedsSnapshotDigest` (schema pin 10→11). New tests: `RestDefenseEvaluatorTests` (5), `BlindSideApproachTests` (5), `TacticTranslationTests` (+4 defensive-ai), `UtilityScorerTests` (+2), `EnumOrdinalStabilityTests`/`FactoryIdentityTests` (+1 each), `MatchEngineTacticTests` (+3), `MatchEngineSnapshotSchemaTests` (+1). **AR-1: 0H+0M+1L, resolved same day** — L: the new spec-section citations for items (2) and (3) collided with pre-existing sections already occupying those numbers in positioning-ai's and decision-tree's own `section-7.md` files (positioning-ai already has a `§7.9`; decision-tree's `§7.10`/`§7.11` were about to collide with this same landing's own numbering) — renumbered throughout code comments, tests, and spec docs to free slots (positioning-ai → `§7.13`; decision-tree → `§7.7`/`§7.8`); pressing-ai's `§7.12` had no collision and is unchanged. **AR-2: 0H+0M+0L — CONVERGENCE.** Full re-check of zero-value-enum/bool traps (`MarkingOrientation`/`LaneId`/`RestDefenseSufficient` all ctor- or Stage0Default-seeded to their identity, none relies on the CLR default), `SNAPSHOT_SCHEMA_VERSION` bump completeness (new field appended last in `WriteTeamTactic`, no prior byte offset moved), asmdef reference direction (`decision-tree → positioning-ai` is a valid AI→Mechanics reference, no cycle), per-tick routing order (`_positioning[t].Tick()` always precedes the per-agent `TacticalContext` build loop within the same `RunMechanicsAI` team iteration, so `GetRestDefenseSufficient()`/`GetLane(i)` never read stale state), and test-arithmetic (`REST_DEFENSE_DEPTH_M` = 105×0.40 = 42 m) found nothing further. (dotnet gate not runnable in this environment — hand-verified brace/paren balance + asmdef JSON validity across all touched files; compile/suite verification pending CI.)) 
> **Last Updated (prior):** July 2, 2026, latest same day (v1.97 — slice-2 AR-3 sweep: 0H+0M+2L resolved, CONVERGENCE (doc-only) — TryTake ordering doc names its live consumer; WorldLoop Spec citations aligned to the wired phases; slice-2 AR cycle closed; see the v1.97 row. Prior: v1.96 — slice-2 AR-2 full-surface sweep: 0H+1M+2L resolved — `ColdStore.Add` mask coherence gate + upfront membership entity validation (two residual strand-the-summary vectors closed, FR-LW-025), unconsumed-constant doc-notes, single-manager cold-store scope doc; +2 locks (24 tests); see the v1.96 row. Prior: v1.95 — slice-2 AR-1: 0H+2M+3L resolved — SpawnArc pin-array snapshot (post-spawn caller mutation desynced resolve unpinning), promotion-path mask check via new `ColdStore.TryPeek` verify-before-take (the doc claimed a check that didn't exist; checking after the destructive take would strand the summary per FR-LW-025), spawnTick overflow gate + 2 doc findings; +3 locks (22 tests); see the v1.95 row. Prior: v1.94 — **Living World #22 season/world loop slice 2 landed**: `ArcEngine` (§3.4 spawn with atomic FR-LW-018 pinning + F1 rollback, state advance, resolve, §6.2 expiry sweep; the `world.arcs` trigger-draw surface stays the documented KD-10 seam per FR-LW-020/031) + `ActiveSetMembership` (§3.5 entry / cold-store promotion on the TryTake ordering contract / deterministic LRU demotion at the FR-LW-023 cap with arc-pin skip / own-club Depart per FR-LW-025); WorldLoop v1.1 wires phases 4+6 (null-injectable); 19-test `ArcMembershipTests` suite — see the v1.94 row. Prior: v1.89 — match viewer AR-4 sweep: 0H+0M+3L resolved, CONVERGENCE (L-only) — Space-key focused-control fix, config-resolved [GT] gate in Export, doc-contract fixes; AR cycle closed; see the v1.89 row. Prior: v1.88 — match viewer AR-3 sweep: 0H+1M+2L resolved — frames list snapshot-copied before validation (ReadOnlyCollection view let post-ctor list mutation bypass every guard), possession-range validation, empty-roster refusal; rest of surface re-swept clean; see the v1.88 row. Prior: v1.87 — match viewer AR-2 full-surface sweep: 0H+3M+3L resolved — empty-replay + non-monotonic-tick refusal, [GT] rows migrated onto GameplayConfig, roster guards on the sibling surface, ball-radius clamp, metadata NaN-gates; see the v1.87 row. Prior: v1.86 — match viewer AR-1 fix pass: 0H+2M+3L resolved — `MatchReplay` incoherent-frame refusal, observation-surface roster-index guards (`MatchEngine.cs` v1.25), exposed-array doc, player-JS interval/clock/jersey fixes; see the v1.86 row. Prior: v1.85 — minimal match viewer landed: new `src/match-viewer/` presentation-tooling assembly (recorder + self-contained HTML canvas replay) over a new public read-only observation surface on `MatchEngine` (v1.24); observer-neutrality digest-locked; see the v1.85 Version History row.)
> **Last Updated (prior):** June 28, 2026 (v1.67 — status check, no production code change. `src/match-engine/` (Match Engine composition root, governed by `docs/tracking/match-engine-design.md`, not a numbered spec) reached Phase A–E complete as of June 27, 2026 — full canonical world-state snapshot serialization at `SNAPSHOT_SCHEMA_VERSION` 8, Physics/Resolve/AI-phase wiring through Positioning #12→Pressing #13→Defensive #14→Attacking #15→DecisionTree #8, and an Events-phase possession-changed producer/consumer with `EventBus.ResetForNewMatch()`. This had not been reflected in this file's history. Remaining: Phase F capstone (closed-loop scenario on the #19 `ScenarioRunner`) — see `CLAUDE.md` OPEN ISSUES. dotnet-ci quarantine confirmed empty (gate enforces full suite, no exclusions). **Update (v1.68, June 28, 2026): Phase F landed — Match Engine integration (Phases A–F) is now complete; see the v1.68 Version History row.**)
> **Last Updated (prior):** June 12, 2026, later same day (v1.66 — Non-certifying Linux compile/test CI gate landed (`tools/dotnet-ci/`): asmdef→csproj generator + ~6-type UnityEngine shim (Unity-exact Vector2/Vector3 approximate `==`, Mathf NaN semantics, Debug/LogType, no-op profiling, UTF-parity LogAssert) lets the ENTIRE src/ tree compile (production netstandard2.1 = Unity 2022.3 BCL surface; tests net8.0 + NUnit3 adapter; LangVersion 9.0) and every NUnit suite execute under plain `dotnet test` on ubuntu CI — `bash tools/dotnet-ci/run-gate.sh`. First-ever full-tree compile found EIGHT never-compiled surfaces: (1) ERR-017-002 (H): EventBus + five EventBusStub files declared Publish/Subscribe overloads distinguished ONLY by generic constraint (CS0111 — illegal C#; the spec §3.2.1/§3.2.2 specified it; section-3.md v1.0.2 patched same commit); replaced with single `where T : struct` methods + new EventTierCache<T> cached marker dispatch + CosmeticChannel.SubscribeFromBus seam (call sites unchanged; FR-EVT-009a enforced at entry); adjacent EventRegistry.EnsureInitialized() boot-order fix (EventOrdinalCache<T> reads never triggered the seeded-row cctor). (2) 18 files imported ProfilerMarker from UnityEngine.Profiling — actual namespace is Unity.Profiling (CS0246 in-engine). (3) SaveManager used File.Move(overwrite:) — .NET Core 3.0+ only, absent from netstandard2.1; now File.Exists ? File.Replace : File.Move. (4) ShotExecutor referenced AgentMovementState members by PascalCase (enum is ALL_CAPS; CS0117). (5) CoverShadowSelector missing `using System;` for Span<T>. (6) GoalkeeperMechanics passed int? into the int event field (CS0266); coalesces to the documented −1 zone sentinel. (7) ShotMechanicsTests: SIXTH stray-brace dead suite (namespace closed before the §5.12 IT- fixture). (8) UtilityScorer missing PerceptionSystem using — decision-tree STILL dead after the June 11 AR-2 fixes. Then 1,165 tests executed for the first time: DefensiveAITests had all 51 [Test] methods `internal` (NUnit rejects non-public — suite could never run); EventSystemTests/ActionSelectorTests used NUnit APIs that don't exist (generic Is.InRange, AreNotEqual-with-tolerance); old SipHash fixture vectors 4–7 were FABRICATED (production correct per independent mirror + the v1.65 64-vector KAT); IsRegistered placeholder test was fixture-order-dependent. Remaining 30 genuine model/expectation failures quarantined (shrinking-only) in tools/dotnet-ci/known-failures.txt + docs/tracking/dotnet-ci-quarantine.md with per-test hypotheses — any NEW failure or compile error fails the gate; quarantined set re-run report-only every CI run. NOT a determinism certification (certification-platform.md v1.2 note). Files: EventBus.cs v1.9, EventTierCache.cs v1.0 (new), CosmeticChannel.cs v1.9, EventRegistry.cs v1.5, 5× EventBusStub.cs, SaveManager.cs v1.3, ShotExecutor.cs v1.7, CoverShadowSelector.cs v1.2, GoalkeeperMechanics.cs v1.3, UtilityScorer.cs v1.3, 18× Unity.Profiling using fixes, pressing-ai/AssemblyInfo.cs (new), ShotMechanicsTests.cs v1.2, DefensiveAITests.cs v1.2, EventSystemTests.cs v1.3, ActionSelectorTests.cs v1.1, GoalkeeperMechanicsTests.cs v1.2, DeterministicSimTests.cs v1.3, 15 tools/dotnet-ci files (new), ci.yml, .gitignore, spec-error-log.md v1.27, event-system/section-3.md v1.0.2, certification-platform.md v1.2, dotnet-ci-quarantine.md v1.0 (new), file-manifest.md.)
> **Last Updated (prior):** June 12, 2026 (v1.65 — Deterministic Simulation #16 golden-vector implementations (§9.5 #4(a)/(b)/(c)): three new KAT fixtures in `src/deterministic-sim/tests/` — `HkdfSha256KatTests` (RFC 5869 A.1–A.3, PRK + full OKM byte-exact; project Test Case 4 pinned), `SipHash24KatTests` (all 64 Aumasson & Bernstein Appendix A vectors — prior suite covered 0–7 only; project RNG_STREAM_HASH draw case pinned), `SerializeCanonicalCorpusTests` (all 41 corpus entries, bytes + SHA-256 each, incl. the chained SnapshotDigest D-07). Production fixes the KATs forced: `HkdfSha256` was T(1)-only (L ≤ 32) and structurally incapable of reproducing the RFC L=42/82 vectors — full §2.3 multi-block Expand implemented with `HkdfExtract`/`HkdfExpand` split (no behaviour change for the production L=16 path); `CanonicalSerializer.WriteF64TierB` added (corpus F-09 f64 NaN→0x7FF8000000000000 had no encodable surface). Latent compile blockers in the SEVENTH consecutive test surface: no `InternalsVisibleTo` existed for the deterministic-sim test assembly (every internal `HkdfSha256`/`SipHash24_64` call in DeterministicSimTests was uncompilable since v1.0) — new `AssemblyInfo.cs`; DeterministicSimTests v1.1 closed the namespace before the appended save/load fixture (global-namespace stranding, CS0246; First Touch ERR-004 / Pass Mechanics AR-9 H-1 defect class) — namespace now closes at EOF. Both stubbed project-specific KAT cases pinned in the same commit (hkdf-sha256-kat.md v1.2 / siphash-2-4-kat.md v1.2), with three stale stub inputs corrected BEFORE pinning per the spec text: info `SMP-RNG-MATCH` → §3.2.4 normative `DS-RNG-KEY-v1` (F-HKDF-02), L 32 → RNG_KDF_OUTPUT_BYTES=16, IKM 32-byte → u64 LE matchSeed; SipHash stub's `StreamKey(16 bytes)` → u64 per the §3.4 HASH_INPUT_FIELD_WIDTHS table the stub itself cited, fixture re-anchored to corpus D-03. All expected values verified by independent Python mirrors (RFC HKDF via hmac/hashlib; hand-rolled SipHash-2-4; the corpus Appendix A reproducer re-run — 45/45 rows match). Files: DeterministicRngService.cs v1.2, CanonicalSerializer.cs v1.2, AssemblyInfo.cs v1.0 (new), tests/DeterministicSimTests.cs v1.2, tests/HkdfSha256KatTests.cs v1.0 (new), tests/SipHash24KatTests.cs v1.0 (new), tests/SerializeCanonicalCorpusTests.cs v1.0 (new), golden-vectors/hkdf-sha256-kat.md v1.2, golden-vectors/siphash-2-4-kat.md v1.2, file-manifest.md.)
> **Last Updated (prior):** June 11, 2026, later same day (v1.64 — Decision Tree #8 comprehensive audit (AR-2; the audit carved out at the April 27 draft-level approval, run against the May 29 implementation): 3H+11M+9L, all resolved same commit except ERR-008-006 (documented-open). H-1: the decision-tree assembly has NEVER compiled — `ActionDispatcher` called the per-agent instance methods `PassExecutor.Execute`/`ShotExecutor.Execute` STATICALLY (CS0120), and `decision-tree.asmdef` lacked the `TacticalDirector.DeterministicSim` reference its EventBusRegistrar requires (asmdef refs are not transitive) — the SIXTH consecutive spec with a structurally dead build surface and the first where the PRODUCTION assembly was dead; executors now constructor-injected (null = sanctioned test/bootstrap seam, logged), asmdef patched + the same missing reference fixed in pass-mechanics/perception-system/heading-mechanics/goalkeeper-mechanics asmdefs. H-2/ERR-008-002: away-team zone modifiers INVERTED — the scorer consumed the shared home-perspective `MatchContext.BallZone` for both teams while §3.2.1.3 defines zones "from own goal line" (away in-range shots scored ×0.10 DEFENSIVE instead of ×1.00; enum mirroring is not exact — home cut points {35,65} mirror to {40,70}); assembler now derives team-relative `DecisionContext.BallZone` from `BallPosition.x` via `PitchGeometry.ComputeFieldZone(posX, teamId)`. H-3: §3.7.2 state machine unimplemented — EXECUTING re-ran the full pipeline every heartbeat mid-pass-windup (the ForcedRefreshThisTick check was a no-op ternary), §3.6.3 same-type forced-refresh suppression absent, HOLD landed in EXECUTING instead of IDLE, malformed snapshots stomped EXECUTING to IDLE; PASS/SHOOT now hold EXECUTING between heartbeats, movement actions continuous per a filed Stage 0 deviation (ERR-008-008). M: §3.4.6 press urgency keyed to the literal AWAY_TEAM (inverted for away agents; spec cites a nonexistent `PossessionState.OPPONENT` — ERR-008-005; new derived `OpponentHasBall`); §3.4.5 line-depth shifted formation slots along Y (touchline!) with no team sign — the spec pseudocode itself carried the axis bug (ERR-008-003; latent at Stage 0 depth=0.5); SHOOT risk used (1−A_Finishing) vs spec (1−GoalOpeningScore); midfield long-shot gate compared the raw form vs the spec's shifted form (§3.2.3.4 derives raw ≥ 11; raw form required ≥ 16); PRESS/INTERCEPT dispatch lost EMERGENCY/TARGET_LOCK profiles and MOVE's near band issued Stop instead of WALKING (new AM `MovementCommand.PressSprint`/`SprintWhileWatching` factories per the WalkTo precedent; DispatcherTests had ENCODED the deviations); INTERCEPT risk carried a non-spec (1−A_Anticipation) factor; goal-opening geometry missing the §3.2.3.2 angular-overlap test + goal-line GK heuristic + clamps/floor; §3.1.3.6 proximity-ordered Decisions cap implemented (was snapshot-order); §3.5.9 FM-DT-10/11/12 pre-dispatch assertions implemented (NaN-gate pattern), FM-DT-09 double-allocation resolved (dispatch failure renumbered FM-DT-14, ERR-008-007), all emits FR-CS-031 gated. L: tiebreak flag reset per INV-SEL-07; INV-GEN-06 ClampToPitch on generated targets; UtilityWeights duplicates of TacticalWeights constants removed (parallel-surface drift); DRAG_APPROX retagged [CROSS]→[EST] (ERR-008-009); constant tally 23→22 (ERR-008-010); stale spec values patched (HOLD 0.25→0.28 ×3, NOISE_MAX 0.20→0.15, §3.1.4.3 post axis ERR-008-011, §3.5.6 XZ-plane note). ROOT CAUSE of the three H/M asymmetry defects: every spec worked example and every AR-1 fixture used the home team. New DecisionContextAssemblerTests (H-2/M-1 locks incl. the x=37 mirror boundary); UT-33..35 state-machine locks; UT-20..22 re-derived to spec profiles; M-1/M-3/M-4/M-9 scorer/generator locks. Spec: 7 section files patched (ERR-008-002..011); canonical deliverable `docs/specs/decision-tree/audit-report.md`. M-11: EventBusRegistrar.Initialize() non-idempotent (double boot threw ERR_EVT_ORDINAL_COLLISION; the integration tests MUST boot the registry since Tier C publish throws unregistered, yet carried no boot) — s_registered guard per the PM v1.2 precedent + [OneTimeSetUp] boot; sibling registrars (shot/perception/heading/goalkeeper) flagged for the same guard, out of DT scope. Files: 18 src + 5 test files in src/decision-tree/, MovementCommand.cs v1.4, 5 asmdefs, spec-error-log.md v1.26, file-manifest.md.)
> **Last Updated (prior):** June 11, 2026 (v1.63 — Pass Mechanics #5 AR-9 fix pass: 1H+3M+5L, then AR-10 sweep: 2L (both resolved in the same commit) — converts the fifth consecutive spec where the unit suite was structurally incapable of catching its defects. H-1: `Tests/PassMechanicsTests.cs` has NEVER compiled since v1.1 (2026-06-01) — the namespace was closed BEFORE the appended IT- fixture, leaving a stray `}` at EOF (CS1022) and the fixture stranded in the global namespace; IDENTICAL defect class to First Touch ERR-004 (FirstTouchTests v1.1, 170/171 braces — here 161/162). Namespace now closes at EOF; `[TestFixture]` added to the IT- fixture. Every AR-2..AR-8 claim of the form "EC-010 already enforces …" was unverifiable while the suite was dead. M-1: PassExecutor Idle-guard rejection wrote `_lastResult` — an Execute() during FollowThrough/Complete destroyed the committed Completed record (ContactFrame replay-sync data) and surfaced Invalid at the next IsIdle; rejection now reported via return value only. M-2: FM-07 gate `d <= 0f` passed NaN (compares false), which `Mathf.Max(NaN, 0.001f)` argument ordering then silently sanitised to a 0.001 m pass; gate is now `!(d > 0f) || IsInfinity(d)` (project NaN-gate pattern: FT AR-8 M-1 / AM AR-10 / CS AR-7). M-3: stale tackle flag — `GetAndClearTackleFlag` is only polled during WINDUP, so a flag set during FollowThrough/Idle (even while not in possession) cancelled the agent's NEXT pass on its first WINDUP frame; Execute() now drains (discards) the flag at INITIATING just before the WINDUP transition (§3.8.5 freshness). L-1: CONTACT pressure re-sample now queries passer position fresh (INITIATING-cached `_passerPosition` removed — was up to ~15 frames stale for a pass on the run; §3.8.6 contact-time intent). L-2: ComputeErrorAngle NaN fallback flipped MinErrorAngle → MaxErrorAngle (failed OPEN — a NaN anywhere in the modifier chain produced a 0.1° laser pass). L-3: PhysicalProfile.DistMin/DominantSpin/IsAerial + PassAgentAttributes.Crossing doc-noted declared-but-unconsumed (CS AR-10 precedent); IsAerial note records the IsAerialFormula parallel-surface drift hazard (9-profile agreement verified). L-4: PassOutcome.Cancelled doc covers the FM-08/FM-04 CONTACT-time cancels; PassAgentState.Position no longer claims a "Z component" on a Vector2. L-5: SPEC-DEVIATION NOTE on ComputeThroughBallAimPoint — kickSpeed derives from IntendedDistance BEFORE the lead projection extends the aim point, so led passes systematically underhit (joins the KD-4 / §7.1 Stage 1 upgrade). New PassExecutorGuardTests fixture (PX-001..004) locks M-1/M-2/M-3 via pure stub seams (no EventBus boot needed — all paths terminate pre-publish). AR-10 sweep over the fix surface: 2L (FM-07 log wording aligned to the widened gate; IsAerial doc-note wording), no functional findings. Files: PassExecutor.cs v1.12, PassErrorCalculator.cs v1.8, PassTargetResolver.cs v1.8, PhysicalProfile.cs v1.2, PassAgentAttributes.cs v1.1, PassAgentState.cs v1.1, PassOutcome.cs v1.3, Tests/PassMechanicsTests.cs v1.2.)
> **Last Updated:** June 10, 2026 (v1.58 — Collision System #3 AR-10 sweep: 2L, no functional findings — MaxIterations [GT] doc-noted as declared-but-unconsumed at Stage 0 (CollisionSystemConstants.cs v1.6); v1.57 history-row AR-8 tally corrected to 1H+1M+1L. Mechanical verification: pair-bitfield index uniqueness over all 253 pairs, 3×3 broad-phase coverage proof, FR-CS-031 gating audit on all 4 emits. Prior same day (v1.57 — Collision System #3 AR-7 fix pass: 1H+3M+3L, then AR-8 sweep: 1H+1M+1L, then AR-9 sweep: 2L doc-only — Agility unconsumed-field pointer (AgentPhysicalProperties.cs v1.3), AGENT_BALL ContactPoint Z claim (CollisionEvent.cs v1.3) (all resolved; ERR-003-001..006 filed and closed same day). Both H findings were closed-loop model defects the test suite ENCODED rather than caught. AR-7 H-1/ERR-003-001: F = j × 60 Hz assumed the whole impulse acts in one 16.7 ms frame — the entire stochastic fall/stumble band (500–1500 N literature values) sat below walking pace (P(fall)=1 at ~0.5 m/s closing; knockdownForceOut pinned at 1.0); new [GT] ContactDurationS (0.15 s), F = j / ContactDurationS, PHYSICS_TICK_HZ removed (sole consumer). AR-8 H-1/ERR-003-005: impulse approach gate INVERTED — with the a1→a2 manifold normal, vRel=(v1−v2)·n>0 is approaching, but the gate returned separation-only for vRel>0, so genuine closing collisions exchanged no momentum and EvaluateFallOrStumble was unreachable for real contacts, while overlapped pairs already moving apart were velocity-reversed back inward (CR-001 rationalised this as a 'passed-through state'); gate + impulse signs corrected (j>0 invariant preserved), restitution verified e·v. M: FROM_BEHIND broken on three surfaces — formula sign (ERR-003-002), unflipped instigator→victim normal at the call site (same ERR), and shadowing by the velocity-only shoulder predicate (ERR-003-006, AR-8); same-team hits above fallThreshold escaped both fall and stumble branches (ERR-003-003); MaxCollisionPairs valve counted broad-phase candidates and aborted the whole frame in goalmouth densities (ERR-003-004 — now counts narrow-phase CONFIRMED collisions, cap = event-buffer capacity). L: non-finite velocity sanitised to zero at the snapshot gate (NaN previously published into CollisionEvent.ImpactForce); both-grounded overlaps no longer emit 60 zero-force events/s; RecordEvent drop warning; CellX/CellY FloorToInt (cell 0 was double-width for negative coords). Spec §3.3/§3.4 pseudocode patched in the same commit (6 ERR anchors); CONTACT_DURATION_S added to the §3.3 catalogue. All test expectations re-derived for the corrected model and verified by a numerical mirror including the xorshift128+ RNG (FL-002 5210/10000 stumbles vs 0.5175 predicted; FL-003 5073 falls; FL-004 90/0; CR-001 ∓1.5 m/s). Files: CollisionResponse.cs v1.6, CollisionSystem.cs v1.6, ContactTypeClassifier.cs v1.3, CollisionSystemConstants.cs v1.5, SpatialHashGrid.cs v1.4, CollisionEvent.cs v1.2, ContactForceData.cs v1.1, AgentAgentCollisionResult.cs v1.2, tests/CollisionSystemTests.cs v1.3, spec-error-log.md v1.24, docs/specs/collision-system/section-3-3.md + section-3-4.md.) Prior June 9, 2026 (v1.56 — Agent Movement #2 AR-12 fix pass (3H+1M+3L) + AR-13 sweep (2M, both resolved). The three H findings are closed-loop speed-control defects that survived AR-1..AR-11 because all prior tests exercised pure functions or injected mid-flight state. H-1: agents at rest could NEVER start moving — `EvaluateFromIdle` exits only on speed > IdleExit while the IDLE locomotion branch only decayed speed to 0; IDLE now accelerates (walk rate) toward the command-capped topSpeed when commandSpeed > 0. H-2: commandSpeed never capped the integration target — jog commands auto-promoted to SPRINTING (reservoir drain) and walk commands flapped WALKING→JOGGING→DECELERATING; Step 4–5 now applies `topSpeed = min(topSpeed, commandSpeed)` and `ApplyAcceleration` gains a max(current, topSpeed) asymptote ceiling so a 1-ulp overshoot cannot trip the strict band-promotion comparisons. H-3: Zeno deceleration — `ApplyDeceleration` re-derived a = v²/(2d) per frame from the decaying speed, so stopping from 6 m/s took ~78 s / ~32 m; new `LocomotionConstants.MinDecelerationFloor` [GT] (2.5 = WALK_DECELERATION) bounds the tail; spec-deviation note records that §3.2.5's normative model is constant-rate (full re-parametrisation deferred — conflicts with the MAX_ACCELERATION=8 cap). M-1: LeanAngle now derives from the Step 8 velocity-direction rotation (real path curvature via new `RotateVelocityToward` out-param overload) instead of facing rotation. L: GroundedReason Stage 0 producer note (SLIDING_TACKLE/DIVING_HEADER dormant until #3/#14); AgentState.PreviousState doc corrected (last DISTINCT state); AUTO_ALIGN one-frame facing lag documented. AR-13 sweep found 1 M in the H-2 interaction surface: an aerobically exhausted agent (pool < AerobicJogFloor) with a jog command flapped WALKING→JOGGING→DECELERATING at ~3 Hz; commandSpeed now clamps to JogEnter under exhaustion. New `MovementCommand.WalkTo` factory (AI previously could not request the WALKING band). Tests: closed-loop fixture T-AM-110..115 (launch / band-respect / bounded-stop / walk-band / exhaustion / HOLD-at-rest), T-AM-108..109 decel-floor units, T-AM-079 re-derived. Files: AgentMovementSystem.cs v1.15, AgentLocomotion.cs v1.5, AgentDirectionalMovement.cs v1.7, AgentMovementConstants.cs v1.9, AgentState.cs v1.5, GroundedReason.cs v1.1, MovementCommand.cs v1.3, AgentMovementTests.cs v2.2, AgentMovementUnitTests.cs v1.1, test-plan.md v0.3, file-manifest.md agent-movement section reconciled.) Prior v1.55 (Ball Physics #1 AR-7 fix pass (2H+4M+3L) + AR-8 sweep (2L, both resolved — clean). H-1: `BallGroundInteraction.ApplyBounce` ground normal `Vector3.up` (Unity +Y) → `(0,0,1)` — project is Z-up (§1.2); restitution/friction acted on the touchline axis so a falling ball NEVER rebounded; spec §3.1.8.1 pseudocode carried the same line and is patched in the same commit (ERR-001-001). H-2: `ValidatePhysicsState` ground clamp no longer zeroes negative Velocity.z for Airborne — the clamp runs before `UpdateBallState` and the Airborne→Bouncing transition requires vz<0, so any descent fast enough to skip the 2 cm exit window (>~1.2 m/s) was trapped in a permanent ground-level Airborne hover (no bounce, no roll). M-1: bounce friction stick impulse divided by the rotational-coupling divisor 1+m·r²/I = 2.5 (`Bounce.StickImpulseCouplingDivisor` [DERIVED]; ERR-001-002; undivided form reversed slip ~150%). M-2: test-suite spin-sign convention corrected — topspin for +X motion is +Y ω (4 tests had it inverted; Bounce_Topspin needs ω>v/r≈45.5 for a forward kick, now ω=60; retention test moved to normal-axis spin so friction torque cannot contaminate it). M-3: Bouncing integration branch gains gravity (was drag-only). M-4: `LongBall_DecaysRealistically` loop condition z≥R → State==Airborne (clamp pins z at R so the loop always hit maxSteps) and windows re-derived (2.6–3.4 m / 1.3–1.8 s; old 4–6 m exceeded the vacuum ceiling for vz=8) — all expectations verified by a numerical mirror of the fixed model. L: 0.01f/0.0001f literals → `Bounce.MinContactSpeed` / `Magnus.MinForceDirectionSqMagnitude` (FR-CS-016); `Ball.MomentOfInertia` [EST]→[DERIVED]; `HasInvalidValues(in BallState)`; catalogue LAYOUT NOTE; new `Airborne_FastDescent_Bounces_NoHoverDeadlock` regression test; AR-8: ApplyBounce precondition doc + ERR-001-003 [EST] inventory entry (FR-CS-020). Files: BallGroundInteraction.cs v1.3.1, BallPhysicsCore.cs v1.4, BallPhysicsConstants.cs v1.8, BallPhysicsCoreTests.cs v1.5, BallIntegrationTests.cs v1.4, docs/specs/ball-physics/section-3-1-8-to-3-1-14.md row 2.8, spec-error-log.md v1.23. Prior v1.54 (June 8, 2026 — Event System #17 Stage 1 boot-wiring smoke test landed + tracking-doc sync. New `src/event-system/tests/EventBusWiringSmokeTests.cs` v0.4 drives boot → publish → drain → digest across the 6 currently-wired EventBusRegistrar.Initialize() call sites (Pass / Shot / Perception / Decision / Heading / Goalkeeper); AM #2 is `[CROSS-PENDING]`. Golden digest pinned via `Assert.Inconclusive` so an unfilled pin surfaces yellow in CI. AR-1 (1H+1M+4L) + AR-2 (0H+1M+4L) + AR-3 (0H+0M+2L cycle-stop) adversarial review cycles complete. `event-system-tests.asmdef` extended with 6 production spec references (Editor-only). file-manifest.md updated with new test file row. Prior v1.53 (Pass Mechanics #5 AR-8 fix pass: 0M + 3L on a fresh-eyes full-surface sweep after AR-7. L-1: AR-7's brace-add for the CrossSubType-ignore warning gate left an empty `if (cond) { }` in production builds; gate hoisted to wrap the entire if-statement since the diagnostic has no functional follow-up. L-2: `ExecuteContact` state transition hoisted above Step 8 `EventBusStub.Publish` so a Publish exception cannot trap the executor in `Contact` and cause re-entry double-kick (FM-08 recheck currently guards but defensive ordering removes the dependence). L-3: forward-reference notes added next to the AR-2 M-2 history rows in `PassExecutor.cs` and `PassTargetResolver.cs` — the "[-1, +1]" characterisation there was the AR-2-era contract, superseded by AR-6 L-1 to "[-1, +1)". Files: PassExecutor.cs v1.11, PassTargetResolver.cs v1.7. Prior v1.52 (Pass Mechanics #5 AR-7 fix pass: 1M+3L all resolved on a fresh-eyes full-surface sweep over all 24 files in `src/pass-mechanics/`. M-1: FR-CS-031 gating drift fixed across sibling files — `PassMechanicsConstants` v1.2 (AR-2 L-13) gated its FM-01 `Debug.LogError` emits but the 12 parallel cold-path emits in `PassExecutor.cs`, `PassTypeProfiles.cs`, and `PassVelocityCalculator.cs` never got the same `#if UNITY_EDITOR || DEVELOPMENT_BUILD` gating; all now gated. L-1: `[-1, +1]` → `[-1, +1)` propagation from AR-6 producer-side correction to `PassTargetResolver.ApplyErrorToDirection` doc and the `PassExecutor.ExecuteContact` Step 3 callsite comment. L-2: `EventBusRegistrar.cs` v1.3 history row's "no `InternalsVisibleTo` on this assembly" rationale was already stale at AR-3 time (`AssemblyInfo.cs` created 2026-06-01 with the attribute); corrected to the mock-at-EventRegistry-boundary rationale alone. L-3: `CrossSubType` and `PassType` enums gained ORDINAL STABILITY paragraphs parallel to `CancelReason` v1.4 — `PassType` additionally documents that `(int)_request.PassType` is the third hash input to `ComputeErrorDirection`, so reordering breaks deterministic error-direction parity even before the digest catches the drift. Files: PassExecutor.cs v1.10, PassTypeProfiles.cs v1.4, PassVelocityCalculator.cs v1.4, PassTargetResolver.cs v1.6, EventBusRegistrar.cs v1.4, CrossSubType.cs v1.1, PassType.cs v1.2. Prior v1.51 (Pass Mechanics #5 AR-6 fix pass: 1M+3L all resolved, converts the AR-5 cycle-stop. `PassErrorCalculator.ComputeErrorDirection` M-1: input-mix primes for `frameNumber` / `passTypeIndex` replaced with xxHash64 PRIME64_3 (`0x165667B19E3779F9`) and PRIME64_5 (`0x27D4EB2F165667C5`) so the Stafford Mix13 finalizer (`0xBF58476D1CE4E5B9` / `0x94D049BB133111EB`) no longer multiplies through the same primes the input-mix already used. AR-5 fixed the same hazard on agentId; AR-6 finishes the invariant — input-mix primes are now disjoint from finalizer primes on all three axes. L-1: `<returns>` upper bound corrected `[-1, +1]` → `[-1, +1)` to match the 24-bit mantissa quantisation (EC-010 already enforces `Assert.Less(dir, 1.0f)`). L-2: comment block rewritten to call out the additive-vs-XOR asymmetry on agentId (AR-5 intent) and record the AR-6 input-mix / finalizer prime disjointness invariant. L-3: bit-extraction literals `0x00FFFFFFu` / `0x01000000u` promoted to named local consts `Mantissa24Mask` / `Mantissa24Scale` with a comment noting the 24-bit window matches float mantissa precision. Files: `src/pass-mechanics/PassErrorCalculator.cs` v1.7. Prior v1.50 (cross-spec routing close-out: `Possession.ControlHeight` ↔ `GroundControlHeight` resolved. Ball Physics #1 §3.1.11 `BallPhysicsConstants.Possession.ControlHeight` declared the authority; `FirstTouchConstants.GroundControlHeight` relocated from `#region GT` to `#region Cross`, retagged `[CROSS]`, RHS now `BallPhysicsConstants.Possession.ControlHeight` (single-consumer mirror per Spec #20 §4.2). Ball Physics XML drift warning replaced with an authority/consumer pointer. Sibling-hazard sweep cleared — no other spec re-declares `ControlRadius` / `ControlVelocity` / `ChallengeRadius`. Files: FirstTouchConstants.cs v1.7, BallPhysicsConstants.cs v1.7, docs/specs/first-touch/section-4-4-to-4-6.md §3.4.3, root CLAUDE.md OPEN ISSUES. Prior v1.49 (AR-hardening sweep complete. New AR rounds since v1.45: Ball Physics #1 AR-6 (4L); Collision System #3 AR-1..AR-6 (final 3L); First Touch #4 AR-5..AR-6 (final 3L); Pass Mechanics #5 AR-2..AR-5 (final 1M+3L cycle-stop — `ComputeErrorDirection` agentId now multiplied by xxHash64 prime 0xC2B2AE3D27D4EB4F before seed addition for better hash quality); Shot Mechanics #6 AR-2..AR-4 (final 3L); Event System #17 AR-8 (2M+1L), AR-9 (1M+1L), AR-10 (1M+2L), AR-11 (no findings — whole-surface sweep clean); Agent Movement #2 AR-10 (1M+2L — `SanitiseCollisionForce` maps NaN/±Inf → 0 before Clamp01 since Unity's Clamp01 does not filter NaN), AR-11 (2L — EvaluateState defaults dropped parallel to AR-9 L-2; OscillationGuard.WriteTime default-arm assert); Performance Optimization #18 AR-3 + AR-3 full-surface (3M+5L / 3M+3L) + AR-4 (2L). v1.48 prior: Performance Optimization #18 RegressionGate.Evaluate now reports skipped-drift (NaN) vs degenerate-milestone (+Infinity) distinctly in MilestoneDriftFraction. v1.47 prior: PO #18 AR-3 full-surface (M-1 RECORD_FORMAT_VERSION_STAGE0_PENDING sentinel constant). v1.46 prior: PO #18 AR-3 (M-1 PerformanceOptimizationConstants RECORD_FORMAT_VERSION_STAGE0_PENDING). — Prior v1.45 (Agent Movement #2 pure-function unit coverage expansion. New `src/agent-movement/Tests/AgentMovementUnitTests.cs` v1.0 adds 59 NUnit tests across 7 `[TestFixture]` classes.) — Prior v1.44 (Agent Movement #2 regression-test roster initiated.) — Prior v1.43 (Agent Movement #2 AR-9 fix pass.) — Prior v1.42 (AR-8 fix pass.) — Prior v1.41 (AR-7 fix pass.) — Prior v1.40 (AR-5+AR-6 fix pass.) — Prior v1.39 (Ball Physics #1 AR-6 fix pass.) — Prior v1.38 (Ball Physics #1 AR-5 fix pass.) — Prior v1.37 (Ball Physics #1 AR-4 fix pass.) — Prior v1.36 (Ball Physics #1 AR-3 fix pass.) — Prior v1.35 (Ball Physics #1 AR-2 fix pass.) — Prior v1.34 (Ball Physics #1 AR-1 fix pass.) — Prior v1.33 (Testing Strategy #19 PR #132 Codex P2 follow-up.) — Prior v1.32 (Testing Strategy #19 AR-5 fix pass.) — Prior v1.27..v1.31 (Testing Strategy #19 scaffold + AR-1..AR-4.) — Prior v1.25..v1.26 (Performance Optimization #18 AR-1..AR-2.)))
> **Last Updated:** June 10, 2026, latest same day (v1.62 — First Touch (#4) closed-loop scenario corpus on the #19 ScenarioRunner, extending the AR-7-motivated Simulation-layer coverage to the fourth consecutive spec whose unit suite encoded rather than caught its model defects. New src/first-touch/Tests/FirstTouchScenarios.cs + FirstTouchScenarioTests.cs: `heavy-touch-runs-on` locks AR-7 H-1 / ERR-004-003 — load-bearing predicate is displacement-velocity COHERENCE (dot(dispXY, velXY) > 0; the pre-fix negated blend displaced backward against forward retention) plus the downstream dx<0 sign and the Apply seam forwarding verbatim (exact component equality per T-AM-115 precedent); `interception-chain-anchors-at-displaced-ball` locks ERR-004-004 + §3.4.5 — opponent 3.3 m from the agent (invisible to BOTH the old agent-anchored 2.5 m gate and the 3.0 m PressureRadius truncation) but 1.37 m from the displaced ball MUST intercept; context produced by the REAL PressureEvaluator (zero pressure + global nearest tracked), redirect alignment ≈ 1.0 asserted, and the §3.4.5 Frame N+1 chain is executed: the interceptor's own touch on the redirected 10.6 m/s ball achieves CONTROLLED (q≈0.946) with possession + dribbling assigned. Envelope windows from the numerical mirror (toucher q=0.150, r=1.933, displaced x=50.57). New src/first-touch/AssemblyInfo.cs (InternalsVisibleTo — pass-mechanics/agent-movement precedent) so the corpus can drive the internal producer; first-touch-tests.asmdef gains the testing-strategy reference. file-manifest reconciled.) Prior same day (v1.61 — First Touch (#4) AR-7 fix pass (1H+3M+3L) + AR-8 sweep (2M, both resolved; ERR-004-003..006 filed, ERR-004-005 documented-open). H-1/ERR-004-003: §3.3.2 direction-blend sign INVERTED — IncomingDir negated ball velocity (“direction the ball came FROM”) while the same spec subsection states four times that a q=0 touch continues ALONG its original path and §3.3.5 retains momentum along +v; a heavy touch teleported the ball up to 2.0 m back toward the passer while its velocity pointed forward (the ball then travelled back through the receiver). Spec §3.3.2 pseudocode patched in the same commit (section-3-1-to-3-5.md v1.4); OrientationDetector's negation is CORRECT (facing-vs-approach) and untouched. The test suite ENCODED BOTH conventions simultaneously (BD-002 travel-direction vs BD-003/BD-004 negated — mutually unsatisfiable) and has NEVER compiled: v1.1 closed the namespace before FirstTouchIntegrationTests leaving 170 '{' vs 171 '}'. M-1/ERR-004-004: INTERCEPTION proximity was agent-anchored via PressureRadius-truncated NearestOpponentDistance — §3.4.2 anchors at newBallPosition (anchor error up to 80% of the 2.5 m radius; opponents 2.5–3 m from the displaced ball but >3 m from the agent were invisible); PressureEvaluator now tracks the global nearest opponent + NearestOpponentPositionXY, PossessionStateMachine measures |opponent − newBallPos| ≤ radius (Stage 0 single-candidate approximation documented; full SpatialQuery + interceptor ID land with ERR-004-002). M-2: degenerate-blend fallback aligned to the §3.3.2 IncomingDir mandate (was agentFacing). M-3: §3.4.5 INTERCEPTION velocity redirect toward the interceptor implemented (was specified, never coded — Frame N+1 contact chain was broken). L: dead q/originalBallVel params dropped from Determine; ControlledThreshold doc no longer claims the CONTROLLED gate; dead PitchHalf* removed + ControlledRadius/DribbleDetachRadius doc-noted unconsumed; test side-comments. AR-8 sweep: M-1 EvaluateFirstTouch NaN gate — Mathf.Clamp01 does not filter NaN so NaN BallVelocity flowed into ControlQuality (EC-001 encoded the contract but could not pass); Step 0 sanitise maps non-finite velocity/direction components to 0 (parallels AM AR-10 / CS AR-7). M-2/ERR-004-006: §5.10 VS-001 hand-calc used an ADDITIVE below-reference velocity modifier (r=0.428 m) contradicting normative §3.2.3 (multiplicative excess-only → r=0.195 m); spec §5.10 v1.1 + §5 changelog v1.4 + test re-derived. ERR-004-005 (documented-open): the DEFLECTION alignment gate is effectively vacuous through the public pipeline — heavy-touch q makes retention dominate so alignment ≈ 1.0; PO-005's low-alignment LOOSE_BALL expectation was unreachable and is re-derived to lock actual behaviour. All expectations re-verified by a full-pipeline numerical mirror (21 scenarios). Files: BallDisplacementProcessor.cs v1.5, PossessionStateMachine.cs v1.3, FirstTouchSystem.cs v1.6, PressureEvaluator.cs v1.3, PressureResult.cs v1.1, FirstTouchContext.cs v1.2, FirstTouchConstants.cs v1.8, Tests/FirstTouchTests.cs v1.2, docs/specs/first-touch/section-3-1-to-3-5.md v1.4 + section-5-7-to-5-13.md v1.1 + section-5-1-to-5-6.md v1.4, spec-error-log.md v1.25, file-manifest.md first-touch rows.) Prior same day (v1.60 — Scenario-corpus expansion onto the #19 ScenarioRunner, extending the AR-7-motivated closed-loop coverage to its own spec and to the first cross-spec chain. (1) Spec #1 per-spec corpus: new `src/ball-physics/tests/BallPhysicsScenarios.cs` + `BallPhysicsScenarioTests.cs` — `drop-and-rebound` locks AR-7 H-1 / ERR-001-001 (Y-up bounce normal: a falling ball never rebounded; load-bearing predicates are the 1.0–1.45 m first-rebound-peak window and exact X/Y purity of a spinless vertical drop) and `fast-descent-grounds-out` locks AR-7 H-2 (ground-clamp hover deadlock; extends the unit regression to the full composed settle: bounce ≤0.3 s, grounded ≤4 s, Stationary ≤7.5 s, resting x 55–61 m). Envelope windows derived from a numerical mirror of the fixed model (drop from 3 m: contact 0.75 s, rebound peak 1.22 m, settle 3.0 s; fast descent: contact 0.12 s, settle 5.9 s, x 58.1 m). `ball-physics-tests.asmdef` gains the testing-strategy reference. (2) First cross-spec corpus (KD-8; paths under `SCENARIO_PATH_CROSS_SPEC_PREFIX`, ≥2 owning specs enforced by the runner): new `src/testing-strategy/Tests/CrossSpecScenarios.cs` + `CrossSpecScenarioTests.cs` — `lofted-pass-kick-bounce-roll` drives the real `PassExecutor` (#5) WINDUP→CONTACT lifecycle into a real `BallState` through the `IPassBallSystem` seam (stub forwards to `BallCollision.ApplyKick`), then the real `BallPhysicsCore` loop (#1) through flight→bounce→roll→rest; #17 EventBus boot wiring + one-tick Resolve-phase lifecycle wraps the CONTACT publish (drained, not asserted — subscribing would trip the FR-EVT-020 boot guard after any prior DrainTick in-process; ledger assertions stay with the #17 smoke test). Owning specs {1, 5}; fixed-value agent/collision query stubs keep #2/#3 out of the asserted surface. Mirror: 25 m lofted pass at attributes 14 → kick 12.79 m/s @ 43.8°, rest x≈46.5 m (receiver at 45 m) after ~6.2 s; windows: contact frame 10–25, settle 2–9 s, resting x 43–50 / y 30–38. `testing-strategy-tests.asmdef` gains ball-physics + pass-mechanics + event-system references (test assemblies are infrastructure; the game-layer import ban is unaffected). file-manifest.md reconciled — including three June-7 ball-physics test rows that had been recorded only in its header note.) Prior v1.59 (Spec #19 ScenarioRunner AR-2 sweep: 0H+0M+2L, both resolved. L-1: a NaN in_range bound slipped past the min>max guard (NaN comparisons false) and masqueraded as a failing predicate — now throws ArgumentException as a harness-authoring error; L-2: the min>max exception message used culture-sensitive float concatenation — now InvariantCulture. ScenarioEnvelope.cs v1.2; ScenarioRunnerTests.cs v1.2 (18→19). AR-2 otherwise clean.) Prior v1.58 ( Spec #19 ScenarioRunner AR-1 fix pass: 0H+4M+6L, all resolved on a fresh-eyes sweep of the v1.57 harness commit. M-1: `ScenarioIndexEntry` could register a `ClosedLoopScenario` under a different manifest instance than the one it executes — load-time validation ran against the entry's manifest while `Run()` used (and reported diagnostics from) the scenario's own; the entry ctor now rejects reference mismatch and `ClosedLoopScenario` exposes an internal `Manifest` for the guard. M-2: the Stage 0 runner silently accepted manifests with non-empty `fixture_refs` it can never load (no `IFixtureValidator` exists until the Stage 0+1 KD-10 loader) — §3.3.4 forbids silent acceptance; `Run` now refuses fixture-backed manifests. M-3: the line-oriented key=value diagnostics were corruptible — exception messages and predicate details containing CR/LF broke the encoding, and a thrown body dropped its stack trace; `ScenarioEnvelope.SanitizeValue` flattens CR/LF at every emission point and an `exception_stack=` line is now emitted. M-4: the v1.0 `ScenarioIndex` doc claimed A.1 name-uniqueness but enforced only path-uniqueness; the index now rejects duplicate names, and the runner enforces §3.3.5 path↔name coherence plus A.1 cross-spec ≥2 owning-spec arity (new `SCENARIO_PATH_CROSS_SPEC_PREFIX` [FIXED] in TestingStrategyConstants v1.4). L-1: `OwningSpecIds` / `FixtureRefs` were castable back to the internal mutable arrays through the `IReadOnlyList` surface — now `ReadOnlyCollection` wrappers (parallels #18 AR-1 L-3). L-2: scenario detail strings used culture-sensitive float `ToString()` — now InvariantCulture, matching the harness. L-3: T-AM-115 `position-unchanged` had been silently weakened in migration — `Vector2.operator==` is approximate (~1e-5) where the original fixture asserted exact `Assert.AreEqual`; restored to exact component equality. L-4: `ScenarioIndexEntry` split to its own file per FILE NAMING precedent (BallCollision AR-2 L-2 / TraceChannel AR-1 H-1); the `ScenarioBody` delegate stays co-located with `ScenarioContext` with rationale documented. L-5: `IScenario.Run` doc read as if the runner guarantees KD-7 seeding — clarified as an implementation obligation. L-6: format_version now validated BEFORE field interpretation (FR-TS-070 gates schema semantics). ScenarioRunnerTests 12→18 (6 new AR-1 locks). Files: ScenarioRunner v1.1, ScenarioIndex v1.1, ScenarioIndexEntry.cs new, ScenarioManifest v1.1, ScenarioEnvelope v1.1, ClosedLoopScenario v1.1, ScenarioResult v1.1, IScenario v1.1, ScenarioContext v1.1, TestingStrategyConstants v1.4, ScenarioRunnerTests v1.1, AgentMovementScenarios v1.1.) Prior v1.57 (Stage 0 closed-loop scenario harness landed: Spec #19 §3.3.3 `ScenarioRunner` pulled forward from the Stage 0+1 schedule after the third consecutive spec (Ball Physics AR-7, Agent Movement AR-12/AR-13) where H/M-class closed-loop defects were encoded by pure-function unit suites rather than caught by them — per-function tests verify the spec as written; only a closed-loop run verifies the spec as composed. New `src/testing-strategy/` surface (9 files): ScenarioStatus / ScenarioResult / ScenarioManifest (in-memory Appendix A.1 entry — the on-disk index.<ext> encoding stays D1-pinned at Stage 0+1, so Stage 0 manifests are authored in code and the runner takes an injected immutable ScenarioIndex while keeping the §3.3.3 Run(manifestPath, seed) contract signature; Stage 0+1 adds the file-loading factory as a parser swap) / ScenarioEnvelope (executable expected_outcome_envelope — zero recorded predicates ⇒ Failed per FR-TS-030; NaN fails in_range) / ScenarioContext + ScenarioBody delegate (KD-7: DeterministicRngService seeded with the verbatim run seed before any subsystem init; manifest seed recorded alongside per FR-TS-025) / IScenario (§4.4.1 single-method contract — manifest travels on ScenarioIndexEntry, not the interface) / ClosedLoopScenario (hermetic per-run execution per FR-TS-023; body exception → Failed with diagnostic) / ScenarioIndex (duplicate-path rejection; runner refuses unindexed scenarios per §3.3.6 / FR-TS-028) / ScenarioRunner (load-time A.1 field + FR-TS-070 format-version validation throws ArgumentException — load-time error, not runtime assertion, per §3.3.4). TestingStrategyConstants.cs v1.3 adds SCENARIO_MANIFEST_FORMAT_VERSION. New Tests/ScenarioRunnerTests.cs (12 contract tests) + testing-strategy-tests.asmdef. First fixture corpus: T-AM-110..115 migrated out of AgentMovementTests.cs (v2.3) into AgentMovementScenarios.cs (bodies + A.1 manifests, Tier B envelopes) + AgentMovementScenarioTests.cs (the project's first Simulation-layer tests; sim_<scenario> naming per #19 §3.1.4); requirement IDs and assertion substance unchanged; agent-movement-tests.asmdef gains the testing-strategy reference (test assemblies are infrastructure — the game-layer runtime import ban is unaffected); test-plan.md v0.4; file-manifest.md gains a per-file src/testing-strategy/ section.) Prior v1.56 (Agent Movement #2 AR-12 fix pass (3H+1M+3L) + AR-13 sweep (2M, both resolved). The three H findings are closed-loop speed-control defects that survived AR-1..AR-11 because all prior tests exercised pure functions or injected mid-flight state. H-1: agents at rest could NEVER start moving — `EvaluateFromIdle` exits only on speed > IdleExit while the IDLE locomotion branch only decayed speed to 0; IDLE now accelerates (walk rate) toward the command-capped topSpeed when commandSpeed > 0. H-2: commandSpeed never capped the integration target — jog commands auto-promoted to SPRINTING (reservoir drain) and walk commands flapped WALKING→JOGGING→DECELERATING; Step 4–5 now applies `topSpeed = min(topSpeed, commandSpeed)` and `ApplyAcceleration` gains a max(current, topSpeed) asymptote ceiling so a 1-ulp overshoot cannot trip the strict band-promotion comparisons. H-3: Zeno deceleration — `ApplyDeceleration` re-derived a = v²/(2d) per frame from the decaying speed, so stopping from 6 m/s took ~78 s / ~32 m; new `LocomotionConstants.MinDecelerationFloor` [GT] (2.5 = WALK_DECELERATION) bounds the tail; spec-deviation note records that §3.2.5's normative model is constant-rate (full re-parametrisation deferred — conflicts with the MAX_ACCELERATION=8 cap). M-1: LeanAngle now derives from the Step 8 velocity-direction rotation (real path curvature via new `RotateVelocityToward` out-param overload) instead of facing rotation. L: GroundedReason Stage 0 producer note (SLIDING_TACKLE/DIVING_HEADER dormant until #3/#14); AgentState.PreviousState doc corrected (last DISTINCT state); AUTO_ALIGN one-frame facing lag documented. AR-13 sweep found 1 M in the H-2 interaction surface: an aerobically exhausted agent (pool < AerobicJogFloor) with a jog command flapped WALKING→JOGGING→DECELERATING at ~3 Hz; commandSpeed now clamps to JogEnter under exhaustion. New `MovementCommand.WalkTo` factory (AI previously could not request the WALKING band). Tests: closed-loop fixture T-AM-110..115 (launch / band-respect / bounded-stop / walk-band / exhaustion / HOLD-at-rest), T-AM-108..109 decel-floor units, T-AM-079 re-derived. Files: AgentMovementSystem.cs v1.15, AgentLocomotion.cs v1.5, AgentDirectionalMovement.cs v1.7, AgentMovementConstants.cs v1.9, AgentState.cs v1.5, GroundedReason.cs v1.1, MovementCommand.cs v1.3, AgentMovementTests.cs v2.2, AgentMovementUnitTests.cs v1.1, test-plan.md v0.3, file-manifest.md agent-movement section reconciled.) Prior v1.55 (Ball Physics #1 AR-7 fix pass (2H+4M+3L) + AR-8 sweep (2L, both resolved — clean). H-1: `BallGroundInteraction.ApplyBounce` ground normal `Vector3.up` (Unity +Y) → `(0,0,1)` — project is Z-up (§1.2); restitution/friction acted on the touchline axis so a falling ball NEVER rebounded; spec §3.1.8.1 pseudocode carried the same line and is patched in the same commit (ERR-001-001). H-2: `ValidatePhysicsState` ground clamp no longer zeroes negative Velocity.z for Airborne — the clamp runs before `UpdateBallState` and the Airborne→Bouncing transition requires vz<0, so any descent fast enough to skip the 2 cm exit window (>~1.2 m/s) was trapped in a permanent ground-level Airborne hover (no bounce, no roll). M-1: bounce friction stick impulse divided by the rotational-coupling divisor 1+m·r²/I = 2.5 (`Bounce.StickImpulseCouplingDivisor` [DERIVED]; ERR-001-002; undivided form reversed slip ~150%). M-2: test-suite spin-sign convention corrected — topspin for +X motion is +Y ω (4 tests had it inverted; Bounce_Topspin needs ω>v/r≈45.5 for a forward kick, now ω=60; retention test moved to normal-axis spin so friction torque cannot contaminate it). M-3: Bouncing integration branch gains gravity (was drag-only). M-4: `LongBall_DecaysRealistically` loop condition z≥R → State==Airborne (clamp pins z at R so the loop always hit maxSteps) and windows re-derived (2.6–3.4 m / 1.3–1.8 s; old 4–6 m exceeded the vacuum ceiling for vz=8) — all expectations verified by a numerical mirror of the fixed model. L: 0.01f/0.0001f literals → `Bounce.MinContactSpeed` / `Magnus.MinForceDirectionSqMagnitude` (FR-CS-016); `Ball.MomentOfInertia` [EST]→[DERIVED]; `HasInvalidValues(in BallState)`; catalogue LAYOUT NOTE; new `Airborne_FastDescent_Bounces_NoHoverDeadlock` regression test; AR-8: ApplyBounce precondition doc + ERR-001-003 [EST] inventory entry (FR-CS-020). Files: BallGroundInteraction.cs v1.3.1, BallPhysicsCore.cs v1.4, BallPhysicsConstants.cs v1.8, BallPhysicsCoreTests.cs v1.5, BallIntegrationTests.cs v1.4, docs/specs/ball-physics/section-3-1-8-to-3-1-14.md row 2.8, spec-error-log.md v1.23. Prior v1.54 (June 8, 2026 — Event System #17 Stage 1 boot-wiring smoke test landed + tracking-doc sync. New `src/event-system/tests/EventBusWiringSmokeTests.cs` v0.4 drives boot → publish → drain → digest across the 6 currently-wired EventBusRegistrar.Initialize() call sites (Pass / Shot / Perception / Decision / Heading / Goalkeeper); AM #2 is `[CROSS-PENDING]`. Golden digest pinned via `Assert.Inconclusive` so an unfilled pin surfaces yellow in CI. AR-1 (1H+1M+4L) + AR-2 (0H+1M+4L) + AR-3 (0H+0M+2L cycle-stop) adversarial review cycles complete. `event-system-tests.asmdef` extended with 6 production spec references (Editor-only). file-manifest.md updated with new test file row. Prior v1.53 (Pass Mechanics #5 AR-8 fix pass: 0M + 3L on a fresh-eyes full-surface sweep after AR-7. L-1: AR-7's brace-add for the CrossSubType-ignore warning gate left an empty `if (cond) { }` in production builds; gate hoisted to wrap the entire if-statement since the diagnostic has no functional follow-up. L-2: `ExecuteContact` state transition hoisted above Step 8 `EventBusStub.Publish` so a Publish exception cannot trap the executor in `Contact` and cause re-entry double-kick (FM-08 recheck currently guards but defensive ordering removes the dependence). L-3: forward-reference notes added next to the AR-2 M-2 history rows in `PassExecutor.cs` and `PassTargetResolver.cs` — the "[-1, +1]" characterisation there was the AR-2-era contract, superseded by AR-6 L-1 to "[-1, +1)". Files: PassExecutor.cs v1.11, PassTargetResolver.cs v1.7. Prior v1.52 (Pass Mechanics #5 AR-7 fix pass: 1M+3L all resolved on a fresh-eyes full-surface sweep over all 24 files in `src/pass-mechanics/`. M-1: FR-CS-031 gating drift fixed across sibling files — `PassMechanicsConstants` v1.2 (AR-2 L-13) gated its FM-01 `Debug.LogError` emits but the 12 parallel cold-path emits in `PassExecutor.cs`, `PassTypeProfiles.cs`, and `PassVelocityCalculator.cs` never got the same `#if UNITY_EDITOR || DEVELOPMENT_BUILD` gating; all now gated. L-1: `[-1, +1]` → `[-1, +1)` propagation from AR-6 producer-side correction to `PassTargetResolver.ApplyErrorToDirection` doc and the `PassExecutor.ExecuteContact` Step 3 callsite comment. L-2: `EventBusRegistrar.cs` v1.3 history row's "no `InternalsVisibleTo` on this assembly" rationale was already stale at AR-3 time (`AssemblyInfo.cs` created 2026-06-01 with the attribute); corrected to the mock-at-EventRegistry-boundary rationale alone. L-3: `CrossSubType` and `PassType` enums gained ORDINAL STABILITY paragraphs parallel to `CancelReason` v1.4 — `PassType` additionally documents that `(int)_request.PassType` is the third hash input to `ComputeErrorDirection`, so reordering breaks deterministic error-direction parity even before the digest catches the drift. Files: PassExecutor.cs v1.10, PassTypeProfiles.cs v1.4, PassVelocityCalculator.cs v1.4, PassTargetResolver.cs v1.6, EventBusRegistrar.cs v1.4, CrossSubType.cs v1.1, PassType.cs v1.2. Prior v1.51 (Pass Mechanics #5 AR-6 fix pass: 1M+3L all resolved, converts the AR-5 cycle-stop. `PassErrorCalculator.ComputeErrorDirection` M-1: input-mix primes for `frameNumber` / `passTypeIndex` replaced with xxHash64 PRIME64_3 (`0x165667B19E3779F9`) and PRIME64_5 (`0x27D4EB2F165667C5`) so the Stafford Mix13 finalizer (`0xBF58476D1CE4E5B9` / `0x94D049BB133111EB`) no longer multiplies through the same primes the input-mix already used. AR-5 fixed the same hazard on agentId; AR-6 finishes the invariant — input-mix primes are now disjoint from finalizer primes on all three axes. L-1: `<returns>` upper bound corrected `[-1, +1]` → `[-1, +1)` to match the 24-bit mantissa quantisation (EC-010 already enforces `Assert.Less(dir, 1.0f)`). L-2: comment block rewritten to call out the additive-vs-XOR asymmetry on agentId (AR-5 intent) and record the AR-6 input-mix / finalizer prime disjointness invariant. L-3: bit-extraction literals `0x00FFFFFFu` / `0x01000000u` promoted to named local consts `Mantissa24Mask` / `Mantissa24Scale` with a comment noting the 24-bit window matches float mantissa precision. Files: `src/pass-mechanics/PassErrorCalculator.cs` v1.7. Prior v1.50 (cross-spec routing close-out: `Possession.ControlHeight` ↔ `GroundControlHeight` resolved. Ball Physics #1 §3.1.11 `BallPhysicsConstants.Possession.ControlHeight` declared the authority; `FirstTouchConstants.GroundControlHeight` relocated from `#region GT` to `#region Cross`, retagged `[CROSS]`, RHS now `BallPhysicsConstants.Possession.ControlHeight` (single-consumer mirror per Spec #20 §4.2). Ball Physics XML drift warning replaced with an authority/consumer pointer. Sibling-hazard sweep cleared — no other spec re-declares `ControlRadius` / `ControlVelocity` / `ChallengeRadius`. Files: FirstTouchConstants.cs v1.7, BallPhysicsConstants.cs v1.7, docs/specs/first-touch/section-4-4-to-4-6.md §3.4.3, root CLAUDE.md OPEN ISSUES. Prior v1.49 (AR-hardening sweep complete. New AR rounds since v1.45: Ball Physics #1 AR-6 (4L); Collision System #3 AR-1..AR-6 (final 3L); First Touch #4 AR-5..AR-6 (final 3L); Pass Mechanics #5 AR-2..AR-5 (final 1M+3L cycle-stop — `ComputeErrorDirection` agentId now multiplied by xxHash64 prime 0xC2B2AE3D27D4EB4F before seed addition for better hash quality); Shot Mechanics #6 AR-2..AR-4 (final 3L); Event System #17 AR-8 (2M+1L), AR-9 (1M+1L), AR-10 (1M+2L), AR-11 (no findings — whole-surface sweep clean); Agent Movement #2 AR-10 (1M+2L — `SanitiseCollisionForce` maps NaN/±Inf → 0 before Clamp01 since Unity's Clamp01 does not filter NaN), AR-11 (2L — EvaluateState defaults dropped parallel to AR-9 L-2; OscillationGuard.WriteTime default-arm assert); Performance Optimization #18 AR-3 + AR-3 full-surface (3M+5L / 3M+3L) + AR-4 (2L). v1.48 prior: Performance Optimization #18 RegressionGate.Evaluate now reports skipped-drift (NaN) vs degenerate-milestone (+Infinity) distinctly in MilestoneDriftFraction. v1.47 prior: PO #18 AR-3 full-surface (M-1 RECORD_FORMAT_VERSION_STAGE0_PENDING sentinel constant). v1.46 prior: PO #18 AR-3 (M-1 PerformanceOptimizationConstants RECORD_FORMAT_VERSION_STAGE0_PENDING). — Prior v1.45 (Agent Movement #2 pure-function unit coverage expansion. New `src/agent-movement/Tests/AgentMovementUnitTests.cs` v1.0 adds 59 NUnit tests across 7 `[TestFixture]` classes.) — Prior v1.44 (Agent Movement #2 regression-test roster initiated.) — Prior v1.43 (Agent Movement #2 AR-9 fix pass.) — Prior v1.42 (AR-8 fix pass.) — Prior v1.41 (AR-7 fix pass.) — Prior v1.40 (AR-5+AR-6 fix pass.) — Prior v1.39 (Ball Physics #1 AR-6 fix pass.) — Prior v1.38 (Ball Physics #1 AR-5 fix pass.) — Prior v1.37 (Ball Physics #1 AR-4 fix pass.) — Prior v1.36 (Ball Physics #1 AR-3 fix pass.) — Prior v1.35 (Ball Physics #1 AR-2 fix pass.) — Prior v1.34 (Ball Physics #1 AR-1 fix pass.) — Prior v1.33 (Testing Strategy #19 PR #132 Codex P2 follow-up.) — Prior v1.32 (Testing Strategy #19 AR-5 fix pass.) — Prior v1.27..v1.31 (Testing Strategy #19 scaffold + AR-1..AR-4.) — Prior v1.25..v1.26 (Performance Optimization #18 AR-1..AR-2.)))
> **Purpose:** Concrete coding rules for any AI agent or developer writing C# source code in this project. Covers file naming, constant catalogues, Unity project structure, and build/test commands. Cites Spec #20 (Code Standards & Style Guide) as the source for every convention here. Read the root `CLAUDE.md` first — this file supplements it, not replaces it.

---

## BEFORE YOU WRITE ANY CODE

1. Read the root `CLAUDE.md` completely.
2. Read Spec #20 (`docs/specs/code-standards/`) for the full rule set with rationale.
3. Read the `§4` (Architecture) file of the spec you are implementing.
4. Check `docs/specs/SPEC_INDEX.md` to confirm the spec's status is `APPROVED`.

---

## UNITY PROJECT STRUCTURE

```
src/
├── CLAUDE.md                          ← You are here
│
├── project-constants/                 ← infrastructure; read-only by all (bottom of the reference graph)
│   ├── project-constants.asmdef       ← one assembly per folder (FR-CS-055); references: [] (autoReferenced)
│   ├── GameplayConfig.cs              ← FR-CS-019: immutable boot-time [GT] key/value store (GetFloat/Int/Bool/String + fallback; fail-loud on malformed)
│   ├── GameplayConfigFileLoader.cs    ← FR-CS-019: on-disk [section] key = value text → GameplayConfig (parser swap; fail-loud)
│   ├── (ProjectConstants.cs)          ← source-of-truth for multi-consumer constants (Spec #20 §4.2) — not yet needed; add when a multi-consumer [CROSS] constant exists
│   └── tests/
│       ├── project-constants-tests.asmdef
│       ├── GameplayConfigTests.cs            ← getter / fallback / fail-loud / case-insensitive / ctor-guard locks
│       └── GameplayConfigFileLoaderTests.cs  ← grammar round-trip + empty→Empty + fail-loud cases
│
├── ball-physics/                      ← Spec #1
│   ├── ball-physics.asmdef            ← references TacticalDirector.DeterministicSim; autoReferenced true
│   ├── BallPhysicsConstants.cs
│   ├── BallState.cs
│   ├── BallPhysicsCore.cs
│   ├── BallStateMachine.cs
│   ├── BallGroundInteraction.cs
│   ├── BallCollision.cs               ← ball-specific collision response; detection geometry lives in collision-system/
│   ├── BallEventLogger.cs
│   ├── SurfaceProperties.cs
│   ├── BodyPart.cs                    ← enum: Foot / Shin / Thigh / Torso / Head / Arm
│   ├── RestartType.cs                 ← enum: None / ThrowIn / GoalKick / Corner / KickOff
│   ├── KickResult.cs                  ← enum: Applied / RejectedNonFiniteVelocity (ApplyKick contract)
│   ├── BodyPartCoefficients.cs        ← static class: per-body-part (speedRetention, spinRetention) lookup
│   └── tests/
│       ├── ball-physics-tests.asmdef  ← EditMode; references ball-physics.asmdef + testing-strategy.asmdef
│       ├── BallPhysicsCoreTests.cs
│       ├── BallIntegrationTests.cs
│       ├── BallStateMachineTests.cs
│       ├── BodyPartCoefficientsTests.cs ← AR-4 L-2 throw-on-unknown + catalogue round-trip
│       ├── SurfacePropertiesTests.cs    ← AR-4 L-2 throw-on-unknown + catalogue round-trip (4 Get* methods)
│       ├── EnumOrdinalStabilityTests.cs ← AR-6 L-3 locks int ordinals for all 6 public enums
│       ├── BallPhysicsScenarios.cs      ← Spec #1 closed-loop scenario corpus on the #19 ScenarioRunner:
│       │                                │   drop-and-rebound (AR-7 H-1 / ERR-001-001 lock) +
│       │                                │   fast-descent-grounds-out (AR-7 H-2 hover-deadlock lock)
│       └── BallPhysicsScenarioTests.cs  ← sim_<scenario> Simulation-layer tests running the corpus through ScenarioRunner
│
├── agent-movement/                    ← Spec #2
│   ├── agent-movement.asmdef
│   ├── AssemblyInfo.cs                ← [InternalsVisibleTo("TacticalDirector.AgentMovement.Tests")]
│   ├── AgentMovementConstants.cs      ← constants: MovementThresholds / FatigueRates /
│   │                                  │   LocomotionConstants / DirectionalConstants /
│   │                                  │   TurnConstants / OscillationGuardConstants /
│   │                                  │   SafetyConstants / PlayerAttributeConstants
│   ├── AgentMovementState.cs          ← enum: AgentMovementState (7 locomotion states)
│   ├── GroundedReason.cs              ← enum: GroundedReason (NONE / COLLISION / SLIDING_TACKLE / DIVING_HEADER)
│   ├── FacingMode.cs                  ← enum: FacingMode (AUTO_ALIGN / TARGET_LOCK)
│   ├── DecelerationMode.cs            ← enum: DecelerationMode (CONTROLLED / EMERGENCY)
│   ├── AgentState.cs                  ← mutable value-type game state (ref-mutated, not readonly)
│   ├── PlayerAttributes.cs
│   ├── PerformanceContext.cs
│   ├── MovementCommand.cs             ← public AI factories + internal ToolingOverrideOnly_NaNInjection (T-AM-030..032)
│   ├── AgentMovementSystem.cs         ← 12-step 60 Hz pipeline orchestrator
│   ├── AgentStateMachine.cs           ← pure state evaluator (no side effects)
│   ├── OscillationGuard.cs            ← ring-buffer anti-oscillation guard
│   ├── AgentLocomotion.cs             ← acceleration / deceleration formulas
│   ├── AgentTurning.cs                ← turn rate / lean angle / stumble probability
│   ├── AgentDirectionalMovement.cs    ← directional multipliers / facing update
│   ├── AgentSafetySystem.cs           ← NaN detection / speed clamp / pitch boundary
│   └── Tests/
│       ├── agent-movement-tests.asmdef  ← EditMode; references agent-movement.asmdef + testing-strategy.asmdef
│       ├── AgentMovementTests.cs        ← T-AM-001..018 / T-AM-030..033 / T-AM-040..043 regression roster
│       ├── AgentMovementScenarios.cs    ← T-AM-110..115 closed-loop scenario corpus: bodies + #19 A.1 manifests
│       │                                │   (migrated from the AR-12/AR-13 in-fixture form June 10, 2026)
│       ├── AgentMovementScenarioTests.cs ← sim_<scenario> Simulation-layer tests running the corpus through #19 ScenarioRunner
│       └── AgentMovementUnitTests.cs    ← T-AM-007..009 / T-AM-019..023 / T-AM-034..039 /
│                                       │   T-AM-044..047 / T-AM-050..052 / T-AM-070..109
│                                       │   pure-function coverage (test-plan.md v0.4)
│
├── collision-system/                  ← Spec #3
│   ├── collision-system.asmdef
│   ├── CollisionSystemConstants.cs
│   ├── CollisionDetection.cs          ← broad-phase and narrow-phase detection
│   ├── SpatialHashGrid.cs             ← spatial hash grid for broad-phase queries
│   ├── CollisionManifold.cs           ← contact manifold (depth, normal, point)
│   ├── CollisionEvent.cs              ← struct event published on confirmed collision
│   ├── CollisionResponse.cs           ← impulse-based response calculations
│   ├── CollisionSystem.cs             ← 60 Hz pipeline orchestrator
│   ├── CollisionPairBitfield.cs       ← processed-pair tracking (no double-processing)
│   ├── AgentAgentCollisionResult.cs   ← result struct for agent–agent resolution
│   ├── AgentBallCollisionData.cs      ← data struct for agent–ball contact
│   ├── AgentPhysicalProperties.cs     ← mass, radius, restitution per agent
│   ├── BallCollisionHandler.cs        ← ball-specific collision response
│   ├── ContactForceData.cs            ← contact force magnitude and direction
│   ├── ContactType.cs                 ← enum: SLIDE / SHOULDER / BLOCK / FOUL
│   ├── ContactTypeClassifier.cs       ← classifies manifold into ContactType
│   ├── CollisionType.cs               ← enum: AGENT_AGENT / AGENT_BALL / AGENT_POST / AGENT_BOUNDARY
│   ├── DeterministicRNG.cs            ← SplitMix64 wrapper for foul/stumble rolls
│   ├── ICollisionEventConsumer.cs     ← interface for systems consuming collision events (Spec #3 §3.4.2)
│   └── tests/
│       └── collision-system-tests.asmdef  ← EditMode; references collision-system.asmdef
│
├── first-touch/                       ← Spec #4
│   ├── FirstTouchConstants.cs
│   ├── FirstTouchContext.cs           ← input: incoming ball state, agent state, env
│   ├── FirstTouchResult.cs            ← output: displaced ball state + possession outcome
│   ├── TouchResult.cs                 ← intermediate touch quality evaluation result
│   ├── FirstTouchSystem.cs            ← main orchestrator
│   ├── BallDisplacementProcessor.cs   ← post-touch ball displacement vector
│   ├── ControlQualityCalculator.cs    ← control quality score (0–1) from attributes + context
│   ├── OrientationDetector.cs         ← body orientation relative to incoming ball
│   ├── PossessionStateMachine.cs      ← LOOSE → CONTROLLED → POSSESSED transitions
│   ├── PressureEvaluator.cs           ← nearby-defender pressure scalar
│   ├── PressureResult.cs              ← pressure evaluation output
│   ├── TouchRadiusCalculator.cs       ← acceptance radius for touch attempt
│   ├── IAgentMovementSystem.cs        ← read-only query boundary to Agent Movement (#2)
│   ├── IBallPhysicsSystem.cs          ← read-only query boundary to Ball Physics (#1)
│   ├── IFirstTouchSystem.cs           ← public interface for First Touch consumers
│   ├── AssemblyInfo.cs                ← [InternalsVisibleTo("TacticalDirector.FirstTouch.Tests")]
│   ├── first-touch.asmdef             ← references BallPhysics, AgentMovement, CollisionSystem
│   └── Tests/
│       ├── first-touch-tests.asmdef   ← EditMode; references first-touch.asmdef + testing-strategy.asmdef
│       ├── FirstTouchTests.cs         ← CQ/TR/PR/OR/PO/EC/BD/VS + invariant suite + IT-001..008 stubs
│       ├── FirstTouchScenarios.cs     ← Spec #4 closed-loop scenario corpus on the #19 ScenarioRunner:
│       │                              │   heavy-touch-runs-on (AR-7 H-1 / ERR-004-003 coherence lock) +
│       │                              │   interception-chain-anchors-at-displaced-ball (ERR-004-004 +
│       │                              │   §3.4.5 redirect + Frame N+1 chain via real PressureEvaluator)
│       └── FirstTouchScenarioTests.cs ← sim_<scenario> Simulation-layer tests running the corpus through ScenarioRunner
│
├── pass-mechanics/                    ← Spec #5
│   ├── PassMechanicsConstants.cs
│   ├── PassRequest.cs                 ← input: passer, target, requested pass type
│   ├── PassResult.cs                  ← output: ball velocity applied + outcome
│   ├── PassAttemptEvent.cs            ← struct event on pass initiation
│   ├── PassCancelledEvent.cs          ← struct event on pass cancellation
│   ├── CancelReason.cs                ← enum: TackleInterrupt (reason pass was cancelled)
│   ├── PassExecutor.cs                ← main orchestrator: full pass pipeline
│   ├── PassVelocityCalculator.cs      ← launch velocity from profile + attributes
│   ├── PassErrorCalculator.cs         ← direction / speed deviation error model
│   ├── PassTargetResolver.cs          ← resolves intended target position
│   ├── PassTypeProfiles.cs            ← PhysicalProfile factory per PassType
│   ├── PhysicalProfile.cs             ← struct: speed range, spin, launch angle per type
│   ├── PassType.cs                    ← enum: Ground / Driven / Lofted / ThroughBall / AerialThrough / Cross / Chip
│   ├── CrossSubType.cs                ← enum: Flat / Whipped / High
│   ├── SpinType.cs                    ← enum: Topspin / Backspin / Sidespin / Mixed
│   ├── PassOutcome.cs                 ← enum: Initiated / Completed / Cancelled / Invalid
│   ├── PassAgentAttributes.cs         ← agent skill attributes consumed by pass system
│   ├── PassAgentState.cs              ← agent state consumed by pass system
│   ├── IPassAgentQuery.cs             ← interface: query agent attributes and state
│   ├── IPassBallSystem.cs             ← interface: apply kick velocity to ball
│   ├── IPassCollisionQuery.cs         ← interface: interception queries into Collision System
│   ├── EventBusStub.cs                ← wired to EventBus.Publish; 3-tier generic IEventA/B/C overloads
│   └── EventBusRegistrar.cs           ← boot-time RegisterExternalRow<T>() for PassAttemptEvent (0x0C) + PassCancelledEvent (0x0D)
├── shot-mechanics/                    ← Spec #6
│   ├── ShotMechanicsConstants.cs      ← all GT/Fixed/Cross constants (velocity, angle, spin, error, body, weak-foot, timing)
│   ├── ContactZone.cs                 ← enum: Centre / BelowCentre / OffCentre
│   ├── ShotOutcome.cs                 ← enum: Completed / Cancelled / Invalid / Initiated
│   ├── ShotCancelReason.cs            ← enum: TackleInterrupt
│   ├── ShotRequest.cs                 ← input struct from Decision Tree to ShotExecutor
│   ├── ShotResult.cs                  ← output struct returned by ShotExecutor
│   ├── ShotAgentAttributes.cs         ← agent attribute snapshot (Finishing, LongShots, etc.)
│   ├── ShotAgentState.cs              ← agent physical state snapshot (position, velocity, etc.)
│   ├── ShotExecutedEvent.cs           ← struct event published at CONTACT completion
│   ├── ShotCancelledEvent.cs          ← struct event published on WINDUP tackle interrupt
│   ├── ShotAnimationData.cs           ← struct event stub for animation system (Stage 1+)
│   ├── BodyMechanicsResult.cs         ← output struct from BodyMechanicsEvaluator
│   ├── GoalGeometry.cs                ← value struct: goal width/height/line/posts/crossbar
│   ├── IShotVelocityCalculator.cs     ← interface for EC-008 NaN injection seam only
│   ├── IShotBallSystem.cs             ← interface: possession check + ApplyKick
│   ├── IShotAgentQuery.cs             ← interface: read agent attributes and state
│   ├── IShotCollisionQuery.cs         ← interface: tackle flag poll + pressure scalar
│   ├── GoalGeometryProvider.cs        ← static goal geometry access + test override seam (SP-009)
│   ├── ShotVelocityCalculator.cs      ← §3.2 velocity formula; singleton; implements IShotVelocityCalculator
│   ├── ShotLaunchAngleCalculator.cs   ← §3.3 launch angle formula; pure static
│   ├── ShotSpinCalculator.cs          ← §3.4 spin vector assembly; pure static
│   ├── ShotPlacementResolver.cs       ← §3.5 goal-relative placement → world-space aim direction
│   ├── BodyMechanicsEvaluator.cs      ← §3.7 body mechanics score (run-up, plant, velocity, lean)
│   ├── WeakFootPenaltyApplier.cs      ← §3.8 weak-foot error/velocity multipliers
│   ├── ShotErrorCalculator.cs         ← §3.6 deterministic angular error (magnitude + direction)
│   ├── ShotEventEmitter.cs            ← publishes ShotExecutedEvent / ShotCancelledEvent / ShotAnimationData
│   ├── EventBusStub.cs                ← wired to EventBus.Publish; 3-tier generic IEventA/B/C overloads
│   ├── EventBusRegistrar.cs           ← boot-time RegisterExternalRow<T>() for ShotExecutedEvent (0x01) + ShotCancelledEvent (0x0E) + ShotAnimationData (0x0F)
│   ├── ShotExecutor.cs                ← sealed orchestrator: 5-state machine (Idle/Windup/Contact/FollowThrough/Complete)
│   └── Tests/
│       └── NaNVelocityStub.cs         ← #if UNITY_EDITOR||DEVELOPMENT_BUILD; returns NaN for EC-008 FM-05 test
├── perception-system/                 ← Spec #7
│   ├── perception-system.asmdef
│   ├── PerceptionConstants.cs         ← all GT/Fixed/Derived/Cross constants (§3.10); 18 spec constants + sizing constants
│   ├── PerceptionAgentAttributes.cs   ← struct: Decisions/Anticipation/TeamId/IsHalfTurned snapshot (§4.2.2)
│   ├── FilteredView.cs                ← FilteredView, PerceptionDiagnostics, PerceivedAgent, ShoulderCheckAnimData, OcclusionDebugRecord, PerceivedAgentDebug
│   ├── PerceptionEvents.cs            ← PerceptionRefreshEvent struct + RefreshTrigger enum (§4.6.3)
│   ├── EventBusStub.cs                ← wired to EventBus.Publish; 3-tier generic IEventA/B/C overloads
│   ├── EventBusRegistrar.cs           ← boot-time RegisterExternalRow<T>() for PerceptionRefreshEvent (0x10)
│   ├── FovCalculator.cs               ← FoV formula (§3.1) + angular candidacy test + blind-side/peripheral arc predicates
│   ├── OcclusionFilter.cs             ← shadow cone geometry (§3.2.3) + opponent occlusion test; Stage 0: opponents only (OQ-1)
│   ├── PressureEvaluator.cs           ← PressureScalar formula (§3.6); reused verbatim from First Touch #4 §3.5
│   ├── BallPerceptionEvaluator.cs     ← ball range/FoV/occlusion tests + BallStalenessFrames tracking (§3.5); no L_rec (OQ-2)
│   ├── RecognitionLatencyTracker.cs   ← per-(observer,target) latency counters, L_rec formula, half-turn bonus, Wang/Jenkins deterministic hash (§3.3)
│   ├── ShoulderCheckScheduler.cs      ← autonomous shoulder check scheduling + window management + blind-side entity L_rec (§3.4)
│   ├── ViewBuilder.cs                 ← pure field-assembly step: sets scalar/count fields on pre-allocated FilteredView + PerceptionDiagnostics (§3.7)
│   ├── PerceptionSystem.cs            ← 10Hz orchestrator; 7-step pipeline for all 22 agents; forced-refresh handler (§3.0–§3.8, §4.1, §4.6)
│   └── Tests/
│       ├── perception-system-tests.asmdef ← EditMode; + references TacticalDirector.TestingStrategy (scenario corpus)
│       ├── PerceptionSystemTests.cs   ← 68 unit tests (Fov / Occlusion / RecognitionLatency / ShoulderCheck / BallPerception / Snapshot); §5.11 multi-agent integration = Play-Mode Assert.Ignore stubs
│       ├── PerceptionScenarios.cs     ← Spec #7 closed-loop corpus on the #19 ScenarioRunner: drives the real 22-agent OnHeartbeat (full-heartbeat-all-snapshots / awareness-cold-start-builds / two-instance-determinism)
│       └── PerceptionScenarioTests.cs ← sim_<scenario> Simulation-layer tests running the corpus through ScenarioRunner
├── decision-tree/                     ← Spec #8
│   ├── decision-tree.asmdef           ← AI layer; references agent-movement, perception-system, pass-mechanics, shot-mechanics, deterministic-sim (+4 more)
│   ├── AssemblyInfo.cs                ← [assembly: InternalsVisibleTo("TacticalDirector.DecisionTree.Tests")]
│   ├── DecisionTree.cs                ← public sealed class: 6-step pipeline orchestrator + state machine (§3.6, §3.7, §4.1)
│   ├── DecisionTreeStateMachine.cs    ← pure state evaluator: IDLE/EVALUATING/EXECUTING/INTERRUPTED transitions (§3.7.2)
│   ├── SnapshotValidator.cs           ← Step 1: validates FilteredView — phase gate, agent identity, ball state (§3.1.1)
│   ├── DecisionContextAssembler.cs    ← Step 2: assembles DecisionContext from all pipeline inputs (§2.2.4, §3.1.1)
│   ├── OptionGenerator.cs             ← Step 3: generates all eligible ActionOption candidates — possession + off-ball branches (§3.1)
│   ├── UtilityScorer.cs               ← Step 4: scores ActionOptions with §3.2 formulas (zone×AM×context×tact×risk)
│   ├── TacticalModifierResolver.cs    ← Step 4 helper: resolves tactical multipliers per action type (§3.4)
│   ├── ActionSelector.cs              ← Step 5: composure noise injection + highest-EffectiveUtility winner (§3.3)
│   ├── ActionDispatcher.cs            ← Step 6: routes selected action to movement controller or physics executor (§3.5)
│   ├── DecisionContext.cs             ← internal struct: all assembled pipeline inputs for one agent-tick (§2.2.4)
│   ├── ActionOption.cs                ← internal struct: one scored candidate with type-specific context fields (§3.1.0)
│   ├── AgentAction.cs                 ← public readonly struct: pipeline output (type, target, pass/shot params, utility) (§2.2.3)
│   ├── DecisionMadeEvent.cs           ← struct event: published after each decision; IEventC (Tier C); wired via EventBusStub (§2.2.7)
│   ├── DtAgentAttributes.cs           ← struct: all DT-consumed player attributes [1–20] raw + CreateDefault factory (§3.1)
│   ├── MatchContext.cs                ← struct: authoritative match state per heartbeat (score, possession, ball, zone) (§2.2.5)
│   ├── TacticalContext.cs             ← struct: pressing mode, passing style, formation slots; Stage0Default factory (§2.2.6)
│   ├── DecisionTreeConstants.cs       ← constants: capacity limits / timing budgets / pipeline invariants (§4.2, §3.7)
│   ├── UtilityWeights.cs              ← constants: all 58+ utility scoring constants (§3.2.11)
│   ├── ComposureWeights.cs            ← constants: NOISE_MAX / COMPOSURE_SUPPRESSION / TIEBREAK_EPSILON (§3.3.3–3.3.5)
│   ├── TacticalWeights.cs             ← constants: tactical multipliers for all action types (§3.4)
│   ├── PitchGeometry.cs               ← static helpers: field zone classification, goal post positions, centre (§3.1.1)
│   ├── IDtMovementController.cs       ← public interface: dispatch boundary to Agent Movement #2 XC-3.5-10 (§3.5)
│   ├── EventBusStub.cs                ← wired to EventBus.Publish (internal; single-sig for DecisionMadeEvent)
│   ├── EventBusRegistrar.cs           ← boot-time RegisterExternalRow<T>() for DecisionMadeEvent (0x11)
│   ├── ActionType.cs                  ← enum: PASS/SHOOT/DRIBBLE/HOLD/MOVE_TO_POSITION/PRESS/INTERCEPT (ordinals are hash inputs)
│   ├── DtState.cs                     ← enum: IDLE / EVALUATING / EXECUTING / INTERRUPTED (§3.7.1)
│   ├── FieldZone.cs                   ← enum: DEFENSIVE / MIDFIELD / ATTACKING — zone boundaries for utility modifiers (§2.2.5)
│   ├── MatchPhase.cs                  ← enum: OPEN_PLAY / SET_PIECE_HOME / SET_PIECE_AWAY / KICK_OFF (§2.2.5)
│   ├── PassingStyle.cs                ← enum: DIRECT / MIXED / SHORT — team passing instruction (§2.2.6)
│   ├── PressingMode.cs                ← enum: HIGH / MEDIUM / LOW — team pressing instruction (§2.2.6)
│   ├── PossessionState.cs             ← enum: HOME_TEAM / AWAY_TEAM / CONTESTED (§2.2.5)
│   └── Tests/
│       ├── decision-tree-tests.asmdef ← EditMode; references decision-tree.asmdef
│       ├── OptionGeneratorTests.cs    ← UT-01..UT-07: OptionGenerator generation gates and candidate logic
│       ├── UtilityScorerTests.cs      ← UT-08..UT-09: UtilityScorer per-action-type utility formulas
│       ├── ActionSelectorTests.cs     ← UT-10..UT-15: ActionSelector composure noise injection + winner selection
│       ├── DispatcherTests.cs         ← UT-16..UT-23: ActionDispatcher movement routing (HOLD/DRIBBLE/MOVE/PRESS/INTERCEPT)
│       ├── DecisionTreeIntegrationTests.cs ← UT-24..UT-35: full pipeline state machine + output (UT-33..35: AR-2 H-3 locks)
│       └── DecisionContextAssemblerTests.cs ← AR-2 H-2 (team-relative BallZone) + M-1 (OpponentHasBall) locks
├── fixed64-math/                      ← Spec #9  (Stage 5+; no runtime code at Stage 0)
├── heading-mechanics/                 ← Spec #10
│   ├── heading-mechanics.asmdef
│   ├── HeadingMechanicsConstants.cs   ← all GT/Fixed/Cross/Derived constants (§3.1)
│   ├── ContactQualityLabel.cs         ← enum: Early / OnTime / Late (telemetry only; KD-2)
│   ├── MistimedDirection.cs           ← enum: None / Early / Late (eligibility output)
│   ├── FailureCause.cs                ← enum: MistimedEarly / MistimedLate / PositionedPoorly / DisturbedInDuel
│   ├── SetPieceContext.cs             ← enum: OpenPlay / Corner / FreeKick (telemetry only)
│   ├── HeadingAgentAttributes.cs      ← struct: Heading/Strength/Balance [1-20], Fatigue [0,1], TeamId
│   ├── HeaderIntent.cs                ← struct: PowerIntent/ContactPointIntent/TargetIntent/AttemptCommittedTick/SetPieceContext (locked at commit; KD-17)
│   ├── HeaderContactState.cs          ← struct: per-attempt mutable state (JumpStartFrame, quality, disturbance, etc.)
│   ├── EligibilityResult.cs           ← struct: IsEligible, PredictedContactFrame, IdealContactFrame, MistimedDirection
│   ├── HeaderExecutedEvent.cs         ← struct: published on successful contact (Tier B event)
│   ├── HeaderAttemptFailedEvent.cs    ← struct: published on failure (Tier C event; no ball-state modification)
│   ├── ContestedDuelContext.cs        ← struct: DuelId, ParticipantCount, WinnerAgentId, BufferStartIndex
│   ├── IHeadingBallSystem.cs          ← interface: GetBallState + ApplyKick
│   ├── IHeadingRngService.cs          ← interface: NextFloat + NextGaussian
│   ├── HeadingRngServiceStub.cs       ← Stage 0 SplitMix64 stub; replace at Stage 1 with #16 wiring
│   ├── EventBusStub.cs                ← wired to EventBus.Publish; 3-tier generic IEventA/B/C overloads
│   ├── EventBusRegistrar.cs           ← boot-time RegisterExternalRow<T>() for HeaderExecutedEvent (0x12 Tier B) + HeaderAttemptFailedEvent (0x13 Tier C)
│   ├── HeadingEligibility.cs          ← pure eligibility predicate (§3.2); no side effects
│   ├── HeadingJumpKinematics.cs       ← FM-010-001 JumpReach + Stage 0 synthetic parabolic Z (KD-18)
│   ├── HeadingContactQuality.cs       ← FM-010-002 contact-quality scalar (asymmetric timing + point error)
│   ├── HeadingPowerAngle.cs           ← FM-010-003 outgoing speed + reflection geometry + own-goal flag (§3.8)
│   ├── HeadingSpinTransfer.cs         ← FM-010-004 head angular-velocity derivation + outgoing spin (§3.6)
│   ├── HeadingDuelResolution.cs       ← FM-010-005 duel scoring; ICollisionEventConsumer; pre-allocated buffers
│   ├── HeadingTelemetry.cs            ← Stage 0 stub; emits §2.4 heading.* trace-pipeline channels at Stage 0+1
│   └── HeadingMechanics.cs            ← 60 Hz orchestrator; two-pass per-frame loop (§4.6)
├── goalkeeper-mechanics/              ← Spec #11
│   ├── goalkeeper-mechanics.asmdef
│   ├── GoalkeeperConstants.cs         ← all GT/Fixed/Cross/Derived constants (§3.4); ~79 constants + 4 draw-site IDs
│   ├── GoalkeeperState.cs             ← enum: GoalkeeperState (11 states)
│   ├── HandlingQualityLabel.cs        ← enum: Caught/Parried/Deflected/Spilled/Missed — telemetry only (KD-2)
│   ├── ReactionLabel.cs               ← enum: Reflexive/Standard/Sluggish — telemetry only (KD-2)
│   ├── FailureCause.cs                ← enum: MissedContact/MistimedDive/WrongDirection/OutOfReach/DisturbedInDuel
│   ├── ClaimType.cs                   ← enum: Cross/Aerial/OneOnOne/ShotCatch — telemetry only
│   ├── RushPhase.cs                   ← enum: Launched/InFlight/Reached/Aborted
│   ├── AbortReason.cs                 ← enum: BallIntercepted/BallCleared/AttackerBeatGK
│   ├── BodyPartEnum.cs                ← enum: Hand/Head/Body/Foot — collision routing (KD-14)
│   ├── HandEnum.cs                    ← enum: Left/Right/Either — KD-1 anatomy lookup carve-out
│   ├── DeliveryKind.cs                ← enum: Throw/Roll/Kick — KD-1 kinematic profile lookup carve-out
│   ├── SaveIntent.cs                  ← struct: TargetHand/ClutchFirmness/DeflectionTarget/AttemptCommittedTick
│   ├── ClaimIntent.cs                 ← struct: TargetContactPoint/ClutchFirmness/AttemptCommittedTick
│   ├── DistributeIntent.cs            ← struct: DeliveryKind/TargetReceiverId/TargetPoint/PowerIntent/SpinIntent
│   ├── RushIntent.cs                  ← struct: RushTarget/CommitmentLevel/AttemptCommittedTick
│   ├── GkContactState.cs             ← struct: per-attempt mutable state (PredictedContactFrame, HandlingQualityScalar, etc.)
│   ├── CrossClaimDuelContext.cs       ← struct: DuelId/ParticipantCount/WinnerAgentId/ContactBodyPart/BufferStartIndex
│   ├── GoalkeeperAgentAttributes.cs   ← struct: all GK attributes [1-20] + Fatigue [0,1] + normalised accessors
│   ├── GoalkeeperPositioningContract.cs ← struct: KD-13 consumer contract; gkBaselineSlot + reactive-radius bounds
│   ├── SaveAttemptedEvent.cs          ← struct event: save attempt (success or failure) with telemetry labels
│   ├── BallClaimedEvent.cs            ← struct event: caught save + releaseTickEarliest (6-second rule)
│   ├── DistributionExecutedEvent.cs   ← struct event: distribution passIntent emitted to Pass Mechanics #5
│   ├── GoalkeeperRushEvent.cs         ← struct event: rush launch/update/abort
│   ├── IGoalkeeperBallSystem.cs       ← interface: GetBallState + ApplyKick + SetPossessor
│   ├── IGoalkeeperRngService.cs       ← interface: NextFloat + NextGaussian (4 registered draw sites)
│   ├── GoalkeeperStateMachine.cs      ← pure state evaluator: EvaluateTacticalTransition + EvaluatePhysicsTransition
│   ├── GoalkeeperReactionPipeline.cs  ← §3.2 ComputeShotDetectedTickMs / ComputeRequiredReactionMs / ComputeReactionWindowAchieved; pure static
│   ├── GoalkeeperDiveKinematics.cs    ← §3.3 Stage 0 synthetic dive: launch impulse, timing jitter, parabolic Z, reach envelope; pure static
│   ├── GoalkeeperHandlingQuality.cs   ← §3.5 handling-quality scalar + parry/deflect/spill velocity helpers; pure static
│   ├── GoalkeeperCrossClaimDuel.cs    ← §3.6 body-part determination + duel arithmetic + tiebreak; ICollisionEventConsumer
│   ├── GoalkeeperRushDispatch.cs      ← §3.7 rush launch impulse + per-frame update; pure static
│   ├── GoalkeeperDistribution.cs      ← §3.8 release-point geometry, windup, accuracy, F-05/F-09 target validation; pure static
│   ├── GoalkeeperTelemetry.cs         ← Stage 0 stub; emits §2.4 gk.* trace-pipeline channels (12 channels)
│   ├── EventBusStub.cs                ← wired to EventBus.Publish; 3-tier generic IEventA/B/C overloads
│   ├── EventBusRegistrar.cs           ← boot-time RegisterExternalRow<T>() for SaveAttemptedEvent (0x14) + BallClaimedEvent (0x15) + DistributionExecutedEvent (0x16) + GoalkeeperRushEvent (0x17)
│   └── GoalkeeperMechanics.cs         ← 10 Hz + 60 Hz orchestrator; constructor-injected; sealed
├── positioning-ai/                    ← Spec #12
│   ├── positioning-ai.asmdef
│   ├── PositioningAIConstants.cs      ← single constant catalogue: pitch/spacing/hysteresis/GK/phase constants, 3 formation tables, pull-factor 13×4 table, lane edges (FR-PA-011/KD-17)
│   ├── Phase.cs                       ← enum: InPoss / OutOfPoss / TransToAtk / TransToDef
│   ├── LineId.cs                      ← enum: Defense / Midfield / Attack
│   ├── LaneId.cs                      ← enum: LW / LH / C / RH / RW (5 × 13.6 m bins)
│   ├── RoleId.cs                      ← enum: 13 roles (GK..ST); row index in 13×4 pull-factor table
│   ├── FormationFamily.cs             ← enum: F442 / F433 / F4231
│   ├── FormationSlotRecord.cs         ← readonly struct: LongPct / LateralPct / Role / DefaultLine / DefaultLane / IsGoalkeeper
│   ├── ContextModifierInputs.cs       ← readonly struct: ScoreDiff / TeamMeanFatigue / TacticalIntensity
│   ├── AgentPositioningData.cs        ← readonly struct: EntityId / SlotIndex / Position / IsActive / Role / IsGoalkeeper
│   ├── AgentHysteresisState.cs        ← struct: CurrentLine / CandidateLine / LineDwellCount / CurrentLane / CandidateLane / LaneDwellCount
│   ├── HysteresisState.cs             ← sealed class: team phase state + AgentHysteresisState[] Agents; SeedFromFormation()
│   ├── PositioningPerceptionSnapshot.cs ← sealed class: pre-allocated tick input (TickIndex / BallPosition / BallVxFiltered / Agents[])
│   ├── PhaseClassifier.cs             ← pure static: ClassifyAndCommit() with PHASE_HYSTERESIS_TICKS dwell; indeterminate → lastCommitted
│   ├── AnchorCalculator.cs            ← pure static: ComputeAnchor / ComputeBallRelativeOffset / ComputeGkSlot (own-half clamp)
│   ├── ContextModifier.cs             ← pure static: ApplyToAll() — lateral + vertical compactness scaling relative to centroid (§3.5)
│   ├── SpacingResolver.cs             ← pure static: EnforceHardSpacing() cost-based displacement up to SPACING_MAX_PASSES (§3.6)
│   ├── ShapeAnalyzer.cs               ← pure static: ResolveAllLines() insertion-sort + LINE_DWELL_TICKS; ResolveAllLanes() LANE_DWELL_TICKS; called AFTER spacing+clamp per AR-S1-03
│   ├── MarkingDwellState.cs           ← #23 §2.2.1 per-agent dwell state (T0; serialized at #23 wiring)
│   ├── MarkingPressureEvaluator.cs    ← #23 pure static: FM-DM-01 pressure + §3.2 dwell machine + FM-DM-02 offset (primitive-span inputs per the layering note)
│   ├── BuildUpZone.cs                 ← #24 enum: OwnThird / MiddleThird / FinalThird
│   ├── BuildUpZoneState.cs            ← #24 §2.2.2 per-team committed zone + suppression countdown (T0)
│   ├── BuildUpZoneClassifier.cs       ← #24 pure static: FM-BU-01 hysteresis classifier + FM-BU-03 suppression arithmetic
│   ├── BuildUpOverlayCatalogue.cs     ← #24 Appendix A [GT] overlay tables (row keys per ERR-024-001)
│   ├── RotationPair.cs                ← #25 §2.2.3 normalized adjacency pair (GK-refusing ctor)
│   ├── RotationPairState.cs           ← #25 §2.2.2 per-pair dwell/rotated/hold state (serialized at v12)
│   ├── RotationAdjacencyCatalogue.cs  ← #25 Appendix A [GT] adjacency tables (F442/F433/F4231)
│   ├── RotationController.cs          ← #25 §3.1–§3.4 controller: FM-RO-01/02 on the serialized LastComposedTarget cache; atomic SlotIndex swap + partner lock; F2/F5/F6 seams; sole post-seed SlotIndex writer (ERR-012-009)
│   ├── RestDefenseEvaluator.cs        ← §3.5/§7.13 rest-defense coverage check (cheap-item addition)
│   ├── SlotComposer.cs                ← pure static: Compose() pipeline (anchor→offset→modifiers→#24 build-up overlay→spacing→#23 dismark offset→clamp→lines→lanes; ERR-012-007/008 stage insertions)
│   ├── PositioningAITick.cs           ← sealed class: 10 Hz orchestrator; zero-alloc hot path; F1 stale detection; #25 RotationController before compose (§4.2); GetFormationSlot / GetLine / GetLane / GetPhase / CaptureRotationState
│   └── Tests/
│       ├── positioning-ai-tests.asmdef
│       ├── PositioningAITests.cs      ← T-U-001..021 (unit) + T-D-001..002 (determinism) + T-I-001..004 (integration) + T-P-001 (perf) + T-T-001 (tactical)
│       ├── RestDefenseEvaluatorTests.cs   ← §3.5/§7.13 rest-defense coverage locks
│       ├── TacticTranslationTests.cs      ← #21 T2 width-scalar seam locks
│       ├── MarkingPressureEvaluatorTests.cs ← #23 T0: FM-DM-01/02 worked examples + dwell machine + gates
│       ├── BuildUpStructureTests.cs       ← #24 T0: FM-BU-01/03 + catalogue bound/identities + ERR-024-001 regression
│       ├── RotationCatalogueTests.cs      ← #25 T0: F1 invariants + pinned Appendix A rows + FR-RO-007 bound
│       ├── SlotComposerStageTests.cs      ← #23/#24 stage-insertion locks (exact Δ/offset, identities, gates, spacing invariant, clamp bounds)
│       └── RotationControllerTests.cs     ← #25 controller locks (dwell/commit/revert/hold, partner lock, cap, phase freeze, Off identity, F2/F5/F6 gates)
├── pressing-ai/                       ← Spec #13
│   ├── pressing-ai.asmdef             ← Mechanics layer; references positioning-ai, pass-mechanics
│   ├── PressingAIConstants.cs         ← single constant catalogue: trigger distances/durations, cover-shadow geometry, stamina costs, pitch constants (GT/Fixed/Derived/Cross regions)
│   ├── TriggerFlags.cs                ← [Flags] enum: None / BadTouch / BackwardPass / SidelineTrap / WeakReceiver
│   ├── PressRole.cs                   ← enum: HoldShape / PrimaryPress / CoverShadow
│   ├── CoverShadow.cs                 ← struct: DefenderId, ReceiverId, TargetPosition
│   ├── PressDirective.cs              ← struct: per-tick output (PrimaryPresserId, PrimaryTargetPosition, Shadow0, Shadow1, CoverShadowCount, ActiveTriggers); static Inactive; IsActive property
│   ├── PressAssignment.cs             ← struct: per-agent output (EntityId, Role, TargetPosition)
│   ├── PressTrigger.cs                ← struct: 8 dwell/release counters (4 dwell + 4 release; zero allocation, no arrays)
│   ├── RoleHysteresisState.cs         ← sealed class: LastRole[], RoleDwell[] arrays; Reset()
│   ├── PressingAgentSnapshot.cs       ← struct: per-agent tick input (EntityId, TeamId, Position, BaselineSlot, Fatigue, FirstTouchAttribute, Line, IsGoalkeeper, HasBall, IsActive)
│   ├── PressingSnapshot.cs            ← sealed class: tick input container (TickIndex, BallPosition, BallVelocity, BallCarrierEntityId, AttackingDirection, PossessionTeamId, PressingTeamId, Agents[22])
│   ├── PassEventRing.cs               ← sealed class: ring buffer for BackwardPass trigger (Push, TryGetLatest, Clear)
│   ├── PositioningAIView.cs           ← readonly struct: facade over PositioningAITick (GetFormationSlot, GetLine, GetPhase, IsSentinelSlot)
│   ├── TriggerEvaluator.cs            ← pure static: Evaluate() debounce pipeline for 4 triggers + ComputeGeometricPressure helper
│   ├── PrimaryPressSelector.cs        ← pure static: Select() best presser by cost; ComputeInterceptionPoint(); GetCarrierPosition() helper
│   ├── CoverShadowSelector.cs         ← pure static: Select() up to 2 cover shadows; threat score + greedy defender assignment
│   ├── RoleHysteresis.cs              ← pure static: Commit() dwell guard; ForceAllHoldShape()
│   ├── StaminaAccumulator.cs          ← pure static: Apply() per-role fatigue cost; ApplyAll() batch apply
│   ├── DisengageResolver.cs           ← pure static: Evaluate() disengage conditions (zone exit + timeout); IsInCooldown()
│   ├── InvariantEnforcer.cs           ← pure static: Enforce() three anti-chaos invariants (MaxPressersBallThird, MinBacklineAgents, MaxPressDisplacementM)
│   └── PressingAITick.cs              ← sealed class: 10 Hz orchestrator; 8-step pipeline; pre-allocated buffers; zero-alloc hot path
├── defensive-ai/                      ← Spec #14
│   ├── defensive-ai.asmdef            ← Mechanics layer; references positioning-ai, pressing-ai
│   ├── DefensiveAIConstants.cs        ← single constant catalogue: assignment/hysteresis/offside-trap/tackle/anti-chaos/GK-zone constants (22 [GT] + 4 [CROSS])
│   ├── MarkMode.cs                    ← enum: Zonal / ManMark / InterceptRunner / CoverGkZone (FR-DA-011)
│   ├── TackleMode.cs                  ← enum: Hold / Jockey / Commit
│   ├── MarkDirective.cs               ← struct: per-tick team-level output (TeamId, OffensiveLineDepth, OffsideTrapActive, StepUpTargetDepth, EmergencyFlag)
│   ├── MarkAssignment.cs              ← struct: per-agent assignment (Mode, TargetEntityId, TargetPosition, ValidThroughTick, OverriddenThisTick); MakeZonal factory
│   ├── TackleIntentRequest.cs         ← struct: per-agent tackle intent (Mode, TargetEntityId, ApproachAngle, CoverageDepth)
│   ├── MarkHysteresisState.cs         ← struct: per-agent dwell-lock state (DwellCounter, CandidateMode, CandidateTargetEntityId, HoldTicks)
│   ├── OffsideLineState.cs            ← struct: per-team offside state (CurrentLineDepth, StepUpDwellCounter, CooldownTicksRemaining, CoverGkZoneActiveTicks)
│   ├── DefensiveAgentSnapshot.cs      ← struct: per-agent tick input (EntityId, TeamId, Position, Velocity, IsActive, IsGoalkeeper, HasBall, BaselineSlot, Line, PressRole, PerceivedFirstTouch)
│   ├── DefensiveSnapshot.cs           ← sealed class: tick input container (TickIndex, DefensiveTeamId, BallPosition, BallVelocity, TeamPhase, DefensiveLineDepth, GkEntityId, GkPosition, Agents[22])
│   ├── HoldShapePoolFilter.cs         ← pure static: BuildPool() excludes GK + PrimaryPress/CoverShadow; SnapshotIndexOf() helper
│   ├── LastManDetector.cs             ← pure static: Evaluate() last-man predicate (§3.8) + COVER_GK_ZONE trigger (§3.9); DefendsX0/DistToOwnGoal/DisplacementCost helpers; LastManResult struct
│   ├── MarkHysteresis.cs              ← pure static: PreCheck() dwell-lock gate; ApplyGate() transition accumulator; Reset() for emergency overrides
│   ├── MarkAssigner.cs                ← pure static: Assign() regular assignment loop (§3.3); ThreatScore() §3.5; SelectBestCandidate(); IsBetter() tie-break
│   ├── TackleIntentEvaluator.cs       ← pure static: Evaluate() tackle intent (§3.6); ComputeCoverageDepth(); SelectMode()
│   ├── OffsideTrapController.cs       ← pure static: Update() dwell counter + fire trigger (§3.7); ExecuteStepUp(); ComputeDefenseLineSpread()
│   ├── InvariantEnforcer.cs           ← pure static: Enforce() 3 anti-chaos invariants (§3.10); 3-pass demotion loop; F4 hard-fallback detection
│   └── DefensiveAITick.cs             ← sealed class: 10 Hz orchestrator; 9-step §3.13 pipeline; pre-allocated buffers; GetMarkDirective/GetAssignment/GetTackleIntentRequests
├── attacking-ai/                      ← Spec #15
│   ├── attacking-ai.asmdef            ← Mechanics layer; references positioning-ai, pressing-ai
│   ├── AttackingAIConstants.cs        ← single constant catalogue: pool/role/run-params/support/width/weak-side/overload/invariant/hysteresis/test constants (GT/Derived/Cross regions)
│   ├── AttackRole.cs                  ← enum: HoldWidth / SupportBall / Runner / WeakSide (FR-AT-012)
│   ├── Flank.cs                       ← enum: Left / Right — overload lateral discriminator (§3.8)
│   ├── RunParameters.cs               ← readonly struct: DepthOffsetM / LateralOffsetM / RunTriggerTick (FR-AT-011; exactly 3 fields)
│   ├── AttackHysteresisState.cs       ← struct: per-agent dwell state (CurrentRole, DwellCounter, CandidateRole, CandidateDwell)
│   ├── TransitionHoldState.cs         ← struct: per-team possession-loss countdown + PrevPhase
│   ├── AttackDirective.cs             ← readonly struct: team-level tick output (TeamId, OverloadActive, OverloadFlank, TransitionHoldTick)
│   ├── AttackIntent.cs                ← readonly struct: per-agent tick output (AgentEntityId, Role, RunParameters?, ValidThroughTick)
│   ├── StyleProfile.cs                ← readonly struct: 5 profile multipliers; static factories Possession/Direct/Counter
│   ├── AttackIntentSnapshot.cs        ← readonly struct: read-only zero-copy view over tick output (Directive, Intents[], IntentCount, TickIndex)
│   ├── AttackingAgentSnapshot.cs      ← readonly struct: per-agent tick input (EntityId, TeamId, Position, BaselineSlot, Line, IsGoalkeeper, HasBall, IsActive, Pace, Stamina, Dribbling)
│   ├── AttackingSnapshot.cs           ← sealed class: pre-allocated tick input container (TickIndex, AttackingTeamId, BallPosition, BallCarrierEntityId, BallCarrierPosition, TeamAttackAngle, Agents[22])
│   ├── AttackPoolEntry.cs             ← internal struct: per-agent scratch entry during pipeline (EntityId, Position, LateralPct, Line, AssignedRole, HasRunParams, run-param fields, RunTargetPosition, TargetPosition)
│   ├── AttackingPoolBuilder.cs        ← pure static: Build() filters snapshot → pool, EntityId-ascending insertion sort; returns −1 on F2 sentinel
│   ├── AttackHysteresis.cs            ← pure static: IsStable() / Update() / Reset() — increment-based dwell (CandidateDwell resets on current-role re-preference)
│   ├── SupportHeuristic.cs            ← pure static: IsWithinSupportRadius() / ComputeEffectiveRadius() — floored at MinEffectiveRadiusM
│   ├── RoleAssigner.cs                ← pure static: Assign() two-pass role assignment (pass 1 counts stable roles; pass 2 evaluates non-stable); GenerateRunParams() §3.4
│   ├── WidthHolder.cs                 ← pure static: Enforce() near-touchline width-holding; promotes non-RUNNER agents to HOLD_WIDTH
│   ├── WeakSideController.cs          ← pure static: EnsureWeakSide() post-check; selects max-deviation non-RUNNER for far-side coverage
│   ├── OverloadDetector.cs            ← pure static: Evaluate() counts non-WEAK_SIDE agents in Y-corridor; fires at ≥ OverloadCount
│   ├── TransitionController.cs        ← pure static: Evaluate() SET-then-DECREMENT transition hold; COUNTER profile (0 ticks) → instant empty
│   ├── InvariantEnforcer.cs           ← pure static: Apply() 3 anti-chaos invariants (max runners, min support, no own-half runs); fallback all→HoldWidth
│   └── AttackingAITick.cs             ← sealed class: 10 Hz orchestrator; §3.13 pipeline (phase gate→pool→roles→width→weak-side→overload→invariants→publish); pre-allocated zero-alloc buffers
│
├── deterministic-sim/                 ← Spec #16  (cross-cutting; referenced by all layers)
│   ├── deterministic-sim.asmdef       ← no references (all layers reference this; it references none)
│   ├── DeterministicSimConstants.cs   ← all [FIXED]/[DERIVED]/[GT] constants: tick rates, error codes, domain tags, field widths, RNG params, digest versions
│   ├── PhaseId.cs                     ← enum: Input=0 / Intent=1 / AI=2 / Physics=3 / Resolve=4 / Events=5 / Snapshot=6 (AR-1 H-4: AI_NoOp removed; Events added)
│   ├── DeterminismTier.cs             ← enum: TierA=0 / TierB=1 / TierC=2 (byte)
│   ├── DivergenceClass.cs             ← enum: None / HardDesync / SoftDrift / Cosmetic (byte)
│   ├── SubsystemOrdinals.cs           ← compile-time const ints: BallPhysics=0..GoalkeeperMechanics=7 (Physics 0–19), PositioningAI=20..AttackingAI=23 (Mechanics 20–39), PerceptionSystem=40, DecisionTree=41 (AI 40–59), EventSystem=60
│   ├── ReplayCursor.cs                ← readonly struct: Tick (ulong), PhaseOrdinal (byte), IsAtEndOfSnapshot property, EndOfSnapshot(tick) factory
│   ├── DespawnEntry.cs                ← readonly struct: EntityId, FinalActionOrdinal, FinalRngCursor, DespawnTick (all fields; Tier A tombstone)
│   ├── DespawnLog.cs                  ← pre-allocated tombstone list: Append / ContainsEntity / GetEntry / Clear; capacity = MaxDespawnEntries
│   ├── EnvironmentFingerprint.cs      ← sealed class: 6 readonly fields + Lock() + ValidateAgainst() → ERR_DS_REPLAY_ENV_MISMATCH + CreateStage0Dev() placeholder/IsDevPlaceholder + CreateStage0MonoCertified() real-hash factory (ERR-016-006)
│   ├── FloatFlagTuple.cs              ← readonly struct: §4.8.3 11-field float-flag tuple + ComputeHash() live-host floatModelHash hasher (ERR-016-006 Option A)
│   ├── RngStreamState.cs              ← mutable struct: StreamKey/RngCursor/ActionOrdinal (ulong), BudgetRemaining/DeclaredBudget/DrawIndex (int), SiteId (string), StreamVersion (ushort), SubsystemOrdinal, EntityId; ClearReservation()
│   ├── MatchClock.cs                  ← sealed class: CurrentTick / CurrentTacticalTick / CurrentMatchTimeMs / IsAiStrideTick; Advance() / RestoreFromSnapshot(tick) — no System.DateTime (FR-CS-042)
│   ├── DeterministicRngService.cs     ← sealed class: HKDF-SHA256 key derivation + SipHash-2-4-64 per-draw hash; RegisterStream / Reserve / DrawReserved / CloseReservation / Skip / RestoreStream; zero-allocation hot path (stackalloc Span<byte>)
│   ├── CanonicalSerializer.cs         ← static class: §3.2.4.1 Write/Read for all wire types; FloatUintUnion explicit-layout struct (AR-1 H-1/H-2: no BitConverter allocs); −0.0→+0.0 normalization; Tier B NaN→0x7FC00000
│   ├── SnapshotHeader.cs              ← sealed class: SchemaVersion / DigestVersion / Tick / PrevSnapshotDigest[32] / CurrentSnapshotDigest[32] / Fingerprint / Cursor; Initialize()
│   ├── SnapshotPayload.cs             ← sealed class: pre-allocated PayloadBytes[MaxSnapshotBytes] / BytesWritten / Reset()
│   ├── SnapshotCodec.cs               ← sealed class: Encode() SHA-256 + digest chain; ValidateHeader() / ValidatePrevDigest() / CommitLoadedDigest(); _prevDigest[32] chain state
│   ├── ReplayEngine.cs                ← sealed class: PrepareReplay() steps 1–7 of §4.2.2; step 6 Stage 0 stub (in-memory RNG state preserved); step 8 delegated to TickOrchestrator
│   ├── SaveManager.cs                 ← sealed class: CommitAtomic() §4.6.1.1 five-step atomic save (temp→fsync→rename-overwrite→dir-fsync); File.Move(overwrite:true) (AR-1 M-2)
│   ├── TickOrchestrator.cs            ← sealed class: RunTick() 7-phase 60 Hz pipeline; AI stride-gated on IsAiStrideTick; System.Action callbacks per phase; 9 ProfilerMarkers; zero-alloc hot path
│   ├── DivergenceDetector.cs          ← static class: CompareDigests / CompareTierAFloat / CompareTierBFloat (AR-1 M-3: one-NaN→SoftDrift) / CompareTierAInt / CompareTierAUlong / Worst()
│   ├── AssemblyInfo.cs                ← [InternalsVisibleTo("TacticalDirector.DeterministicSim.Tests")] (golden-vector pass — internal Hkdf*/SipHash24_64 calls need it)
│   └── tests/
│       ├── deterministic-sim-tests.asmdef  ← EditMode; references deterministic-sim.asmdef
│       ├── DeterministicSimTests.cs   ← HKDF RFC 5869 KAT; SipHash-2-4 ref vectors 0–7; canonical serialization (bool/u32/u64/-0.0/DT bits); T-DS-ORDER-001 MatchClock; T-DS-RNG-002 branch cursor parity; T-DS-SNAP-003 u64 round-trip; T-DS-FAULT-009..014; AI stride; DespawnLog
│       ├── HkdfSha256KatTests.cs      ← §9.5 #4(a) full KAT: RFC 5869 A.1–A.3 PRK + full OKM (L=42/82) + pinned project Test Case 4 (RNG_KDF → (k0,k1))
│       ├── SipHash24KatTests.cs       ← §9.5 #4(b) full KAT: all 64 Appendix A vectors + pinned project RNG_STREAM_HASH draw-preimage case
│       └── SerializeCanonicalCorpusTests.cs ← §9.5 #4(c) full corpus: all 41 serialize-canonical-corpus.md entries, bytes + SHA-256 each (incl. chained SnapshotDigest D-07)
│
├── event-system/                      ← Spec #17  (cross-cutting; referenced by all layers)
│   ├── event-system.asmdef            ← references TacticalDirector.DeterministicSim; autoReferenced true
│   ├── EventSystemConstants.cs        ← all [GT]/[CROSS] constants: queue/dispatch/handler/slot capacities + error codes + DomainTagEventLedger
│   ├── IEventA.cs                     ← marker interface: Tier A (authoritative, digest-included)
│   ├── IEventB.cs                     ← marker interface: Tier B (bounded-authoritative; Stage 5+ only)
│   ├── IEventC.cs                     ← marker interface: Tier C (cosmetic; immediate-dispatch; excluded from digest)
│   ├── EventHandler.cs                ← delegate: void EventHandler<T>(in T evt) where T : struct
│   ├── SubscriptionToken.cs           ← readonly struct: EventTypeOrdinal + SubscriberIndex; zero allocation (FR-EVT-073)
│   ├── EventRegistry.cs               ← Appendix A registry: 11 seeded rows; RegisterRow<T>/RegisterRowRaw/RegisterExternalRow; EventOrdinalCache<T> O(1) lookup
│   ├── EventLedger.cs                 ← ring buffer + typed dispatch; EventSlotMeta (FM-017-002 sort key); EventTypeDispatchBase/EventTypeDispatcher<T>; DrainTick BFS; InsertionSort; SerializeLedger; Subscribe
│   ├── CosmeticChannel.cs             ← Tier C immediate-dispatch; per-ordinal pub-count table; >= maxPerTick drop predicate (FR-EVT-043); stackalloc span dispatch (zero-alloc FR-EVT-048)
│   ├── EventBus.cs                    ← public static API: BeginTick/BeginPhase/DrainTick/SerializeLedger/OnTickBoundary; Publish/Subscribe overloads per tier; debug phase assertion #if UNITY_EDITOR||DEVELOPMENT_BUILD
│   ├── PossessionChangedEvent.cs      ← Tier A 0x04: PreviousHolder/NewHolder/Reason
│   ├── FoulCommittedEvent.cs          ← Tier A 0x05: Offender/Victim/Location(Vector3)/FoulKind
│   ├── CardIssuedEvent.cs             ← Tier A 0x06: Recipient/CardKind/FoulOrdinal(ushort; 0xFFFF=procedural)
│   ├── GoalAwardedEvent.cs            ← Tier A 0x07: Scorer/Assister/ScoringTeam/BallPosition(Vector3)
│   ├── SubstitutionEvent.cs           ← Tier A 0x08: Outgoing/Incoming/Team/SubstitutionReason
│   ├── TickHeartbeatEvent.cs          ← Tier C 0x09: empty payload; MaxPerTick=1; CLR min size 1 byte
│   ├── VfxImpactCue.cs                ← Tier C 0x0A: ImpactPoint(Vector3)/ImpactKind/Intensity; MaxPerTick=64
│   ├── UiNotificationCue.cs           ← Tier C 0x0B: NotificationKind/SubjectEntity; MaxPerTick=32
│   └── tests/
│       ├── event-system-tests.asmdef  ← EditMode; autoReferenced false; references TacticalDirector.EventSystem + DeterministicSim + 6 production spec assemblies (Pass / Shot / Perception / Decision / Heading / Goalkeeper) for the boot-wiring smoke test
│       └── EventBusWiringSmokeTests.cs ← SMOKE-EVT-WIRING-001 boot→publish→drain→digest smoke test across 6 currently-wired EventBusRegistrars; AM #2 carved out as [CROSS-PENDING]; golden digest pinned via Assert.Inconclusive until AM lands
│
├── performance-optimization/          ← Spec #18  (owns trace pipeline; minimal game-loop code)
│   ├── performance-optimization.asmdef  ← autoReferenced false; references TacticalDirector.DeterministicSim; game-layer assemblies MUST NOT import
│   ├── HotPathAllocExemptAttribute.cs   ← §3.7.5 governance attribute; Justification (required) + SignOffRef (optional) properties
│   ├── ChannelVerbosity.cs              ← F.0 schema enum: Minimal / Standard / Debug / Exhaustive (FR-PO-055)
│   ├── ChannelSamplingRule.cs           ← F.0 schema enum: EveryTick / PerNTicks / EventDriven (FR-PO-056)
│   ├── ChannelDeterminismClass.cs       ← F.0 schema enum: TierA / TierB / TierC (FR-PO-058a)
│   ├── TraceChannelDescriptor.cs        ← F.0 11-field sealed descriptor; constructor enforces SamplingRule/SampleN invariant
│   ├── TraceChannelRegistry.cs          ← Stage 0 anchor rows: PerfBudget / PerfAlloc / PerfTrace
│   ├── PerformanceOptimizationConstants.cs  ← Fixed: HOT_PATH_ALLOC_BUDGET_BYTES=0 / LOOP_TAG_TACTICAL_10HZ / LOOP_TAG_PHYSICS_60HZ; GT: PerPrRegressionFraction / AbsoluteDriftFraction / BaselineSampleCount / MaxFlakeRate / HeadroomMultiplierMin/Max / PromotionToleranceFraction / ReproducibilityToleranceFraction; EST: SamplerDefaultHz / StatisticalSignificanceN / FirstTickWarmupCount
│   ├── LoopTag.cs                        ← enum: TacticalTenHz / PhysicsSixtyHz (KD-8; Appendix A on-disk loop-tag keys)
│   ├── BaselinePassFail.cs               ← enum: Pass / Fail / Advisory (capture-time verdict per Appendix A)
│   ├── HardwareCounterSnapshot.cs        ← readonly struct: CpuModel / CoreCount / ThermalState (§3.3.2 session manifest field)
│   ├── SessionManifest.cs                ← sealed class: all §3.3.2 required fields (GitSha / Seed / EnvironmentFingerprint #16 §4.8 / PlatformPin / ScenarioManifestId / timestamps / HardwareCounters / HarnessVersion); IsComplete() validator
│   ├── BaselineRecord.cs                 ← sealed class: SessionManifest + Loop + P50Ms + P99Ms + PerMethodAllocBytes + PassFail + ThresholdCited (Appendix A / §4.3.2)
│   ├── BudgetRollupEntry.cs              ← readonly struct: SpecId / SubroutineName / Loop / BudgetMs / AllocBudgetBytes / Citation (§4.3.3 / §3.1.3 roll-up columns)
│   ├── HotPathEntry.cs                   ← readonly struct: SpecId / MethodName / Loop / BudgetMs / HasAllocExemption (§3.7.2 union entry)
│   ├── IPerfHarness.cs                   ← interface: BeginSession / RecordTickSample / FinalizeSession (§4.3.1 / §4.4; both sides specified per CLAUDE.md "Interface Design Principle")
│   ├── IBudgetSource.cs                  ← interface: SpecId property + GetEntries() → IReadOnlyList<BudgetRollupEntry> (§4.4; both sides specified)
│   ├── RegressionResult.cs               ← readonly struct: PerPrPassed / AbsoluteDriftPassed / DeltaFraction / MilestoneDriftFraction / AllPassed (FR-PO-031 evaluation output)
│   ├── RegressionGate.cs                 ← static class: PassesPerPrCheck / PassesAbsoluteDriftCheck / Evaluate (FR-PO-031 / §3.5.2 / §3.5.6)
│   ├── ReproducibilityResult.cs          ← readonly struct: IsReproducible / OriginalP50Ms / RecapturedP50Ms / AbsDeltaFraction / ScenarioMatched / SeedMatched (FR-PO-067 audit output)
│   └── BaselineReproducibilityAuditor.cs ← sealed class: Validate(original, recaptured) → ReproducibilityResult (§3.4.4 / §5.4 / FR-PO-067)
├── testing-strategy/                  ← Spec #19  (CI orchestration tooling; no game-loop code)
│   ├── testing-strategy.asmdef        ← autoReferenced false; references TacticalDirector.DeterministicSim + TacticalDirector.PerformanceOptimization; game-layer assemblies MUST NOT import
│   ├── TestingStrategyConstants.cs    ← §3.10 governance constants: pyramid bounds / coverage thresholds / quarantine + eviction windows / pre-commit budget / [FIXED] MATCH_LENGTH_MINUTES + SCENARIO_MANIFEST_FORMAT_VERSION
│   ├── TestTier.cs                    ← enum: TierA / TierB / TierC (mirror of #16 §1.1.1; consumed not owned per KD-1)
│   ├── TestLayer.cs                   ← enum: Unit / Integration / Simulation / Determinism / EndToEndSoak (§3.1.1 five-layer taxonomy, FR-TS-001)
│   ├── GoldenVectorKind.cs            ← enum: HkdfSha256Kat / SipHash24Kat / CanonicalSerializeCorpus (#16 §9.5 #4 a/b/c)
│   ├── GoldenVectorEntry.cs           ← readonly struct: Kind / Name / SourcePath / Citation — one row in the corpus catalogue
│   ├── GoldenVectorResult.cs          ← readonly struct: Entry / Passed / VectorsExecuted / VectorsFailed / Diagnostic
│   ├── GoldenVectorRunner.cs          ← static class: Catalogue() + Run(in entry) + RunAll(); Stage 0 deferred-status (D1 test-runner pin); Stage 0+1 parses corpus + invokes DeterministicRngService / CanonicalSerializer
│   ├── DeterminismTierKind.cs         ← enum: Unit / Integration / Scenario / Soak (#16 §5 canonical tier order; FR-TS-011 / FR-TS-018)
│   ├── DeterminismTierResult.cs       ← readonly struct: Tier / Passed / TestsExecuted / TestsFailed / Diagnostic
│   ├── DeterminismSuiteResult.cs      ← sealed class: TierResults[] + GoldenVectorResults[] + AllPassed (FR-DS-009-GATE union)
│   ├── DeterminismGate.cs             ← static class: RunTiers() — single integration point per FR-TS-016; aggregates §5 tiers + golden-vector corpus
│   ├── PerfGateReport.cs              ← sealed class: SpecId / LoopTag / ScenarioManifestId / Regression — CI-friendly wrapper over Spec #18 RegressionResult
│   ├── PerfGateRunner.cs              ← static class: Run(specId, loopTag, baseline, current, milestoneMs) → PerfGateReport; delegates verdict to #18 RegressionGate.Evaluate (KD-3 boundary)
│   ├── ScenarioStatus.cs              ← enum: Passed / Failed / Quarantined (§3.3.3; Quarantined is Stage 0+1 flake-layer only)
│   ├── ScenarioResult.cs              ← sealed class: Status / Diagnostics (key=value lines) / DurationMs / #16 §4.8 Fingerprint (§3.3.3 contract value)
│   ├── ScenarioManifest.cs            ← sealed class: Appendix A.1 entry (name / owning_spec_ids / seed / tier / fixture_refs / format_version) + load-time field validation; Stage 0 in-code authoring (D1 pins on-disk encoding at Stage 0+1)
│   ├── ScenarioEnvelope.cs            ← sealed class: executable expected_outcome_envelope — CheckTrue / CheckEquals / CheckInRange; zero predicates ⇒ Failed (FR-TS-030); NaN fails in_range
│   ├── ScenarioContext.cs             ← sealed class + ScenarioBody delegate: manifest / verbatim run seed / KD-7-seeded DeterministicRngService / envelope
│   ├── IScenario.cs                   ← interface (§4.4.1): single method ScenarioResult Run(ulong seed)
│   ├── ClosedLoopScenario.cs          ← sealed IScenario: fresh RNG + context per run (hermetic, FR-TS-023); body drives a real subsystem loop; exceptions → Failed with diagnostic
│   ├── ScenarioIndex.cs               ← sealed class: immutable in-memory root manifest; duplicate paths AND names rejected (A.1); runner refuses unindexed scenarios (§3.3.6 / FR-TS-028)
│   ├── ScenarioIndexEntry.cs          ← one index row (path + manifest + scenario); AR-1 M-1 guard — a ClosedLoopScenario must be registered under the manifest instance it executes
│   ├── ScenarioRunner.cs              ← sealed class: §3.3.3 single entry point Run(manifestPath, seed) — FR-TS-070 format version first, then A.1 fields / §3.3.5 path↔name / cross-spec arity / fixture_refs refusal (§3.3.4) → IScenario.Run
│   └── Tests/
│       ├── testing-strategy-tests.asmdef ← EditMode; references testing-strategy + deterministic-sim + ball-physics + pass-mechanics + event-system (cross-spec corpus)
│       ├── ScenarioRunnerTests.cs     ← runner contract tests: index refusal / format-version + kebab-case rejection / implicit-pass (FR-TS-030) / NaN / exception capture / KD-7 seed plumbing / hermeticity
│       ├── CrossSpecScenarios.cs      ← cross-spec scenario corpus (KD-8; tests/scenarios/cross-spec/ paths, ≥2 owning specs):
│       │                              │   lofted-pass-kick-bounce-roll — PassExecutor (#5) → IPassBallSystem seam → BallPhysicsCore (#1)
│       │                              │   with #17 boot wiring + tick lifecycle around the CONTACT publish; owning specs {1, 5}
│       └── CrossSpecScenarioTests.cs  ← sim_<scenario> Simulation-layer tests running the cross-spec corpus through ScenarioRunner
├── match-viewer/                      ← Presentation tooling (not a numbered spec; observes the match-engine composition root)
│   ├── match-viewer.asmdef            ← references match-engine + deterministic-sim + ball-physics + agent-movement
│   ├── MatchViewerConstants.cs        ← IFAB pitch-marking geometry [FIXED] + canvas/recording presentation [GT]
│   ├── ReplayFrame.cs                 ← one sampled frame: tick / ball / possession / agent positions (value copies)
│   ├── MatchReplay.cs                 ← immutable frame sequence + roster/pitch/cadence metadata
│   ├── MatchReplayRecorder.cs         ← ticks a MatchEngine, sampling its public observation surface (observer-neutral)
│   ├── HtmlReplayExporter.cs          ← self-contained HTML canvas replay: play/pause/scrub/speed; fail-loud non-finite gate
│   ├── AssemblyInfo.cs                ← [InternalsVisibleTo("TacticalDirector.MatchViewer.Tests")] (LiveMatchStreamer.TickOnce seam)
│   ├── LiveMatchFrame.cs              ← interactive match view: one live-captured frame (tick/ball/possession/positions/score/matchEnded)
│   ├── LiveMatchStreamer.cs           ← interactive match view: real-time-paced MatchEngine tick loop; lock-protected latest-frame surface; pause/resume/speed; full-time auto-pause
│   ├── LiveMatchServer.cs             ← interactive match view: loopback-only hand-rolled HTTP server (TcpListener) — GET / (viewer page) / /frame (polled JSON) / /control (playback-only)
│   └── tests/
│       ├── match-viewer-tests.asmdef
│       ├── MatchViewerTests.cs        ← cadence / on-pitch / bitwise determinism / observer-neutral digest / exporter locks
│       ├── LiveMatchStreamerTests.cs  ← latest-frame handoff / observer-neutrality digest / auto-pause / speed guards / lifecycle
│       └── LiveMatchServerTests.cs    ← real-loopback-socket routing / control / error-path / abuse-guard / shutdown locks
├── living-world/                      ← Spec #22  (off-pitch layer; engine-free, noEngineReferences; references DeterministicSim)
│   ├── living-world.asmdef            ← references TacticalDirector.DeterministicSim (slice 3: world.text stream + §4.6 serializer)
│   ├── LivingWorldConstants.cs        ← Appendix A catalogue; Fixed (world.text stream ids + WORLD_SNAPSHOT_FORMAT_VERSION) / Cross (DomainTagLivingWorld) / GT
│   ├── LivingWorldMath.cs             ← §3.1 ApplyEvent/ApplyDecay + FR-LW-021 CompareEvictability
│   ├── RelationshipLayer.cs / EventKind.cs / ArcKind.cs / InteractionIntent.cs   ← byte enums, APPEND-only
│   ├── RelationshipEdge.cs / MemoryEpisode.cs / SpawnCause.cs / Arc.cs / ColdSummary.cs  ← value types
│   ├── InteractionSlots.cs            ← §3.3 slot-fact carrier: match-engine facts + optional cited episode (FR-LW-013)
│   ├── WorldClock.cs                  ← season-calendar clock (KD-4): worldTick = calendar day; never the match loops
│   ├── WorldLoop.cs                   ← §4.2 per-tick orchestrator (phases 3/4/6 live; 1/2/5 documented seams); phase-4 arc-trigger evaluate (5th nullable seam)
│   ├── MemoryStore.cs                 ← deep-tier store: canonical-order edges, §3.2 episode evict/decay, FR-LW-018 pins
│   ├── ColdStore.cs                   ← cold tier + §3.5 Compress/Rehydrate (Residue-A v1 schema recorded)
│   ├── ArcEngine.cs                   ← §3.4 arc spawn (atomic pinning)/state/resolve + §6.2 expiry
│   ├── ArcCanonSource.cs              ← arc-triggers KD-1/KD-2: concrete nullable canon-input seam + nested Builder (Stage-0 producer)
│   ├── ArcTrigger.cs / ArcTriggerCatalogue.cs ← arc-triggers KD-3: catalogue row + the APPEND-only Stage-0 trigger table
│   ├── ArcTriggerEvaluator.cs         ← arc-triggers KD-3..KD-7: world.arcs stream (distinct key) + FR-LW-017/021 walk + rising-edge latch + SpawnArc
│   ├── ActiveSetMembership.cs         ← §3.5 entry / LRU demotion at the cap / own-club Depart (FR-LW-023/025)
│   ├── InteractionTextCorpus.cs       ← §3.3 Stage-0 in-code authored templates (per-intent) + episode clauses (per EventKind); APPEND-only order
│   ├── InteractionTextGenerator.cs    ← §3.3 deterministic text: world.text sub-stream draw + slot expansion + §3.2 citation gate (FR-LW-011/012/013/020)
│   ├── WorldStateSerializer.cs        ← §4.6 canonical block (Appendix B order) via #16 CanonicalSerializer; rebuild-through-seams deserialize
│   ├── WorldStore.cs                  ← KD-10 season composition root: owns/wires the 6 services + DeterministicRngService + InteractionTextGenerator (world.text) + ArcTriggerEvaluator (world.arcs) + nullable ArcCanonSource; AdvanceDay drives the loop; Snapshot/Restore = §4.6 block + managerId + world.text + world.arcs (cursor+latch, E2 v3) + FR-LW-022 roster
│   └── Tests/
│       ├── living-world-tests.asmdef  ← + references TacticalDirector.DeterministicSim (RngStreamState/CanonicalSerializer)
│       ├── LivingWorldTests.cs        ← T0: enum ordinals, §3.1 worked examples, eviction tiebreak
│       ├── SeasonWorldLoopTests.cs    ← slice 1: clock/loop/memory/cold-store contracts + determinism
│       ├── ArcMembershipTests.cs      ← slice 2: arc lifecycle/pin-rollback/expiry + membership LRU/depart/promotion
│       ├── WorldTextSnapshotTests.cs  ← slice 3: text determinism (T-LW-DET-003/004) + §3.2 citation gate + §4.6 round-trip/fail-loud
│       ├── WorldStoreTests.cs         ← KD-10: construction/wiring, AdvanceDay phases (decay/arc-expiry), Snapshot/Restore round-trip + determinism + fail-loud gates
│       └── ArcTriggerTests.cs         ← arc-triggers E1+E2: distinct stream key, flag-off round-trip, stub-canon spawn + KD-7 single-fire/re-arm, FR-LW-017/021 order, E2 save@N→restore→advance + latch serialization + re-fire-after-restore lock
├── season-save/                       ← Unified season save-file root (not a numbered spec; the ONLY assembly above BOTH match-engine + living-world — resolves FR-LW-003; unified-season-save-design.md)
│   ├── season-save.asmdef             ← references match-engine + living-world + deterministic-sim
│   ├── SeasonSaveConstants.cs         ← [FIXED] SEASON_SAVE_FORMAT_VERSION = 1 (a fourth format version; KD-4)
│   ├── SeasonSaveBlobs.cs             ← deframe result: WorldBlob (always) + MatchBlob (null if no match) — two opaque sub-blobs (KD-2/KD-3)
│   ├── SeasonSaveCodec.cs             ← pure static: Encode(worldBlob, matchBlobOrNull) / Decode → version-gated frame + matchPresent flag + 2 length-prefixed opaque blocks; overflow-safe bounds + fail-loud (KD-7/KD-8)
│   ├── SeasonSaveContents.cs          ← Load result: reconstructed WorldStore (never null) + nullable MatchEngine
│   ├── SeasonSaveManager.cs           ← static: Save(world, matchOrNull, path) (capture both → Encode → atomic temp→fsync→rename) / Load(path, ISquadProvider = null) → SeasonSaveContents (KD-1/KD-5/KD-6/KD-8)
│   └── tests/
│       ├── season-save-tests.asmdef   ← EditMode; references season-save + match-engine + living-world + deterministic-sim + player-database
│       └── SeasonSaveManagerTests.cs  ← disk round-trip determinism (no-match / neutral+distinct-squad match via ISquadProvider) + SeasonSaveCodec round-trip/fail-loud + manager fail-loud paths
├── code-standards/                    ← Spec #20  (governance only; no runtime code)
├── player-database/                    ← Squad/Player Data Layer (T0+T1/T2; candidate spec #27, design-supplement stage)
│   │                                    │   References only DeterministicSim (roster-generation RNG stream).
│   │                                    │   Wired into MatchEngine since T1 (July 17, 2026) via
│   │                                    │   match-engine/PlayerAttributeProjection.cs + ConfigureSquads
│   │                                    │   (player-attribute-projection-design.md; T3 restore fidelity open).
│   ├── player-database.asmdef
│   ├── PlayerDatabaseConstants.cs       ← Fixed/Derived/GT catalogue: attribute bounds, generation tuning, [4][31] position-bias table
│   ├── AttrIdx.cs                       ← ordinal mapping for the 31 int[1,20] attribute fields, shared by ToArray/FromArray/RosterGenerator/SquadFileLoader
│   ├── PlayerAttributes.cs              ← canonical 31-field [1,20] record + WeakFootRating [1,5]; reconciles all 7 existing per-spec attribute structs
│   ├── PlayerPosition.cs                ← enum: Goalkeeper/Defender/Midfielder/Forward (coarse; NOT positioning-ai's 13-value RoleId)
│   ├── PlayerRecord.cs                  ← one player: club-scoped PlayerId + name/age/position + PlayerAttributes
│   ├── Squad.cs                         ← one club's roster (≤ CLUB_SQUAD_SIZE=25 players)
│   ├── NameCatalogue.cs                 ← Stage-0 in-code first/last name pools (32 each)
│   ├── RosterGenerator.cs               ← deterministic generation over DeterministicRngService; stateless, caller registers the stream
│   ├── PlayerGenerationRng.cs           ← shared DrawBounded (biased-but-accepted generation mapping) + Clamp, used by RosterGenerator (#27) + RegenGenerator (#28)
│   ├── SquadFileLoader.cs               ← Stage-0 human-authoring text import (mirrors TeamTacticFileLoader's grammar)
│   └── tests/
│       ├── player-database-tests.asmdef
│       ├── PlayerAttributesTests.cs     ← clamp/array round-trip, identity defaults, position-bias table exact-value locks
│       ├── RosterGeneratorTests.cs      ← determinism, club-scoped PlayerId uniqueness, bounds, exact RNG-budget-per-player lock
│       └── SquadFileLoaderTests.cs      ← grammar round-trip, empty-file default squad, every fail-loud gate
│
├── player-progression/                ← Player Progression & Lifecycle #28 (T0; APPROVED spec)
│   │                                    │   References PlayerDatabase + DeterministicSim only (§4.1). World-tick,
│   │                                    │   draw-free aging core + the pure single-player regen generator.
│   │                                    │   T1 (save codec) / T2 (ProgressionEngine + regen stream) deferred.
│   ├── player-progression.asmdef
│   ├── PlayerProgressionConstants.cs    ← Appendix A catalogue (Fixed/Derived/Cross/GT); the 0x20/82 RNG mirrors land at T2 (KD-B)
│   ├── PlayerLifecycle.cs               ← per-player overlay value type (§2.2): PA / CA cache / long GrowthCursor / BirthWorldDay / retirement
│   ├── TrainingInput.cs                 ← the #29 seam value type (Neutral identity, §4.5 — no phantom interface)
│   ├── AbilityModel.cs                  ← pure: ComputeCA + ClassifyAgeBand + weighted spend/drain + the AgeBand enum (§3.1.2/§3.2)
│   ├── GrowthProjection.cs              ← pure per-player daily step (§3.1; sole attribute-mutation path; curve-off KD-8 identity)
│   ├── RegenGenerator.cs                ← pure single-player generation (§3.3; fixed PROGRESSION_REGEN_FIELDS budget; fresh id)
│   └── tests/
│       ├── player-progression-tests.asmdef
│       ├── PlayerProgressionConstantsTests.cs ← balance-pass invariant locks (POINT_COST==DAYS_PER_YEAR, band order, regen-field derivation)
│       ├── AbilityModelTests.cs         ← T-PG-CA-001/002/003: CA determinism, F1 PA ceiling, weighted spend/drain order
│       ├── GrowthProjectionTests.cs     ← T-PG-DET-001/002 + ID-001/002: byte-exact growth/decline, value-copy save, age gap-independence
│       └── RegenGeneratorTests.cs       ← T-PG-REG-001/003: regen determinism, exact budget, bounds, CA≤PA room-to-grow (test-local ordinal, KD-B)
│
└── tactical-instructions/             ← Spec #21  (T0 — bottom-of-graph data assembly; behaviour-neutral)
    │                                  │   References only project-constants (FR-TI-002); empty asmdef refs until that assembly exists.
    │                                  │   Seams into #8/#11–#15 land at T2–T3 (gated on match-engine Phase C/D + [GT] loader).
    ├── tactical-instructions.asmdef
    ├── Mentality.cs / Tempo.cs / TacticWidth.cs / TacticDefWidth.cs / LineOfEngagement.cs
    ├── TransitionPlan.cs / GkDistributionPolicy.cs / FocusPlay.cs / TacticPassing.cs / TacticPressing.cs
    ├── TacticTriggerMask.cs ([Flags]) / TacticFormation.cs / Duty.cs / PlayerRole.cs / InstrBias.cs / SetPieceDutyFlags.cs ([Flags])
    ├── MarkingOrientation.cs / DismarkIntensity.cs / BuildUpStructure.cs / RotationFreedom.cs  ← appended dials (cheap-item + ERR-021-005/006/007 back-props; zero/seeded identities)
    ├── TeamTactic.cs / PlayerInstructions.cs / PlayerTactic.cs   ← readonly-struct carriers + identity factories (Balanced / Default)
    ├── TacticPreset.cs / TacticPresetLibrary.cs                  ← #26 T0: named #21-space points + the pinned A.1 ladder catalogue
    ├── TacticalPresetsConstants.cs                               ← #26 §3.5 + A.2/A.3 catalogue (manager-decision cadence/adaptation scalars + archetype/affinity [GT] tables)
    ├── TacticalInstructionsConstants.cs                          ← Appendix A catalogue (Fixed/Derived/GT); identity rows exact
    └── Tests/
        ├── tactical-instructions-tests.asmdef
        ├── EnumOrdinalStabilityTests.cs   ← FR-TI-007 ordinal / bit-position / byte-backing locks (19 enums)
        ├── FactoryIdentityTests.cs        ← FR-TI-031 identity-factory + catalogue identity-row locks
        └── TacticPresetLibraryTests.cs    ← #26 T0: pinned ladder order + A.1 compositions + KD-7 identity discipline
```

**One folder per spec. One `.asmdef` per folder. Folder names match `docs/specs/` exactly.**

> **Note on `.asmdef` coverage:** Every spec folder listed above requires a
> `.asmdef` file (e.g., `pressing-ai/pressing-ai.asmdef`). Only a subset is shown
> in the tree for brevity. See each spec's `§4` (Architecture) file for the exact
> `.asmdef` reference list. GUIDs are blocked on Unity project initialization (see
> "WHAT IS NOT HERE YET").
>
> **Test assemblies:** Every `tests/` subfolder requires its own `.asmdef` with
> `testPlatforms: [EditMode]` (or as specified per Spec #19 §7.5 D2) and a reference
> to the parent spec's `.asmdef`. Test assemblies are excluded from production builds
> via platform filtering. Only the expanded spec folders in the tree above show the
> `.asmdef` entry; all `tests/` subfolders follow the same pattern.

### Assembly Layer Taxonomy

The authoritative layer taxonomy is Spec #20 §3.5.2. The three layers and their
members are reproduced here verbatim — do not infer layer membership from folder
order or spec number.

| Layer | Assemblies |
|---|---|
| **Physics** | ball-physics, agent-movement, collision-system, first-touch, pass-mechanics, shot-mechanics, heading-mechanics, goalkeeper-mechanics |
| **Mechanics** | positioning-ai, pressing-ai, defensive-ai, attacking-ai |
| **AI** | decision-tree, perception-system |
| **UI** | (Stage 1+ — not yet specified) |

The `deterministic-sim` and `event-system` assemblies are cross-cutting foundations
referenced by all layers (not members of any single layer).

The following assemblies are **infrastructure-only** and are NOT members of any
gameplay layer. Game-layer code (Physics / Mechanics / AI) MUST NOT import them
at runtime:

| Assembly | Role |
|---|---|
| `project-constants` | Constants shared across ≥ 2 spec assemblies; read-only by all |
| `performance-optimization` | Trace pipeline only (Spec #18 KD-3); no game-loop types |
| `testing-strategy` | CI orchestration tooling only (Spec #19); no game-loop types |
| `code-standards` | Governance only (Spec #20); no runtime types |

### Reference Direction

**AI depends on Mechanics. Mechanics depends on Physics. Never the reverse.**

```
project-constants  (read-only by all assemblies)

Physics  ←  Mechanics  ←  AI  ←  UI
```

`←` means "is referenced by" — `A ← B` means B depends on A (B imports from A).
The AI assembly imports types from Mechanics, which imports types from Physics.
A Physics assembly MUST NOT import from Mechanics or AI; a Mechanics assembly MUST NOT
import from AI. These prohibited import directions are enforced as build errors via
`.asmdef` reference declarations (FR-CS-046).

For upward event notification (e.g., a physics event consumed by AI), use a struct
event on the event bus — no direct assembly reference (FR-CS-047).

For the specific `.asmdef` references each assembly declares, read that spec's `§4`
(Architecture) file. Do not infer the intra-layer dependency chain from this document.

---

## BUILD AND TEST COMMANDS

> **Note:** The certification platform is now pinned in `docs/tracking/certification-platform.md` v1.3 (Windows 11 / Unity 6000.4.9f1 / DX11 / Mono / x64 / SSE4.2 / 1 worker / deterministic compiler flags) but is **⏳ RECERT REQUIRED** — no certification run has executed against the Unity-6 tuple yet (`cert-run-runbook.md` P1/P2). The batch-mode command below is defined but requires a Unity host to execute; the Linux path (`dotnet-ci` gate) is the day-to-day compile/test gate.

**Format check (pre-commit gate):**
```bash
dotnet format --verify-no-changes
```

**Build with warnings-as-errors:**
```bash
dotnet build /p:TreatWarningsAsErrors=true
```

**Run tests:**
```bash
dotnet test
```

**Linux compile/test gate (non-certifying; runs in CI on every push):**
```bash
bash tools/dotnet-ci/run-gate.sh
```
Generates plain .NET projects from the asmdefs (production `netstandard2.1` —
Unity 2022.3's BCL surface; tests `net8.0`), compiles the whole tree against the
`tools/dotnet-ci/UnityShim` UnityEngine shim, and runs every NUnit suite minus
the quarantine in `tools/dotnet-ci/known-failures.txt` (tracked in
`docs/tracking/dotnet-ci-quarantine.md`). Any compile error or non-quarantined
test failure fails the gate. NOT a determinism certification — see
`docs/tracking/certification-platform.md`.

**Unity batch-mode test run (pinned host — FR-PO-052 certified perf capture):**
```bash
# Run on the pinned certification host (Windows 11 / Unity 6000.4.9f1 / DX11 / Mono),
# after creating the Assets/Scripts junction into src/ (see Assets/README.md).
# TD_PERF_RUN_COUNT=100 (= BaselineSampleCount) drives the certified capture; unset ⇒ CI-fast 2.
Unity -batchmode -runTests -projectPath . \
      -testPlatform EditMode \
      -testFilter "TacticalDirector.MatchEngine.MatchEngineCapstonePerfHarnessTests" \
      -testResults ./perf-results.xml -logFile -
```
The test (`MatchEngineCapstonePerfHarnessTests`) drives the real `MatchEngine`
capstone through `MatchEngineCapstonePerfHarness.CaptureBaseline` and logs the
per-tick `p50`/`p99` via `TestContext` for the operator to transcribe into the
`.cert.md` corpus entry (`cert-run-runbook.md` Step 3). The same test runs on the
Linux `dotnet-ci` gate (non-certifying) as the harness's compile+execute proof.
This command requires a Unity host — it is **not** runnable from the Linux gate.

**Stage 0 verification:** Manual code review against Spec #20 §5.4 checklist (7 categories, 73 FRs). Static analysis tooling (Roslyn analyzers, BannedSymbols.txt, `.editorconfig`) activates at Stage 1.

---

## FILE NAMING

- One public type per file. Filename must match the type name exactly (case-sensitive).
  - `BallState.cs` contains `public struct BallState`
  - `BallPhysicsConstants.cs` contains `public static class BallPhysicsConstants`
- Tests live in a sibling `tests/` folder under the same spec folder.
- No version suffixes in filenames. Git tracks history.

---

## NAMING CONVENTIONS

| Identifier | Convention | Example |
|---|---|---|
| Types, methods, properties, events | PascalCase | `BallState`, `ApplyKick` |
| Local variables, parameters | camelCase | `deltaTime`, `agentId` |
| Private instance fields | `_camelCase` | `_clock`, `_agentCount` |
| Private static fields | `s_camelCase` | `s_updateMarker`, `s_runTickMarker` |
| `[FIXED]` constants | `ALL_CAPS` | `BALL_RADIUS`, `DRAG_COEFFICIENT` |
| All other constants (`[GT]`, `[EST]`, `[DERIVED]`, `[CROSS]`) | PascalCase | `MaxSubsteps`, `TerminalVelocity` |
| Interfaces | `I` prefix + PascalCase | `IEventBus`, `ICollisionConsumer` |
| Assembly names / namespaces | `TacticalDirector.<SpecName>` | `TacticalDirector.BallPhysics` |

No Hungarian notation. No other prefix/suffix schemes (FR-CS-001/002).

**`var` policy (FR-CS-013):** Use `var` only when the type is immediately obvious from the RHS. `var state = new BallState()` is clear. `var result = Compute();` is not — write the explicit type.

---

## STYLE

**Indentation:** 4 spaces. Tabs are prohibited (FR-CS-011). Enforced by `.editorconfig` at Stage 1.

**Brace style:** Allman — opening brace on its own line (FR-CS-012).

```csharp
// COMPLIANT — FR-CS-012
public void Update(ref BallState state)
{
    if (state.IsGrounded)
    {
        ApplyFriction(ref state);
    }
}

// VIOLATION — K&R brace style
public void Update(ref BallState state) {
    if (state.IsGrounded) {
        ApplyFriction(ref state);
    }
}
```

**Explicit access modifiers:** Every type, method, property, field, and event declaration MUST carry an explicit access modifier. Relying on C#'s implicit `private` or `internal` is prohibited (FR-CS-014).

---

## NAMESPACES

One namespace per assembly. Sub-folders do not introduce sub-namespaces (FR-CS-007).

```csharp
// File: src/ball-physics/simulation/DragIntegrator.cs
namespace TacticalDirector.BallPhysics   // flat — no sub-namespace
{
    internal readonly struct DragIntegrator { … }
}

// VIOLATION:
namespace TacticalDirector.BallPhysics.Simulation { … }
```

---

## CONSTANT CATALOGUES

Every constant lives in `<SpecName>Constants.cs`. No literals in formula or system files (FR-CS-016).

**Naming:** PascalCase folder name + `Constants.cs`

| Spec folder | Catalogue file |
|---|---|
| `ball-physics/` | `BallPhysicsConstants.cs` |
| `agent-movement/` | `AgentMovementConstants.cs` |
| `collision-system/` | `CollisionSystemConstants.cs` |
| *(all specs)* | `<SpecName>Constants.cs` |
| *(cross-spec)* | `project-constants/ProjectConstants.cs` |

**Region order inside every catalogue (most-immutable first):**

```csharp
#region Fixed      // [FIXED]   → public const float BALL_RADIUS = 0.11f;
#region Derived    // [DERIVED] → public static readonly float TerminalVelocity = Mathf.Sqrt(GRAVITY / DRAG_COEFFICIENT);
#region Cross      // [CROSS]   → public static readonly float PhysicsTickHz = ProjectConstants.PHYSICS_TICK_HZ;
#region GT         // [GT]      → public static readonly int MaxSubsteps = 8; // TODO: replace with config loader (Stage 1)
#region EST        // [EST]     → public static readonly float LiftCoefficient = 0.35f; // TODO: validate
```

Omit a region entirely if the spec has no constants with that tag. Empty regions are prohibited.

**Region name convention:** The first three region names use Title Case (`Fixed`, `Derived`, `Cross`). `GT` and `EST` match their tag names exactly since those are already **all-caps abbreviations**. Do not use ALL_CAPS (`FIXED`) or lowercase for region names.

**`[DERIVED]` constants:** The XML doc must include the tag, the formula, and the source constants (FR-CS-021). Substitute actual formula references (FM-NNN, §x.y) from the implementing spec:

```csharp
#region Derived
/// <summary>
/// [DERIVED] Terminal velocity (m/s) at which drag force equals gravity.
/// Formula: sqrt(GRAVITY / DRAG_COEFFICIENT). FM-NNN. Ball Physics #1 §3.x.
/// Source constants: BallPhysicsConstants.GRAVITY, BallPhysicsConstants.DRAG_COEFFICIENT.
/// </summary>
public static readonly float TerminalVelocity =
    Mathf.Sqrt(BallPhysicsConstants.GRAVITY / BallPhysicsConstants.DRAG_COEFFICIENT);
```

**`[GT]` loading mechanism (FR-CS-019):** The loader landed June 30, 2026 as
`TacticalDirector.ProjectConstants.GameplayConfig` + `GameplayConfigFileLoader`
(`src/project-constants/`). `GameplayConfigFileLoader.Parse(text)` reads the on-disk
text format — line-oriented case-insensitive `key = value` pairs under `[section]`
headers (the section is the owning catalogue, conventionally its spec folder name),
`#` comments, blank lines ignored — into an immutable `GameplayConfig`. A catalogue
reads each constant at boot via `GetFloat/GetInt/GetBool/GetString(section, key, fallback)`:

```csharp
using static TacticalDirector.ProjectConstants.GameplayConfigHolder;
// …
#region GT
/// <summary>[GT] Maximum physics substeps per frame. Config key [ball-physics] MaxSubsteps. Code Standards #20 §3.2.3.</summary>
public static readonly int MaxSubsteps = Config.GetInt("ball-physics", "MaxSubsteps", 8);
```

Contract: an **absent** key returns the supplied design-time fallback, so an empty/partial
config file leaves every constant at today's baseline (behaviour-neutral); a **present** key
whose value does not parse to the requested type throws `FormatException` (a config typo fails
loud at boot). `GameplayConfig` is immutable and **constructor-injected** (never a static mutable
singleton — stays clear of FR-CS-051..054); getters run once at boot, never on the 60 Hz path. The
text grammar is a human-authoring format, NOT a determinism-pinned wire format — only the loaded
values feed the sim, so a future binary/richer grammar is a pure parser swap leaving the catalogues
untouched (the #19 `ScenarioIndex` / #21 `TeamTacticFileLoader` precedent).

**Boot-sequencing resolution (`GameplayConfigHolder`, landed 2026-06-30):** a `[GT]` field above is
`public static readonly` — it is assigned in the catalogue's own static constructor, a point no
ordinary constructor-injection call site can reach (there is no object yet to inject into).
`src/project-constants/GameplayConfigHolder.cs` is the single explicit binding point: a composition
root calls `GameplayConfigHolder.Bind(config)` once, before referencing any `[GT]` catalogue; every
catalogue's `Config.GetX(...)` call reads `GameplayConfigHolder.Config`. Until `Bind` is called,
`Config` resolves to `GameplayConfig.Empty`, so an unbound process (any test today; Stage 0 before a
composition root exists) is behaviour-neutral — every constant keeps its literal fallback. The FIRST
read of `Config` locks the binding; a `Bind` call after that point throws `InvalidOperationException`
instead of silently leaving whichever catalogue read first stuck on default values forever — the
ordering hazard fails loud at the moment it would otherwise become an undetectable bug. This is a
one-shot boot-time wire-up, not the banned static-mutable-singleton pattern (FR-CS-051..054 targets
hidden, repeatedly-mutated ambient *game* state); it is the same boundary the project already accepts
for `EventBusRegistrar.Initialize()` / `EventRegistry.EnsureInitialized()`. No `MatchEngine` call site
calls `Bind` yet — wiring an actual on-disk config load into match-engine boot is a follow-up.

**Migration status:** 509 of the 520 `[GT]` declarations the root `CLAUDE.md` OPEN ISSUES entry
tracked are migrated (17 catalogues; the `using static GameplayConfigHolder;` import + the
`Config.GetX(...)` call shown above). Two categories are explicitly carved out, left as literals
with their original `// TODO:` marker:
- **11 array/table-valued `[GT]` constants** (all in `TacticalInstructionsConstants.cs` — e.g.
  `MentalityRiskMult`, `TempoActionBias`) — `GameplayConfig` has no array getter, and these tables
  carry their own cross-cell invariant tests (`BalancePassInvariantsTests` — identity-row exactness,
  monotonicity) that only run against compile-time literals; making them config-overridable without
  an equivalent runtime invariant check would let a config file silently violate the pinned §5.6/G2
  balance pass. Needs its own design pass, not a mechanical `GetFloat` call.
- `decision-tree` / `living-world` / `positioning-ai` / `pressing-ai` carry `[GT]` constants that
  never picked up the `// TODO: replace with config loader (Stage 1)` marker at all (an earlier
  authoring inconsistency, confirmed by grep) — out of scope for this mechanical pass; flagged here
  rather than silently swept in.

Files: `src/project-constants/GameplayConfigHolder.cs` (+ tests), and 17 `<Spec>Constants.cs` +
`.asmdef` pairs (agent-movement, attacking-ai, ball-physics, collision-system, defensive-ai,
deterministic-sim, event-system, first-touch, goalkeeper-mechanics, heading-mechanics, match-engine,
pass-mechanics, perception-system, performance-optimization, shot-mechanics, tactical-instructions,
testing-strategy).

**`[EST]` constants:** Every `[EST]` constant requires a `spec-error-log.md` entry (FR-CS-020). The constant must be promoted to `[GT]`, `[FIXED]`, `[DERIVED]`, or `[CROSS]` before the system that consumes it is implemented. If the validated value is derivable via formula, use `[DERIVED]` (document the formula per FR-CS-021). If it already exists authoritatively in another spec, use `[CROSS]` (cite the authoritative spec and section per FR-CS-022).

**`[CROSS]` mirrors — routing rule (Spec #20 §4.2):**
- **Multi-consumer** (constant used by ≥ 2 spec assemblies): declare in `ProjectConstants.cs`; each consuming catalogue mirrors from there.
- **Single-consumer** (constant used by exactly 1 spec assembly, e.g., a domain tag allocated in Spec #16 §3.4 used only by one spec): the consuming catalogue mirrors directly from the source spec's catalogue — not via `ProjectConstants.cs`.

A `[CROSS]` mirror must not diverge from its source. Naming is PascalCase per §3.2.3. Cite the authoritative spec and section:

```csharp
// Multi-consumer mirror: declare in ProjectConstants.cs; each consuming catalogue mirrors from there.
/// <summary>
/// [CROSS] Physics/render loop tick rate (Hz).
/// Authoritative source: ProjectConstants.cs — PHYSICS_TICK_HZ.
/// Ball Physics #1 §1.2. Value: 60 Hz.
/// </summary>
public static readonly float PhysicsTickHz = ProjectConstants.PHYSICS_TICK_HZ;

// Single-consumer mirror: source spec's catalogue directly, NOT via ProjectConstants.cs
/// <summary>
/// [CROSS] Goalkeeper subsystem domain tag.
/// Authoritative source: DeterministicSimConstants.DOMAIN_TAG_GOALKEEPER.
/// Deterministic Simulation #16 §3.4. Value: 0x1D.
/// </summary>
public static readonly uint DomainTagGoalkeeper =
    DeterministicSimConstants.DOMAIN_TAG_GOALKEEPER;
```

> **Note — naming discrepancy in Spec #20 §4.2 (ERR-020-001, resolved):** The §4.2
> worked example originally showed `PHYSICS_TICK_HZ` (ALL_CAPS) for the `[CROSS]`
> *mirror* field in `BallPhysicsConstants.cs`. This contradicts §3.2.3, which is the
> rule-definition section and states PascalCase for `[CROSS]`. §3.2.3 is authoritative —
> use PascalCase for the mirror field name. Spec #20 §4.2 has been patched to show
> `PhysicsTickHz` (PascalCase). Note that the source constant in `ProjectConstants.cs`
> is tagged `[FIXED]` and correctly uses ALL_CAPS (`PHYSICS_TICK_HZ`); the right-hand
> side of the mirror assignment must reference that ALL_CAPS name.

---

## GAME-LOOP RULES (ZERO ALLOCATION)

The 60 Hz physics/render path must produce **zero managed-memory allocations per frame** (FR-CS-066).

**Required patterns:**
- Game-state data in `readonly struct`, not `class`
- State passed by `ref` parameter
- Pre-allocated fixed-size buffers for temp arrays
- Struct-based events on the event bus (not `event Action<T>`)
- `stackalloc` with `Span<T>` for transient buffers with statically bounded size (C# 7.2+; no `unsafe` block required). The pointer form (`int* p = stackalloc int[n]`) requires `unsafe` and therefore lead-developer sign-off per FR-CS-010 — use the `Span<T>` form by default
- `private static readonly ProfilerMarker` field on every system class for profiling (one-time alloc at startup); call `.Auto()` at each entry point to bracket the measurement scope (FR-CS-070)
- **Dependency injection via constructor parameters** — see "Banned Architectural Patterns" below for the full rule and the four anti-patterns it replaces

**Banned constructs on hot paths (FR-CS-027–034):**
- `new` class objects or managed arrays
- Boxing (value type → object cast)
- LINQ (`.Where`, `.Select`, `.ToList`, etc.)
- `params` array parameters
- String formatting (`$"…"`, `string.Format`, `+` concatenation)
- Closures capturing local variables
- `foreach` over any type that does not expose a concrete struct `GetEnumerator()` at the call site — including `List<T>` or `Dictionary<K,V>` via an interface variable (both `List<T>.Enumerator` and `Dictionary.Enumerator` are structs, but both are boxed when the collection variable is typed as an interface); use arrays or `Span<T>` for hot-path iteration
- Reflection

**Banned language features in game-loop and game-state code (FR-CS-010):**
- `dynamic` — bypasses compile-time type safety; introduces non-deterministic dispatch paths
- `async`/`await` in game-loop / game-state-modifying code — breaks deterministic tick ordering; continuations resume on unpredictable frames. Permitted in initialization code, editor tooling, and loading pipelines that do not touch game state.
- `unsafe` without lead-developer sign-off recorded in the PR description
- `try`/`catch` inside per-frame inner loops (FR-CS-069)
- Virtual method calls inside per-frame inner loops (FR-CS-068)

**Banned architectural patterns in game-state assemblies (FR-CS-051–054):**
- **Service locator** (`ServiceLocator.Get<T>()`) — hides dependencies; breaks deterministic testing
- **Ambient context** (`MatchContext.Current`) — hidden state; breaks replay rewind
- **Static mutable singleton** — cannot be reset between deterministic replay ticks
- **Generic DI container on the hot path** (Zenject, VContainer, `Microsoft.Extensions.DependencyInjection`) — reflection-based; allocates; violates zero-alloc budget

The required alternative to all four is **constructor injection**: pass dependencies as constructor parameters.

The `ProfilerMarker` field is `private static readonly`, named per the
`s_<EntryPointName>Marker` convention (see "Profiler Markers" section).

```csharp
// COMPLIANT — sealed instance class; dependencies injected via constructor per FR-CS-051–054
// Note: `state with { … }` requires C# 10+ on readonly structs. Verify the
// Unity LTS + backend in certification-platform.md before using this pattern.
public sealed class BallPhysicsSystem
{
    private readonly MatchClock _clock;
    private static readonly ProfilerMarker s_updateMarker =
        new ProfilerMarker("BallPhysics.Update");

    public BallPhysicsSystem(MatchClock clock)
    {
        _clock = clock;
    }

    public void Update(ref BallState state, float dt)
    {
        using var _ = s_updateMarker.Auto();
        state = state with { Velocity = state.Velocity * (1f - BallPhysicsConstants.DRAG_COEFFICIENT * dt) };
    }
}

// VIOLATION — copies BallState by value; wastes memory bandwidth
public void Update(BallState state, float dt) { … }
```

---

## DETERMINISM RULES

No `System.Random`, no `DateTime.Now`, no `Guid.NewGuid()`, no `Task.Run` or `Parallel.*`, no hardware-intrinsic FMA in game logic (FR-CS-036–040).

| Need | Use | Owning assembly |
|---|---|---|
| Random numbers | `SplitMix64` helper (FR-CS-041) | `deterministic-sim/` (Spec #16) |
| Simulation time | `MatchClock` (injected) (FR-CS-042) | `deterministic-sim/` (Spec #16) |
| Trigonometry / math | Project math helper (FR-CS-043) | `project-constants/` — exact class TBD at Stage 1 |
| Deterministic IDs | Pre-allocated deterministic ID ranges (Spec #16 §3.2.5) | `deterministic-sim/` (Spec #16) |

**Hardware-intrinsic FMA (FR-CS-040):** Fused multiply-add instructions can produce different results from separate multiply + add on different hardware or compiler versions. FMA intrinsics are banned unless the platform is pinned and the lead developer has signed off.

**64-bit multiplication** must use `unchecked { }` with a `// Spec #16 §3.4.4` comment
(FR-CS-044), regardless of which assembly the code lives in. The citation always refers
to Spec #16 §3.4.4 (SplitMix64 state update) — not the local spec's §3.4.4:

```csharp
unchecked  // Spec #16 §3.4.4: deliberate 64-bit wrap-around; not an overflow bug
{
    state += 0x9E3779B97F4A7C15UL;
}
```

**Python tooling** that mirrors C# SplitMix64 constants (FR-CS-045): omit the `UL` suffix and mask intermediates with `& 0xFFFFFFFFFFFFFFFF`. Do not mix `unchecked` into Python (it has no meaning there) or mask operators into C# (that would introduce a different semantic).

---

## NUMERIC TYPES

- `float` everywhere at Stage 0 (FR-CS-071).
- `double` is banned by default; override requires lead-developer sign-off and inline comment.
- `decimal` is always banned.
- Fixed64 migration is a Stage 5+ concern (Spec #9).

---

## INTERFACE DESIGN

Write an interface only when both the producer and consumer are specified. No phantom interfaces for unspecified systems (ERR-001, ERR-004, FR-CS-048/049).

An `interface` file MUST reside in the same assembly as at least one of its specified consumers (FR-CS-048). Access modifier is `public` only if callers cross the assembly boundary; `internal` otherwise (FR-CS-015).

**Event-vs-interface decision tree (FR-CS-050):**
- Same assembly → direct method call
- Cross-assembly, consumer not yet specified → wait; create nothing
- Cross-assembly, consumer specified, multiple implementations → interface (in consumer's assembly)
- Cross-assembly, single implementation, lower→higher layer notification → struct event on event bus
- Cross-assembly, single implementation, same or downward layer → direct method call

---

## FILE HEADER (REQUIRED ON EVERY FILE)

```csharp
// File:     src/ball-physics/BallPhysicsCore.cs
// Created:  2026-05-19
// Modified: 2026-05-19
// Author:   <name or handle>
// Spec:     Ball Physics #1, Code Standards #20
// Purpose:  Implements core ball physics calculations (gravity, drag, Magnus effect).
//           Does not manage state; all state is passed by ref parameter.

namespace TacticalDirector.BallPhysics
{
    // …
}

#region VersionHistory
// | Version | Date       | Author           | Notes                   |
// | 1.0     | 2026-05-19 | <name or handle> | Initial implementation. |
#endregion
```

**Required fields (FR-CS-056/057):** file path (relative to repo root), created date (ISO), modified date (must match latest version-history row), author, governing specs, purpose (≤ 2 sentences).

Version history lives at the end of the file; rows are appended, never deleted.

When a file is authored or modified by an automated agent with no named individual, use `—` in the Author field.

---

## XML DOC COMMENTS

Every `public` type, method, property, and event requires `/// <summary>`. Every constant (any access level) requires `/// <summary>` that includes its tag (FR-CS-060/061).

```csharp
/// <summary>[FIXED] Ball radius in metres. Ball Physics Spec #1 §2.1.</summary>
public const float BALL_RADIUS = 0.11f;

/// <summary>Applies drag to ball velocity for one physics step.</summary>
/// <param name="velocity">Current velocity vector (m/s).</param>
/// <param name="dt">Time delta in seconds.</param>
public static Vector3 CalculateDrag(Vector3 velocity, float dt) { … }
```

---

## INLINE COMMENTS

Write a comment only when the **why** is non-obvious. Do not comment what the code already says (FR-CS-064).

```csharp
// COMPLIANT — hidden constraint
unchecked  // Spec #16 §3.4.4: deliberate 64-bit wrap-around; not an overflow bug
{ … }

// VIOLATION — states the obvious
int count = agentList.Count;  // Get the number of agents
```

**Commented-out code is prohibited** in any commit to a shared branch (FR-CS-065). Delete disabled code; version control preserves the history.

---

## `using` DIRECTIVE ORDER

System → Unity → Project, each group separated by a blank line (FR-CS-006):

```csharp
using System;
using System.Collections.Generic;

using UnityEngine;
using UnityEngine.Profiling;

using TacticalDirector.BallPhysics;
using TacticalDirector.EventSystem;
```

Alphabetical within each group is recommended but not enforced.

---

## PROFILER MARKERS

Every system entry point (`Update`, `Tick`, `RunStep`, or similarly named method) must be wrapped in a `ProfilerMarker.Auto()`. The marker is a `private static readonly` field (allocated once at startup — zero per-frame cost) (FR-CS-070).

> **Note:** These are custom methods on game system classes — **not** Unity MonoBehaviour
> lifecycle callbacks (`FixedUpdate()` / `Update()` with no parameters). The MonoBehaviour
> / PlayerLoop integration layer is a Stage 1 concern; see "WHAT IS NOT HERE YET" below.

**Field naming convention:** `s_<EntryPointName>Marker` — e.g., `s_updateMarker` for `Update`, `s_runTickMarker` for `RunTick`.

**Marker string format:** `<SpecName>.<MethodName>` (e.g., `"BallPhysics.Update"`, `"DeterministicSim.RunTick"`).

```csharp
using UnityEngine.Profiling;

// Profiler-relevant fields shown; constructor and injected dependencies
// follow the same pattern as the Game-Loop Rules COMPLIANT example above.
public sealed class BallPhysicsSystem
{
    private static readonly ProfilerMarker s_updateMarker =
        new ProfilerMarker("BallPhysics.Update");

    public void Update(ref BallState state, float dt)
    {
        using var _ = s_updateMarker.Auto();
        // …
    }
}
```

---

## STAGE 0 VERIFICATION

No static analysis tooling yet. Verify each file manually against the Spec #20 §5.4 checklist before marking it complete. Roslyn analyzers, `BannedSymbols.txt`, and `.editorconfig` activate at Stage 1 once `certification-platform.md` is fully pinned.

---

## WHAT IS NOT HERE YET

These items are deferred pending Unity project setup and platform pinning:

| Item | Blocked on |
|---|---|
| `.asmdef` content (GUIDs, `allowUnsafeCode`, `autoReferenced`, `testPlatforms`, `versionDefines`) | Unity project initialization |
| Exact Unity LTS revision | `docs/tracking/certification-platform.md` pinned |
| `dotnet test` framework args | Stage 0+1 setup (Spec #19 §7.5 D2 — framework pin deferred to Stage 0+1) |
| Unity batch-mode CI commands | **Command DEFINED July 13, 2026** — the FR-PO-052 certified perf capture command is in "BUILD AND TEST COMMANDS" above, driving `MatchEngineCapstonePerfHarnessTests` → the real `MatchEngine` capstone via `MatchEngineCapstonePerfHarness`. The harness itself (`StopwatchPerfHarness` + the capstone runner) runs on the Linux `dotnet-ci` gate as a non-certifying compile+execute proof. Still requires a **Unity host** to execute the certified capture (`cert-run-runbook.md` P2 + Steps 2–4); a full batch-mode *build* pipeline (as opposed to the test-runner invocation) remains for Unity project initialization. |
| `.editorconfig` path and contents | Stage 1 setup |
| C# language version pin | `certification-platform.md` pinned |
| `[GT]` config loader class / method | **Mechanism landed June 30, 2026** — `TacticalDirector.ProjectConstants.GameplayConfig` + `GameplayConfigFileLoader` (`src/project-constants/`). **Migration landed June 30, 2026** — `GameplayConfigHolder` resolves the boot-sequencing design point; 509/520 `[GT]` declarations across 17 catalogues now read `Config.GetX(...)`. See the "`[GT]` loading mechanism" + "Boot-sequencing resolution" + "Migration status" sections above for the mechanism, the 11-array-table + 4-untagged-catalogue carve-outs, and the still-open follow-up (no composition root calls `GameplayConfigHolder.Bind` yet — Stage 0 has no on-disk config load wired into match-engine boot). |
| Project math helper class name / assembly | Stage 1 setup — update determinism table when defined |
| `src/tactical-instructions/` seams into #8/#11–#15 (Spec #21 T2–T3) | **T0 landed June 21, 2026** — the `TacticalDirector.TacticalInstructions` assembly (16 enums + `TeamTactic`/`PlayerTactic`/`PlayerInstructions` + identity factories + catalogue + ordinal/identity tests) is in the tree above, behaviour-neutral. **T2 Decision Tree (#8) seam landed June 28, 2026** (behaviour-neutral): `decision-tree/TacticTranslation.cs` (rank-mapped TacticPressing/TacticPassing → #8 enums + §3.1 F5 clamp + §3.2 Mentality risk/line resolvers), a `Mentality` routing field on `TacticalContext` (`Stage0Default` seeds Balanced = identity), and the §3.2/§3.3 Mentality risk multiplier in `UtilityScorer.ComputeUtility` (×1.0 at Balanced). `decision-tree(.Tests).asmdef` gain the `TacticalInstructions` ref. **Runtime activation (Phase-D single-writer for #8) landed June 28, 2026** (behaviour-neutral): `MatchEngine` holds a per-team `TeamTactic` (default `Balanced`), exposes `public SetTeamTactic(teamId, tactic)` (stages pending; committed pending→active at the AI-stride boundary per FR-TI-027), and `RunMechanicsAI` overlays the active tactic's `Mentality` + translated `Pressing`/`Passing` into each `TacticalContext`. `TacticTranslation` promoted internal→public (the match-engine is its §3.1 caller). `match-engine(.Tests).asmdef` gain the `TacticalInstructions` ref; `MatchEngineTacticTests.cs` added. **T2 Pressing AI (#13) consumer seam landed June 28, 2026** (behaviour-neutral): `pressing-ai/TacticTranslation.cs` maps `LineOfEngagement` → a multiplicative scalar on the #13 press-trigger radius (`PressTriggerDistanceM`) via `LineOfEngagementScalar` (direct ordinal lookup + §3.1 F5 clamp; Standard ⇒ ×1.0), a `LineOfEngagement` routing field on `PressingSnapshot` (ctor-seeded `Standard` = identity; zero-value default is `VeryLow`), and `PrimaryPressSelector.Select` scaling its eligibility radius by that scalar (×1.0 at Standard = byte-identical). `pressing-ai(.Tests).asmdef` gain the `TacticalInstructions` ref; `Tests/TacticTranslationTests.cs` added. **The #13 match-engine Phase-D writer landed June 29, 2026** (MatchEngine.cs v1.18, behaviour-neutral): `FillPressingSnapshot` routes the pressing team's active `TeamTactic.LineOfEngagement` → `PressingSnapshot.LineOfEngagement` (default Balanced ⇒ Standard ⇒ ×1.0; new `TestOnly_PressLineOfEngagement` seam; `MatchEngineTacticTests.cs` v1.1). **The #12/#14/#15 consumer seams landed June 29, 2026** (behaviour-neutral): each gains `<assembly>/TacticTranslation.cs` + a snapshot routing field seeded to identity — #12 `TacticWidth`/`TacticDefWidth` → lateral-compactness scalar on `ContextModifierInputs`, fully wired into `ContextModifier.ApplyToAll` (Standard ⇒ ×1.00 exact); #14 `OffsideTrap` → `DefensiveSnapshot.OffsideTrapRequested` (false identity, arming-gate consumption deferred — KD-9 request-not-guarantee, gating today's autonomous trap behind a default-false toggle is not neutral); #15 `FocusPlay` → `AttackingSnapshot.FocusPlay` (Mixed zero-value identity, `OverloadDetector` flank-preference consumption deferred). **The #14/#15 match-engine Phase-D writers landed June 29, 2026** (MatchEngine.cs v1.19, behaviour-neutral): `FillDefensiveSnapshot` routes `TeamTactic.OffsideTrap` → `DefensiveSnapshot.OffsideTrapRequested` via fully-qualified `DefensiveAI.TacticTranslation` (CS0104 — five `TacticTranslation` types in match-engine scope per the #13 v1.17 lesson); `FillAttackingSnapshot` routes `TeamTactic.FocusPlay` → `AttackingSnapshot.FocusPlay` (enum passthrough). Default Balanced ⇒ false / Mixed = identities, byte-identical to pre-#21. Active consumption still deferred (#14 `OffsideTrapController` per KD-9; #15 `OverloadDetector` per §5.6/G2). New `TestOnly_OffsideTrapRequested`/`TestOnly_FocusPlay` seams; `MatchEngineTacticTests.cs` v1.2. **The #12 match-engine Phase-D writer landed June 29, 2026** (MatchEngine.cs v1.20, behaviour-neutral) — the last of the three Mechanics writers: `RunMechanicsAI` builds `ContextModifierInputs` via the 5-arg ctor, routing the active `TeamTactic.Width`/`DefensiveWidth` (translated by `ContextModifier` to the in-poss / OOP lateral-compactness scalar). Default Balanced ⇒ Standard/Standard ⇒ ×1.00 = byte-identical (5-arg both-Standard ≡ 3-arg identity ctor). Per-team `_posModifiers` captured for the new `TestOnly_PositioningWidth`/`TestOnly_PositioningDefWidth` seams; `MatchEngineTacticTests.cs` v1.3. **All three Mechanics Phase-D writers (#12/#13/#14/#15) are now closed.** **Active #14/#15 consumption landed June 29, 2026** (behaviour-neutral on default): #14 `OffsideTrapController.Update` consumes `OffsideTrapRequested` as the KD-9 additive request (requested ⇒ arms after reduced `[GT] OffsideTrapRequestedDwellTicks` ≤ baseline; the §3.7.2 conditions still adjudicate; false ⇒ baseline); #15 `OverloadDetector` gains a 5-arg `Evaluate(…, Flank? preferredFlank, …)` (4-arg delegates null) using `FocusPlay`→`Flank?` as a bias (preferred ball-side flank lowers the trigger count by `[GT] OverloadFocusCountBias`; null/non-ball-side ⇒ unchanged); `AttackingAITick` threads it. Both `[GT]` magnitudes illustrative pending §5.6/G2. **The TeamTactic config-loader in-code source + boot applier landed June 29, 2026** (the runtime-activation gate that populates `SetTeamTactic`): `match-engine/TeamTacticConfig.cs` (immutable per-team `TeamTactic`, index = teamId; `Default` = Balanced-for-every-team identity, FR-TI-031; `ForTeam` bounds-guarded) + `match-engine/TeamTacticConfigApplier.cs` (static `Apply(engine, config)` stages each team via the public `SetTeamTactic` before kickoff — committed at the first stride per FR-TI-027). Per the #19 `ScenarioIndex` D1 precedent the **on-disk file format is deferred** (the FR-CS-019 `[GT]` loader is Stage 1; encodings D1-pinned at Stage 0+1) — the Stage 0+1 disk loader is a pure parser swap producing a `TeamTacticConfig` and feeding `Apply` unchanged; no format invented, no production `MatchEngine.cs` change. `match-engine/tests/TeamTacticConfigTests.cs` added. **The §3.3 per-agent PlayerTactic utility product seam landed June 29, 2026** (v1.80): `decision-tree/TacticTranslation.PlayerTacticActionMultiplier` composes `RoleWeightModifiers`×dutyBias×instrBias×`TempoActionBias`; `TacticalContext` gains `Tempo` + `PlayerTactic` routing fields (Stage0Default identity-seeded); `UtilityScorer` applies it per option (identity ⇒ ×1.0). `RunMechanicsAI` routes the active team `Tempo`; the per-agent `PlayerTactic` stays the Stage-0 identity (no per-agent config surface). **The on-disk tactic-file loader landed June 29, 2026** (v1.81): `TeamTacticFileLoader.Parse(text) → TeamTacticConfig` (Stage-0 `key=value` text format; empty ⇒ Default ⇒ neutral; fail-loud). **ERR-021-002 resolved June 29, 2026** (v1.82): per-team active+pending `TeamTactic` serialized into the snapshot, `SNAPSHOT_SCHEMA_VERSION` 8 → 9 — a mid-match change is now restore-deterministic. Still pending (Stage-1): the **per-agent tactic config surface** (all agents are the identity `PlayerTactic` at Stage 0) + the **§5.6/G2 balance pass** pinning the illustrative `[GT]` magnitudes; the §3.4 `DefensiveLine` depth recompute (#12/#14 depth-ownership) remains separately deferred. The #21 assembly's own asmdef `references` array is empty until `project-constants` exists (FR-TI-002 — T0 consumes nothing from it). |
| MonoBehaviour / PlayerLoop integration pattern | Unity project initialization — how Unity's lifecycle loop calls into struct-based game systems; until defined, system entry points are pure C# instance methods named `Update`, `Tick`, or similar |
| `AgentState` as `readonly struct` + `with` expressions | C# language version pin in `certification-platform.md` — `with` on `readonly struct` requires C# 10+. Until pinned, `AgentState` (and equivalent game-state structs) are mutable structs mutated by `ref` parameter; migration to readonly + with is a Stage 0+1 cleanup task once the language version is locked. |

Update this file when those items are resolved.

---

## VERSION HISTORY

| Version | Date | Author | Notes |
|---|---|---|---|
| 2.34 | 2026-07-24 | — | **Living World (#22) arc-triggers Slice 1/E1 + Slice 2/E2 landed** — the opt-in arc-trigger evaluator + `world.arcs` RNG sub-stream, per the converged `docs/tracking/living-world-arc-triggers-design.md` (item 1, Path B). New `src/living-world/`: `ArcCanonSource.cs` (the concrete KD-1/KD-2 nullable canon-input seam + nested `Builder` — a single concrete class, not an interface, FR-LW-031), `ArcTrigger.cs` / `ArcTriggerCatalogue.cs` (the APPEND-only Stage-0 trigger table — two entity-scoped rows + two board rows in ArcKind ordinal order), `ArcTriggerEvaluator.cs` (registers `world.arcs` with a DISTINCT entity sentinel `WORLD_STREAM_ENTITY_ARCS = -2`, KD-4 — distinct `ComputeStreamKey` from `world.text` -1; walks the catalogue in FR-LW-017/021 order; applies the **KD-7 rising-edge armed-off latch** — a sustained signal fires exactly once, drop-below re-arms; one `world.arcs` draw per rising edge; resolves a minimal FR-LW-018 pin; calls `ArcEngine.SpawnArc`). `WorldLoop` gains a 5th nullable seam (the evaluator) + `RunWorldTick(ArcCanonSource canon = null)` as a per-tick argument (KD-5, no stale-canon field); `WorldStore` owns the evaluator + a nullable `_canon` (`SetArcCanon` / a canon ctor overload / `Restore(payload, canon)` thread it, never persisted — the `ISquadProvider` precedent). `LivingWorldConstants` gains the `world.arcs` stream ids, the world-scoped entity-id sentinel block, `ARC_BOARD_SCOPE_KEY`, the signal keys, and per-trigger thresholds/lifetimes; `InteractionTextGenerator` uses the cataloged `WORLD_STREAM_ENTITY_TEXT` (behaviour-identical). **Slice 1/E1:** default-off + byte-identical; a flag-on run is deterministic FORWARD but `Snapshot()` fails loud (not yet snapshot-safe); no `WORLD_STORE_FORMAT_VERSION` change. **Slice 2/E2:** `Snapshot`/`Restore` serialize the `world.arcs` cursor + the KD-7 latch (canonical-order block with a strict-order/no-dup RestoreLatched gate) between the `world.text` block and the membership roster — `WORLD_STORE_FORMAT_VERSION` **2 → 3** (v2 payloads rejected fail-loud, no migration); the E1 fail-loud is dropped, so a flag-on `save@N → Restore(canon) → advance to N+K` is byte-identical to the uninterrupted run and a still-latched trigger does NOT re-fire on restore. `season-save/SeasonSaveManager.cs` v1.1: `Load` gains an optional `ArcCanonSource canon` threaded into `WorldStore.Restore` (§8.9(a)) so a flag-on world keeps evaluating through the season file. New `Tests/ArcTriggerTests.cs` (design §9 tests 1-4, 6-11 + a latch-serialization seam test) + `SeasonSaveManagerTests` +1 (flag-on world through the season file, with a null-canon negative control). **Full dotnet gate run locally (SDK 8.0.129 via apt): PASSED, 0 failures (whole tree green; 118 living-world tests, 20 season-save). Slice-1 default path byte-identical (no version change); Slice-2 comparative round-trip, no absolute rebaseline.** |
| 2.37 | 2026-07-25 | — | **Season & Competition Loop #30 T1 landed** — the season save/restore path (roadmap item A2). New `SeasonStateCodec.cs` (Appendix B encode/decode; version gate, overflow-safe element-wise length bounds, trailing-byte guard, decode-through-the-validating-constructors, and the serialized-vs-derived goal-difference coherence check). `SeasonSaveCodec` v1.1 + `SeasonSaveBlobs` v1.1: a third opaque sub-blob between the world and match blocks; **`SEASON_SAVE_FORMAT_VERSION` 1 → 2** (FR-SN-020 — world/match blobs byte-untouched; a v1 file is rejected). `SeasonSaveManager` v1.2 + `SeasonSaveContents` v1.1: `Save(world, season, matchOrNull, path)` / `Load → { World, Season, Match }` (FR-SN-021); the season is never optional. Code self-AR: the length bound moved from a `count * width` byte product (overflowable) to an element-wise `remaining / width`; and `SeasonState.cs` v1.3 now requires a calendar mapping ≥ 1 round, closing an encode/decode asymmetry where an empty schedule with a `default(SeasonCalendar)` was constructible but not decodable. **ERR-030-011** filed + patched same commit (spec-error-log v1.42): §3.6's `EncodeSeason` omitted `ManagedClubId` (Appendix B row 3a); Appendix B row 11's `f32/u8` job security pinned to `i32` per-mille. New `tests/SeasonStateCodecTests.cs` + `SeasonSaveManagerTests` v1.3. **Full dotnet gate: PASSED, 0 failures (whole tree green; season-save 112 → 135).** |
| 2.36 | 2026-07-25 | — | **#30 T0 adversarial-review pass 3: 2M + 1L, all fixed.** Pass 2 returned clean; pass 3 re-attacked that verdict. M-1: `FixtureScheduler.NextUInt64` violated FR-CS-044 — SplitMix64 add + two 64-bit multiplies with no `unchecked { }` and no `// Spec #16 §3.4.4` citation, while all four sibling SplitMix64 copies in-tree and the canonical `DeterministicRngService` comply; fixed (`FixtureScheduler.cs` v1.1). M-2: `SeasonState` lacked an instance-field-count coupling guard, so a T1 field omitted from both the codec and `FieldsEqual` would pass the FR-SN-022 round-trip vacuously and silently drop save data; added `SeasonState_InstanceFieldCount_MatchesFieldsEqualSet` (8 fields), the `PassExecutorStateTests.cs:348` precedent (`SeasonStateTests.cs` v1.1). L: single-threaded-access contract + T2 marshaling obligation recorded on `SeasonState` (v1.2) — #38 already runs a sim thread beside a UI thread (FR-UI-023/F6). Withdrawn rather than reported: SplitMix64 duplication (4 pre-existing in-tree copies, no public helper to reuse — the project's norm, not a new defect). **Full dotnet gate: PASSED, 0 failures (whole tree green; season-save 106 → 107); meta integrity clean.** Pass 4 found the M-2 fix INCOMPLETE (1M, fixed): `SeasonState.FieldsEqual` traverses five aggregate value types (`LeagueTableRow`/`Fixture`/`SeasonCalendar`/`BoardState`/`BoardObjective`), so a column added to any of them left `SeasonState`'s own count at 8 and the round-trip still passed vacuously — guard extended to all five (`SeasonStateTests.cs` v1.2; season-save 107 → 112, gate green). Pass 5 (1M, fixed): `SeasonLoopConstants`' four `[GT]` rows were `public const` ALL_CAPS under a `GameplayTuned` region — the pattern the June-30 FR-CS-019 migration removed tree-wide, re-introduced in a catalogue authored after it landed; a `const` cannot take `Config.GetInt`, structurally locking them out of that migration. Renamed PascalCase, converted to `static readonly` off `GameplayConfig` (literal fallbacks ⇒ behaviour-neutral), region → `GT`, asmdef gains `ProjectConstants` (`SeasonLoopConstants.cs` v1.1). |
| 2.35 | 2026-07-24 | — | **#28 T0 adversarial-review follow-ups + CI meta fix.** (1) Doc-only: `RegenGenerator.GenerateRegen`'s `clubId` `<param>` corrected — it is inert at T0 (retained for the §3.3 signature / T2 use), not the stream/id scoper the prior doc claimed (`streamIndex` scopes the draw; `PlayerId` is the explicit `newPlayerId`). (2) Duplication Low: the `DrawBounded` bounded-draw modulo mapping (+ its accepted-bias rationale) and `Clamp`, previously copy-pasted in `RegenGenerator` (#28) and `RosterGenerator` (#27), extracted to a new shared `src/player-database/PlayerGenerationRng.cs` (kept in the generation assembly, NOT `DeterministicSim`, so the deliberately-biased mapping stays scoped to player generation and is not exposed as a general RNG-service primitive a uniform-draw consumer might misuse). Both generators delegate; **byte-identical** — `RosterGenerator.cs` v1.3 / `RegenGenerator.cs` v1.2, verified by the unchanged `PlayerDatabase` (42) / `PlayerProgression` (24) / `MatchEngine` (307) suites. (3) CI: generated the Unity `.meta` sidecars (deterministic `md5(path)` GUIDs via `tools/unity-ci/generate-missing-metas.sh`) for the new `player-progression/` assembly + `PlayerGenerationRng.cs` — the "Unity .meta integrity" PR check had failed (14 tracked paths under `src/` lacked a `.meta`; every other check, incl. the Linux gate + C# format, passed). Local meta-integrity check: no missing / orphan / duplicate GUIDs. **Full dotnet gate: PASSED, 0 failures (whole tree green).** |
| 2.34 | 2026-07-24 | — | **Player Progression & Lifecycle #28 T0 landed** — the draw-free aging/growth core + the pure single-player regen generator, per the file-by-file `docs/tracking/progression-t0-implementation-plan.md` (grounded in the APPROVED #28 section files). New `src/player-progression/` assembly (`TacticalDirector.PlayerProgression`; references `PlayerDatabase` + `DeterministicSim` only — §4.1, world-tick, not a hot path). Six production files: `PlayerProgressionConstants.cs` (Appendix A catalogue, region order Fixed→Derived→Cross→GT; `[GT]` magnitudes illustrative pending the §5.6 balance pass; the `DOMAIN_TAG_PLAYER_PROGRESSION` 0x20 / `SUBSYSTEM_ORDINAL_PLAYER_PROGRESSION` 82 mirrors deliberately deferred to T2 with the production stream — KD-B), `PlayerLifecycle.cs` (the §2.2 overlay value type — `int PotentialAbility` / `int CurrentAbility` derived cache / `long GrowthCursor` the sole accumulator / `uint BirthWorldDay` the KD-A age anchor / retirement flag+day; the [1,20] attributes stay on #27's `PlayerRecord`, not duplicated — FR-PG-003), `TrainingInput.cs` (the #29 seam value type — empty at T0, `Neutral => default`; a value type not an interface per FR-PG-009/FR-LW-031), `AbilityModel.cs` (pure `ComputeCA` = integer position-weighted mean [1,20]→[0,ABILITY_MAX], `ClassifyAgeBand` with the Appendix-A strict boundaries `<GROWTH_AGE` / `>DECLINE_AGE`, and the deterministic weighted `TrySpendOnePoint`/`DrainOnePoint` — highest-bias-first spend / lowest-bias-first drain, ties ascending `AttrIdx`, F1 PA-ceiling skip; the `AgeBand` enum lives here per §4.2), `GrowthProjection.cs` (the §3.1 daily step verbatim — the sole attribute-mutation path FR-PG-008; age derived with no rollover KD-A, `long` cursor accrual, spend/drain at `POINT_COST`, CA recompute; `curveEnabled` accepted but the flat §4.3 band step is the KD-8 curve-off identity, the deep curve is T3), `RegenGenerator.cs` (the §3.3 pure single-player regen — fixed `PROGRESSION_REGEN_FIELDS`=37 reservation mirroring #27's `RosterGenerator.GenerateOne` draw pattern; returns `(PlayerRecord, PlayerLifecycle)` so the drawn PA is not dropped; attrs generated then PA floored above CA so a young regen has room to grow; fresh caller-supplied `PlayerId` — FR-PG-011). KD-B honoured: `RegenGeneratorTests` registers the `player-progression.regen` stream under a documented test-local ordinal literal `82`, so no early `deterministic-sim` change (the production `SubsystemOrdinals.PlayerProgression` + stream land at T2). CS0104 preflight clean (no `PlayerProgression.PlayerAttributes` introduced — #28 consumes #27's directly, §4.4). New `tests/` (4 files, 24 tests): constant balance-invariant locks, `T-PG-CA-001/002/003` (CA determinism + F1 ceiling + weighted order), `T-PG-DET-001/002` + `T-PG-ID-001/002` (byte-exact growth/decline, value-copy "save" == uninterrupted run, age gap-independence, `TrainingInput.Neutral` == default), `T-PG-REG-001/003` (regen determinism, exact budget, bounds, CA≤PA). **Full dotnet gate run locally (SDK 8.0.129 via apt): PASSED, 0 failures (whole tree green; 24 new player-progression tests).** Not built (later phases): T1 `ProgressionSaveCodec` / T2 `ProgressionEngine` + world-tick wiring + the production regen stream / T3 deep CA-PA curve + #29 input. |
| 2.33 | 2026-07-23 | — | **DT-emitted goalkeeper SAVE action (ERR-008-013)** — the keeper's save is now a first-class Decision Tree (#8) action instead of the engine-side world-state heuristic, per the new converged supplement `docs/tracking/gk-heading-dt-producer-design.md` (Option-B outline killed by AR-1 as unsound — the DT off-ball PRESS/INTERCEPT model run-to-the-ball, which inverts the save case; owner chose Option A; outline AR-2/AR-3, plan AR-4 (a real missed-save defect — `ScoreSave=ceiling` is not robust: INTERCEPT can reach the clamp ceiling via `MentalityRiskMult`×`RoleWeightModifiers` and win the lower-ordinal tiebreak → fixed by emitting SAVE as the SOLE off-ball option)/AR-5, impl AR-6 all CONVERGED). `decision-tree`: `ActionType.SAVE = 7` (fits the 3-bit composure-noise field; HEADER=8 would overflow → deferred), `TacticalContext.SaveAvailable` (false = identity, `Stage0Default`-seeded), `OptionGenerator.GenerateOffBallBranch` short-circuits to `GenerateSaveCandidate` (SAVE alone) when `SaveAvailable`, `UtilityScorer` `ScoreSave` = `U_BASE_SAVE` + SAVE exempted from `PlayerTacticActionMultiplier` (its 7-wide #21 tables index by the action ordinal → OOB at a=7), new `IDtSaveDispatch` seam (primitives only) + `ActionDispatcher` SAVE case + `DecisionTree` ctor param. SAVE is continuous for free (`IsContinuousAction(type) => type != PASS && != SHOOT`; no state-machine edit). `match-engine`: `MatchEngine.cs` v1.47 `HostSaveDispatch` sink (agent→GK slot map + v18 `_saveCommittedForGk` latch + `ToGoalkeeper` projection + `CommitSaveIntent` — the same Stage-0 `SaveIntent` the removed heuristic built), `RunMechanicsAI` sets `SaveAvailable` for the threatened keeper under `EnableGkHeading` (from `GkHeadingIntentSource.SaveArmed`) + clears the latch when no longer armed, `DriveGkHeadingTactical` drops `TryCommitSaveIntents` (header trigger stays). **No `SNAPSHOT_SCHEMA_VERSION` change** (SAVE reuses `AgentAction.Type`/`TargetPosition`); flag-off byte-identical (SaveAvailable false ⇒ off-ball branch untouched, SAVE=7 never enters the noise field, the `!= SAVE` guard is always-true so `PlayerTacticActionMultiplier` runs identically). Spec back-prop: `ERR-008-013` (`spec-error-log.md` v1.34) + `decision-tree/section-3-1.md` §3.1.13 + `section-3-2.md` v1.6 anchor notes. New `OptionGeneratorTests` sole-option lock, `UtilityScorerTests` crash-guard lock, `MatchEngineGkHeadingTests` DT-path save-commit + `SaveDecision_SurvivesAdversarialTactic` (AR-4 missed-save regression lock); the `DecisionTree_InstanceFieldCount` (9→10, `_saveDispatch` injected-not-cross-tick) + `PlayerTacticActionMultiplier_Identity` (skips SAVE=7) guards updated. **Full dotnet gate run locally (SDK 8.0.129 via apt): PASSED, 0 failures (whole tree green; DecisionTree 84, MatchEngine 306).** Still deferred: DT-emitted HEADER, attribute-modulated commit, flip the flag default on. |
| 2.32 | 2026-07-22 | — | **Goalkeeper #11 + Heading #10 engine integration, Phase 1 (opt-in)** — the GK/Heading attribute projections landed with a live consumer, per the new converged supplement `docs/tracking/gk-heading-engine-integration-design.md`. `MatchEngine.cs` v1.44: boot constructs both sealed orchestrators + four stateless nested ball/RNG adapters (`HeadingBallWorldAdapter`/`GoalkeeperBallWorldAdapter`/`HeadingRngWorldAdapter`/`GoalkeeperRngWorldAdapter`) and registers `heading.mechanics` + `goalkeeper.mechanics` RNG streams (the card-severity precedent; `SubsystemOrdinals.HeadingMechanics`=6 / `GoalkeeperMechanics`=7, `entityId: -1`). Public `EnableGkHeading()` opts in (default off — byte-identical to pre-wiring, no `SNAPSHOT_SCHEMA_VERSION` change); when on, `DriveGkHeadingTactical` (10 Hz, in `RunAiPhase`: baselines → `TacticalTick` → §4 triggers) + `DriveGkHeadingPhysics` (60 Hz, in `RunPhysicsPhase` before the Resolve goal check: `Update` on both) run, and the §4 Stage-0 world-state triggers commit a `SaveIntent` seeded from `PlayerAttributeProjection.ToGoalkeeper` (loose on-target ball near the defended goal) + a `HeaderIntent` from `ToHeading` (nearest agent to a loose airborne ball) — the projections' live consumer. RNG adapters draw the single per-subsystem stream via `DrawStreamFloat01`/`DrawStreamGaussian`, accept-and-ignoring the draw-site id (the stub posture). `CaptureDurableHeader`/`CaptureDurablePayload` fail loud (`NotSupportedException`) when on (Phase 1 not snapshot-safe); `SerializeWorldState` untouched. `PlayerAttributeProjection.cs` v1.2: `ToGoalkeeper` (int→float widen of Reflexes/Handling/Composure/Strength/Aerial/Balance/OneVsOne/Pace/Throwing/Kicking) + `ToHeading` (raw Heading/Strength/Balance) + runtime TeamId/Fatigue (KD-P4); the KD-P8 "deliberately ABSENT" note removed. `MatchEngineConstants.cs` v1.25 (+6 `[GT]` trigger constants). `match-engine.asmdef` + `match-engine-tests.asmdef` gain the HeadingMechanics + GoalkeeperMechanics refs. Because the orchestrator CLASS names collide with their own NAMESPACES (CS0118), `_heading`/`_goalkeeper` are fully qualified at decl + construction. Code self-AR (verified against the gate): CS0118 fully-qualification; `RefreshGkAgentIds` each drive (ConfigureSquads reassigns `_isGoalkeeper`, so the boot keeper roster would go stale + GK subs move the slot); guard on the durable-capture seams not `SerializeWorldState`. New `tests/MatchEngineGkHeadingTests.cs` (8 — flag semantics; flag-off default determinism + commits-nothing; save/header commit the projection; distinct-squad roster GK Pace=19 flows through; flag-on forward determinism; durable-capture fails-loud-on / succeeds-off) + `PlayerAttributeProjectionTests.cs` v1.1 (+2 field-scale). **Full dotnet gate run locally (SDK 8.0.129 via apt): PASSED, 0 failures (whole tree green; 290 match-engine tests). Default engine byte-identical (the 279-test snapshot/determinism/restore suite unchanged).** Phase 2 (deferred): serialize the two RNG cursors + both orchestrators' in-flight state (`SNAPSHOT_SCHEMA_VERSION` 17 → 18) + flip default on + digest rebaseline; DT-driven producer; `CollisionConsumer` duel fan-out; the closed-loop `#19 ScenarioRunner` scenario. |
| 2.31 | 2026-07-22 | — | **Unified season save landed — snapshot-deserialize N2 / match-engine Phase G-Phase 3 season save-file root**, per the new converged supplement `docs/tracking/unified-season-save-design.md` v0.4 (AR-1 0H+2M+3L → AR-2 0H+0M+1L CONVERGENCE) + a follow-up code AR over the shipped diff (0H+0M+2L, both fixed — L-1 `MatchSaveManager.Save` engine-before-path guard order restored behaviour-identical (`MatchSaveManager.cs` v1.2); L-2 the R4 no-match-with-provider test lock, 18 → 19). New `src/season-save/` assembly `TacticalDirector.SeasonSave` (references `TacticalDirector.MatchEngine` + `TacticalDirector.LivingWorld` + `TacticalDirector.DeterministicSim`) — the composition/persistence layer **above both**, the only assembly that may see both blobs, which is how **FR-LW-003** is honoured (verified: neither `match-engine` nor `living-world` references the other; the season root sits above, the `match-viewer`-over-`match-engine` layer class). Five files: `SeasonSaveConstants.cs` (`[FIXED] SEASON_SAVE_FORMAT_VERSION = 1` — a FOURTH format version, distinct from the two snapshot schema versions + `MATCH_SAVE_FORMAT_VERSION` + `WORLD_STORE_FORMAT_VERSION`), `SeasonSaveBlobs.cs` (the deframe result — two opaque byte sub-blobs, `MatchBlob` null when no match), `SeasonSaveCodec.cs` (pure static: `byte[] Encode(worldBlob, matchBlobOrNull)` / `SeasonSaveBlobs Decode(byte[])`; a `SEASON_SAVE_FORMAT_VERSION`-gated frame around a `matchPresent` flag byte + two length-prefixed opaque sub-blobs — the codec never parses either sub-blob (each keeps its own version gate); overflow-safe `Require(offset, need, total)` bound + fail-loud on null blob / bad format version / bad `matchPresent` flag / out-of-bounds length prefix / trailing bytes — the `MatchSaveCodec` posture), `SeasonSaveContents.cs` (the `Load` result: reconstructed `WorldStore` never-null + nullable `MatchEngine`), `SeasonSaveManager.cs` (static: `Save(WorldStore world, MatchEngine matchOrNull, string path)` = capture both (`world.Snapshot()` + `MatchSaveManager.Encode(engine)`) fully before opening the file → `SeasonSaveCodec.Encode` → atomic temp→fsync→rename (§4.6.1.1 re-implemented); `Load(string path, ISquadProvider squads = null) → SeasonSaveContents` = `ReadAllBytes` → `SeasonSaveCodec.Decode` → `WorldStore.Restore(worldBlob)` +, when `MatchBlob` present, `MatchSaveManager.Restore(matchBlob, squads)`). **KD-2:** the file is a thin frame over two SELF-CONTAINED existing blobs — `WorldStore.Snapshot()` (always) + the match save blob (only when a match is in progress, KD-3) — so all four inner versions are untouched (no `SNAPSHOT_SCHEMA_VERSION` / `WORLD_STORE_FORMAT_VERSION` / `MATCH_SAVE_FORMAT_VERSION` change; the season file only adds the fourth frame version). **KD-5:** `MatchSaveManager.cs` v1.1 gains public `Encode(MatchEngine engine) → byte[]` (capture + `MatchSaveCodec.Encode`) + `Restore(byte[] blob, ISquadProvider squads = null) → MatchEngine` (`MatchSaveCodec.Decode` + `RestoreFromSnapshot`) — the "match save as a value" blob API the season root composes without reaching the internal capture seams; `Save`/`Load` refactored to delegate (behaviour-identical — all 279 match-engine tests still green). **KD-6:** the `ISquadProvider` is a Load-time parameter, never persisted, threaded into `MatchSaveManager.Restore` only when a match blob is present (a neutral / no-match season ignores it); the match restore's fingerprint + MXCSR float-mode gates run on season `Load` unchanged. New `season-save/tests/SeasonSaveManagerTests.cs` (19): disk round-trip determinism for a no-match season (world field-identical via re-`Snapshot()` byte-equality + `world.text` resumes; `Match` null) and a season with a neutral / distinct-squad match via `ISquadProvider` (match digest chain byte-identical AND world field-identical, both through one file — the distinct-squad case exercises the provider threading through season `Load`), `SeasonSaveCodec` round-trip (with match / without match / empty world) + all fail-loud gates (null world-blob, null blob, wrong format version, bad `matchPresent` flag, oversize world length, truncated match block, trailing bytes), and `SeasonSaveManager` missing-file / corrupt-file / distinct-squad-no-provider / null-world / overwrite-atomically. The booking-cursor (KD-8) case is a nested-match-blob property already locked by `MatchSaveManagerTests` (needs a match-test-internal cursor seam), not re-driven at the season level — the season frame provably cannot alter it (KD-2). No asmdef change to `match-engine` / `living-world` (the layering is entirely in the new `season-save.asmdef`). **Full dotnet gate run locally (SDK via apt): PASSED, 0 failures (whole tree green; 19 new season-save tests). This closes snapshot-deserialize Phase 3 / the unified season save.** |
| 2.30 | 2026-07-21 | — | **On-disk match save format landed — snapshot-deserialize Phase 3 `SaveManager` fold (N1)**, per the new converged supplement `docs/tracking/match-save-file-design.md` v0.3 (AR-1 0H+3M+2L → AR-2 0H+0M+1L CONVERGENCE). Three new `src/match-engine/` files: `MatchSaveCodec.cs` (pure static byte codec — `byte[] Encode(matchSeed, header, payload)` / `MatchSaveContents Decode(byte[])`; a single `MatchEngineConstants.MATCH_SAVE_FORMAT_VERSION` [FIXED]=1-gated blob = the KD-7 boot-`matchSeed` boot-header + the `SnapshotHeader` incl. its `EnvironmentFingerprint` (6 `ValidateAgainst` fields) + digest chain + cursor + the `SnapshotPayload`, all via `CanonicalSerializer`; fail-loud decode on version mismatch / an out-of-bounds length prefix / trailing bytes — the `WorldStateSerializer.ReadCount` posture — with an overflow-safe `Require(offset, need, total)` bound guard), `MatchSaveContents.cs` (the decode-result readonly struct: `MatchSeed` + `Header` + `Payload`), and `MatchSaveManager.cs` (static: `Save(engine, path)` = capture → Encode → atomic temp→fsync→rename (the §4.6.1.1 contract; re-implemented, since `SaveManager` exposes no raw-blob write) and `Load(path, ISquadProvider squads = null) → MatchEngine` = ReadAllBytes → Decode → `RestoreFromSnapshot`). `MatchEngine.cs` v1.43: new public `MatchSeed` property (the boot seed the payload does not carry — KD-2/KD-7) + the two durable-capture seams promoted `TestOnly_CaptureDurableHeader/Payload` → production internal `CaptureDurableHeader/Payload` (now consumed by `MatchSaveManager`; `MatchEngineSnapshotRestoreTests` repointed to the production names). `MatchEngineConstants.cs`: `MATCH_SAVE_FORMAT_VERSION` [FIXED]=1 (a THIRD version, distinct from the #16 `SnapshotHeader` framing schema and the match-engine body `SNAPSHOT_SCHEMA_VERSION`). **Decisions made at this N1 root** (deferred to it by snapshot-deserialize KD-7 O1 / O3): the file carries the boot `matchSeed`; the `EnvironmentFingerprint` is serialized so the KD-6 float-mode gate runs end-to-end through disk (a save under `CreateStage0Dev` validates on load, a tampered/foreign fingerprint is rejected — the on-disk header no longer writes `Fingerprint = null` the way the deterministic-sim `SaveManager` does); the `ISquadProvider` is a Load-time parameter, never persisted (the file references rosters by ClubId). No `SNAPSHOT_SCHEMA_VERSION` change (a file frame around the unchanged reader/writer); no asmdef change (DeterministicSim + PlayerDatabase already referenced). New `tests/MatchSaveManagerTests.cs` (16 tests): disk round-trip determinism (neutral / booking-before-save / distinct-squad via provider), `MatchSaveCodec` round-trip + fail-loud (bad format version, oversize payload length, trailing bytes, truncation, null blob, tampered-fingerprint→restore refusal), `MatchSaveManager` missing-file / corrupt-file / no-provider / null-engine / overwrite-atomically. Code self-adversarial review: 1 hardening (the `Require` bound guard rewritten overflow-safe — a corrupt near-`int.MaxValue` length prefix could wrap `offset + need` negative and slip past the guard; now compares against `total − offset`), locked by `Codec_OversizePayloadLength_FailsLoud`. **Full dotnet gate run locally (SDK via apt): PASSED, 0 failures (279 match-engine tests; whole tree green).** Remaining Phase 3: the native MXCSR live-mode query (host-blocked native interop; the KD-6 seam now has a through-disk consumer) + the N2 unified season save (FR-LW-003 + season save-file root). |
| 2.29 | 2026-07-20 | — | **Snapshot-deserialize Phase 2 landed — distinct-squad restore re-projection (#27 T3 / KD-3).** New public `ISquadProvider` (`src/match-engine/ISquadProvider.cs`, the caller-supplied `ClubId → Squad` resolver) threaded into `MatchEngine.RestoreFromSnapshot(…, ISquadProvider squads = null)` (`MatchEngine.cs` v1.42). New `ReprojectDistinctSquads` replaces the Phase-1 distinct-squad fail-loud: neutral fast-path returns immediately; each team with a non-sentinel `_rosterClubId` resolves its roster (ClubId-check + `ValidateSquadSize` + `ValidateSelectedRecords`, both teams before any apply — the `ConfigureSquads` validate-both-before-write rule), re-runs `LineupSelector.Select` + `PlayerAttributeProjection` for the base lineup (`ReprojectBaseLineup` — attribute arrays + the bench GK flags `_benchIsGoalkeeper`, a boot-constant NOT serialized; NOT the on-pitch `_isGoalkeeper`, which is serialized and already restored — so a substituted slot keeps its restored bench-GK flag), then replays the substitutions the serialized `_activeBenchSlot` records (`ReprojectSubstitutions`, the attribute half of `SubstitutePlayer`). Fail-loud on absent provider / unresolvable ClubId (`NotSupportedException`) or mismatched returned ClubId (`InvalidOperationException`). Determinism rests on the provider returning the SAME roster the saved match loaded (`LineupSelector` + `PlayerAttributeProjection` are pure). New `TestOnly_BenchIsGoalkeeper` seam; `MatchEngineSnapshotRestoreTests` v1.1 gains distinct-squad G3 round-trip (base / mid-match sub / post-restore sub / post-restore keeper-for-keeper sub) + the three provider fail-loud gates. No `SNAPSHOT_SCHEMA_VERSION` change. **Full dotnet gate run locally (SDK via apt): PASSED, 0 failures (263 match-engine tests; whole tree green).** Implementation finding: `_benchIsGoalkeeper` is NOT serialized (only on-pitch `_isGoalkeeper` is), so it is re-projected here for post-restore keeper substitutions — locked by the keeper-for-keeper test. Out-of-scope discovery (a Phase-1 snapshot-completeness follow-up, root `CLAUDE.md` OPEN ISSUES): a post-restore substitution that FLIPS a slot's GK status (keeper onto an outfield slot — never done in realistic play) diverges via a Positioning-AI (#12) formation-slot interaction with the flag flip. Files: `ISquadProvider.cs` (new), `MatchEngine.cs` v1.42, `tests/MatchEngineSnapshotRestoreTests.cs` v1.1, `snapshot-deserialize-design.md` v0.8, root `CLAUDE.md` OPEN ISSUES, `file-manifest.md`. |
| 2.28 | 2026-07-19 | — | **FR-PO-052 kickoff perf baseline promoted PENDING → CERTIFIED** — the certified-capture follow-up to v2.27 (ERR-016-006 Option A). Now that `CreateStage0MonoCertified` exists, the 2026-07-19 pinned-host 100-run capture (p50=0.4768 / p99=2.5669 ms/tick) has a complete `SessionManifest`: field 2 (host Mono version) recorded `mono-bundled-unity6000.4.9f1` (Unity versions its forked Mono by editor release) ⇒ `floatModelHash 73c47ad5…b6bb756a`, computed by `FloatFlagTuple.ComputeHash()` and independently reproduced by a Python mirror validated byte-for-byte against the pinned golden vector (`89f50a31…` for the `"6.13.0"` test input) before recording. Code: `CertifiedPerfBaselineTests.cs` v1.1 — `KickoffPending()` → `KickoffCertified()` (real manifest via `CreateStage0MonoCertified` + `Certified(...)`), + tests `IsCertified_CarriesRealMetric` / `CarriesRealMonoFloatModelHash_NotPlaceholder` / `BuildsRecord` / `SelfComparePasses`; the generic `Pending()`/`Certified()` API tests (incl. `Pending_RejectsEmptyArguments`) kept since the `Pending()` factory remains. Docs: `kickoff-multi-second.cert.md` v1.2 (Status ✅ CERTIFIED, OPEN GAP section rewritten as resolved + a Residual section). **`MatchEngineCapstoneTests` deliberately UNCHANGED:** the Linux capstone perf gate keeps its generous Stage-0 anchor — comparing a Linux measurement against the Windows-certified 0.4768 ms via +5% is apples-to-oranges and would flake CI; the certified record's FR-PO-031 usability is proven by the self-compare in `CertifiedPerfBaselineTests` instead (an honest deviation from a literal reading of runbook Step 3.3, documented in the .cert.md + runbook). **Scope: FR-PO-052 perf baseline only** — the §4.8.2 runtime MXCSR validation (native interop, unbuilt) and the platform-determinism KAT run stay host-blocked, so `certification-platform.md` remains ⏳ RECERT REQUIRED. Owner sign-off recorded via the PR merge. (dotnet gate not runnable in this environment — the hash is golden-vector-validated by the Python mirror; suite verified in CI on push.) |
| 2.27 | 2026-07-19 | — | **ERR-016-006 Option A landed (owner sign-off) — live-host floatModelHash hasher + §4.8.3 Mono mapping.** New `deterministic-sim/FloatFlagTuple.cs` (`ComputeHash()` = `SHA-256(SerializeCanonical(0x14 ‖ tuple))` — the §4.8.3 hasher that did not exist) + `EnvironmentFingerprint.cs` v1.3 (`CreateStage0MonoCertified(monoRuntimeVersion)` builds a genuine non-placeholder fingerprint from the Option-A Mono fields + the §4.8.3 pinned Stage-0 flag values; `Stage0Mono*`/`Stage0SimdLevel` consts; `monoRuntimeVersion` host-supplied, not synthesised). Spec reconciled to the Mono backend: `deterministic-sim/section-4.md` v1.1 (§4.8.3 field 1 +`"Mono"`, field 4 accepts `"MONO"` at Stage-0 with reject-MONO/IL2CPP-required → Stage 5+, Mono-mapping paragraph) + `section-5.md` v1.1 (§5.5 row 0 backend → Mono, §5.5.1 Mono note). Tests: golden vector `89f50a31…f343e7` (Python-mirror-verified) + determinism/per-field-sensitivity + `CreateStage0MonoCertified` non-placeholder/real-hash/null-guard, in `DeterministicSimTests`. ERR-016-006 status → spec-resolved; proposal `env-fingerprint-float-model-hash-mono-mapping.md` v0.2 APPROVED. **Host-blocked remainder (NOT done):** the §4.8.2 runtime MXCSR validation (native interop) + the certified capture (real Mono version + pinned Unity/Mono host; `⏳ RECERT REQUIRED`). Files: `FloatFlagTuple.cs` v1.0 (new), `EnvironmentFingerprint.cs` v1.3, `tests/DeterministicSimTests.cs`, `section-4.md`/`section-5.md` v1.1, `spec-error-log.md` (ERR-016-006), `env-fingerprint-float-model-hash-mono-mapping.md` v0.2, root `CLAUDE.md` OPEN ISSUES, `file-manifest.md`. (dotnet gate not runnable in this environment; the hasher is pure and golden-vectored — verified by manual review + Python mirror.) |
| 2.26 | 2026-07-19 | — | **ERR-016-006 filed + code-side honesty fix; §4.8.3 Mono-mapping proposal opened (no live hasher).** `EnvironmentFingerprint.cs` v1.2: `CreateStage0Dev()` `simdFeatureLevel` `"SSE2"` → `"SSE4.2"` (matched no pin — the certification-platform.md baseline is SSE4.2); the placeholder `floatModelHash` lifted to a named `FloatModelHashDevPlaceholder` sentinel + a new `IsDevPlaceholder` property so a future certification-run gate can reject a placeholder fingerprint (the analogue of §4.8.3's "reject MONO" rule for the still-unimplemented float-model hasher); `FloatModelHash`/`CreateStage0Dev` docs now flag the missing live §4.8.3 hasher and the IL2CPP-vs-Mono spec contradiction. **No live-host hasher added, no §4.8.3 spec edit** — the §4.8.3 tuple (fields 1–4: compilerToolchain/compilerVersion/targetTriple/il2cppVersion) is IL2CPP-shaped and §5.5 requires IL2CPP, contradicting the Stage-0 Mono pin (§4.8.3/§5.5 predate the June-7 platform pin); synthesising those fields under Mono would be fabrication. The gap is filed as **ERR-016-006** (`spec-error-log.md`) + a root-`CLAUDE.md` OPEN ISSUES entry, and the resolution is drafted for owner sign-off in `docs/tracking/env-fingerprint-float-model-hash-mono-mapping.md` (v0.1 — options A/B/C, recommend A; Deterministic-Sim + Platform-Cert owners). Latent, not live: `SaveManager` writes the header `Fingerprint = null` and the fingerprint is load-bearing only at a real cert run (itself `⏳ RECERT REQUIRED`, no Unity host). New test `EnvironmentFingerprint_Stage0Dev_IsPlaceholder_AndMatchesPinnedSimdLevel` (locks IsDevPlaceholder + the SSE4.2 value) + the T-DS-FAULT-013 `live` fingerprint realigned to differ only in `workerCount` (it now matches the dev SSE4.2/sentinel on the other fields, keeping the mismatch-isolation honest). Files: `EnvironmentFingerprint.cs` v1.2, `tests/DeterministicSimTests.cs`, `spec-error-log.md` (+ERR-016-006), root `CLAUDE.md` OPEN ISSUES, `env-fingerprint-float-model-hash-mono-mapping.md` v0.1 (new), `file-manifest.md`. (dotnet gate not runnable in this environment — no `EnvironmentFingerprint` behaviour change for any process that never runs a certified capture; verified by manual review.) |
| 2.25 | 2026-07-17 | — | **T1/T2 repeat adversarial review (AR-4 + AR-5 sweep) — 1M+4L total, all doc-only, all fixed; CONVERGENCE, cycle closed** — see the v2.25 Last-Updated header entry. M-1: `AttackingAgentSnapshot.cs` v1.1 (Pace/Dribbling docs aligned to the live KD-P3 ÷ATTRIBUTE_MAX writer convention; the (raw−1)/19 math switch stays a recorded deferred question). L: `MatchEngine.cs` v1.38 (three stale neutral-placeholder comments), `MatchEngineConstants.cs` v1.22 (STAGE0_NEUTRAL_* stale ERR-007 TODOs retired — production-unconsumed since T1, retained as KD-P7 references), `PlayerAttributeProjection.cs` v1.1 (ToNormalized note), `ConfigureSquads` extra-players doc note (AR-5). Verified clean, no change: projected-array writer completeness, 3-site FirstTouchAbility inventory, Perception non-mutation of `_perceptionAttrs`, DT internal normalization (KD-P2), #13 WeakReceiver distinct-squad activation (default path unchanged). Full dotnet gate re-run: PASSED, 0 failures. |
| 2.24 | 2026-07-17 | — | **Squad/Player Data Layer T1/T2 landed** — see the v2.24 Last-Updated header entry for the full description. New `src/match-engine/PlayerAttributeProjection.cs` (pure canonical→per-spec projections; KD-P1 derived KickPower; KD-P3 `÷ATTRIBUTE_MAX` normalization; KD-P8 no GK/Heading phantom targets); `MatchEngine.cs` v1.37 (`_canonicalAttrs`/`_benchCanonicalAttrs` + every seeding site converted + public `ConfigureSquads` + `SubstitutePlayer` canonical swap/re-projection + 6 TestOnly seams; no schema change — default path byte-identical, KD-P7; distinct-squad restore deferred to T3, KD-P10); `match-engine(.tests).asmdef` +PlayerDatabase. Self-AR: 1 M fixed (both-squads validate-before-write) + clean sweeps. New: `tests/PlayerAttributeProjectionTests.cs`, `tests/MatchEngineSquadTests.cs`. Docs: projection design v0.4 (Technique inventory addition), #27 design v0.5 + `PlayerAttributes.cs` v1.1 (KD-P9 reserved→consumed). Full dotnet gate: PASSED, 0 failures. |
| 2.23 | 2026-07-17 | — | **Fourth repeat adversarial review (AR-4): 0H+0M+1L doc-only, fixed — CONVERGENCE, cycle closed** — see the v2.23 Last-Updated header entry. Complete sent-off participation matrix walked clean (incl. the in-flight executor composition: card ⇒ `ApplyRestart` clears possession before the executor advance; adapters read the live `_possessingAgentId`, so FM-08/FM-05 cancel at CONTACT). L: `_lastHolderAgentId` writer comment aligned to the last-settled-holder approximation (deflection-chain goal may credit a non-kicker, possibly sent off since). Files: MatchEngine.cs v1.36 (doc-only), file-manifest.md, root CLAUDE.md. Full dotnet gate re-run: PASSED, 0 failures. |
| 2.22 | 2026-07-17 | — | **Third repeat adversarial review (AR-3 of the July-16 cycle): 1M, fixed** — see the v2.22 Last-Updated header entry. M-1: `ApplyFoulIfCaptured` discards a foul candidate when either participant is sent off (pre-fix a frozen red-carded agent repeatedly won free kicks and drew cards against opponents who ran into them — the foul interpretation was the remaining participation surface without the `_isSentOff` gate; physical collision response deliberately unchanged). Gated at the application site (timing-equivalent; covers the injection seam; single gate avoids sibling drift). Files: MatchEngine.cs v1.35, tests/MatchEngineFoulCardTests.cs v1.1 (+2), file-manifest.md, root CLAUDE.md. Full dotnet gate re-run: PASSED, 0 failures. |
| 2.21 | 2026-07-16 | — | **Repeat adversarial review (AR-2 of the July-16 cycle): 1M+1L, both fixed** — see the v2.21 Last-Updated header entry. M-1: `_isSentOff` exclusion added to `RunFirstTouch`'s receiver scan (a sent-off agent could receive the ball into un-releasable possession, deadlocking play); regression-locked against the exact CONTROLLED-receive geometry. L (doc): `AttrIdx` Technical group-count comment 8 → 7. Files: MatchEngine.cs v1.34, tests/MatchEngineFirstTouchTests.cs v1.1 (+1), AttrIdx.cs v1.1, file-manifest.md, root CLAUDE.md. Full dotnet gate re-run: PASSED, 0 failures. |
| 2.20 | 2026-07-16 | — | **Adversarial-review fix pass over the v2.17/v2.18/v2.19 landings (2M+4L, all fixed)** — see the v2.20 Last-Updated header entry for the full description. M-1: `SubstitutePlayer` resets the slot's yellow-card count (discipline attaches to the player, not the slot; pre-fix a substitute inherited the outgoing player's booking and was sent off on their own first yellow). M-2: `SquadFileLoader` age range-checked to [AgeMin, AgeMax] (was the one unbounded numeric key). L: post-full-time `SubstitutePlayer` refusal (queued event could never flush); `RestartResolver` lastTouchTeam doc aligned to the actual caller input (last settled holder, not last toucher); live-viewer clock rounds seconds before the minute split (the `m:60` class `HtmlReplayExporter` AR-1 fixed, reintroduced); `LiveMatchServer` post-Stop connection threads answer 503 via a volatile shutdown flag; `RosterGenerator.DrawBounded` modulo-bias doc note. Files: MatchEngine.cs v1.33, RestartResolver.cs v1.1, tests/MatchEngineSubstitutionTests.cs v1.1 (+2), LiveMatchServer.cs v1.1, SquadFileLoader.cs v1.2, tests/SquadFileLoaderTests.cs v1.1 (+2), RosterGenerator.cs v1.2, file-manifest.md, root CLAUDE.md. |
| 2.19 | 2026-07-15 | — | **Squad/Player Data Layer T0 landed** (`docs/tracking/squad-player-data-design.md`, new — candidate spec #27, design-supplement stage, Stage-1-forward pull mirroring the #21/#22 precedent). New `src/player-database/` assembly (`TacticalDirector.PlayerDatabase`; references only `TacticalDirector.DeterministicSim`): `PlayerDatabaseConstants.cs` (Fixed/Derived/GT catalogue incl. the `[4][31]` position-bias table), `AttrIdx.cs` (ordinal mapping shared by the array round-trip / generator / loader), `PlayerAttributes.cs` (canonical 31-field `[1,20]` record reconciling `AgentMovement.PlayerAttributes` / `DtAgentAttributes` / `PerceptionAgentAttributes` / `PassAgentAttributes` / `ShotAgentAttributes` / `HeadingAgentAttributes` / `GoalkeeperAgentAttributes` + `WeakFootRating` on its own `[1,5]` scale — the natural place to finally close the long-open `ERR-007` gap, once a later T-phase wires the projections), `PlayerPosition.cs` (coarse 4-value enum, deliberately not positioning-ai's 13-value `RoleId`), `PlayerRecord.cs`/`Squad.cs` (club-scoped roster container, `CLUB_SQUAD_SIZE`=25 per master plan §4.2 — distinct from the unrelated `MatchEngineConstants.SQUAD_SIZE`=22), `RosterGenerator.cs` (deterministic over `DeterministicRngService`; stateless — caller registers the stream, so it's unit-testable without booting a match; new `DOMAIN_TAG_PLAYER_DATABASE`=0x1F + `SubsystemOrdinals.PlayerDatabase`=81 back-propped into `deterministic-sim`, off-pitch band alongside `LivingWorld`=80), `SquadFileLoader.cs` (Stage-0 human-authoring text import, grammar mirrors `TeamTacticFileLoader` exactly). 31 new tests across 3 suites (`PlayerAttributesTests`/`RosterGeneratorTests`/`SquadFileLoaderTests`). Design doc self-adversarial-reviewed to convergence over 2 rounds (AR-1: corrected a club-identity-vs-match-`teamId` conflation in the RNG-stream-keying/`PlayerId` draft via KD-3; trimmed the canonical attribute list to "consumed by an existing spec" ∪ "reserved, master-plan-only, same pattern as `DtAgentAttributes.Crossing`"; isolated `WeakFootRating`'s `[1,5]` scale from the `[1,20]` clamp/array helpers. AR-2: position-bias-table test strategy switched to direct constant-value assertions rather than statistical sampling over generated squads). Code adversarial review over 2 passes caught 3 real defects before landing: (1) `PlayerRecord.Position` had no RNG draw at all in the first implementation pass — `FIELDS_PER_PLAYER` undercounted 35→36, `IDENTITY_DRAWS_PER_PLAYER` 4→5; (2) `WeakFootRating`'s jitter reused `AttributeSpread`(±4) against its own much narrower `[1,5]` range, clamping roughly 6 of every 9 draws to the boundary instead of spreading around `WeakFootBase` — given its own `WeakFootSpread`=2 (exactly spans `[1,5]`, no clamping); (3) `SquadFileLoader.BuildPlayer`'s identity default computed `PlayerId` from the raw section-local index (`PlayerRecord.CreateDefault(localIndex)`) instead of the club-scoped formula `RosterGenerator` uses (design doc KD-3) — caught by a round-trip test (`Parse_FullRecord_RoundTrips`) that would have failed against the bug; fixed by threading `clubId` into `BuildPlayer`. Also flagged (documented, not yet triggered): `PlayerDatabase.PlayerAttributes` shares its bare type name with the pre-existing, unrelated `AgentMovement.PlayerAttributes` — no collision today since nothing references this new assembly yet, but the exact CS0104 class this file's own v1.73 row hit (`TacticTranslation`) will recur the moment a future T-phase (T1) wires both into `MatchEngine`; noted in the design doc §4 as a hazard for that pass to fully-qualify from the start. **Deliberately NOT built in this pass**: any `MatchEngine` wiring (T1 — replacing `CreateDefault()` seeding is intentionally NOT behaviour-neutral, unlike a typical T0, since the entire point is giving agents distinct attributes, so it needs its own reviewed change, not a silent default-identity landing), the per-spec projection updates that would close `ERR-007` for real (T2), a snapshot roster-reference field (T3), on-disk save-format squad persistence / transfer market / aging (Stage 1+, master plan §4.3/§4.4, explicitly out of scope per the design doc's own §0). Full dotnet gate not runnable in this environment (`apt-get install dotnet-sdk-8.0` 404s against the mirror, consistent with every prior entry) — verified by exhaustive manual review in place of `dotnet test`. |
| 2.18 | 2026-07-15 | — | **Interactive match view landed** (`docs/tracking/interactive-match-view-design.md`, new) — upgrades the passive post-hoc HTML replay into a live-updating viewer watched *during* a real match (full in-Unity rendering stays blocked on Unity host access per the existing OPEN ISSUE; this is the "at minimum a live-updating viewer" floor). `MatchEngine.cs` v1.32: 3 trivial read-only observation-surface properties (`HomeScore`/`AwayScore`/`MatchEnded`), same section as the v1.24 `BallView`/`AgentView` surface — no schema change. New `src/match-viewer/`: `LiveMatchFrame.cs` (one captured live frame); `LiveMatchStreamer.cs` (paces a real `MatchEngine` at wall-clock speed on a background thread via a drift-corrected Stopwatch scheme; the only thing that ever calls `RunTick()`; lock-protected latest-frame handoff; pause/resume/speed; auto-pauses at full time; `TickOnce`/`ApplyCapturedFrame` internal seams for deterministic tests); `LiveMatchServer.cs` (loopback-only hand-rolled HTTP server over `TcpListener` — no package dependency — serving `GET /` the viewer page, `GET /frame` polled JSON, `GET /control` playback-only pause/resume/speed; never holds a `MatchEngine` reference, only a `LiveMatchStreamer`, so the "can mutate the match" and "reachable by an HTTP request" surfaces stay disjoint by construction). New `AssemblyInfo.cs` (`InternalsVisibleTo` the test assembly). Design went through 2 self-review rounds to convergence before implementation (catching, among other things, that the control endpoint must never become a gameplay-mutation channel, and that naive per-tick `Thread.Sleep` pacing would accumulate drift over a 90-minute match); the implementation was then itself adversarially reviewed over 2 passes, catching and fixing: an identical `Start()`/`Stop()` race in both `LiveMatchStreamer` and `LiveMatchServer` (the running-state flag flipped true inside the lifecycle lock before the background thread was actually assigned, so a `Stop()` racing into that window could join a null thread while a fresh thread got spawned against an already-stopped listener); two missing `using TacticalDirector.MatchEngine;` directives that would not have compiled; a default-port constructor parameter that illegally referenced a config-resolved `static readonly` constant (the same constraint `MatchReplayRecorder`'s own AR-2 M-3 fix already hit); and a test needing an `internal` engine seam this assembly has no `InternalsVisibleTo` access to (resolved by splitting `TickOnce` into `TickOnce` + `ApplyCapturedFrame`). New tests: `LiveMatchStreamerTests.cs`, `LiveMatchServerTests.cs` (real-loopback-socket routing), plus small additions to `MatchEngineGoalTests.cs`/`MatchEngineMatchFlowTests.cs` locking the new public properties. Full dotnet gate not runnable in this environment (no SDK reachable — `apt-get install dotnet-sdk-8.0`/`10.0` both 404 against the mirror) — verified by exhaustive manual review in place of `dotnet test`. |
| 2.17 | 2026-07-14 | — | **Match-flow completion landed** (see the v2.17 Last-Updated header entry for the full description): throw-ins/corners/goal kicks (`RestartResolver.cs` new + `MatchEngine.CheckRestartAndApply`/`ApplyRestart`), fouls/cards (`MatchFlowCollisionConsumer` + `match-flow.card-severity` RNG stream + `DetermineCardKind`/`ApplyCardAndCheckSentOff`), offside (`OffsideEvaluator.cs` new + `EvaluateAndApplyOffside` hooked into `RunFirstTouch`), substitutions (public `SubstitutePlayer` + `SubstitutionReason.cs` new + AR-5 pending-event queue), half-time/full-time (`CheckMatchFlowTransitions`, called every Input phase — half-time resets the ball only, no ends-swap per AR-4; full-time sets `_matchEnded`, freezing AI/Physics/Resolve). Three new Tier A events (`OffsideCalledEvent` 0x18 / `RestartAwardedEvent` 0x19 / `MatchPhaseChangedEvent` 0x1A, `EventRegistry.cs` v1.8). `MatchEngine.cs` v1.31; **SNAPSHOT_SCHEMA_VERSION 14 → 15** (discipline/substitution/match-flow-clock state). New tests: `MatchEngineRestartTests`/`MatchEngineOffsideTests`/`MatchEngineFoulCardTests`/`MatchEngineSubstitutionTests`/`MatchEngineMatchFlowTests`; `MatchEngineSnapshotSchemaTests` v1.12 (pin 15 + 2 probes). Design doc `docs/tracking/match-flow-completion-design.md` (new, v2.0) went through AR-1..AR-6 to convergence; the implementation was then itself adversarially reviewed to convergence, catching (among other things) an `OffsideEvaluator` degenerate-input bug (see the header entry) fixed before landing. Full dotnet gate not runnable in this environment (no SDK access) — verified by exhaustive manual review instead. |
| 2.15 | 2026-07-11 | — | **Engine substrate + #26 activation** (see the v2.15 Last-Updated header entry for the full description): `MatchEngineConstants` v1.20 (MATCH_LENGTH_MINUTES / MATCH_TICKS_TOTAL / HALF_TIME_BOUNDARY_TICK; SNAPSHOT_SCHEMA_VERSION 13 → 14), `MatchEngine.cs` v1.30 (Resolve-phase CheckGoalAndRestart + _goals/_lastHolderAgentId + v14 serialization + RunManagerDecisionPoints live inputs + 4 TestOnly seams), `ManagerDecisionGate.cs` v1.1 (half-time trigger active), `ManagerAdaptation.cs` (docs — live-input framing), `TacticalPresetsConstants.cs` v1.1 (doc — MATCH_TICKS_TOTAL allocated engine-side). Tests: `MatchEngineGoalTests.cs` v1.0 (new, 6), `ManagerAITests.cs` v1.1 (+4), `MatchEngineSnapshotSchemaTests.cs` v1.11 (pin 14 + ScoreState probe). Spec: #26 section-1 v0.3 / section-2 v0.4 / section-3 v0.3 ([CROSS] promotion) / section-9 v0.5; match-engine-design.md v1.4. Full dotnet gate: PASSED, 0 failures. |
| 2.14 | 2026-07-11 | — | **#26 T1–T4 manager-AI wiring landed**, default-behaviour-neutral (ManagerMode.Human zero-init = inert identity, KD-4). T1: `TacticalPresetsConstants.cs` (§3.5 scalars + A.2/A.3 [GT] tables; MATCH_TICKS_TOTAL absent, [CROSS-PENDING]) + `TacticPresetProjection.cs` (FM-TP-01; FR-TP-014 gate at the consuming seam). T2: `ManagerDecisionGate.cs` (FM-TP-02 — kickoff + interval; half-time gated on the engine halves model, PASS-1 M-1; evaluated only in RunAiPhase's stride branch before the FR-TI-027 commit per FR-TP-018/F5). T3: `ManagerProfile.cs` (F4 NaN-gate; A.2 factory) + `ManagerAdaptation.KickoffScore/SelectKickoffPreset` (B.1 exact; tie → lowest ordinal, KD-8) + `ApplyKickoff` (FR-TP-004 boot path via the existing appliers; seeds LastDecisionTick = 0). T4: `StepToward`/`EvaluateLadder` (B.2 exact; URGENCY_DIFF_CAP) + `RunDecisionPoint` (FR-TP-005 mid-match path, never the appliers — F3; decrement-then-check hold, the #25 precedent); the live engine call passes the engine-TRUE goalDiff = 0 so the ladder is provably inert until goal detection + MATCH_TICKS_TOTAL land (§3.4 PASS-1 M-1). `MatchEngine.cs` v1.29 (`_managerStates`, ConfigureManager, GetManagerState/SeedManagerKickoff, TestOnly_ManagerState) + `MatchEngineConstants.cs` v1.19: **SNAPSHOT_SCHEMA_VERSION 12 → 13** (per-team ManagerState, Appendix C order). New `ManagerMode.cs`/`ManagerState.cs`. Tests: `ManagerAITests` (21) + `MatchEngineSnapshotSchemaTests` v1.10 (pin 13 + ManagerState probe). Full dotnet gate: PASSED, 0 failures. Remaining: the #26 engine-substrate gates (half-time; live goalDiff/MATCH_TICKS_TOTAL) + the KD-6 on-disk preset format. |
| 2.13 | 2026-07-11 | — | **Specs #23/#24/#25 wiring landed** (the post-T0 T-phase step), default-behaviour-neutral. (a) `SlotComposer` v1.2 gains the #24 build-up overlay (Step 3b, before spacing) + #23 dismark offset (Step 4b, after spacing / before the clamp) stages per ERR-012-007/008 and the #24 §4.2 combined order; `PositioningPerceptionSnapshot` v1.1 carries the routing dials + per-agent pressure/marker carriers. (b) New `RotationController.cs` (#25 §3.1–§3.4: FM-RO-01 on the serialized `LastComposedTarget` cache, dwell/commit/hold/revert, atomic swap + partner lock, phase-exit freeze, F2/F5/F6 validating seams) wired into `PositioningAITick` v1.3 per §4.2/ERR-012-009 (identity binding never rewrites a row — pre-#25 fills byte-identical). (c) #23 §3.4 marked-pass-target penalty in `UtilityScorer` v1.10 (passer-view proximity × passer awareness; Off ⇒ exact ×1.0); `TacticalContext` v1.7 `DismarkIntensity` field; `TacticalWeights` v1.5 `TargetMarkedUtilityMult` [GT] + `MarkedPassRadiusM` [CROSS]; `decision-tree(.Tests).asmdef` +PositioningAI. (d) `MatchEngine.cs` v1.28 / `MatchEngineConstants.cs` v1.18: `SNAPSHOT_SCHEMA_VERSION` 11 → 12 — Phase-D dial writers + one-stride-stale dismark carriers, per-agent dwell update in the perception pass (FR-DM-003), #24 classify/check-then-decrement pre-pass + FM-BU-03 team-level regain arming in `OnPossessionChanged` (settledTeam diff; Balanced HoldShape never arms), v12 serialization (dwell / zone+settledTeam / rotation binding+cache+pairs / 3 dials in `WriteTeamTactic`), 9 TestOnly seams. Tests: +`SlotComposerStageTests` (7) + `RotationControllerTests` (12); `UtilityScorerTests` v1.5 (+4), `MatchEngineTacticTests` v1.5 (+5), `MatchEngineSnapshotSchemaTests` v1.9 (pin 12, +2 probes). Full dotnet gate: PASSED, 0 failures. Next: #26 T1–T4. |
| 2.12 | 2026-07-10 | — | **T0 AR-1 fix pass (0H+1M+3L, all resolved)** over the v2.11 landing. M-1: `TacticPreset` ctor snapshot-copies `Players` (retained live caller array bypassed the FR-TP-014 gate — slice-2 AR-1 M-1 / match-viewer AR-3 M-1 class) + new regression lock. L-1: the four `TacticPresetLibraryTests` composition tests gain inherited-dial == Balanced asserts (the Compose-defaults coherence was claimed but unlocked for dials some presets touch); Compose doc de-overclaimed. L-2: `TacticPreset` FR-TP-014 docs re-anchored to the consuming applier seam (no library-side validation call exists; vacuously satisfied at Stage 0, test-locked). L-3: `TeamTacticFileLoader.cs` header missing `// Modified:` (FR-CS-056). Verified clean: #23/#24 worked examples spec-exact incl. NaN-gate semantics; #25 Appendix A/D row-for-row; #26 A.1; ERR-024-001 regression holds; ordinal locks. Files: TacticPreset.cs v1.1, TacticPresetLibrary.cs v1.1, Tests/TacticPresetLibraryTests.cs v1.1, TeamTacticFileLoader.cs (header). Gate re-run: PASSED, 0 failures. |
| 2.11 | 2026-07-10 | — | **Specs #23–#26 T0 scaffolding** (all four APPROVED July 10, 2026; per the supplements' §6 step 8 / the #26 §1 T0 row), behaviour-neutral — zero-value dials are exact identities and nothing is orchestrator- or engine-wired. tactical-instructions: + `DismarkIntensity.cs`/`BuildUpStructure.cs`/`RotationFreedom.cs` enums, `TeamTactic.cs` v1.3 field appends via defaulted ctor params (ERR-021-005/006/007 code side; not serialized until each spec's wiring), + `TacticPreset.cs`/`TacticPresetLibrary.cs` (#26 T0 — five A.1 presets in the pinned ladder order, `Compose` defaults = Balanced identity), Tests + `TacticPresetLibraryTests.cs`, `EnumOrdinalStabilityTests.cs` v1.3, `FactoryIdentityTests.cs` v1.3. match-engine: `TeamTacticFileLoader.cs` v1.2 (+3 keys, omitted ⇒ identity). positioning-ai: + `MarkingDwellState.cs`/`MarkingPressureEvaluator.cs` (#23 — primitive-span signatures per the Mechanics-cannot-import-AI layering note; FR-DM-001 provenance enforced at the match-engine call seam), + `BuildUpZone.cs`/`BuildUpZoneState.cs`/`BuildUpZoneClassifier.cs`/`BuildUpOverlayCatalogue.cs` (#24), + `RotationPair.cs`/`RotationPairState.cs`/`RotationAdjacencyCatalogue.cs` (#25), `PositioningAIConstants.cs` v1.2 (+17 constants), Tests + `MarkingPressureEvaluatorTests.cs`/`BuildUpStructureTests.cs`/`RotationCatalogueTests.cs`. **ERR-024-001 (H) filed + resolved same commit:** the #24 Appendix A v0.2 row keys matched no slot in any family table (fullbacks recorded LH/RH, not wide L/R) — the catalogue was a structural no-op; re-keyed to the recorded `DefaultLane` values, spec Appendix A v0.3 + §3.2 v0.3 patched, regression test locks per-family coverage. Also #26 §2.2.2 stale ordinal list aligned to the A.1 ladder (spec v0.3). Deferred to later T-phases: SlotComposer stage insertions, RotationController, dwell/zone/pair state serialization + schema bumps, `TacticalWeights.TARGET_MARKED_UTILITY_MULT` (#8 wiring stage), preset→config projection (T1), manager decision gate/scoring (T2–T4). |
| 2.10 | 2026-07-07 | — | No code change. Two design supplements opened at `docs/tracking/advanced-positional-behaviors-design.md` (dismarking, scripted build-up structures, positional rotations — candidate specs #23–#25) and `docs/tracking/game-model-ai-manager-design.md` (tactical preset library + AI-manager selection/adaptation — candidate #26), both pre-promotion DESIGN SUPPLEMENT stage per the #21/#22 precedent. AR-1 (0H+0M+2L) + AR-2 (0H+0M+1L) + AR-3 (clean, CONVERGENCE) on the design-note text itself (citation/prefix-collision fixes verified against real `src/` source — see root `CLAUDE.md` OPEN ISSUES for detail), then both notes gained a §6 Implementation Plan (spec-promotion pipeline + T-phase code-landing sequence). Now at v0.3 / v0.4 respectively. This coding guide's tree, layer taxonomy, and rules are unaffected until one is promoted to section files and reaches its own T0 scaffolding milestone. |
| 2.9 | 2026-07-07 | — | **Tactical-theory research cross-reference: four cheap-item additions landed**, all default-behaviour-neutral — `MarkingOrientation` dial (#14 MAN_MARK radius scalar), Positioning AI #12 rest-defense coverage check (dampens risky PASS/SHOOT/DRIBBLE via `TacticalContext.RestDefenseSufficient`), half-spaces PASS bonus (`TacticalContext.AgentLane` routes the existing #12 `LaneId` into #8's `UtilityScorer`), curving-press blind-side bias (#13 `BlindSideApproach`). `SNAPSHOT_SCHEMA_VERSION` 10 → 11. AR-1 (0H+0M+1L, §7.x citation-collision renumbering) + AR-2 (clean, CONVERGENCE). See the header note above and root `CLAUDE.md` OPEN ISSUES for full detail. |
| 2.10 | 2026-07-07 | — | Tactical-theory cross-reference items (2)/(3)/(4) corrected/reverted after user review — see the v2.10 "Last Updated" header entry for the full description. Files: UtilityScorer.cs, TacticalContext.cs, TacticalWeights.cs, decision-tree.asmdef, decision-tree-tests.asmdef, Tests/UtilityScorerTests.cs, MatchEngine.cs, tests/MatchEngineTacticTests.cs, BlindSideApproach.cs + Tests/BlindSideApproachTests.cs (deleted), CoverShadowCurve.cs + Tests/CoverShadowCurveTests.cs (new), PressingAgentSnapshot.cs, PressingAITick.cs, PressingAIConstants.cs, decision-tree/section-7.md, pressing-ai/section-7.md. |
| 2.9 | 2026-07-07 | — | Tactical-theory research cross-reference: four cheap-item additions landed — see the v2.9 "Last Updated (prior)" header entry for the full description (superseded in part by v2.10 above, same day). |
| 2.8 | 2026-07-04 | — | Cross-spec **Mechanics chain** scenario corpus on the #19 `ScenarioRunner` (KD-8; owned by #19) — the first cross-spec chain beyond the physics-tier `{1,5}` `lofted-pass-kick-bounce-roll`, and the first that chains two **Mechanics-tier** orchestrators. New `src/testing-strategy/Tests/CrossSpecMechanicsScenarios.cs` + `CrossSpecMechanicsScenarioTests.cs` (owning specs **{12, 13}**, Tier B; paths under `SCENARIO_PATH_CROSS_SPEC_PREFIX`): drive the real `PositioningAITick` (#12) 10 Hz orchestrator live and read its per-tick formation-slot / line / phase output into a real `PressingAITick` (#13) snapshot each tick — the exact `FillPositioningSnapshot → Tick → FillPressingSnapshot → Tick` handoff `MatchEngine` performs. This is the composition surface the two per-spec suites each verify only their own half of: the #12 corpus ticks positioning against a static input, and the #13 corpus (v2.5) feeds pressing a **static** positioning state (never ticked, `SnapshotFactory` baseline slots). Here both real orchestrators run in sequence with #12's live output wired into #13's input each tick. **Scope caveat (adversarial-review finding):** the handoff is exercised *by construction* (the `Step()` slot/line copy into the pressing snapshot), NOT *asserted* by a predicate — the envelope checks below would still pass if that copy were removed and pressing read only its static baseline, and the determinism digest compares two identically-wired chains; the value is that the whole chain runs end-to-end deterministically, not a consumption proof. Two scenarios: `positioning-feeds-pressing` (20 ticks so the #12 phase-hysteresis + #13 debounce/role dwell advance; asserts the handoff carries real data + both halves stay well-formed — every own agent's live #12 slot is finite/non-sentinel/on-pitch, the #13 directive's `CoverShadowCount ∈ [0,2]`, `PrimaryPresserId ≥ −1`, every own pressing agent carries a defined `PressRole`) and `positioning-pressing-two-instance-determinism` (byte-identical FNV-1a-64 **composite** digest across two independent chains — #12 phase + per-agent lines AND the #13 directive + assignments; the pressing assignments are a downstream function of the #12 slots via `BaselineSlot`, so slot determinism is transitively locked without hashing raw float bits). Construction mirrors `MatchEngine` exactly: `new PositioningAITick(F442, 21)` → `SeedFromFormation(new PositioningPerceptionSnapshot(11))` (team-0 out of possession, opponent carries the ball ⇒ OOP shape) → `new PositioningAIView(pos)` → `new PressingAITick(view, new PassEventRing(8), 21)`, both snapshots carrying the monotonic tick index each F1 stale-guard requires. `testing-strategy-tests.asmdef` gains `PositioningAI` + `PressingAI` + `TacticalInstructions` refs. **No production change** — reads through the existing public orchestrator surfaces. Adversarial review: 0H+1M(scope, documented not code-fixed)+0L — M is the handoff-consumption caveat above (predicates check each half independently; the chain is run, not proven to consume — softened in the file-header SCOPE NOTE + scenario comment rather than papered over with a fragile `PrimaryPresserId >= 0` assertion the local env can't validate). Otherwise confirmed the `PositioningAITick.Tick`/`GetFormationSlot`/`GetLine`/`GetPhase` + `PressingAITick.Tick`/`LastDirective`/`GetAssignment` + `PressDirective`/`CoverShadow`/`PressAssignment`/`PressRole` member names against source; the F1 guard passes on tick 0 (`_lastProcessedTick` default 0, `0 < 0` false); struct-array element mutation (`snap.Agents[i].BaselineSlot = …`) is legal (array element is a variable, not a copy); the digest is float-free; and each chain builds fully independent snapshots + orchestrators (no cross-instance static mutation — the per-spec #13 determinism precedent). Files: CrossSpecMechanicsScenarios.cs (new), CrossSpecMechanicsScenarioTests.cs (new), testing-strategy-tests.asmdef. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.7 | 2026-07-03 | — | Goalkeeper Mechanics (#11) closed-loop scenario corpus on the #19 `ScenarioRunner` — extends the "orchestrator never executed" audit (Perception #7 v2.4 → Pressing #13 v2.5 → Attacking #15 v2.6, all Mechanics/AI-tier) to the last Physics-tier subsystem with a real 10 Hz + 60 Hz orchestrator. Audit of the 1,523-line `GoalkeeperMechanicsTests.cs` (11 fixtures) confirmed the gap: all coverage is pure sub-system functions (`GoalkeeperStateMachine` / reaction pipeline / dive kinematics / handling quality / cross-claim duel / rush dispatch / distribution) — `GoalkeeperMechanics` is instantiated nowhere and `TacticalTick` (10 Hz) / `Update` (60 Hz) are called nowhere. Unlike the Mechanics orchestrators, #11 is constructor-injected (`IGoalkeeperBallSystem` + `IGoalkeeperRngService`) with **no public state accessor**, so the corpus observes it *through its own injected dependencies* — the FR-CS-051..054 design point that makes it testable — via two recording test doubles (RNG draw log + ball-system call log). New `src/goalkeeper-mechanics/Tests/GoalkeeperScenarios.cs` + `GoalkeeperScenarioTests.cs` (owning spec {11}, Tier B) drive the real orchestrator over a **committed save whose dive misses a far ball**: GK0 (team 0, near its own goal) is carried Resting→Set→Anticipate by ball position + the Stage-0 anticipation stub, a `SaveIntent` is committed once it reaches Anticipate, and the orchestrator carries it Anticipate→Diving→Airborne, launching the dive. The ball is parked ~73 m away (deep in GK0's attacking third, x=75 ≥ `BallAttackingThirdXM`≈70) so the dive envelope can never reach it — the save **misses**, so the whole run is **publish-free** (no contact ⇒ no `SaveAttemptedEvent`/`BallClaimedEvent` ⇒ **no EventBus boot required**). Two scenarios: `save-launch-executes-dive` (15 tactical ticks × 6 Updates; asserts the composition actually ran the save pipeline through the injected deps — exactly one §3.3.2 dive-timing-jitter Gaussian draw fired from the correct draw site `DrawSiteDiveTimingJitter` under `DomainTagGoalkeeper`, zero uniform draws, and a missed save mutated no ball state: 0 ApplyKick / 0 SetPossessor) and `two-instance-determinism` (byte-identical FNV-1a-64 digest of both injected logs across two independent orchestrators). Behavioural depth stays covered by the sub-system suite; this locks the *composition* (both entry points run end-to-end deterministically, RNG threaded through the correct draw site). `goalkeeper-mechanics-tests.asmdef` gains the `TacticalDirector.TestingStrategy` ref (BallPhysics/AgentMovement already present). **No production change** — observation is purely through the injected interfaces. Self-review: no H/M — verified the exactly-one-draw invariant (the Diving-block dive-launch guard `_diveLaunchFrames<0` fires once, `_saveIntentActive` is cleared on Airborne→Recovering so there is no re-dive; GK1 gets no intent so never draws; no `NextFloat` call sites exist in the orchestrator so `FloatCount==0`), that the miss path touches neither the ball-system stub nor EventBus (contact/publish are reach-gated and unreachable at 73 m), that telemetry is a pure no-op stub, and that each `RunSaveMiss` builds independent `agents`/`ball` state (no cross-instance contamination; the shared `GkAgentIds` array is read-only to the orchestrator). Files: GoalkeeperScenarios.cs (new), GoalkeeperScenarioTests.cs (new), goalkeeper-mechanics-tests.asmdef. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.6 | 2026-07-03 | — | Attacking AI (#15) closed-loop scenario corpus on the #19 `ScenarioRunner` — closes the "orchestrator never executed" audit series that ran Perception (#7, v2.4) → Pressing (#13, v2.5) → **Attacking (#15)**. `AttackingAITick` was the last of the two Mechanics orchestrators the v2.5 audit found instantiated/ticked *nowhere* (0/0 across its 104 tests, all pure-function). New `src/attacking-ai/Tests/AttackingScenarios.cs` + `AttackingScenarioTests.cs` (owning spec {15}, Tier B) drive the real `AttackingAITick.Tick` 10-step orchestrator headlessly, building the positioning view + snapshot via the exact recipe `MatchEngine.FillAttackingSnapshot` uses (`new PositioningAITick(F442, 21)` → `SeedFromFormation(new PositioningPerceptionSnapshot(11))` — seeds `Phase.InPoss` so the IN_POSSESSION pipeline runs, not the OutOfPoss empty-directive branch → `PositioningAIView` → `AttackingAITick(view, StyleProfile.Possession, 21)`) over a team-0-in-possession attack (GK + carrier excluded, 9 outfielders in the pool with finite baseline slots so it's never the F2 SENTINEL_NO_SLOT). Two scenarios: `attack-pipeline-runs-and-assigns` (20 ticks so the `AttackHysteresis` dwell counters advance; asserts structural well-formedness robust to behaviour — every pool agent's `AttackIntent` carries a defined `AttackRole`; the `RunParameters?`-non-null-iff-`Role==Runner` contract holds per FR-AT-011/§2.2.2; the §3.11 InvariantEnforcer runner cap `runnerCount ≤ StyleProfile.MaxRunners` holds; a declared overload carries a defined `Flank`) and `two-instance-determinism` (identical FNV-1a-64 digest of directive + intents across two independent instances). Behavioural depth stays covered by the 104 units; this locks the *composition* (the orchestrator runs end-to-end deterministically). `attacking-ai-tests.asmdef` gains the `TacticalDirector.TestingStrategy` ref (`PositioningAI`/`PressingAI` already present). Self-review: no H/M — verified `SeedFromFormation` reports `Phase.InPoss` (the loose-ball/OutOfPoss guards are not tripped), the pool is non-empty (finite baseline slots), `RoleAssigner` sets `HasRunParams` for every committed Runner and no later pipeline step promotes to Runner (so the run-params coherence check is exact), hysteresis indexing is in bounds (pool entity ids 2..10 < capacity 22), and the digest is float-free. **The Mechanics/AI-tier orchestrator-coverage series is now complete** (Positioning + Defensive were already exercised). Files: AttackingScenarios.cs (new), AttackingScenarioTests.cs (new), attacking-ai-tests.asmdef. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.5 | 2026-07-03 | — | Pressing AI (#13) closed-loop scenario corpus on the #19 `ScenarioRunner` — extends the Perception (#7, v2.4) "orchestrator never executed" fix to the Mechanics tier. An audit of the four Mechanics `Tick` orchestrators found Positioning (#12) and Defensive (#14) *are* exercised (23/2 instantiations), but **`PressingAITick` (#13) and `AttackingAITick` (#15) are instantiated and ticked nowhere** across their 91/104 tests — all pure-function coverage. New `src/pressing-ai/Tests/PressingScenarios.cs` + `PressingScenarioTests.cs` (owning spec {13}, Tier B) drive the real `PressingAITick.Tick` 8-step orchestrator headlessly, building the positioning view + pass ring + snapshot via the exact recipe `MatchEngine` uses (`new PositioningAITick(F442, 21)` → `SeedFromFormation(new PositioningPerceptionSnapshot(11))` → `PositioningAIView` → `PressingAITick`) and the existing test-assembly `SnapshotFactory` for a realistic back-three-vs-carrier press. Two scenarios: `press-pipeline-runs-and-assigns` (20 ticks so the debounce / role-dwell hysteresis actually advances; asserts structural well-formedness robust to behaviour — bounded `CoverShadowCount ∈ [0,2]`, `PrimaryPresserId ≥ −1`, every queried agent carries a defined `PressRole`) and `two-instance-determinism` (identical FNV-1a-64 digest of directive + assignments across two independent instances). Behavioural depth stays covered by the 91 units; this locks the *composition* (the orchestrator runs end-to-end deterministically — the class of gap the project's closed-loop scenarios exist for). `pressing-ai-tests.asmdef` gains the `TacticalDirector.TestingStrategy` ref (`PositioningAI` already present). Self-review: no H/M — `SeedFromFormation` on an empty snapshot is the proven match-engine construction path (MatchEngine.cs:353), sentinel formation slots are handled by PressingAITick's F1 stale-slot detection (structural assertions + determinism are robust to them), the digest is float-free, and separate per-instance snapshots mean stamina mutation can't cross-contaminate. **Sibling still open: `AttackingAITick` (#15)** — same gap, natural next. Files: PressingScenarios.cs (new), PressingScenarioTests.cs (new), pressing-ai-tests.asmdef. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.4 | 2026-07-03 | — | Perception System (#7) closed-loop scenario corpus on the #19 `ScenarioRunner` — the Simulation-layer coverage the Physics-tier specs + match-engine capstone have but the AI/Mechanics/Perception tier lacked. Motivation: the 68 existing perception tests are all pure-function units; `PerceptionSystem.OnHeartbeat` (the 22-agent 10 Hz production orchestrator) was called *nowhere* in the suite — every §5.11 multi-agent integration test is an `Assert.Ignore("requires Play Mode 22-agent simulation")` stub, and the ScenarioRunner is exactly the headless mechanism those stubs deferred to. New `src/perception-system/Tests/PerceptionScenarios.cs` + `PerceptionScenarioTests.cs` (owning spec {7}, Tier B): build a real 22-agent two-line facing world + ball, populate the broad-phase grid each tick (mirroring `MatchEngine.PopulatePerceptionGrid`), and drive `OnHeartbeat` headlessly. Three scenarios: `full-heartbeat-all-snapshots` (every view stamped with its own ObserverId + heartbeat FrameNumber, perceived counts within their pre-allocated buffers per INV-10, pipeline perceives non-trivial entities — the `FullHeartbeat_22Agents_AllSnapshotsProducedNoNulls` stub made real); `awareness-cold-start-builds` (aggregate perception non-decreasing and non-zero as recognition latency elapses from cold start — the `Awareness_BuildsOverFirstTicks_ColdStart` stub made real); `two-instance-determinism` (byte-identical FNV-1a-64 digest of all FilteredView integer content across two independent instances over 10 heartbeats — the Simulation-layer determinism lock). `perception-system-tests.asmdef` gains the `TacticalDirector.TestingStrategy` ref; the class-vs-namespace `PerceptionSystem` collision is aliased (match-engine composition-root precedent). Self-review: no H/M — verified `ViewBuilder` stamps `FrameNumber = frameNumber` (so the `==29` assertion holds), recognition-latency confirmation is monotonic on a static in-range/in-FoV opponent (so cold-start `late ≥ early` and the warmed-up `total > 0` hold), and the digest is float-free (pure determinism signal, no Tier-A/B float-compare subtlety). Also corrects the stale "Tests/ (empty)" tree note. Files: PerceptionScenarios.cs (new), PerceptionScenarioTests.cs (new), perception-system-tests.asmdef, src/CLAUDE.md tree. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.3 | 2026-07-03 | — | Living World (#22) slice 6 — the deep-memory→text connection: a new auto-cite `WorldStore.GenerateInteractionText(intent, entityId, subject, opponent, homeGoals, awayGoals)` overload pulls the contact's most-salient **citable** §3.2 episode from the store's own `MemoryStore` (the manager→entityId edge) and cites it, rather than leaving the caller to build the `InteractionSlots` cited episode by hand. Selection: scan the edge's live `Memory` buffer for the highest-salience episode with a citable kind (not `EventKind.None`) that clears `SALIENCE_REF_THRESHOLD` (the §3.2 referencing gate, negated-compare fail-closed on the NaN the store seams already reject); deterministic tiebreak salience→worldTick→episodeId. No qualifying episode (no edge for the contact, or none above threshold) ⇒ generate with no citation. Either path consumes exactly one `world.text` draw, so replay parity holds across both and the whole selection is a pure function of the serialized memory — it resumes byte-identically across `Snapshot`/`Restore`. **No new serialized state / no `WORLD_STORE_FORMAT_VERSION` bump** (the cited episode is read from the already-serialized MemoryStore; the draw folds into the existing cursor). Entirely within-assembly — the `world.arcs`/BackgroundTierSim/unified-save items stay blocked on their nonexistent upstreams (FR-LW-031). `Tests/WorldStoreTests.cs` +4: fresh-episode cite, full-tie episodeId tiebreak, no-edge omits citation, and the save-boundary auto-cite determinism lock. Self-review: no H/M — confirmed `edge.Memory` holds exactly the live episodes (RecordEpisode grows/evicts to depth, no default padding), `EpisodeClause` covers all six non-None `EventKind` members (so a citable-kind candidate never trips the generator's clause throw), and the overload keeps the existing slots overload untouched. Files: WorldStore.cs v1.3, Tests/WorldStoreTests.cs, CLAUDE.md OPEN ISSUES #22. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.2 | 2026-07-03 | — | Living World (#22) slice-5 AR-1 fix pass: 0H+0M+3L, all resolved. Fresh-eyes review of the `world.text` save/restore/resume path. L-1: `WorldStore.Restore` ignored `DeterministicRngService.RestoreStream`'s return code — guaranteed 0 today (the generator just registered stream index 0 on a service with streamCount 1), but a future registration-order change would swallow the error and silently resume the text stream at cursor 0 (a silent replay divergence, the class the project fails loud on elsewhere); now throws `InvalidOperationException` on a non-zero code (parallels the `InteractionTextGenerator.Generate` slice-3-AR-1 L-3 unreachable-internal-invariant abort). L-2: documented the load-bearing invariant that lets the block serialize only `RngCursor` + `ActionOrdinal` (not `BudgetRemaining`/`DeclaredBudget`/`DrawIndex`) — the stream is always at a closed reservation at rest because `GenerateInteractionText` is atomic (`Reserve`…`CloseReservation`, no yield), so fresh registration reconstructs the reservation fields exactly; a future held-reservation API must extend the block. L-3: `WorldStore.cs` file-header purpose line reordered to match the actual byte order (world.text block before the membership roster; the method-level docs were already correct). No behaviour change; the core resume logic was verified sound by hand (re-derived `k0/k1` + recomputed `StreamKey` + restored `ActionOrdinal` ⇒ byte-identical next draw) and is closed-loop-locked by `GenerateInteractionText_ResumesDeterministicallyAfterRestore`. Files: WorldStore.cs v1.2. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.1 | 2026-07-03 | — | Living World (#22) slice 5 — the `InteractionTextGenerator` (slice 3) wired into the `WorldStore` composition root, folding the `world.text` RNG cursor into the composite save (closes one of the OPEN ISSUES #22 "Still open" items). `WorldStore.cs` v1.1 now owns a `DeterministicRngService` (from a `worldSeed`) + the generator: new `WorldStore(int managerId, ulong worldSeed)` overload; the existing `WorldStore(int managerId)` seeds deterministically from `managerId` (same manager ⇒ same text stream; two managers never share a cursor). New public `GenerateInteractionText(intent, in slots)` delegates to the generator (player-triggered, off the tick loop — not subject to the 60 Hz zero-alloc rules). `Snapshot()`/`Restore()` fold a world.text RNG block (world seed + stream `RngCursor` + `ActionOrdinal`) into the composite between the §4.6 four-store block and the membership roster (membership stays last, so the existing byte-offset fail-loud tests are unaffected); the draw keys on the seed-derived `k0/k1` + `streamKey` + `ActionOrdinal` (#16 §3.2.5 — `StreamKey = SipHash(k0,k1, subsystem‖entity‖version)`, `drawValue = SipHash(k0,k1, tag‖streamKey‖actionOrdinal‖index)`), so Restore re-derives the service from the persisted seed and resumes the exact stream position — generation continues byte-for-byte across the save boundary. `WORLD_STORE_FORMAT_VERSION` 1 → 2 (composite field-order change, per the constant's own doc). `Tests/WorldStoreTests.cs` +3 (`GenerateInteractionText_ExpandsSlots`; `_ResumesDeterministicallyAfterRestore` — the save-boundary determinism lock; `_DivergesWithWorldSeed` — seed selects the draw sequence) and `PopulatedStore` now runs two generations so every round-trip/determinism test exercises a non-zero cursor. The generator's cursor lives in the WorldStore's own composite save, NOT yet the unified match/season save (that fold stays blocked on FR-LW-003 + the season save-file root). Files: WorldStore.cs v1.1, LivingWorldConstants.cs (WORLD_STORE_FORMAT_VERSION 2), Tests/WorldStoreTests.cs, CLAUDE.md OPEN ISSUES #22. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 2.0 | 2026-07-03 | — | Living World (#22) KD-10 season composition root landed — the `WorldStore` persistent world store + season-calendar loop, the one KD-10 prerequisite §7.1 assigns to this spec ("persistent world store + season-calendar loop") and the composition root every prior slice deferred to. New `src/living-world/WorldStore.cs` (sealed): constructs and wires the six existing services (`WorldClock` + `MemoryStore` + `ColdStore` + `ArcEngine` + `ActiveSetMembership` + `WorldLoop`) in dependency order for one manager, exposes them, drives one calendar day per `AdvanceDay` (§4.2 — runs the phases that have producers: 3 salience decay, 4 arc expiry, 6 external-cap LRU; phases 1 match-outcome ingest / 2 vol-2 read / 5 background-tier stay the WorldLoop's documented null seams — this root does NOT invent them, FR-LW-031), and `RecordInteraction` stamps the episode with the store's current day. **Persistence (§4.6 / FR-LW-022):** `Snapshot()`/`Restore()` round-trip the whole store as one canonical payload — a fail-loud composite header (WORLD_STORE_FORMAT_VERSION + DomainTagLivingWorld + managerId), the existing §4.6 four-store block (`WorldStateSerializer`, bitwise via #16 CanonicalSerializer), then the FR-LW-022 active-set membership roster (entityId + isOwnClub per member) — the one piece the §4.6 block does not carry (its snapshot entry was flagged "a follow-up"; this is that follow-up). Restore rebuilds through the validating store seams and re-wires the loop; fail-loud on unknown version, wrong tag, negative managerId, corrupt length/count prefixes (a `ReadCount` parallel to the serializer's), out-of-range membership flag, and trailing bytes. `ActiveSetMembership` v1.3 gains the roster-serialization seam: `MemberCount`/`GetMemberAt` (canonical read) + `RestoreMember` (rebuilds one entry, validated against the rebuilt `MemoryStore` for the membership↔edge live-edge invariant, FR-LW-025). `LivingWorldConstants` gains `[FIXED] WORLD_STORE_FORMAT_VERSION`. The composite `SNAPSHOT_SCHEMA_VERSION` fold into the match/season save stays deferred (FR-LW-003 bars the match engine from referencing this assembly); the `world.text` RNG cursor belongs to a separately-attached `InteractionTextGenerator`, so no RNG service is owned here (no phantom). New `Tests/WorldStoreTests.cs` (14 tests): construction/wiring, AdvanceDay calendar+decay, AdvanceDay arc expiry across the lifetime boundary, current-tick interaction stamping, field-identical + idempotent Snapshot/Restore round-trip (incl. arc pin re-take + own-club/external roster classes + a departed→cold contact), empty-store round-trip, two-run determinism, and six fail-loud restore gates. **Adversarial review: 0H+0M (converged first pass).** Residual low (managerId not cross-checked against edge FromIds on an empty-roster corrupted save — corruption-only, non-crashing, caught whenever the roster is non-empty, consistent with WorldStateSerializer's own no-managerId scope) left as-is. Files: WorldStore.cs v1.0 (new), ActiveSetMembership.cs v1.3, LivingWorldConstants.cs, Tests/WorldStoreTests.cs (new), CLAUDE.md OPEN ISSUES #22. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.99 | 2026-07-03 | — | Living World (#22) slice-3 AR-1 fix pass: 0H+0M+3L, all resolved. L-1: `WorldStateSerializer.Deserialize` now routes every internal length prefix (edge/arc/input/pin/episode/cold counts) through a new `ReadCount` helper that fails loud with `ArgumentException` on a negative count (previously desynced the read offset, caught only incidentally by the trailing-byte gate) or an absurd positive count (previously threw `OverflowException`/`OutOfMemoryException` on the element allocation) — bound is `0 ≤ count ≤ remaining bytes` (every record is ≥ 1 byte), matching the version/tag/trailing fail-loud contract. L-2: `SpawnCause.Input.Value` — the one float in the §4.6 digest block with no store-seam finiteness gate — is now NaN/±Inf-gated at the serialize seam (negated-compare pattern) before it can enter the digest preimage. L-3 (doc): the (unreachable) `InteractionTextGenerator.Generate` `DrawReserved`-failure branch documented as an internal-invariant corruption abort — its `CloseReservation` cursor advance is deliberate (keeps the stream usable), not a normal pre-draw refusal (those all return before opening the reservation). +2 regression locks in `WorldTextSnapshotTests` (corrupt count-prefix refusal; non-finite `Input.Value` refusal). Files: WorldStateSerializer.cs v1.1, InteractionTextGenerator.cs v1.1, Tests/WorldTextSnapshotTests.cs v1.1. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.98 | 2026-07-02 | — | Living World (#22) slice 3 landed — InteractionTextGenerator (§3.3) + §4.6 snapshot serializer, closing the two remaining implementable #22 services (BackgroundTierSim stays the documented phase-5 seam — see below). **Cross-spec allocations (ERR-022-001):** `DeterministicSimConstants.DOMAIN_TAG_LIVING_WORLD = 0x1E` (next after 0x1D; 0x18/0x1C stay orphaned per ERR-016-003) + `SubsystemOrdinals.LivingWorld = 80` (new off-pitch band 80–99, disjoint from the match Physics/Mechanics/AI bands). **`InteractionTextGenerator.cs` (§3.3/KD-6):** registers the aperiodic **`world.text`** sub-stream on an injected `DeterministicRngService` (the first #22 draw site — one `Reserve`/`DrawReserved`/`CloseReservation` per interaction, FR-LW-020) and is its only consumer, so it's not the phantom surface `world.arcs` would be; `Generate(intent, in slots)` selects a template (draw mod count) from the Stage-0 in-code `InteractionTextCorpus` (APPEND-only per-intent rows + per-EventKind episode clauses — the §7.2 AI-authored corpus is a pure data swap) then expands `{subject}`/`{opponent}`/`{score}` from `InteractionSlots` (match-engine facts + an optional §3.2 cited episode, FR-LW-013) with the citation gated at `SALIENCE_REF_THRESHOLD` (now consumed — its slice-2 AR-2 unconsumed-note retired). All validation (intent roster, slots, salience gate NaN-closed, clause lookup) runs BEFORE the draw, so a refused call consumes no cursor and replay parity holds across fail-loud paths (T-LW-DET-003/004); no runtime model inference (FR-LW-012); provenance implicit per §3.6. **`WorldStateSerializer.cs` (§4.6):** canonical living-world block — version + `DomainTagLivingWorld` + worldTick, then edges (with NextEpisodeId + episode buffers), live arcs (SpawnCause + pins), cold summaries — in the Appendix B pinned field order via the #16 `CanonicalSerializer` (raw IEEE-754 bits, −0.0 normalised ⇒ bitwise round-trip). Deserialize rebuilds through the validating store seams (`InsertEdge`/`SpawnArc`+`AdvanceState`/`ColdStore.Add`), so `SpawnArc` re-takes every pin and reconstructs the FR-LW-018 refcounts exactly (arcs are the only pin holders); fail-loud on unknown format version, wrong domain tag, trailing bytes, out-of-roster EventKind, and `MaxLifetimeTick <= SpawnTick`. The composite `SNAPSHOT_SCHEMA_VERSION` bump that folds this block into the match/season snapshot stays at the KD-10 season composition root (FR-LW-003 bars the match engine from referencing this assembly; the season root does not exist yet). New `InteractionSlots.cs` value type; `LivingWorldConstants` gains a Fixed region (`WORLD_TEXT_STREAM_SITE_ID`/`_VERSION`, `WORLD_SNAPSHOT_FORMAT_VERSION`) + `DomainTagLivingWorld` [CROSS] mirror; `living-world(.Tests).asmdef` gain the `DeterministicSim` reference. New `Tests/WorldTextSnapshotTests.cs` (~20 tests): text determinism, no-draw-on-refusal replay-parity lock, world.text↔sibling-stream non-interference (T-LW-DET-004), §4.6 field-identity round-trip incl. shared-pin refcount reconstruction, bitwise-determinism, fail-loud gates. **BackgroundTierSim (§3.5) deliberately NOT built** — it stays the documented WorldLoop phase-5 seam: it would summarise club-AI/transfer/sacking outcomes that do not exist yet (a consumer would be the FR-LW-031 phantom-interface class — the exact reasoning that keeps `world.arcs` unregistered in this same task), and §3.5 specifies no background-tier update formula to implement. Files: DeterministicSimConstants.cs v1.3, SubsystemOrdinals.cs v1.1, LivingWorldConstants.cs, InteractionSlots.cs v1.0 (new), InteractionTextCorpus.cs v1.0 (new), InteractionTextGenerator.cs v1.0 (new), WorldStateSerializer.cs v1.0 (new), living-world.asmdef, Tests/living-world-tests.asmdef, Tests/WorldTextSnapshotTests.cs v1.0 (new), CLAUDE.md OPEN ISSUES #22. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.97 | 2026-07-02 | — | Living World (#22) slice-2 AR-3 sweep (third fresh-eyes full-surface pass over all 19 files): 0H+0M+2L, all resolved same day — **CONVERGENCE (doc-only round)**; slice-2 AR cycle closed per the project convention (match-viewer AR-4 / slice-1 AR-3 precedent). L-1: `ColdStore.TryTake`'s ordering-contract doc still attributed the verify-before-take sequencing to "the §3.5 membership service (follow-up slice)" — stale since slice 2 landed that service; the doc now names `ActiveSetMembership.RecordInteraction` as the live owning consumer and points pre-take validation at `TryPeek`. L-2: `WorldLoop`'s header `Spec:` citations (FR-LW-019/027/032/034) had drifted from the wired phases — gains FR-LW-014 (phase-4 arc liveness bound) and FR-LW-023 (phase-6 external cap). Verified sound with no change: ArcEngine spawn/rollback/expiry incl. the AR-1 snapshot semantics, membership entry/LRU/depart chains under the AR-2 gates, pin/evict interplay under the new unpin call sites, LRU tiebreak + spawn-order determinism, ColdStore gate ordering, T0 math/enums, asmdefs, all three suites (24 + 28 + T0 hold; no expectation touched by the doc edits). No functional change; no new tests. Files: ColdStore.cs v1.6, WorldLoop.cs v1.2. |
| 1.96 | 2026-07-02 | — | Living World (#22) slice-2 AR-2 full-surface sweep (all 19 `src/living-world/` files incl. the slice-1/T0 surface + both AR-1 fix layers, fresh eyes): 0H+1M+2L, all resolved same day. M-1: the promotion path could still strand a summary on two INCOHERENT-COLD-STATE vectors the AR-1 M-2 peek-check does not cover — (a) `ColdStore.Add`'s coherence gates covered ids/salience/net but NOT the ActiveLayers mask, so an undefined-bit summary was accepted at Add, passed the caller-mask equality check (caller passes the same junk), and threw at `InsertEdge`'s mask gate only AFTER the destructive take (FR-LW-025 violation); (b) a summary with `EntityId == managerId` (insertable via direct Add — the managed demotion path can never produce a self-edge) threw at `ValidatePair` post-take. Fixes: `Add` gains the mask coherence gate reusing `MemoryStore.DefinedLayersMask` (promoted private → internal — same-assembly single source, extends automatically on roster APPEND); `RecordInteraction` validates entityId up front (non-negative, != managerId); defensive take-result guard (peek/take drift fails loud instead of inserting a default edge). L-1: `SALIENCE_REF_THRESHOLD` / `CLIQUE_THRESHOLD` gain the declared-but-unconsumed doc-notes their `LAYER_*` siblings got in the slice-1 AR-3 (consumers: §3.3 InteractionTextGenerator gate / DressingRoomSplit trigger reading vol-2 clique state — both KD-10-deferred). L-2: the cold store is SINGLE-MANAGER by shape (`ColdSummary` §2.2.5 is keyed by EntityId alone, no manager field — the #22 model is manager-centric): two managers sharing an instance would collide in Add; assumption documented on both `ColdStore` and the `ActiveSetMembership` injection contract. Verified sound with no change: ArcEngine spawn/rollback/expiry + the AR-1 snapshot fix, pin/edge lifecycle invariants under the new unpin call sites, evict/EnforceDepth interplay, LRU tiebreak determinism, WorldClock/WorldLoop phase wiring, T0 math/enums, asmdefs, both older suites (the new Add mask gate breaks no existing fixture — hand-built summaries use defined masks or default 0). +2 regression locks (`ArcMembershipTests` 22→24). Files: MemoryStore.cs v1.4, ColdStore.cs v1.5, ActiveSetMembership.cs v1.2, LivingWorldConstants.cs (doc), Tests/ArcMembershipTests.cs v1.2. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.95 | 2026-07-02 | — | Living World (#22) season/world loop slice-2 AR-1 fix pass: 0H+2M+3L, all resolved same day. M-1: `ArcEngine.SpawnArc` stored the CALLER'S live `pinnedEpisodes` array in the Arc — post-spawn mutation desynchronised resolve/expiry unpinning from the holds actually taken (a stale hold leaks protection-forever — the F1 class — or `UnpinEpisode` fails loud on a never-pinned key at resolve; the match-viewer AR-3 M-1 live-buffer class on the pin table); the array is now snapshot-copied BEFORE any hold is taken, and pin/rollback/resolve all operate on the snapshot. M-2: `ActiveSetMembership.RecordInteraction`'s promotion path silently IGNORED the caller's `activeLayers` while its XML claimed it was "checked for consistency" (a false contract; a conflicting mask was accepted and later updates misrouted — the AR-1 L-1 conflicting-mask class). The check is now real and runs against a new `ColdStore.TryPeek` (non-destructive verify-before-take companion, ColdStore v1.4) BEFORE the destructive `TryTake` — checking after the take and throwing would strand the summary outside both tiers, violating exactly the FR-LW-025 ordering contract the July-2 AR-3 documented. L-1: `spawnTick + maxLifetimeDays` could wrap uint, silently producing an instantly-expired arc; overflow now refused. L-2 (doc): worldTicks are caller-supplied and non-monotonic, so an entering external contact bearing the stalest last-interaction can itself be the immediate LRU victim of the cap enforcement its own call runs — spec-conformant/deterministic; documented with the F1 fail-loud consequence for pinning the returned episodeId. L-3 (doc): membership manages exactly one manager→contact edge per contact (the shape ColdSummary compresses); reverse-direction Trust(contact→manager) edges are vol-2-era state outside the service. +3 regression locks (`ArcMembershipTests` 19→22). Files: ArcEngine.cs v1.1, ActiveSetMembership.cs v1.1, ColdStore.cs v1.4, Tests/ArcMembershipTests.cs v1.1. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.94 | 2026-07-02 | — | Living World (#22) season/world loop slice 2 landed — the two remaining implementable services. `ArcEngine.cs` (§3.4): `SpawnArc` = the complete spawn path (steps 1–3 — SpawnCause captured inline by the trigger caller per FR-LW-016/KD-8; **atomic FR-LW-018 pinning** — an F1 dangling-id failure rolls back every hold already taken so a failed spawn leaks no pins; ArcKind gate refuses undefined ordinals, AR-2 M-1 class; lifetime gated to [1, ARC_MAX_LIFETIME_DAYS] per §6.2), `AdvanceState` (kind-specific byte, APPEND-only per FR-LW-028), `ResolveArc` (unpin + remove; refcount integration — a shared episode stays pinned until the LAST arc releases), and `Update(worldTick)` = the §6.2 expiry sweep in deterministic spawn order (order matters: unpin order can select different §3.2 shrink evictions when several expiring arcs pin episodes on one edge). **Trigger evaluation + the `world.arcs` RNG sub-stream are the documented KD-10 seam** (FR-LW-020/031): the triggers read vol-2/vol-3 canon that does not exist, so no stream is registered and no rng reference is held — a registered stream with zero draw sites would be the phantom surface FR-LW-031 forbids. `ActiveSetMembership.cs` (§3.5, FR-LW-023/025): entry on first interaction; cold-history promotion honours the ColdStore.TryTake destructive-take ordering (verify-no-live-edge FIRST — the AR-3 L-2 contract's first consumer); deterministic LRU demotion at ACTIVE_SET_EXTERNAL_CONTACTS_MAX (last-interaction = max episode worldTick, ties → lowest EntityId via strict-< canonical iteration; arc-pinned edges skipped — all-pinned ⇒ transient over-cap, the §3.2 parallel); own-club members exempt at the club, `Depart` demotes via the same path as an external contact (FR-LW-025 re-sign/face-again history) with a **pinned departure deferring as external** until the arc resolves. `WorldLoop.cs` v1.1 wires phase 4 (`ArcEngine.Update`) + phase 6 (`EnforceExternalCap`), both null-injectable (sanctioned not-wired seam; 2-arg ctor kept — slice-1 hosts boot unchanged). New `Tests/ArcMembershipTests.cs` (19 tests): arc spawn/pin + provenance, F1 rollback, validations, resolve, shared-pin, expiry strict-> boundary, state; membership entry/repeat/fail-louds, cap LRU→cold, tie→lowest-id, pin-skip, own-club exemption, Depart→cold + FR-LW-009 re-entry resume (episodeId 3 after 3 retained), pinned-Depart deferral chain, Depart refusals; loop phase-4/6; two-run field-identity determinism. Consumers landed for ARC_MAX_LIFETIME_DAYS / ACTIVE_SET_EXTERNAL_CONTACTS_MAX / COLD_SUMMARY_RETAINED_EPISODES. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.93 | 2026-07-02 | — | Living World (#22) season/world loop AR-3 sweep: 0H+0M+3L, all resolved same day — **CONVERGENCE (doc-only round)**; AR cycle closed per the project convention (match-viewer AR-4 precedent). L-1: `LivingWorldMath.ApplyDecay` + `LAYER_DECAY_RATE`/`LAYER_VOLATILITY_DEFAULT` doc-noted declared-but-unconsumed in production (CS AR-10 / PM AR-9 L-3 precedent) — the §3.1 layer-relaxation caller defers with the phase-2 vol-2 baseline wiring, the volatility default with the phase-1 event-ingest producer (KD-10); exercised by T-LW-U-009/010 meanwhile. L-2: `ColdStore.TryTake` ordering contract documented — removal is destructive, so the §3.5 promotion flow must verify no live edge exists BEFORE taking (a post-take `InsertEdge` duplicate throw would strand the summary outside both tiers, violating FR-LW-025 neither-loses-nor-duplicates); the membership service (follow-up slice) owns that sequencing. L-3: `UnpinEpisode`'s edge-lookup guard documented as defensive-only, backed by the invariant that every pin references a live edge (`PinEpisode` requires a live episode; `RemoveEdge` refuses pinned edges) — not a sanctioned silent-skip path. Verified sound with no change: pin/edge lifecycle invariants, evict-before-append + tiebreaks, both AR-2 coherence seams, `DefinedLayersMask` const-expression, Compress output coherence by construction (ascending retained ids < high-water; decayed salience stays in [0,1]), WorldClock/WorldLoop, both test suites. No functional change; no new tests (28 hold). Files: LivingWorldMath.cs, LivingWorldConstants.cs (header notes), MemoryStore.cs v1.3, ColdStore.cs v1.3. |
| 1.92 | 2026-07-02 | — | Living World (#22) season/world loop AR-2 full-surface sweep (all 17 files incl. the June-21 T0 surface, fresh eyes): 0H+1M+2L, all resolved same day. M-1: `MemoryStore.ApplyEvent` guarded PlayerEdge explicitly but routed the write by `if (Affinity) … else Trust` — ANY out-of-roster enum cast (e.g. `(RelationshipLayer)5`) fell into the Trust write; and since `GetOrCreateEdge`/`InsertEdge` never validated the caller-supplied `ActiveLayers` byte, an undefined mask bit made `IsLayerActive` pass for that junk layer, completing a silent Trust mutation on a digest-relevant path. Fixed with an explicit owned-layer allowlist in `ApplyEvent` (AOORE) + a `DefinedLayersMask` built from the enum members (extends automatically on roster APPEND) enforced at both edge-entry seams. L-1: the AR-1 L-3 FR-LW-009 coherence gate existed only on `ColdStore.Add` and only checked the high-water mark — `InsertEdge` (the rehydration seam) accepted Memory with non-ascending or duplicate episodeIds or ids ≥ `NextEpisodeId`, letting the next `RecordEpisode` re-allocate a LIVE id and break the durable-handle contract arc pins depend on (pin identity is (edge, episodeId)); both seams now enforce strictly-ascending ids below the high-water mark (the store maintains this invariant internally — append order = allocation order, eviction preserves order). L-2: episode `Salience` (doc contract [0,1]) was unguarded at both seams — NaN flowed into the eviction comparator (deterministic via `CompareTo`'s total order but semantically corrupt) and the future §3.3 `refThreshold` checks; NaN-gated to [0,1] at both seams. Verified-sound and unchanged: evict-before-append + FR-LW-021 tiebreaks, canonical binary-search iteration, ref-counted pins (AR-1), `Compress`'s unstable `Array.Sort` (episodeId uniqueness makes the comparator a total order), WorldClock/WorldLoop, T0 math/data types. +3 regression locks (`SeasonWorldLoopTests` 25→28). Files: MemoryStore.cs v1.2, ColdStore.cs v1.2, Tests/SeasonWorldLoopTests.cs v1.2. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.91 | 2026-07-02 | — | Living World (#22) season/world loop AR-1 fix pass: 0H+2M+4L, all resolved same day. M-1: `MemoryStore` pins were single-entry idempotent — two arcs pinning the same episode (nothing in §3.4 forbids it; e.g. MediaVendetta + BoardPatienceCollapse sharing a press-trap episode) lost FR-LW-018 protection the moment the FIRST arc resolved: `UnpinEpisode` removed the only entry and the still-referenced episode became evictable — the exact F1 dangling-episode class the pin exists to prevent. Pins are now reference-counted (`PinEntry.Count`); an episode stays exempt until the LAST hold releases, and only that release triggers the §3.2 shrink-back. Latent (ArcEngine not yet built) but contract-breaking for its specified consumer. M-2: `RemoveEdge` (the §3.5 demotion seam) removed an edge that held arc-pinned episodes and left orphan entries in `_pins` — a stale pin surviving a remove→`Compress`→`Rehydrate` cycle either dangles (F1) or silently re-pins a retained episode it no longer owns; removal now refuses pinned edges per the §3.5 LRU-demotion skip rule (which also gives `EdgeHasPinnedEpisode` its production consumer — it was declared-but-unconsumed). L-1: `GetOrCreateEdge` silently ignored a conflicting `ActiveLayers` mask on an existing edge (caller believes the new mask applied; updates misroute) — now throws. L-2: `InsertEdge` (the rehydration seam) accepted non-finite / out-of-range active-layer values — F6 gate added (active layers finite in [0,1], negated-compare NaN-gate; inactive layers not checked per F6's explicit scope — asserting on their contents belongs to the §6 invariant fuzzer). L-3: `ColdStore.Add` accepted incoherent summaries — NetRelationship now NaN-gated to [0,1] and every retained episodeId must sit below `NextEpisodeId` (else rehydration re-allocates a retained id and breaks FR-LW-009 monotonicity). L-4: `ValidatePair` reported `nameof(fromId)` for a negative `toId`. +5 regression locks in `SeasonWorldLoopTests.cs` (20→25): ref-count survival across first unpin + eviction after last unpin; pinned-edge removal refusal then legal removal post-resolve; mask-conflict; F6 NaN/range inserts; cold-summary coherence. Files: MemoryStore.cs v1.1, ColdStore.cs v1.1, Tests/SeasonWorldLoopTests.cs v1.1. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.90 | 2026-07-02 | — | Living World (#22) season/world-loop slice 1 landed — the first of the KD-10 "persistent world store + season-calendar loop" prerequisites. New `src/living-world/` services on the existing T0 data types: `WorldClock.cs` (KD-4/FR-LW-019 — one worldTick = one calendar day; Advance/RestoreFromSnapshot; distinct from `MatchClock`, never advanced by the 10/60 Hz loops), `WorldLoop.cs` (§4.2 per-tick orchestrator: clock advance + phase-3 salience decay; phases 1 match-outcome ingest / 2 human-systems read / 4 arcs / 5 background tier / 6 budget are documented seams — their producers don't exist and FR-LW-031 forbids phantom interfaces; §3.1 layer relaxation deferred with phase 2 since the per-entity baseline is vol-2-owned), `MemoryStore.cs` (deep-tier store: edges kept sorted on the canonical (FromId,ToId) key so every pass is FR-LW-021-canonical; §3.2 **evict-before-append** — on a full buffer the lowest-salience unpinned PRE-EXISTING episode evicts (ties → oldest worldTick → lowest episodeId), matching the §3.2 worked example where the incoming episode is always remembered; all-pinned ⇒ transient growth, shrunk back on unpin; FR-LW-018 pin bookkeeping with F1 dangling-id fail-loud; §3.1 `ApplyEvent` on owned layers only — PlayerEdge write refused (FR-LW-004/KD-9), inactive-layer write refused (F6), NaN-gated δ/v), `ColdStore.cs` (cold tier sorted by EntityId + pure §3.5 `Compress`/`Rehydrate` transforms; **Residue-A v1 compression schema recorded**: NetRelationship = mean of ACTIVE owned layers, PlayerEdge never summarised — canon re-read at wiring; retained top-N by inverse eviction order, stored episodeId-ascending; rehydration resumes from `ColdSummary.NextEpisodeId` per FR-LW-009). Day-cadence layer — explicitly not subject to the 60 Hz zero-alloc/ProfilerMarker hot-path rules (assembly stays engine-free). New `Tests/SeasonWorldLoopTests.cs` (20 tests): T-LW-U-011..018, T-LW-I-011..014 (F5 retained-fields round-trip = T-LW-FAIL-005), T-LW-DET-002/006/007 subsets, two-run field-identity determinism. No asmdef change (no new references). Remaining #22 services (ArcEngine / InteractionTextGenerator / BackgroundTierSim + world RNG sub-streams + snapshot-schema entry §4.6) land as their KD-10 upstreams are wired. (dotnet gate not runnable in this environment — compile + suite verified in CI on push.) |
| 1.89 | 2026-07-02 | — | Match viewer AR-4 sweep: 0H+0M+3L, all resolved same day — **convergence reached** (L-only round; recorder / exporter data path / determinism surfaces clean for the second consecutive sweep). L-1 (player JS): the document-level Space toggle preventDefault'ed with a CONTROL focused — a focused `<select>` (speed) lost its native dropdown activation and a focused button double-toggled; the handler now skips BUTTON/SELECT/INPUT targets. L-2: `Export` gates the config-resolved [GT] presentation values (`PixelsPerMetre` finite > 0, `CanvasMarginPx` finite ≥ 0, NaN-gate pattern) — post-AR-2-M-3 these come from `GameplayConfig`, and a malformed config (0/negative scale) emitted a silently broken document. L-3 (doc): `ReplayFrame.Tick` no longer claims "0 = kickoff" (a pre-run engine records from its current tick); `MatchReplay.SampleStride` marked provenance-only (the player times playback from frame tick deltas, never this value — declared-but-unconsumed-by-exporter note); `DefaultSampleStride` notes the recorder's stride guard rejects a config-supplied non-positive value. JS re-checked with node. Files: HtmlReplayExporter.cs v1.3, ReplayFrame.cs v1.1, MatchReplay.cs v1.4, MatchViewerConstants.cs v1.2. |
| 1.88 | 2026-07-02 | — | Match viewer AR-3 sweep: 0H+1M+2L, all resolved same day. M-1: `MatchReplay` wrapped the CALLER'S live frames list in `ReadOnlyCollection` — a VIEW, not a copy — so `frames.Add(...)` after construction injected unvalidated rows into an already-constructed replay, bypassing every AR-1/AR-2 ctor guard (incoherent length, non-monotonic ticks, all of it); the list is now snapshot-copied BEFORE validation and the validated snapshot is what the instance exposes (no TOCTOU between guard and copy). L-1: `ReplayFrame.PossessingAgentId` validated to −1 (loose) or a roster index — an out-of-range id rendered no possession ring while the HUD named a nonexistent agent. L-2: zero-length roster refused (degenerate agentless document). New tests: `Replay_SnapshotsFramesAtConstruction` (post-ctor mutation neither changes the replay nor reaches the exporter) + possession-range + empty-roster cases (`MatchViewerTests.cs` v1.3). Rest of the surface (recorder, exporter data path, player JS, constants static-init order, asmdefs, MatchEngine v1.25 surface) re-swept clean — convergence: findings confined to the public `MatchReplay` ctor validation set. Files: MatchReplay.cs v1.3. |
| 1.87 | 2026-07-02 | — | Match viewer AR-2 full-surface sweep (all 8 files + the MatchEngine v1.24/v1.25 additions, fresh eyes): 0H+3M+3L, all resolved same day. M-1: `MatchReplay` accepted an EMPTY frames list — the exported player crashed at first render (`drawFrame(undefined)` TypeError after `drawPitch()`: blank pitch, dead controls, no visible error); ctor now refuses it. M-2: non-strictly-increasing frame ticks refused — the AR-1 L-2 per-interval playback timing divides by consecutive tick deltas, so a dt ≤ 0 frame pair stalls (÷0 ⇒ Infinity cursor jump) or reverses playback; recorder can't produce one, the public ctor could. M-3: `MatchViewerConstants` [GT] rows migrated onto `GameplayConfig` (`Config.GetFloat/GetInt`, `"match-viewer"` section) — the catalogue was authored plain-const one day after the June-30 17-catalogue migration (convention drift; a designer couldn't tune the viewer without recompiling); [FIXED] IFAB rows stay const; `match-viewer.asmdef` gains the `ProjectConstants` ref; the recorder's default-stride parameters became explicit overloads (a config-resolved `static readonly` cannot be a default parameter value, CS1736). L-1: `MatchReplay.TeamId`/`IsGoalkeeper` gain the roster-index guard (same class as AR-1 M-2 — the sweep found the AR-1 fix had not been applied to the sibling surface). L-2 (player JS): ball marker radius clamped ≥ 1 px — a corrupt-but-finite negative ball z drove `arc()` radius ≤ 0 ⇒ IndexSizeError killing EVERY frame draw. L-3: `MatchReplay` metadata guards (ticksPerSecond/sampleStride > 0; pitch dims finite > 0 via the `!(x > 0f)` NaN-gate pattern). Division of finiteness responsibility documented: `MatchReplay` validates structure, the exporter's non-finite gate validates values. New/extended tests: `Replay_RefusesDegenerateMetadata` + non-monotonic-tick case (`MatchViewerTests.cs` v1.2). JS re-checked with node. Files: MatchViewerConstants.cs v1.1, MatchReplay.cs v1.2, MatchReplayRecorder.cs v1.1, HtmlReplayExporter.cs v1.2, match-viewer.asmdef. |
| 1.86 | 2026-07-02 | — | Match viewer AR-1 fix pass: 0H+2M+3L, all resolved same day. M-1: `MatchReplay` ctor refuses incoherent frames (`AgentPositions` null or ≠ roster length) — an incoherent frame previously reached the exporter as a short/long data row the canvas renderer SILENTLY no-opped on (`arc(NaN)`/`undefined` reads), violating the fail-loud convention; InvariantCulture detail. M-2: the three indexed `MatchEngine` observation accessors (`AgentView`/`AgentTeamId`/`AgentIsGoalkeeper`, v1.25) gain the public-surface roster-index guard (`ArgumentOutOfRangeException`, parallel to `SetPlayerTactic`) instead of a bare array `IndexOutOfRangeException`. L-1: `MatchReplay` "immutable" claim qualified — per-frame position arrays are NOT copied; hand-over convention documented (parallels the #18 AR-1 L-3 exposed-array seam). L-2 (player JS): playback advanced at a UNIFORM frame interval inferred from frames 0→1, mistiming the forced-final-capture short interval; now advances at the current frame's actual tick interval. L-3 (player JS): clock rounded tenths AFTER the minute split (59.96 s displayed `0:60.0`; now rounds first ⇒ `1:00.0`); shirt numbers were `i % (TEAM.length/2) + 1` (assumed contiguous team blocks); now derived per-team from the TEAM array. New tests: `ObservationSurface_GuardsFailLoud` + `Replay_RefusesIncoherentFrames` (`MatchViewerTests.cs` v1.1). JS re-verified headlessly with node (minute-boundary clock + interleaved-roster jerseys + non-uniform final interval). Files: MatchEngine.cs v1.25, MatchReplay.cs v1.1, HtmlReplayExporter.cs v1.1. |
| 1.85 | 2026-07-02 | — | Minimal match viewer landed — the first presentation-layer surface (`src/match-viewer/`, `TacticalDirector.MatchViewer`; tooling, not a numbered spec; the UI layer proper stays Stage 1+). `MatchEngine.cs` v1.24 adds the public observation surface `BallView` / `AgentView(i)` / `AgentTeamId(i)` / `AgentIsGoalkeeper(i)` / `PossessingAgentId` — read-only value-type COPIES (no live-buffer reference escapes, no mutation path, no behaviour change). New files: `MatchViewerConstants.cs` (IFAB marking geometry `[FIXED]` + canvas/recording `[GT]`; presentation-only, nothing feeds the sim or digest), `ReplayFrame.cs` / `MatchReplay.cs` (immutable sampled frames + roster/pitch/cadence metadata; ReadOnlyCollection + cloned arrays), `MatchReplayRecorder.cs` (ticks an engine and samples between ticks; seed + pre-configured-engine overloads for `TeamTacticConfig`-applied matches; fail-loud guards; kickoff frame + final tick always captured), `HtmlReplayExporter.cs` (single self-contained HTML file: canvas pitch markings, home/away/GK/possession/ball-height cues, play/pause/scrub/speed + space toggle; InvariantCulture throughout; non-finite coordinate ⇒ throw per the project NaN-gate pattern; output is NOT a determinism-pinned wire format — rounded display coordinates only). `tests/MatchViewerTests.cs`: frame cadence (kickoff + stride + forced final capture), on-pitch finiteness, bitwise two-run determinism, **observer-neutrality digest lock** (a recorded run's `CurrentSnapshotDigest` == an unobserved same-seed run), fail-loud guards, exporter structure/no-NaN locks. Player JS syntax + headless render smoke-checked with node against a synthetic-data mirror of the template. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.84 | 2026-06-30 | — | `[GT]` catalogue migration onto `GameplayConfig` landed — resolves the boot-sequencing design point the FR-CS-019 loading mechanism (v1.83-era) left open. New `src/project-constants/GameplayConfigHolder.cs`: the single boot-time `Bind(config)` call point a composition root uses before any `[GT]` catalogue is referenced; `Config` resolves to `GameplayConfig.Empty` until bound (every constant keeps today's literal default — behaviour-neutral for any process, e.g. every existing test, that never calls `Bind`); the FIRST read of `Config` locks the binding, so a `Bind` call after that point throws `InvalidOperationException` instead of leaving a catalogue silently stuck on default values. Mechanically migrated 509 of the 520 `[GT]` declarations the prior entry counted, across 17 catalogues (`agent-movement`, `attacking-ai`, `ball-physics`, `collision-system`, `defensive-ai`, `deterministic-sim`, `event-system`, `first-touch`, `goalkeeper-mechanics`, `heading-mechanics`, `match-engine`, `pass-mechanics`, `perception-system`, `performance-optimization`, `shot-mechanics`, `tactical-instructions`, `testing-strategy`): each gains `using static TacticalDirector.ProjectConstants.GameplayConfigHolder;` + a `TacticalDirector.ProjectConstants` asmdef reference, and every `[GT]` field's literal RHS becomes `Config.GetFloat/GetInt("<section>", "<key>", <original literal>)` (nested-class catalogues like `BallPhysicsConstants` key as `ClassName.FieldName` to stay unique; the lone enum-typed constant, `MatchEngineConstants.STAGE0_FORMATION`, goes through `Config.GetString` + `Enum.Parse`); nine `attacking-ai`/`defensive-ai` fields that were still `public const` (never updated to `static readonly` per the Spec #20 v1.1 rule) are converted as part of the same edit — confirmed safe by grepping every call site (no `case`/attribute/array-bound usage). Two categories explicitly carved out (left as literals with their `// TODO:` marker): 11 array/table-valued `[GT]` constants in `TacticalInstructionsConstants.cs` (`GameplayConfig` has no array getter, and these tables carry their own compile-time cross-cell invariant tests — `BalancePassInvariantsTests` — that a config override could silently violate; needs its own design, not a mechanical `GetFloat` call) and `decision-tree`/`living-world`/`positioning-ai`/`pressing-ai`, whose `[GT]` constants never carried the canonical `// TODO: replace with config loader (Stage 1)` marker at all (a pre-existing authoring inconsistency, confirmed by grep, flagged rather than silently swept in). No production behaviour change — every migrated constant's value is unchanged for any process that never calls `Bind` (which is every process today; no composition root calls it yet). New `GameplayConfigHolderTests.cs` locks the Empty-default / Bind-before-lock / lock-on-first-read / Bind-after-lock-throws / ResetForTests contract. Files: `GameplayConfigHolder.cs` + `AssemblyInfo.cs` + `tests/GameplayConfigHolderTests.cs` (new), 17×`<Spec>Constants.cs` + 17×`.asmdef`, `MatchEngineConstants.cs` (+`using System;` for `Enum.Parse`). (dotnet gate not runnable locally — runs in CI on push.) |
| 1.83 | 2026-06-30 | — | Tactical Instructions (#21) §3.3 — per-agent on-disk tactic-file loader landed (the per-agent sibling of the v1.81 `TeamTacticFileLoader`, completing the Stage-1 config-loader text-format surface). New `match-engine/PlayerTacticFileLoader.cs`: `Parse(text) → PlayerTacticConfig` over a line-oriented case-insensitive `key = value` grammar under `[agent N]` section headers (N = roster index 0..SQUAD_SIZE−1) + `#` comments. Every `PlayerTactic` / `PlayerInstructions` field has a key (`role`/`duty`/`riskyPasses`/`shootTendency`/`dribbleTendency`/`crossTendency`/`positioningFreedom`/`closeDown`/`tightMarking`/`markTarget`/`setPieceRoles`); an omitted key inherits the `PlayerTactic.Default(PlayerRole.Default)` identity and an omitted/empty agent section is the full identity tactic, so an empty/comment-only/`null` file ⇒ `PlayerTacticConfig.Identity` ⇒ behaviour-neutral (FR-TI-031). Unknown key/section, out-of-range or non-numeric agent index, unparsable value, duplicate key, and duplicate section all throw `FormatException` (fail loud, never silent fallback). Scope identical to the team loader: the Stage-0 human-authoring text format, NOT a determinism-pinned wire format — only the resulting `PlayerTactic` VALUES enter the digest (via v10), never the grammar; the Stage-1 `[GT]` loader (FR-CS-019) may replace the grammar leaving `Apply` untouched. New `tests/PlayerTacticFileLoaderTests.cs` (round-trip onto fields incl. omitted-key/section inheritance, empty/comment-only/null ⇒ Identity, applied-empty digest-chain behaviour-neutrality, fail-loud cases, parsed config fed through `Apply` and routed per agent). No production `MatchEngine.cs` / asmdef change (the loader rides the existing public `SetPlayerTactic` via `PlayerTacticConfigApplier`; match-engine + tests asmdefs already reference `TacticalInstructions`). Self-adversarial review: no H/M; confirmed enum member names against the #21 enums, brace balance, that `[agent N]` index parsing rejects `[agent]`/`[agent x]`/out-of-range, that omitted sections stay identity (the `perAgent[i]==null` ⇒ identity path), and that applying the parsed empty config is digest-identical (the active per-agent tactics are already identity-seeded). Files: PlayerTacticFileLoader.cs v1.0 (new), tests/PlayerTacticFileLoaderTests.cs v1.0 (new), CLAUDE.md OPEN ISSUES #21, file-manifest.md. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.82 | 2026-06-29 | — | Tactical Instructions (#21) — **ERR-021-002 resolved**: per-team `TeamTactic` serialized into the world-state snapshot, `SNAPSHOT_SCHEMA_VERSION` 8 → 9. `MatchEngine.SerializeWorldState` now writes both the active AND pending per-team `TeamTactic` (×`TEAM_COUNT`, Appendix B field order — enums i32, `DefensiveLine` f32, `TimeWasting` u8) via a new `WriteTeamTactic` helper, after the perception block. A tactic changed **mid-match** is now restore-deterministic (the pending tactic is captured before its stride commit). Default Balanced stays byte-stable across same-seed runs. The per-agent `PlayerTactic` / team `Tempo` in `TacticalContext` need no snapshot field — re-assembled each AI tick from the serialized team tactic + the boot identity (no per-agent tactic config at Stage 0). The `_activeTeamTactics` field comment, the `SetTeamTactic` doc, and the cross-tick-coverage proof updated; `MatchEngineSnapshotSchemaTests` pin 8 → 9 + new `TeamTactic_FeedsSnapshotDigest` probe (a staged non-Balanced tactic reaches the digest preimage via the serialized pending tactic). Self-adversarial review: no H/M; confirmed the 64 KB payload buffer has ample headroom (~+260 bytes), no match-engine test hardcodes a digest value (all two-run comparisons survive a schema bump), and the ctor arg order in the probe matches `TeamTactic`. Files: MatchEngine.cs v1.22, MatchEngineConstants.cs v1.16, tests/MatchEngineSnapshotSchemaTests.cs v1.6, match-engine-design.md (D6), CLAUDE.md OPEN ISSUES #21, file-manifest.md. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.81 | 2026-06-29 | — | Tactical Instructions (#21) — on-disk tactic-file loader landed (the parser swap `TeamTacticConfig`/`Applier` were authored to receive). New `match-engine/TeamTacticFileLoader.cs`: `Parse(text) → TeamTacticConfig` over a line-oriented case-insensitive `key = value` grammar under `[home]`/`[away]` headers + `#` comments. Every `TeamTactic` field has a key; an omitted key inherits the Balanced identity, so an empty/comment-only/`null` file ⇒ `TeamTacticConfig.Default` ⇒ behaviour-neutral (FR-TI-031). Unknown key/section, unparsable value, duplicate key, and out-of-range `TimeWasting` all throw `FormatException` (fail loud, never silent fallback). Scope: the Stage-0 human-authoring text format, NOT a determinism-pinned wire format — only the resulting `TeamTactic` VALUES enter the digest (via v9), never the grammar; the Stage-1 `[GT]` loader (FR-CS-019) may replace the grammar leaving `Apply` untouched. `TeamTacticConfig` v1.1 / `Applier` remarks updated to point at the loader. New `tests/TeamTacticFileLoaderTests.cs` (round-trip onto fields, Balanced-identity for empty input, fail-loud cases, parsed config fed through `Apply` and routed per team). Files: TeamTacticFileLoader.cs v1.0 (new), TeamTacticConfig.cs v1.1, TeamTacticConfigApplier.cs, tests/TeamTacticFileLoaderTests.cs v1.0 (new), CLAUDE.md OPEN ISSUES #21, file-manifest.md. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.80 | 2026-06-29 | — | Tactical Instructions (#21) §3.3 — per-agent `PlayerTactic` utility product seam landed, behaviour-neutral. `decision-tree/TacticTranslation.cs` v1.2 gains `PlayerTacticActionMultiplier(in PlayerTactic, Tempo, ActionType)` composing the §3.3 product (`RoleWeightModifiers` × dutyBias[`DutyAggressionBias`] × instrBias[`InstrBiasMult`] × `TempoActionBias`) from the existing `TacticalInstructionsConstants` tables + an `InstructionBiasForAction` helper (each individual instruction modulates exactly its named #8 action term). `TacticalContext` v1.3 gains `Tempo` + per-agent `PlayerTactic` routing fields; `Stage0Default` seeds `Tempo.Standard` / identity `PlayerTactic.Default(PlayerRole.Default)`. `UtilityScorer.ComputeUtility` v1.6 applies the product per scored option before the clamp, next to the Mentality multiplier. Identity tactic at Standard ⇒ exactly ×1.0 (FR-TI-031) ⇒ a default match is byte-identical; magnitudes illustrative pending §5.6/G2. `MatchEngine.RunMechanicsAI` v1.21 routes the active team `Tempo` (Balanced ⇒ Standard); the **per-agent `PlayerTactic` stays the Stage0Default identity — no per-agent tactic config surface exists at Stage 0** (it lands with the G2 balance pass). `decision-tree/Tests/TacticTranslationTests.cs` extended (identity ⇒ ×1.0 for every action + role/tempo/instruction/duty directional shapes + F5 clamp). No asmdef change (decision-tree already references `TacticalInstructions`). Self-adversarial review: no H/M; confirmed every `TacticalContext` construction goes through `Stage0Default` (so the new fields are always identity-seeded — no UtilityScorer test perturbed), the duty additive→multiplicative fold is documented illustrative, and HOLD/INTERCEPT have no individual hook (follow the team default). Files: TacticTranslation.cs v1.2, TacticalContext.cs v1.3, UtilityScorer.cs v1.6, MatchEngine.cs v1.21, Tests/TacticTranslationTests.cs, CLAUDE.md OPEN ISSUES #21, file-manifest.md. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.79 | 2026-06-29 | — | Tactical Instructions (#21) — active #14/#15 consumption landed, behaviour-neutral on the Balanced default. **#14 (OffsideTrap, KD-9 additive request):** `OffsideTrapController.Update` (v1.2) now reads `DefensiveSnapshot.OffsideTrapRequested` — when `true` the trap arms after the reduced `[GT] OffsideTrapRequestedDwellTicks` (=1, ≤ baseline `OffsideTrapDwellTicks`=3); the four §3.7.2 trigger conditions still adjudicate (request, not guarantee). Default `false` (Balanced) keeps the baseline dwell ⇒ byte-identical to pre-#21. New `[GT] OffsideTrapRequestedDwellTicks` (illustrative, §5.6/G2-pending). **#15 (FocusPlay flank bias):** `OverloadDetector` (v1.1) gains a 5-arg `Evaluate(ballPos, pool, count, Flank? preferredFlank, out Flank)` overload; the existing 4-arg delegates with `null` (so the 6 test call sites are unchanged). When `preferredFlank` (from `TacticTranslation.PreferredFlank(FocusPlay)`) matches the ball-side flank, the trigger count drops by `[GT] OverloadFocusCountBias` (=1) — a bias, not a gate: `null` (Mixed/ThroughMiddle) or a ball on the non-preferred flank leaves the threshold unchanged ⇒ behaviour-neutral. `AttackingAITick` threads the translated preference into Step 8. Both magnitudes are illustrative pending the §5.6/G2 balance pass (the contract is the shape/direction). New tests: `DefensiveAITests.T_DA_032_RequestedTrapArmsAfterReducedDwell` (requested arms at reduced dwell; baseline does not), `AttackingAITests.Evaluate_FocusBias_LowersThresholdOnPreferredFlank` + `_UnchangedOnNonPreferredFlank`. No match-engine / asmdef / `SNAPSHOT_SCHEMA_VERSION` change. Self-adversarial review: no H/M; confirmed false/null defaults are exact pre-#21 (the writers already route Balanced ⇒ false/Mixed), the reduced dwell ≤ baseline invariant is asserted in the test, and the 4-arg overload preserves the autonomous baseline. Files: OffsideTrapController.cs v1.2, DefensiveAIConstants.cs (+OffsideTrapRequestedDwellTicks), DefensiveSnapshot.cs v1.3 (doc), OverloadDetector.cs v1.1, AttackingAIConstants.cs (+OverloadFocusCountBias), AttackingAITick.cs, AttackingSnapshot.cs v1.3 (doc), attacking-ai/TacticTranslation.cs (doc), Tests/DefensiveAITests.cs, Tests/AttackingAITests.cs, CLAUDE.md + src/CLAUDE.md OPEN ISSUES/WHAT-IS-NOT-HERE #21, file-manifest.md. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.78 | 2026-06-29 | — | Tactical Instructions (#21) T2 — Positioning (#12) match-engine Phase-D writer landed, behaviour-neutral — the **last of the three Mechanics writers** (closes #12/#13/#14/#15). `MatchEngine.RunMechanicsAI` now builds `ContextModifierInputs` via the 5-arg ctor, routing the active `TeamTactic.Width` / `DefensiveWidth` (which `ContextModifier.ApplyToAll` translates to the in-poss / OOP lateral-compactness scalar via `PositioningAI.TacticTranslation`, already wired in v1.75). Default Balanced ⇒ Standard / Standard ⇒ scalar ×1.00 ⇒ byte-identical to pre-#21 — the 5-arg both-Standard ctor is definitionally identical to the 3-arg identity-seeding ctor (which already seeds Standard), so the digest chain is unchanged (the existing `ExplicitBalanced_IsBehaviourNeutral_DigestUnchanged` + `NonBalancedTactic_IsDeterministic` tests still hold; the new cases assert the routed value, not a digest change). Unlike #13/#14/#15 the width does not land in a persisted snapshot — the modifier struct is a transient per-tick input passed by value to `_positioning[t].Tick`, so it is captured per-team in a new `_posModifiers` field **only** to back the `TestOnly_PositioningWidth` / `TestOnly_PositioningDefWidth` seams (allocated in the ctor next to `_posSnapshots`; assigned each AI tick before the `.Tick` call). No `SNAPSHOT_SCHEMA_VERSION` bump — the modifier is not cross-tick serialized state (a mid-match width change is not yet restore-deterministic, same class as ERR-021-002). `ContextModifierInputs` resolves unambiguously in match-engine (no CS0104 — it is a single type in `PositioningAI`, and the writer uses the ctor, not a `TacticTranslation` call). No asmdef change (match-engine already references `PositioningAI` + `TacticalInstructions`). Self-adversarial review: no H/M; confirmed the 5-arg-Standard ≡ 3-arg identity (so no perturbation of the §3.5.3 worked-example test or the default digest), per-team mapping (`_activeTeamTactics[t]`), the `WithWidth` factory differs from Balanced only in Width/DefWidth, and the new seams read the captured struct (the live input handed to `.Tick`, not a re-derivation). New `MatchEngineTacticTests.cs` v1.3 cases (per-team VeryWide/Wide vs VeryNarrow/Narrow routing + Standard default). Files: MatchEngine.cs v1.20, tests/MatchEngineTacticTests.cs v1.3, ContextModifierInputs.cs v1.2 (doc), file-manifest.md, CLAUDE.md OPEN ISSUES #21, src/CLAUDE.md WHAT-IS-NOT-HERE #21 row. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.77 | 2026-06-29 | — | Tactical Instructions (#21) T2 — Defensive (#14) + Attacking (#15) match-engine Phase-D writers landed, behaviour-neutral (the #14/#15 analogues of the v1.74 #13 writer). `MatchEngine.FillDefensiveSnapshot` routes the active `TeamTactic.OffsideTrap` → `DefensiveSnapshot.OffsideTrapRequested` via fully-qualified `TacticalDirector.DefensiveAI.TacticTranslation.OffsideTrapRequested` (CS0104 — five `TacticTranslation` types are now in match-engine scope per the v1.73 #13 lesson; identity passthrough). `MatchEngine.FillAttackingSnapshot` routes the active `TeamTactic.FocusPlay` → `AttackingSnapshot.FocusPlay` (enum passthrough; the consumer translates to `Flank?`). Default Balanced ⇒ false / Mixed = the routing identities (FR-TI-022 / FR-TI-021), so a default match is byte-identical to pre-#21 (verified by the existing `ExplicitBalanced_IsBehaviourNeutral_DigestUnchanged` + `NonBalancedTactic_IsDeterministic` digest-chain tests, which still pass — the new cases assert the routed value, not a digest change). **Active consumption stays deferred** exactly as the routing-field docs state: #14 `OffsideTrapController`'s §3.7.2 autonomous arming is unchanged per KD-9 (gating it behind a default-false toggle is not behaviour-neutral — consumption lands with the §3.7.2 additive-request design at activation); #15 `OverloadDetector` flank-preference lands with the §5.6/G2 balance pass. No `SNAPSHOT_SCHEMA_VERSION` bump — both fields are per-tick assembled inputs, not cross-tick serialized state (a mid-match change is not yet restore-deterministic, same class as ERR-021-002). New `TestOnly_OffsideTrapRequested(teamId)` / `TestOnly_FocusPlay(teamId)` seams; `MatchEngineTacticTests.cs` v1.2 adds per-team routing + identity-default cases. No asmdef change (match-engine already references DefensiveAI / AttackingAI / TacticalInstructions). Self-adversarial review: no H/M; confirmed the CS0104 fully-qualified call (DefensiveAI), that the #15 writer needs no qualifier (it routes the raw enum, no `TacticTranslation` call at the writer), and that both writers run inside the per-stride `Fill*Snapshot` before the corresponding `.Tick`. Files: MatchEngine.cs v1.19, tests/MatchEngineTacticTests.cs v1.2, DefensiveSnapshot.cs v1.2 (doc), AttackingSnapshot.cs v1.2 (doc), file-manifest.md, CLAUDE.md OPEN ISSUES #21, src/CLAUDE.md WHAT-IS-NOT-HERE #21 row. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.76 | 2026-06-29 | — | Tactical Instructions (#21) T2 — TeamTactic config-loader in-code source + boot applier landed (the runtime-activation gate that populates `SetTeamTactic`). New `src/match-engine/TeamTacticConfig.cs`: immutable per-team `TeamTactic` (index = teamId 0 home / 1 away); `Default` = `TeamTactic.Balanced` for every team (FR-TI-031 behaviour-neutral identity); `ForTeam(teamId)` bounds-guarded. New `src/match-engine/TeamTacticConfigApplier.cs`: static `Apply(engine, config)` stages each team's tactic via the existing public `SetTeamTactic` before kickoff (committed at the first AI-stride boundary per FR-TI-027); null-guards both args; applying `Default` is byte-identical to an unconfigured match. **Scope decision (user-confirmed):** the on-disk file format is NOT built — the `[GT]` config-loader (FR-CS-019) is a Stage 1 deliverable and on-disk encodings are D1-pinned at Stage 0+1, so per the #19 `ScenarioIndex` precedent Stage 0 authors the config in code and the Stage 0+1 disk loader is a pure parser swap that produces a `TeamTacticConfig` and feeds `Apply` unchanged (no invented format, no phantom interface per ERR-001/ERR-004). No production `MatchEngine.cs` change — the applier rides the existing public `SetTeamTactic` (which already stages pending → commits at stride). Both match-engine asmdefs already reference `TacticalInstructions` (v1.70) — no asmdef change. New `src/match-engine/tests/TeamTacticConfigTests.cs`: Default-Balanced-for-every-team; per-team `ForTeam` + bounds throw; applier null-guards; `Apply` routes Attacking/Defending per team through the stride boundary (translated Pressing.High→HIGH / Passing.Direct→DIRECT etc. via `TestOnly_*`); applying `Default` is digest-chain-identical to the unconfigured run. Self-adversarial review: no H/M; confirmed the applier is init-time (not hot-path, so `new`/loop/exceptions are fine), the 2-team assumption is explicit in the (home, away) ctor signature matching SetTeamTactic's "0 (home) / 1 (away)" contract, and `Default`-as-allocating-property mirrors the `TeamTactic.Balanced` precedent. Files: TeamTacticConfig.cs v1.0 (new), TeamTacticConfigApplier.cs v1.0 (new), tests/TeamTacticConfigTests.cs v1.0 (new), file-manifest.md, CLAUDE.md OPEN ISSUES #21, src/CLAUDE.md WHAT-IS-NOT-HERE #21 row. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.75 | 2026-06-29 | — | Tactical Instructions (#21) T2 — Positioning (#12) / Defensive (#14) / Attacking (#15) AI consumer seams landed, behaviour-neutral. Repeats the v1.69/v1.73 `TacticTranslation` + snapshot-routing-field pattern in all three Mechanics consumers. **#12 (active-neutral, fully wired):** new `positioning-ai/TacticTranslation.cs` maps `TacticWidth`/`TacticDefWidth` → lateral-compactness scalar via `WidthScalar`/`DefWidthScalar` (direct ordinal lookup + §3.1 F5 clamp; both `Standard` rows = ×1.00); `ContextModifierInputs` gains `Width`/`DefensiveWidth` routing fields (3-arg ctor seeds `Standard` so every existing caller stays byte-identical — zero-value enum defaults are VeryNarrow/Narrow; new 5-arg ctor for the Phase-D writer); `ContextModifier.ApplyToAll` multiplies `lateralScale` by the phase-selected width scalar (in-poss → Width, OOP → DefensiveWidth), ×1.00 exact ⇒ byte-identical to pre-#21. **#14 (routing seam, consumption deferred):** new `defensive-ai/TacticTranslation.cs` (`OffsideTrapRequested` bool passthrough); `DefensiveSnapshot.OffsideTrapRequested` field defaults to the `false` identity. Per KD-9 the toggle is a *request, never a guarantee* — gating today's autonomous §3.7.2 trap arming behind a default-false toggle would NOT be behaviour-neutral, so `OffsideTrapController` is unchanged and the arming-gate consumption lands with the Phase-D writer + activation; the field is the routing seam. **#15 (routing seam, consumption deferred):** new `attacking-ai/TacticTranslation.cs` maps `FocusPlay` → preferred `Flank?` (Mixed/ThroughMiddle → null identity; Left/RightFlank → the flank); `AttackingSnapshot.FocusPlay` auto-property zero-value is `Mixed` = identity; `OverloadDetector` flank-preference consumption deferred to Phase-D + §5.6. Each consuming assembly's production + tests asmdef gains the `TacticalInstructions` ref (refs are not transitive). New `Tests/TacticTranslationTests.cs` in each (catalogue mapping + Standard/Mixed/false identity + monotone shape + F5 clamp + default-snapshot seed neutrality). No `SNAPSHOT_SCHEMA_VERSION` bump — all three are per-tick assembled inputs, not cross-tick state. **No match-engine change** — the Phase-D writers that route the active `TeamTactic` into these three snapshots are the follow-up (and per the v1.73 CS0104 lesson, each must fully-qualify its per-assembly `TacticTranslation` at the composition root). Self-adversarial review: no H/M; confirmed #12 ×1.00 exactness preserves the §3.5.3 worked-example test (3-arg callers seed Standard), no asmdef cycle (the #21 assembly references nothing), and that #14/#15 fields are read by no production path yet (honest routing seams, not dead — awaiting the writer). Still pending (T2/T3): per-agent `PlayerTactic` field + §3.3 product factors (gated on §5.6/G2), the #12/#14/#15 match-engine Phase-D writers + active #14/#15 consumption, and the `[GT]` config-loader. Files: positioning-ai/{TacticTranslation.cs (new), ContextModifierInputs.cs v1.1, ContextModifier.cs v1.2, positioning-ai.asmdef, Tests/positioning-ai-tests.asmdef, Tests/TacticTranslationTests.cs (new)}; defensive-ai/{TacticTranslation.cs (new), DefensiveSnapshot.cs v1.1, defensive-ai.asmdef, Tests/defensive-ai-tests.asmdef, Tests/TacticTranslationTests.cs (new)}; attacking-ai/{TacticTranslation.cs (new), AttackingSnapshot.cs v1.1, attacking-ai.asmdef, Tests/attacking-ai-tests.asmdef, Tests/TacticTranslationTests.cs (new)}; file-manifest.md, CLAUDE.md OPEN ISSUES #21. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.74 | 2026-06-29 | — | Tactical Instructions (#21) T2 — Pressing AI (#13) match-engine Phase-D writer landed, behaviour-neutral. Completes the seam started in v1.73: `MatchEngine.FillPressingSnapshot` now routes the pressing team's active `TeamTactic.LineOfEngagement` into `PressingSnapshot.LineOfEngagement` (overwriting the v1.73 ctor `Standard` seed), the #13 analogue of the v1.70 #8 single-writer. `PrimaryPressSelector` already scales its trigger radius by `PressingAI.TacticTranslation.PressTriggerRadiusScalar(LineOfEngagement)`, so the writer is the only remaining wiring. Default Balanced ⇒ Standard ⇒ ×1.0 ⇒ byte-identical to pre-#21 (verified: `MatchEngineTacticTests.ExplicitBalanced_IsBehaviourNeutral_DigestUnchanged` already covers the digest chain; the new cases assert the routed value, not a digest change). New `TestOnly_PressLineOfEngagement(teamId)` seam reads `_pressSnapshots[teamId].LineOfEngagement`. No `SNAPSHOT_SCHEMA_VERSION` bump — `PressingSnapshot` is a per-tick assembled input, not cross-tick serialized state (so a mid-match line-of-engagement change is not yet restore-deterministic, same class as ERR-021-002). `LineOfEngagement` resolves unambiguously in match-engine (the #21 enum is the only type of that name in scope — no CS0104 analogue to the v1.73 TacticTranslation clash, which was a *type* collision across two assemblies; this is a field/enum-value access). Self-adversarial review: no H/M; confirmed the writer runs inside `FillPressingSnapshot` (executed on every AI-stride tick before `_pressing[t].Tick`), the per-team mapping (`_activeTeamTactics[team]`), and that the new `WithLine` test factory differs from Balanced only in `LineOfEngagement`. New `MatchEngineTacticTests.cs` v1.1 cases (per-team VeryHigh/VeryLow routing + Standard default). Files: MatchEngine.cs v1.18, tests/MatchEngineTacticTests.cs v1.1, file-manifest.md, CLAUDE.md OPEN ISSUES #21. (dotnet gate not runnable locally — runs in CI on push.) | Completes the seam started in v1.73: `MatchEngine.FillPressingSnapshot` now routes the pressing team's active `TeamTactic.LineOfEngagement` into `PressingSnapshot.LineOfEngagement` (overwriting the v1.73 ctor `Standard` seed), the #13 analogue of the v1.70 #8 single-writer. `PrimaryPressSelector` already scales its trigger radius by `PressingAI.TacticTranslation.PressTriggerRadiusScalar(LineOfEngagement)`, so the writer is the only remaining wiring. Default Balanced ⇒ Standard ⇒ ×1.0 ⇒ byte-identical to pre-#21 (verified: `MatchEngineTacticTests.ExplicitBalanced_IsBehaviourNeutral_DigestUnchanged` already covers the digest chain; the new cases assert the routed value, not a digest change). New `TestOnly_PressLineOfEngagement(teamId)` seam reads `_pressSnapshots[teamId].LineOfEngagement`. No `SNAPSHOT_SCHEMA_VERSION` bump — `PressingSnapshot` is a per-tick assembled input, not cross-tick serialized state (so a mid-match line-of-engagement change is not yet restore-deterministic, same class as ERR-021-002). `LineOfEngagement` resolves unambiguously in match-engine (the #21 enum is the only type of that name in scope — no CS0104 analogue to the v1.73 TacticTranslation clash, which was a *type* collision across two assemblies; this is a field/enum-value access). Self-adversarial review: no H/M; confirmed the writer runs inside `FillPressingSnapshot` (executed on every AI-stride tick before `_pressing[t].Tick`), the per-team mapping (`_activeTeamTactics[team]`), and that the new `WithLine` test factory differs from Balanced only in `LineOfEngagement`. New `MatchEngineTacticTests.cs` v1.1 cases (per-team VeryHigh/VeryLow routing + Standard default). Files: MatchEngine.cs v1.18, tests/MatchEngineTacticTests.cs v1.1, file-manifest.md, CLAUDE.md OPEN ISSUES #21. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.73 | 2026-06-28 | — | Tactical Instructions (#21) T2 — Pressing AI (#13) consumer seam landed, behaviour-neutral. New `src/pressing-ai/TacticTranslation.cs`: resolves `LineOfEngagement` → a multiplicative scalar on the #13 press-trigger radius (`PressTriggerDistanceM`) via `TacticalInstructionsConstants.LineOfEngagementScalar`. New `src/pressing-ai/TacticTranslation.cs`: resolves `LineOfEngagement` → a multiplicative scalar on the #13 press-trigger radius (`PressTriggerDistanceM`) via `TacticalInstructionsConstants.LineOfEngagementScalar`. Unlike the #8 Pressing/Passing maps (whose #21 and #8 enums order oppositely, forcing a rank remap), `LineOfEngagement` ordinals (VeryLow=0…VeryHigh=4) index the scalar array DIRECTLY, so this is a straight ordinal lookup with the §3.1 F5 widening clamp; Standard ⇒ ×1.0 (FR-TI-031). `PressingSnapshot` gains a `LineOfEngagement` routing field, **constructor-seeded `Standard`** — the zero-value enum default is `VeryLow` (×0.80), so the explicit seed is load-bearing for behaviour-neutrality (same zero-value-trap class as the #8 `TacticalContext.Mentality` field, here handled in the ctor since `PressingSnapshot` has no identity factory). `PrimaryPressSelector.Select` scales its eligibility radius by the scalar before the squared compare — at Standard the scalar is exactly 1.0f so `triggerDistSq` is byte-identical to pre-#21 (the field's sole production consumer; verified by grep). `pressing-ai.asmdef` + `pressing-ai-tests.asmdef` gain the `TacticalInstructions` reference (asmdef refs are not transitive). New `Tests/TacticTranslationTests.cs` (scalar validity + Standard identity + default-snapshot ctor-seed behaviour-neutrality + monotone shape + F5 clamp). No `SNAPSHOT_SCHEMA_VERSION` bump — `PressingSnapshot` is a per-tick assembled input, not cross-tick serialized state. The match-engine Phase-D **writer** that routes the active `TeamTactic.LineOfEngagement` into the snapshot is the follow-up (the #13 analogue of the #8 v1.70 single-writer — `TeamTactic.LineOfEngagement` already exists, `Balanced` = `Standard`, so the seam is genuinely connectable). Self-adversarial review: no H/M; confirmed exact-1.0f identity (×1.0 float multiply is exact ⇒ existing PressingAITests + match-engine default match unchanged), the single-consumer grep, the layer direction (Mechanics→#21 data layer is the intended #8/#11–#15 design, no cycle — #21 asmdef refs are empty), and the internal-fixture-with-public-methods convention (matches the sibling #8 TacticTranslationTests + shot-mechanics WeakFootTests/LaunchAngleTests). Still pending (T2/T3): the per-agent `PlayerTactic` field + §3.3 product factors (gated on §5.6/G2), the #13 match-engine writer, and the #12/#14/#15 consumer maps + snapshot fields. Files: TacticTranslation.cs v1.0 (new), PressingSnapshot.cs v1.1, PrimaryPressSelector.cs v1.2, pressing-ai.asmdef, pressing-ai-tests.asmdef, Tests/TacticTranslationTests.cs v1.0 (new), file-manifest.md, CLAUDE.md OPEN ISSUES #21. **Follow-up build fix (CS0104, caught by CI run #147 on PR #190):** the new `PressingAI.TacticTranslation` collided by name with the existing `DecisionTree.TacticTranslation`, and `MatchEngine` references both assemblies — the two bare `TacticTranslation.ToPressingMode/ToPassingStyle` calls in `RunMechanicsAI` (from the #21 Phase-D writer, MatchEngine.cs v1.16) became ambiguous. Fully qualified to `TacticalDirector.DecisionTree.TacticTranslation` (MatchEngine.cs v1.17; no behaviour change). My self-review had only checked same-assembly resolution and missed the composition-root cross-assembly name clash — a hazard for every future per-assembly `TacticTranslation` seam (#12/#14/#15). (dotnet gate not runnable locally — runs in CI on push.) |
| 1.72 | 2026-06-28 | — | FR-PO-052 certified perf baseline corpus machinery. The Phase F capstone (v1.68) activated the per-tick perf gate against a *generous in-code Stage-0 anchor* explicitly tagged NON-certifying; this adds the certified-baseline layer that anchor stands in for. New `src/performance-optimization/CertificationStatus.cs` (Pending/Certified, append-only ordinals, Pending=0 = safe default) + `CertifiedPerfBaseline.cs` (a certification-tagged corpus entry). At Stage 0 the kickoff entry is **Pending**: it carries NO measured metric (NaN p50/p99) and `TryBuildBaselineRecord` returns false — a certified number MUST come from the pinned Windows/Unity tuple (`certification-platform.md` v1.2) and the Linux compile/test gate is explicitly NON-certifying, so admitting a Linux number would be a fabricated certification. `Certified(manifest, loop, p50, p99, threshold)` validates a complete `SessionManifest` (`IsComplete()`) + finite positive metrics (p99≥p50; all fail-closed) and projects to a corpus `BaselineRecord` ready for `PerfGateRunner`/`RegressionGate`. Platform-pin tokens `Stage0CertPlatformPin` (the cert tuple) + `LinuxNonCertPlatformPin`; `MatchEngineCapstoneTests.cs` v1.1 swaps its hardcoded non-cert pin string for the named constant (behaviour-neutral). First on-disk corpus artifact `docs/specs/performance-optimization/baselines/match-engine/kickoff-multi-second.cert.md` (PENDING_CERT_RUN + promotion runbook) — the `baselines/` dir previously held only `.gitkeep`. New `CertifiedPerfBaselineTests.cs` locks the PENDING refusal, the certified projection (self-compare through `PerfGateRunner` → pass), and the fail-closed invariants. **No production `MatchEngine.cs` change.** Self-adversarial review: fixed a fabricated `EmptyReadOnlyDictionaryFactory` reference (→ plain empty `Dictionary`) and a wrong doc path prefix (`cross-spec/` → `tests/scenarios/cross-spec/`), removed an unused `using`; confirmed `float.IsFinite`/`Assert.IsNaN`/exact `Assert.Throws<T>` types, asmdef refs already present, and that pin strings live on the cert type (not the §3.10-mirrored constants catalogue — no spec-sync obligation). Files: CertificationStatus.cs v1.0 (new), CertifiedPerfBaseline.cs v1.0 (new), tests/CertifiedPerfBaselineTests.cs v1.0 (new), kickoff-multi-second.cert.md v1.0 (new), MatchEngineCapstoneTests.cs v1.1, match-engine-design.md v1.3, file-manifest.md, CLAUDE.md OPEN ISSUES. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.71 | 2026-06-28 | — | Decision Tree (#8) deferred away-team closed-loop scenario landed on the #19 ScenarioRunner — the audit-report follow-up, enabled by the #21 runtime-activation single-writer (the DT orchestrator seam it was gated on). New `src/match-engine/tests/MatchEngineAwayTeamScenarios.cs` + `MatchEngineAwayTeamTests.cs`: registers `away-team-tactic-mirror` (Tier B; owning specs `{2,8,16,19,21}`; path under `SCENARIO_PATH_CROSS_SPEC_PREFIX`), boots a real `MatchEngine`, sets home=defending / away=attacking, ticks 300× (5 s), and locks through the composed host that every away agent carries the away (attacking) routed tactic, every home agent the home (defending) one, the two partitions distinct (the composition-level inverse of the H-2/M-1/M-2 "away served the home perspective" root cause), away agents in bounds, and a two-run determinism digest match; `MatchEngineAwayTeamTests` runs it through `ScenarioRunner.Run` → `Passed`. No production change — reads world state via the existing `TestOnly_Mentality/Pressing/Passing` + `TestOnly_AgentSnapshot` seams. `match-engine-tests.asmdef` already carried every ref used. Self-adversarial review: no H/M (overlay confirmed to write the tactic for all 11 agents incl. the GK at k=0; tick 300 is a stride boundary so the active tactic is fully committed; both runs set the same asymmetric tactic so the digest chains match). Files: MatchEngineAwayTeamScenarios.cs v1.0 (new), MatchEngineAwayTeamTests.cs v1.0 (new), CLAUDE.md OPEN ISSUES (DT #8 follow-up → LANDED), file-manifest.md. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.70 | 2026-06-28 | — | Tactical Instructions (#21) T2 runtime activation — match-engine Phase-D single-writer for Decision Tree (#8), behaviour-neutral. `MatchEngine` gains per-team `_active`/`_pendingTeamTactics` (default `TeamTactic.Balanced`), a public `SetTeamTactic(teamId, in TeamTactic)` that stages a pending change, a pending→active commit at the AI-stride boundary in `RunAiPhase` (FR-TI-027 — RunAiPhase runs only on stride ticks), and a `RunMechanicsAI` overlay that writes the active tactic's `Mentality` (drives the §3.2/§3.3 `UtilityScorer` risk mult) + `TacticTranslation.ToPressingMode/ToPassingStyle` results into each agent's `TacticalContext`. `TacticTranslation` promoted internal→public (the match-engine is its §3.1 caller). Default Balanced ⇒ MEDIUM/MIXED/×1.0 = `Stage0Default`, so a default match is byte-identical to pre-#21 (`TacticalContext` + the tactic arrays are NOT serialized → no `SNAPSHOT_SCHEMA_VERSION` bump; a mid-match change is therefore not yet restore-deterministic — ERR-021-002 deferred). A non-Balanced tactic activates both the new Mentality multiplier and the pre-existing `TacticalModifierResolver` Pressing/Passing modifiers (previously frozen at MEDIUM/MIXED). `DefensiveLineDepth` stays the #14 output (the §3.4 mentality-line recompute is deferred with the #12/#14 depth-ownership wiring). New `TestOnly_Mentality/Pressing/Passing` seams; `match-engine.asmdef` + `match-engine-tests.asmdef` gain the `TacticalInstructions` ref. New `MatchEngineTacticTests.cs` (per-team routing + translation + FR-TI-027 stride-gating + explicit-Balanced behaviour-neutrality vs the default digest chain + same-tactic determinism). Self-adversarial review: no H/M; N1 (translate-per-stride, acceptable) + N2 (mid-match not restore-deterministic, documented) noted. Files: MatchEngine.cs v1.16, TacticTranslation.cs v1.1, match-engine.asmdef, match-engine-tests.asmdef, tests/MatchEngineTacticTests.cs v1.0 (new), match-engine-design.md, file-manifest.md, CLAUDE.md + src/CLAUDE.md OPEN ISSUES/WHAT-IS-NOT-HERE. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.69 | 2026-06-28 | — | Tactical Instructions (#21) T2 — Decision Tree (#8) consumer seam landed, behaviour-neutral. New `src/decision-tree/TacticTranslation.cs`: rank-mapped TacticPressing→PressingMode / TacticPassing→PassingStyle (the #21 enums order ascending Low/Short=0 while the #8 enums order descending HIGH/DIRECT=0, so a raw cast inverts — mapped by intensity rank instead) + §3.1 F5 widening-clamp + §3.2 Mentality risk/line resolvers off `TacticalInstructionsConstants`. `TacticalContext` gains a `Mentality` routing field (`Stage0Default` seeds `Balanced` = identity, FR-TI-031; zero-value struct default is `VeryDefensive`, so consume only via the factory — documented, same contract as `Pressing`/`DefensiveLineDepth`). `UtilityScorer.ComputeUtility` multiplies each option by the Mentality risk multiplier before the clamp (§3.2/§3.3) — Balanced ⇒ ×1.0, so scoring is unchanged until the match-engine Phase-D writer routes a live tactic. `decision-tree.asmdef` + `decision-tree-tests.asmdef` gain the `TacticalInstructions` reference (asmdef refs are not transitive). New `Tests/TacticTranslationTests.cs` (translation validity + non-inversion + F5 clamp; Balanced identity + Stage0Default no-op; monotone risk/line shape). No `SNAPSHOT_SCHEMA_VERSION` bump — `TacticalContext` is a per-tick assembled input, not cross-tick state (§3.4). Self-adversarial review: F1 (Mentality field doc clarified — zero-value is VeryDefensive not Balanced) + F2 (TacticTranslation.cs missing `// Modified:` header, added) resolved; F3 (mentalityRiskMult is uniform-across-actions by §3.2 design, so on its own it only reorders via the clamp — the relative-to-HOLD gradation arrives with the deferred per-action factors) noted, no change. Still pending (T2/T3): per-agent `PlayerTactic` + the `RoleWeightModifiers`/duty/instr/tempo product (gated on §5.6/G2 balance pass), the #12–#15 maps + snapshot fields, runtime activation. Files: TacticTranslation.cs v1.0 (new), TacticalContext.cs v1.2, UtilityScorer.cs v1.5, decision-tree.asmdef, decision-tree-tests.asmdef, Tests/TacticTranslationTests.cs v1.0 (new), file-manifest.md, README.md (Phase F status), CLAUDE.md OPEN ISSUES #21. (dotnet gate not runnable locally — runs in CI on push.) |
| 1.68 | 2026-06-28 | — | Match Engine Phase F (capstone) landed — `src/match-engine/` integration (Phases A–F) complete. New `src/match-engine/tests/MatchEngineCapstoneScenarios.cs` registers `match-engine-kickoff-multi-second` on the #19 `ScenarioRunner` (owning specs {1,2,3,4,5,6,7,8,12,13,14,15,16,17,19}, Tier B): boots a real `MatchEngine`, ticks it 600× (10 s @ 60 Hz) through all 7 phases, and records gameplay-invariant predicates (tick-count 600; `ai-stride-cadence` = `NumTicks / AI_PHASE_STRIDE` = 100, locking the 10 Hz/60 Hz loop separation; ball + agents finite and on-pitch every tick; chained snapshot digest advances every tick) plus a two-run same-seed determinism digest match (also re-locks `EventBus.ResetForNewMatch()` across two in-process matches). New `MatchEngineCapstoneTests.cs` runs the scenario through `ScenarioRunner.Run` → `Passed`, adds a direct two-run digest-chain equality test, and activates the FR-PO-052 per-tick perf gate via `PerfGateRunner.Run` against a generous Stage-0 anchor `BaselineRecord` (loop `PhysicsSixtyHz`; NON-certifying Linux gate — proves the perf-gate wiring, not the budget; authoritative per-tick budget stays on the pinned Windows/Unity tuple per certification-platform.md v1.2). No production `MatchEngine.cs` change — the scenario reads world state through the existing internal `TestOnly_*` seams + the public `CurrentTick`/`AiPhaseRunCount`/`CurrentSnapshotDigest`. `match-engine-tests.asmdef` gains `TacticalDirector.TestingStrategy` + `TacticalDirector.PerformanceOptimization`. Files: MatchEngineCapstoneScenarios.cs v1.0 (new), MatchEngineCapstoneTests.cs v1.0 (new), match-engine-tests.asmdef, match-engine-design.md v1.0, file-manifest.md. (dotnet gate not runnable locally — no .NET SDK in this environment; runs in CI on push.) |
| 1.67 | 2026-06-21 | — | Tactical Instructions (#21) T0 landed — first slice of #21 §7.2. New `src/tactical-instructions/` assembly (`TacticalDirector.TacticalInstructions`): 16 byte-backed enums (Mentality / Tempo / TacticWidth / TacticDefWidth / LineOfEngagement / TransitionPlan / GkDistributionPolicy / FocusPlay / TacticPassing / TacticPressing / TacticTriggerMask[Flags] / TacticFormation / Duty / PlayerRole / InstrBias / SetPieceDutyFlags[Flags]); 3 readonly-struct carriers (TeamTactic / PlayerInstructions / PlayerTactic) with identity factories (Balanced / Default / Default(role)) that reproduce today's no-instruction baseline (KD-10 / FR-TI-031); TacticalInstructionsConstants.cs (Appendix A — Fixed/Derived/GT). Behaviour-neutral; no consumer wired (seams are T2–T3 at match-engine Phase D). asmdef `references` empty until project-constants exists (FR-TI-002 — T0 consumes nothing from it). The two [DERIVED] identity-row constants are expression-bodied properties, NOT static readonly fields, to dodge the static-init-order trap (#20 region order Fixed→Derived→GT places them before their GT source arrays; same defect class as Perception BASE_FOV_HALF_ANGLE / EventRegistry). Tests: EnumOrdinalStabilityTests (FR-TI-007 — 16-enum ordinal/bit-position/byte-backing/8-flag-ceiling locks) + FactoryIdentityTests (FR-TI-031 identity factories + catalogue identity rows + RoleWeightModifiers [0.5,2.0] T-TI-U-029 + table dimensions). src/CLAUDE.md tree + WHAT-IS-NOT-HERE row updated; file-manifest.md section added. |
| 1.65 | 2026-06-12 | — | Deterministic Simulation (#16) golden-vector implementations (§9.5 #4(a)/(b)/(c)). New KAT fixtures: HkdfSha256KatTests (RFC 5869 A.1–A.3 PRK + full OKM; pinned project Test Case 4), SipHash24KatTests (all 64 Appendix A vectors; pinned project draw case), SerializeCanonicalCorpusTests (all 41 corpus entries, bytes + SHA-256). Production: HkdfSha256 T(1)-only → full RFC 5869 §2.3 multi-block Expand (HkdfExtract/HkdfExpand split; production L=16 path unchanged); CanonicalSerializer.WriteF64TierB added (corpus F-09). Compile blockers: new AssemblyInfo.cs InternalsVisibleTo (internal Hkdf*/SipHash calls in the test assembly had never been compilable); DeterministicSimTests v1.1 stray namespace closure fixed (save/load fixture stranded in global namespace — ERR-004 defect class, seventh instance). KAT stubs pinned with stale inputs corrected first (info SMP-RNG-MATCH → DS-RNG-KEY-v1 per §3.2.4; L 32→16; IKM 32-byte→u64 LE; StreamKey 16-byte→u64 per §3.4). All values verified by independent Python mirrors + the corpus reproducer (45/45). Files: DeterministicRngService.cs v1.2, CanonicalSerializer.cs v1.2, AssemblyInfo.cs v1.0, tests ×4, golden-vectors ×2 (v1.2), file-manifest.md. |
| 1.66 | 2026-06-12 | — | Non-certifying Linux compile/test CI gate (`tools/dotnet-ci/`): asmdef→csproj generator + UnityEngine shim; entire tree compiles and all suites run under `dotnet test` on CI. Eight never-compiled surfaces fixed (headline ERR-017-002: constraint-only EventBus Publish/Subscribe overloads, CS0111, spec §3.2.1/§3.2.2 patched same commit → single-method EventTierCache<T> dispatch; plus Unity.Profiling namespace ×18, netstandard2.1 File.Move(overwrite:), ALL_CAPS enum members, missing usings, int? coercion, sixth stray-brace dead suite, 51 internal [Test] methods, fabricated SipHash vectors 4–7). First-ever execution of 1,165 tests; 30 genuine model/expectation failures quarantined shrinking-only in known-failures.txt + docs/tracking/dotnet-ci-quarantine.md. See the Last-Updated header entry for the full file list. |
| 1.63 | 2026-06-11 | — | Pass Mechanics (#5) AR-9 fix pass (1H+3M+5L) + AR-10 sweep (2L, both resolved same commit). H-1: Tests/PassMechanicsTests.cs never compiled since v1.1 — namespace closed before the appended IT- fixture (stray `}` at EOF, CS1022; fixture stranded in global namespace; identical defect class to First Touch ERR-004). Namespace now closes at EOF; [TestFixture] added. M-1: PassExecutor Idle-guard rejection no longer writes `_lastResult` (an Execute() during FollowThrough/Complete destroyed the committed Completed record incl. ContactFrame replay-sync data). M-2: FM-07 gate `d <= 0f` → `!(d > 0f) \|\| IsInfinity(d)` — NaN distance previously passed the gate and was silently sanitised to a 0.001 m pass by Mathf.Max ordering (project NaN-gate pattern). M-3: stale tackle flag drained (discarded) at INITIATING — the flag is cleared only by WINDUP polling, so a tackle registered during FollowThrough/Idle cancelled the NEXT pass on its first WINDUP frame. L-1: CONTACT pressure re-sample queries passer position fresh (`_passerPosition` field removed; was up to ~15 frames stale). L-2: ComputeErrorAngle NaN fallback MinErrorAngle → MaxErrorAngle (fail-closed; was producing a 0.1° laser pass on corrupted input). L-3: PhysicalProfile.DistMin/DominantSpin/IsAerial + PassAgentAttributes.Crossing doc-noted unconsumed; IsAerialFormula parallel-surface hazard recorded (9-profile agreement verified). L-4: PassOutcome.Cancelled + PassAgentState.Position doc corrections. L-5: ComputeThroughBallAimPoint SPEC-DEVIATION NOTE — kickSpeed derived from IntendedDistance before lead projection ⇒ led passes underhit (KD-4 / §7.1 Stage 1 upgrade). New PassExecutorGuardTests (PX-001..004) lock M-1/M-2/M-3 via stub seams (no EventBus boot required). AR-10: FM-07 log wording + IsAerial doc wording. Files: PassExecutor.cs v1.12, PassErrorCalculator.cs v1.8, PassTargetResolver.cs v1.8, PhysicalProfile.cs v1.2, PassAgentAttributes.cs v1.1, PassAgentState.cs v1.1, PassOutcome.cs v1.3, Tests/PassMechanicsTests.cs v1.2. |
| 1.62 | 2026-06-10 | — | First Touch (#4) closed-loop scenario corpus on the #19 ScenarioRunner, extending the AR-7-motivated Simulation-layer coverage to the fourth consecutive spec whose unit suite encoded rather than caught its model defects. New src/first-touch/Tests/FirstTouchScenarios.cs + FirstTouchScenarioTests.cs: `heavy-touch-runs-on` locks AR-7 H-1 / ERR-004-003 — load-bearing predicate is displacement-velocity COHERENCE (dot(dispXY, velXY) > 0; the pre-fix negated blend displaced backward against forward retention) plus the downstream dx<0 sign and the Apply seam forwarding verbatim (exact component equality per T-AM-115 precedent); `interception-chain-anchors-at-displaced-ball` locks ERR-004-004 + §3.4.5 — opponent 3.3 m from the agent (invisible to BOTH the old agent-anchored 2.5 m gate and the 3.0 m PressureRadius truncation) but 1.37 m from the displaced ball MUST intercept; context produced by the REAL PressureEvaluator (zero pressure + global nearest tracked), redirect alignment ≈ 1.0 asserted, and the §3.4.5 Frame N+1 chain is executed: the interceptor's own touch on the redirected 10.6 m/s ball achieves CONTROLLED (q≈0.946) with possession + dribbling assigned. Envelope windows from the numerical mirror (toucher q=0.150, r=1.933, displaced x=50.57). New src/first-touch/AssemblyInfo.cs (InternalsVisibleTo — pass-mechanics/agent-movement precedent) so the corpus can drive the internal producer; first-touch-tests.asmdef gains the testing-strategy reference. file-manifest reconciled. |
| 1.61 | 2026-06-10 | — | First Touch (#4) AR-7 fix pass (1H+3M+3L) + AR-8 sweep (2M, both resolved; ERR-004-003..006 filed, ERR-004-005 documented-open). H-1/ERR-004-003: §3.3.2 direction-blend sign INVERTED — IncomingDir negated ball velocity (“direction the ball came FROM”) while the same spec subsection states four times that a q=0 touch continues ALONG its original path and §3.3.5 retains momentum along +v; a heavy touch teleported the ball up to 2.0 m back toward the passer while its velocity pointed forward (the ball then travelled back through the receiver). Spec §3.3.2 pseudocode patched in the same commit (section-3-1-to-3-5.md v1.4); OrientationDetector's negation is CORRECT (facing-vs-approach) and untouched. The test suite ENCODED BOTH conventions simultaneously (BD-002 travel-direction vs BD-003/BD-004 negated — mutually unsatisfiable) and has NEVER compiled: v1.1 closed the namespace before FirstTouchIntegrationTests leaving 170 '{' vs 171 '}'. M-1/ERR-004-004: INTERCEPTION proximity was agent-anchored via PressureRadius-truncated NearestOpponentDistance — §3.4.2 anchors at newBallPosition (anchor error up to 80% of the 2.5 m radius; opponents 2.5–3 m from the displaced ball but >3 m from the agent were invisible); PressureEvaluator now tracks the global nearest opponent + NearestOpponentPositionXY, PossessionStateMachine measures |opponent − newBallPos| ≤ radius (Stage 0 single-candidate approximation documented; full SpatialQuery + interceptor ID land with ERR-004-002). M-2: degenerate-blend fallback aligned to the §3.3.2 IncomingDir mandate (was agentFacing). M-3: §3.4.5 INTERCEPTION velocity redirect toward the interceptor implemented (was specified, never coded — Frame N+1 contact chain was broken). L: dead q/originalBallVel params dropped from Determine; ControlledThreshold doc no longer claims the CONTROLLED gate; dead PitchHalf* removed + ControlledRadius/DribbleDetachRadius doc-noted unconsumed; test side-comments. AR-8 sweep: M-1 EvaluateFirstTouch NaN gate — Mathf.Clamp01 does not filter NaN so NaN BallVelocity flowed into ControlQuality (EC-001 encoded the contract but could not pass); Step 0 sanitise maps non-finite velocity/direction components to 0 (parallels AM AR-10 / CS AR-7). M-2/ERR-004-006: §5.10 VS-001 hand-calc used an ADDITIVE below-reference velocity modifier (r=0.428 m) contradicting normative §3.2.3 (multiplicative excess-only → r=0.195 m); spec §5.10 v1.1 + §5 changelog v1.4 + test re-derived. ERR-004-005 (documented-open): the DEFLECTION alignment gate is effectively vacuous through the public pipeline — heavy-touch q makes retention dominate so alignment ≈ 1.0; PO-005's low-alignment LOOSE_BALL expectation was unreachable and is re-derived to lock actual behaviour. All expectations re-verified by a full-pipeline numerical mirror (21 scenarios). Files: BallDisplacementProcessor.cs v1.5, PossessionStateMachine.cs v1.3, FirstTouchSystem.cs v1.6, PressureEvaluator.cs v1.3, PressureResult.cs v1.1, FirstTouchContext.cs v1.2, FirstTouchConstants.cs v1.8, Tests/FirstTouchTests.cs v1.2, docs/specs/first-touch/section-3-1-to-3-5.md v1.4 + section-5-7-to-5-13.md v1.1 + section-5-1-to-5-6.md v1.4, spec-error-log.md v1.25, file-manifest.md first-touch rows. |
| 1.0 | 2026-05-19 | — | Initial creation. All 20 Stage 0 specs approved; coding begins. |
| 1.1 | 2026-05-19 | — | Adversarial review v1.0 fix pass. H-1: layer taxonomy rebuilt from §3.5.2. H-2/H-3: dependency arrows corrected. H-4: Author and Purpose added to file header template. M-1: FMA ban added. M-2: dynamic/async/unsafe bans added. M-3: four architectural anti-patterns added. M-4: phantom TacticalDirector.Shared replaced. M-5: [CROSS] naming contradiction flagged. M-6: Spec #19 blocker resolved to §7.5 D2. L-1: style section added (indentation, Allman braces). L-2: project-constants.asmdef added to tree. L-3: commented-out code ban added. L-4: [EST] spec-error-log requirement added. L-5: var policy added. |
| 1.2 | 2026-05-19 | — | Adversarial review v1.1 fix pass (2H · 7M · 8L). H-1: arrow label corrected to "is referenced by." H-2: ConfigLoader fabrication removed; [GT] loading noted as Stage 1 TBD. M-1: s_fixedUpdateMarker declaration added to game-loop example; field naming convention added. M-2: [CROSS] mirror RHS corrected to ProjectConstants.PHYSICS_TICK_HZ (ALL_CAPS). M-3: tree comment for ProjectConstants.cs: wrong tag and scope fixed. M-4: single vs multi-consumer [CROSS] routing rule documented. M-5: C# 10+ note added to `with {}` example. M-6: infrastructure assembly table added to taxonomy section. M-7: .asmdef coverage note added under tree. L-1: Last Updated header field added. L-2: ProfilerMarker field naming rule added. L-3: `using UnityEngine.Profiling;` added to profiler example. L-4: var policy semicolon fixed. L-5: owning assembly column added to determinism table. L-6: BallCollision.cs vs collision-system/ note added to tree. L-7: [CROSS] XML doc updated to cite spec+section. L-8: foreach ban reworded for technical accuracy. |
| 1.3 | 2026-05-19 | — | Adversarial review v1.2 fix pass (2H · 5M · 4L). H-1: project-constants diagram line fixed; removed broken ← arrow (RHS was prose). H-2: // §3.4.4 → // Spec #16 §3.4.4 in Determinism Rules and Inline Comments sections. M-1: Physics→AI prohibition rewritten in prose (inconsistent arrow direction). M-2: async/await entry scoped to "game-loop / game-state-modifying"; heading updated to match. M-3: tests/ .asmdef entries added to all five expanded spec folders; .asmdef coverage note extended with test-assembly rule. M-4: foreach parenthetical covers both List<T>.Enumerator and Dictionary.Enumerator. M-5: [GT] region comment updated to match actual code pattern (= 8; // TODO:). L-1: — author placeholder documented in File Header section. L-2: .asmdef deferral entry expanded to all unresolved fields. L-3: DI bullet in required-patterns replaced with cross-reference to Banned Architectural Patterns section. L-4: ProfilerMarker naming comment moved outside game-loop code block. |
| 1.4 | 2026-05-22 | — | Adversarial review v1.3 fix pass (1H · 4M · 3L). H-1: Game-Loop COMPLIANT example rewritten as sealed instance class (public void); VIOLATION updated to match. M-1: [EST] promotion targets extended to [GT] / [FIXED] / [DERIVED] / [CROSS] with guidance for each path. M-2: Profiler Markers entry-point list changed from FixedUpdate/Update to Update/Tick/RunStep; MonoBehaviour-not-applicable note added; examples updated (FixedUpdate → Update, s_fixedUpdateMarker → s_updateMarker); WHAT IS NOT HERE YET row added for MonoBehaviour/PlayerLoop integration. M-3: Naming discrepancy note updated with ERR-020-001 reference and confirmation that §4.2 has been patched. M-4: stackalloc Span<T> vs pointer distinction added. L-1: §3.2 → §3.2.3 in [GT] XML doc. L-2: [DERIVED] worked example added; region comment shows formula instead of ellipsis. L-3: #region name convention (Title Case vs acronym) documented. |
| 1.5 | 2026-05-22 | — | Adversarial review v1.4 fix pass (1H · 1M · 5L). H-1+M-1 (combined): Game-Loop COMPLIANT example rewritten to show constructor injection (_clock field + constructor body); method renamed Update, field renamed s_updateMarker, profiler string "BallPhysics.Update"; VIOLATION moved inside class as commented-out method. L-1: "two-letter acronyms" → "all-caps abbreviations" (EST has 3 letters). L-2: VIOLATION was orphaned outside class at file scope (invalid C#); now inside BallPhysicsSystem as commented-out member. L-3: Root CLAUDE.md "Heartbeat Tick Rate" removed from [CROSS] XML doc example (non-spec citation); Ball Physics #1 §1.2 alone is sufficient. L-4: ProfilerMarker required-patterns bullet rewritten to distinguish the field declaration (one-time alloc) from the .Auto() call at entry points. L-5: Single-consumer [CROSS] mirror example added alongside multi-consumer example. |
| 1.6 | 2026-05-22 | — | Adversarial review v1.5 fix pass (0H · 1M · 2L). M-1: Profiler Markers BallPhysicsSystem example gained a note "Profiler-relevant fields shown; constructor and injected dependencies follow Game-Loop Rules COMPLIANT example." L-1: commented-out VIOLATION removed from inside COMPLIANT class body (violated FR-CS-065); restored as standalone labeled snippet outside the class. L-2: private static field naming convention (s_camelCase) added to NAMING CONVENTIONS table. |
| 1.7 | 2026-05-25 | — | Agent Movement adversarial review fix pass (M-A / L-A). M-A: agent-movement/ tree expanded to all 13 implemented files with role annotations. L-A: readonly struct deferral row added to WHAT IS NOT HERE YET; explains why AgentState is mutable pending C# version pin. |
| 1.8 | 2026-05-25 | — | Pass-4 follow-up. Constants tree: PlayerAttributeConstants added to AgentMovementConstants.cs annotation (8 classes). |
| 1.9 | 2026-05-25 | — | Tracking-doc sync. ball-physics/ tree: `BallStateSystem.cs` removed (never implemented); `BallStateMachineTests.cs` added (exists at `src/Core/Physics/Ball/Tests/`); structural deviation warning added (actual path is `src/Core/Physics/Ball/`, not `src/ball-physics/`). |
| 1.10 | 2026-05-27 | — | Tree expanded for Collision System (#3) 18 files with role annotations; First Touch (#4) 15 files with role annotations; Pass Mechanics (#5) 22 files with role annotations. agent-movement/ tests updated: `AgentMovementTests.cs` added (from AR-2/AR-3 pass May 27). |
| 1.11 | 2026-05-27 | — | AR-1 pass-mechanics adversarial review fix pass. L-2: corrected stale enum value descriptions for PassType.cs / CrossSubType.cs / SpinType.cs / PassOutcome.cs. M-3: PassEvents.cs renamed to CancelReason.cs in tree. |
| 1.12 | 2026-05-28 | — | Shot Mechanics (#6) tree expanded: 27 files + Tests/NaNVelocityStub.cs. AR-1 fix pass applied (H-1: BodyMechanicsResult extracted; H-2: GoalGeometry extracted; H-3: underscore GT constants renamed to PascalCase; M-1: unused AdvanceWindup param removed; M-2/M-3/M-4: magic literals promoted to constants). |
| 1.13 | 2026-05-28 | — | Heading Mechanics (#10) tree expanded: 25 files with role annotations (heading-mechanics.asmdef + 23 .cs files). |
| 1.14 | 2026-05-28 | — | Goalkeeper Mechanics (#11) tree expanded: 36 files with role annotations (goalkeeper-mechanics.asmdef + 35 .cs files). AR-1 (5H+1M) + AR-2 (2M) review cycles completed; all findings fixed. |
| 1.15 | 2026-05-29 | — | Perception System (#7) tree expanded: 14 files with role annotations (perception-system.asmdef + 13 .cs files). AR-1 (3M+3L) review cycle completed; all findings fixed. |
| 1.16 | 2026-05-29 | — | Decision Tree (#8) tree expanded: 36 files with role annotations (35 .cs files + 1 asmdef). AR-1 (2H+3M+4L) review cycle completed; all findings fixed: H-1 AssemblyInfo.cs InternalsVisibleTo; H-2 SplitMix64 unchecked{}; H-3 DecisionContextAssembler possession classification bug; M-1 ScoreMove possession source; M-2 OptionGenerator using directive; M-3 DtAgentAttributes.Stamina doc; L-1 magic numbers → named constants; L-2 AttributeNormMin XML doc; L-3 UTILITY_FLOOR/CEILING XML docs; L-4 CrossSubType tautological ternary. AR-2 full sweep: no new findings. |
| 1.17 | 2026-05-29 | — | Positioning AI (#12) tree expanded: 20 files (19 .cs + 1 asmdef) with role annotations. AR-1 (2H+4M+1L) + AR-2 (2M+3L) + AR-3 clean review cycles completed. Key fixes: H-1 entityIdArr zero-alloc (SlotComposer parameter + PositioningAITick field); H-2 squad-size validation; M-1 LANE_DWELL_TICKS constant; M-2 dead _entityIdMap removed; M-3 LaneEdgesM literals → PITCH_WIDTH_M fractions; M-4 dead `slots` variable in ShapeAnalyzer; VersionHistory regions all 20 files. |
| 1.18 | 2026-05-29 | — | Pressing AI (#13) tree expanded: 21 files (20 .cs + 1 asmdef) with role annotations. AR-1 (3H+1M+1L) + AR-2 clean review cycles completed. Key fixes: H-1 IsActive field added to PressingAgentSnapshot + guards in 5 files (TriggerEvaluator, PrimaryPressSelector, CoverShadowSelector, InvariantEnforcer, PressingAITick); H-2 unit mismatch in TriggerEvaluator.EvaluateBackwardPass (len→len*len vs SpacingEpsilonM2); H-3 PrimaryPressSelector eligibility uses carrier position not interception point; M-1 PressingAITick missing using TacticalDirector.PositioningAI; L-1 PressTrigger stale doc comment UpdateDebounce→Evaluate. |
| 1.19 | 2026-05-29 | — | Defensive AI (#14) tree expanded: 19 files (18 .cs + 1 asmdef) with role annotations. AR-1 (2H+1M) + AR-2 clean review cycles completed. Key fixes: H-1 DefensiveAIConstants.SQUAD_SIZE corrected from literal 22 to mirror PressingAIConstants.SQUAD_SIZE (true [CROSS] reference); H-2 MarkAssigner.Assign now refreshes ValidThroughTick during PreCheck dwell lock (external consumers saw stale tick on retained assignments); M-1 LastManDetector.Evaluate guards GkEntityId < 0 before GK-zone distance check (Vector2.zero gave false COVER_GK_ZONE trigger for x=105-defending teams with no GK). |
| 1.20 | 2026-05-29 | — | Attacking AI (#15) tree expanded: 24 files (23 .cs + 1 asmdef) with role annotations. AR-1 (2H+4M) + AR-2 (0H+0M+2L) + AR-3 (1L) review cycles completed; AR-3 clean. Key fixes: H-1 MinEffectiveRadiusM moved from SupportHeuristic local const to AttackingAIConstants catalogue (FR-CS-016); H-2 magic literals 5.0/40.0/±34.0 in GenerateRunParams promoted to MinRunDepthM/MaxRunDepthM/MaxLateralOffsetM constants; M-1 AttackHysteresis.Update resets CandidateDwell when current role re-preferred (prevented premature transitions on interrupted evaluation windows); M-2 WidthHolder promotion loop now skips near-side WeakSide agents (already counted, must not re-promote); M-3 Math.Round(double) → Mathf.RoundToInt in GenerateRunParams; M-4 dead firstLossThisTick branch removed from AttackingAITick; L-1/L-2 doc updates; AR-3 L-1 AttackAngleEpsilon (0.01f) extracted to catalogue. |
| 1.21 | 2026-05-29 | — | Deterministic Simulation (#16) tree expanded: 23 files (21 .cs + 2 asmdef) with role annotations. AR-1 (4H+4M) + AR-2 (1L) + AR-3 (1L) review cycles completed; AR-3 clean. Key fixes: H-1/H-2 FloatUintUnion explicit-layout struct in CanonicalSerializer (zero-alloc SingleToUInt32Bits/UInt32BitsToSingle); H-3 stackalloc Span<byte>[21] in DeterministicRngService.ComputeDrawValue + SipHash24_64 ReadOnlySpan<byte> signature; H-4 PhaseId enum corrected (AI_NoOp ordinal removed; Events=5 added; Physics=3, Resolve=4, Snapshot=6); M-1 AI_PHASE_STRIDE const→static readonly; M-2 File.Move(overwrite:true) in SaveManager; M-3 one-canonical-NaN→SoftDrift in DivergenceDetector; AR-2 L-1 T-DS-FAULT-014 comment "phase 3 (AI)"→"phase 3 (Physics)"; AR-3 L-1 empty for-loop → comment stub in ReplayEngine step 6. |
| 1.22 | 2026-05-30 | — | Event System (#17) tree expanded: 20 files (18 .cs + 2 asmdef) with role annotations. AR-1 (3H+3M+2L) + AR-2 (1L) + AR-3 clean review cycles completed. Key fixes: H-1 drop predicate corrected `>` → `>=` maxPerTick (off-by-one allowed MaxPerTick+1 publishes per tick); H-2 HandlerSecondaryPublishCount reset moved inside per-handler loop (was per-slot, violating FR-EVT-046a per-invocation semantics); H-3 InDrainDispatch flag wrapped in try/finally (handler exception left flag stuck true corrupting subsequent ticks); M-1 AddHandler bounds check added (0x1701 overflow error); M-2 debug phase assertion added (#if UNITY_EDITOR\|\|DEVELOPMENT_BUILD in EventBus.Publish<IEventA>); AR-2 L-1 AddHandler error code corrected to 0x1701 (was 0x1705); L-1 CardIssuedEvent.FoulOrdinal doc corrected (byte cannot hold -1; now says 0xFF); L-2 TickHeartbeatEvent comment corrected (CLR min size 1 byte, not zero). |
| 1.23 | 2026-05-30 | — | Stage 1 event-bus wiring + ball-physics relocation + performance-optimization scaffold. (1) EventBus wired in 6 specs: PassAttemptEvent/PassCancelledEvent (Tier A 0x0C/0x0D), ShotExecutedEvent/ShotCancelledEvent (Tier A 0x01/0x0E), ShotAnimationData (Tier C 0x0F), PerceptionRefreshEvent (Tier C 0x10), DecisionMadeEvent (Tier C 0x11), HeaderExecutedEvent (Tier B 0x12), HeaderAttemptFailedEvent (Tier C 0x13), SaveAttemptedEvent/BallClaimedEvent/DistributionExecutedEvent (Tier A 0x14–0x16), GoalkeeperRushEvent (Tier C 0x17); IEventA/B/C marker interfaces added to all structs; Tier A/B structs gained 12-byte header fields per §3.4 layout. (2) 6 EventBusStub.cs files replaced no-op with 3-tier generic EventBus.Publish forwarding (decision-tree keeps internal single-sig overload). (3) 6 EventBusRegistrar.cs files added with boot-time RegisterExternalRow<T>() calls. (4) EventRegistry.cs 0x0C–0x17 placeholder rows added. (5) 4 .asmdef files gained TacticalDirector.EventSystem reference (heading-mechanics, goalkeeper-mechanics, perception-system, decision-tree). (6) ball-physics/ relocated from src/Core/Physics/Ball/ to src/ball-physics/ via git mv; ball-physics.asmdef + ball-physics-tests.asmdef added; deviation warning removed. (7) performance-optimization/ created: performance-optimization.asmdef (autoReferenced false), HotPathAllocExemptAttribute.cs, TraceChannel.cs (F.0 schema + Stage 0 anchor rows), PerformanceOptimizationConstants.cs. Tree annotations + EventBusStub/EventBusRegistrar annotations updated throughout. |
| 1.24 | 2026-06-01 | — | Performance Optimization (#18) scaffold promoted to real implementation. (1) asmdef gained TacticalDirector.DeterministicSim reference (SessionManifest uses EnvironmentFingerprint #16 §4.8). (2) PerformanceOptimizationConstants.cs v1.1: Fixed region LOOP_TAG_TACTICAL_10HZ + LOOP_TAG_PHYSICS_60HZ added (§3.2.2); GT region PromotionToleranceFraction + ReproducibilityToleranceFraction added (§3.9.1 / FR-PO-067); EST region SamplerDefaultHz + StatisticalSignificanceN + FirstTickWarmupCount added (§3.3.4 / §3.4.3 / §3.9.4); region order corrected to Fixed→GT→EST. (3) 13 new C# files: LoopTag / BaselinePassFail / HardwareCounterSnapshot / SessionManifest / BaselineRecord / BudgetRollupEntry / HotPathEntry / IPerfHarness / IBudgetSource / RegressionResult / RegressionGate / ReproducibilityResult / BaselineReproducibilityAuditor. (4) tools/run-perf-local.sh (FR-PO-070 Stage 0 runbook / Appendix E). (5) tools/budget-auditor.py (§5.3 schema-conformance + §5.5 loop-tag auditor; FR-PO-070). (6) tools/select-seed.py (KD-6 seed selection; Stage 0 fixed seed; Stage 0+1 SHA-derived). (7) tools/perf-harness/run.sh + tools/perf-harness/scenarios/anchor-baseline.manifest.json (§4.1 Stage 0 anchor scenario). (8) docs/specs/performance-optimization/baselines/.gitkeep (Appendix A §A.3 Stage 0 baseline storage). |
| 1.33 | 2026-06-02 | — | Testing Strategy (#19) PR #132 Codex P2 follow-up: `PerfGateRunner.Run` now rejects mismatched perf-baseline pairs via `ArgumentException` before delegating to `RegressionGate.Evaluate`. FR-PO-031 defines the +5% gate only for the same scenario, seed, platform pin, and loop; `RegressionGate.Evaluate` compares only `P50Ms` and would silently green-light a meaningless pair. Run validates `baseline.Loop == current.Loop` unconditionally and `ScenarioManifestId` / `Seed` / `PlatformPin` when both records carry a non-null `SessionManifest` (preserves AR-1 H-1 missing-manifest tolerance). Mismatches throw `ArgumentException` naming the field, the baseline value, the current value, and the FR-PO-031 binding. Files: PerfGateRunner.cs v1.2. |
| 1.39 | 2026-06-03 | — | Ball Physics (#1) AR-6 fix pass: 0M + 4L all fixed against the merged AR-5 state (post-PR #134). Lands on a fresh branch — PR #134 was already merged into main before AR-6 fixes were authored. L-1: `BodyPart` enum XML doc gains the ORDINAL STABILITY paragraph, completing the full-surface sweep (AR-3 L-2 covered `BallEventType`; AR-4 L-1 propagated to `BallStateType` / `SurfaceType` / `RestartType`; AR-5 L-1 added `KickResult`; `BodyPart` was the last public enum without the paragraph). `BodyPart` is cross-spec embedded in `collision-system/AgentBallCollisionData.BodyPart` so the insertion-shifts-ordinals hazard applies. L-2: `BallPhysicsCore.cs` file-header `Modified` annotation refreshed `"(AR-4 fix pass)"` → `"(AR-6 fix pass)"` so a reader scanning headers for the latest-pass anchor lands on the correct review row; date unchanged. L-3: new `EnumOrdinalStabilityTests.cs` mechanically enforces the APPEND-only contract on all six public enums via direct `Assert.AreEqual(N, (int)EnumMember)` assertions (6 tests, one per enum). Converts the AR-3 L-2 / AR-4 L-1 / AR-5 L-1 / AR-6 L-1 documentation into enforced contracts — any future maintainer who inserts a value in the middle of an enum fails the suite immediately. L-4: `BallEventLogger.cs` HISTORICAL NOTE (AR-5 L-3) rephrased — Stage 0 never persisted event logs, so the original "pre-AR-2 serialised event streams are NOT compatible" wrongly implied any existed; reworded conditionally ("if any … had existed it would NOT be compatible"). Files: BodyPart.cs v1.1, BallPhysicsCore.cs v1.3.2, BallEventLogger.cs v1.6; new EnumOrdinalStabilityTests.cs v1.0. Tree row added for the new test file. |
| 1.38 | 2026-06-03 | — | Ball Physics (#1) AR-5 fix pass: 1M + 4L all fixed against the AR-4 v1.3/v1.4/v1.6 state. M-1: file header `Modified` field added to 4 files that lacked it (FR-CS-056): `BodyPart.cs` and `KickResult.cs` (created in AR-2 L-2 split, missed by AR-3); `BodyPartCoefficientsTests.cs` and `SurfacePropertiesTests.cs` (created in AR-4 L-2, same shortcoming). For brand-new files `Modified == Created`. L-1: `KickResult` enum XML doc gains the ORDINAL STABILITY paragraph parallel to AR-3 L-2 / AR-4 L-1 on the four sibling public enums; though KickResult is currently a method-return-only value with no struct embedding, Stage 1+ kick-outcome logs will inherit the insertion-shifts-ordinals hazard so the APPEND-only rule applies pre-emptively. L-2: new `Get_EveryDeclaredBodyPart_HasCatalogueEntry` test in `BodyPartCoefficientsTests` and `EveryDeclaredSurfaceType_HasEntryInAllFourLookups` test in `SurfacePropertiesTests` iterate `Enum.GetValues(typeof(BodyPart))` / `typeof(SurfaceType)` so a future enum extension without a matching catalogue row (or, for SurfaceType, without atomic updates to all four switch arms) fails the test suite immediately — the hardcoded round-trip tests above would silently miss the gap. L-3: `BallEventType` XML doc gains a HISTORICAL NOTE clarifying that the AR-2 L-3 drop of four unused members (`Header` / `Deflection` / `OutOfPlay` / `PossessionChange`) RENUMBERED the remaining ordinals (Bounce 3→2, GoalPostHit 5→3, Goal 7→4) before the AR-3 L-2 APPEND-only rule was established; Stage 0 has no persisted log so the renumbering had no consumer impact, but pre-AR-2 serialised event streams are NOT compatible with the current ordinals. L-4: `BallPhysicsCore.cs` version history gains a 1.3.1 doc-only clarification note recording that the NaN-recovery `LogError` emit is a plain string literal (no `$"…"` interpolation), so its AR-4 M-1 gating was for surface symmetry with the three sibling clamp emits, not direct FR-CS-031 necessity. Files: BodyPart.cs v1.0.1, KickResult.cs v1.1, BallEventLogger.cs v1.5, BallPhysicsCore.cs v1.3.1, BodyPartCoefficientsTests.cs v1.1, SurfacePropertiesTests.cs v1.1. |
| 1.37 | 2026-06-03 | — | Ball Physics (#1) AR-4 fix pass: 1M + 4L all fixed against the AR-3 v1.4/v1.5 state. M-1: the four `Debug.LogError` / `Debug.LogWarning` emit blocks in `BallPhysicsCore.ValidatePhysicsState` (NaN-recovery, velocity clamp, spin clamp, height clamp) gated behind `#if UNITY_EDITOR \|\| DEVELOPMENT_BUILD` — same FR-CS-031 carve-out pattern AR-3 L-3 applied to `BallCollision.ApplyKick`. ValidatePhysicsState runs at 60 Hz inside `UpdateBallPhysics` so the gating concern is stronger here than for once-per-kick emits; AR-3 only finished half the job and the AR-4 fix restores symmetry. L-1: ordinal-stability XML doc paragraphs added to `BallStateType` / `SurfaceType` / `RestartType` parallel to the AR-3 L-2 paragraph on `BallEventType` — all four enums are embedded in serialised data (`BallState.State`, `BallEvent.ResultingState` / `BallEvent.Surface`, `BallCollision.CheckBoundaries` return, persisted match config) and share the same insertion-shifts-ordinals hazard for replay / save / analytics consumers + FR-DS-009 digest compatibility. L-2: two new test files lock the AR-1 L-4 throw-on-unknown contracts plus the AR-3 M-1 catalogue round-trips. `BodyPartCoefficientsTests.cs` covers `Get(BodyPart)` known-value round-trip across all 6 body parts (asserting the catalogue value reaches the consumer) + `(BodyPart)100` cast-from-int throws `ArgumentOutOfRangeException`. `SurfacePropertiesTests.cs` covers all four `Get*(SurfaceType)` methods with 5-known-surface round-trips + `(SurfaceType)100` throw branches — 8 tests total. L-3: `BodyPartCoefficients.s_coefficients` field retyped `Dictionary<…>` → `IReadOnlyDictionary<…>` (backed by the same Dictionary instance) so future code inside the class cannot mutate the lookup contents through the field reference; matches the AR-2 / Testing Strategy `IReadOnlyList<T>` pattern. L-4: `BodyPartRetention` constant XML docs use "factor" instead of "multiplier" for terminology consistency with the consuming `BodyPartCoefficients` class (which uses retention / coefficient / factor vocabulary). Files: BallPhysicsCore.cs v1.3, BallState.cs v1.4, SurfaceProperties.cs v1.4, RestartType.cs v1.1, BodyPartCoefficients.cs v1.2, BallPhysicsConstants.cs v1.6; new BodyPartCoefficientsTests.cs v1.0, SurfacePropertiesTests.cs v1.0. Tree updated with the 2 new test files. |
| 1.36 | 2026-06-03 | — | Ball Physics (#1) AR-3 fix pass: 1M + 5L all fixed against the AR-2 v1.3/v1.4 state. M-1: 12 per-body-part `(speedRetention, spinRetention)` literals inside `BodyPartCoefficients.cs` (which AR-2 L-2 lifted verbatim out of `BallCollision.cs`) replaced with references to a new `BallPhysicsConstants.BodyPartRetention` nested class catalogue (`FootSpeed`/`FootSpin`/`ShinSpeed`/`ShinSpin`/…/`ArmSpeed`/`ArmSpin`, 12 `[GT]` constants tagged for the Stage 1 config-loader migration) — FR-CS-016 ("No magic numbers anywhere else in the ball-physics assembly") is now satisfied. L-1: the AR-2 L-1 inline `//` comments at the home-goal and away-goal branches of `BallCollision.CheckBoundaries` carried `&lt;` / `&gt;` XML entity escapes left over from a draft XML doc; replaced with raw `<` / `>` (the comments are plain source, not XML). L-2: `BallEventType` XML doc gains an explicit ORDINAL STABILITY paragraph instructing future maintainers to APPEND new members at the end of the enum — inserting in the middle shifts ordinals 2/3/4 and breaks both Stage 1+ analytics pipelines and FR-DS-009 digest compatibility on any serialised event stream. L-3: the four `Debug.LogError` / `Debug.LogWarning` emit blocks in `BallCollision.ApplyKick` gated behind `#if UNITY_EDITOR || DEVELOPMENT_BUILD` (mirrors the event-system EventBus debug-assertion pattern); the functional rejection return `KickResult.RejectedNonFiniteVelocity` and the velocity/spin clamping stay outside the gates, so production builds emit no `$"…"` interpolation per FR-CS-031 while editor and development builds keep the diagnostic surface. L-4: parallel `Debug.LogWarning` added for spin clamping in `ApplyKick` so the two symmetric clamp operations (velocity at `Limits.MaxVelocity`, spin at `Limits.MaxSpin`) now have symmetric editor-gated diagnostics. L-5: root `CLAUDE.md` OPEN ISSUES gains a new entry `Possession.ControlHeight ↔ GroundControlHeight cross-spec routing` (since 2026-06-03) tracking the deferred Spec #20 §4.2 routing decision; `Possession.ControlHeight` XML doc back-references the entry by title so the deferral has a discoverable anchor. Files: BallPhysicsConstants.cs v1.5, BodyPartCoefficients.cs v1.1, BallCollision.cs v1.4, BallEventLogger.cs v1.4; root `CLAUDE.md` OPEN ISSUES entry added. |
| 1.35 | 2026-06-03 | — | Ball Physics (#1) AR-2 fix pass: 2M + 5L all fixed against the AR-1 v1.2/v1.3 state. M-1: `BallEventLogger._lastSnapshotTime` initialised to `float.NegativeInfinity` instead of the `NeverSnapshotted = -1f` sentinel + float-equality branch — `matchTime − NegativeInfinity == +Infinity ≥ SnapshotInterval` for any finite interval, so the first snapshot always emits without the explicit equality test, and the magic literal + named const both vanish. Survives future `SnapshotInterval` tuning that would have broken the `-1f` sentinel (any interval > 1.0 s suppressed the first call). M-2: `LogAssert.Expect` added to `BallPhysicsCoreTests.Validation_DetectsNaN_AndRecovers` and `Validation_DetectsInfinity_AndRecovers` so the `Debug.LogError("[BallPhysics] NaN/Infinity detected …")` emitted by `ValidatePhysicsState`'s recovery path does not fail the Unity NUnit runner (parallels the AR-1 H-1 follow-on already applied on the two integration-test counterparts). L-1: `BallCollision.IsInHomeGoal` / `IsInAwayGoal` zero-information wrappers folded — `CheckBoundaries` now calls `IsBetweenPostsUnderCrossbar` directly with `// Home goal` / `// Away goal` inline comments at the call sites; the two redundant one-line indirections introduced by AR-1 M-3 are gone. L-2: `BallCollision.cs` (5 public types) split into single-type files per `src/CLAUDE.md` FILE NAMING — `BodyPart.cs` / `RestartType.cs` / `KickResult.cs` / `BodyPartCoefficients.cs` / `BallCollision.cs`. `BodyPartCoefficients` retains the AR-1 M-2 (`s_coefficients`) + AR-1 L-4 (throw on unknown enum) fixes verbatim. Unused `using System.Collections.Generic;` dropped from BallCollision.cs and the redundant `UnityEngine.` prefix dropped from `Debug` / `Vector2` (already in scope via the file's `using UnityEngine;`). L-3: dead `BallEventType` members `Header` / `Deflection` / `OutOfPlay` / `PossessionChange` dropped — no producer existed in `BallEventLogger`. Enum XML doc records that Stage 1+ re-additions MUST land atomically with the producing `Log*` method to prevent silent default-valued events leaking to consumers. L-4: `BallEvent` XML doc gains a per-`Type` validity map listing which detail fields each event type populates (`PositionSnapshot` header-only, `Kick`→ResultingState, `Bounce`→Surface/RestitutionUsed/VnBefore/VnAfter, `GoalPostHit`→ContactPoint, `Goal`→TeamID); consumers MUST switch on `Type` before reading any detail field. `FormatDetail` default arm now throws `ArgumentOutOfRangeException` (consistent with the closed enum) and `PositionSnapshot` gets an explicit empty-string arm so silent zero leakage is impossible. L-5: `BallPhysicsConstants.Possession.ControlHeight` XML doc records a cross-spec-drift warning naming `FirstTouchConstants.GroundControlHeight` as a parallel `[GT]` declaration at the same value (0.50 m); per Spec #20 §4.2 routing one of the two specs must become the authority and the other a `[CROSS]` mirror — routing decision deferred to a dedicated cross-spec pass. Tree: 4 new file rows added under `ball-physics/`. Files: BallEventLogger.cs v1.3, BallCollision.cs v1.3, BallPhysicsCoreTests.cs v1.4, BallPhysicsConstants.cs v1.4; new: BodyPart.cs v1.0, RestartType.cs v1.0, KickResult.cs v1.0, BodyPartCoefficients.cs v1.0. |
| 1.34 | 2026-06-02 | — | Ball Physics (#1) AR-1 fix pass: 3H + 7M + 6L all fixed across the 8 implementation files + 3 tests. H-1: `BallEventLogger` per-event string interpolation in `LogBounce` / `LogKick` / `LogGoalPostHit` removed (FR-CS-031 hot-path violation; logger is called from `BallPhysicsCore.UpdateBallPhysics` at 60 Hz). `BallEvent` now carries typed `Surface` / `RestitutionUsed` / `VnBefore` / `VnAfter` / `ContactPoint` / `ResultingState` / `TeamID` / `AngularVelocity` fields; `LogKick` signature changed from `(…, string kickType, …)` → `(…, BallStateType resultingState, …)`; `FormatDetail(in BallEvent)` is an off-hot-path string helper for diagnostic export. H-2: all 8 source files + 3 test files had stale `// File: src/Core/Physics/Ball/<Name>.cs` headers from before the v1.23 git-mv; updated to `src/ball-physics/…`. H-3: `BallStateMachine.IsOutOfBounds` now applies the same `z < Ball.Diameter` Stage-0 gate as `BallCollision.CheckBoundaries` — a high-flying ball over the touchline is no longer silently classified as OutOfPlay by the state machine while CheckBoundaries returns `(false, None)`; the two predicates now agree. New unit test `OutOfBounds_HighAboveTouchline_ReturnsFalse` locks the gate. M-1: `BallCollision.cs` + `BallEventLogger.cs` using directives reordered `System → Unity` per FR-CS-006. M-2: `BodyPartCoefficients._coefficients` → `s_coefficients` per FR-CS-002 (private static naming). M-3: dead `isHomeGoal` parameter on the private `IsInGoal` helper removed — replaced with `IsInHomeGoal` / `IsInAwayGoal` wrappers over a shared `IsBetweenPostsUnderCrossbar` (both goals have identical Y/Z gates; the caller already validates X). M-4: `BallStateType`, `BallEventType`, `RestartType`, `SurfaceType` members renamed ALL_CAPS_SNAKE → PascalCase per FR-CS-001 / Spec #20 §3.2.3 (`STATIONARY → Stationary`, `OUT_OF_PLAY → OutOfPlay`, `GRASS_DRY → GrassDry`, `THROW_IN → ThrowIn`, `KICKOFF → KickOff`, etc.); the four enums are not consumed by member name outside ball-physics so the rename is contained. M-5: `BallCollision.ApplyKick` signature changed `void → KickResult { Applied, RejectedNonFiniteVelocity }` so callers can detect non-finite-velocity rejection without scraping `Debug.LogError`; new `Possession_NaNKick_RejectedWithFeedback_BallStateUnchanged` test locks the contract. M-6: `BallEventLogger` is now `sealed` (FR-CS-068 virtual-dispatch avoidance). M-7: `_lastSnapshotTime = -999f` magic literal replaced with `private const float NeverSnapshotted = -1f` plus an explicit "first call always emits" sentinel branch. L-1: `UpdateSpinDecay` XML doc records the rationale for the hybrid empirical-linear + analytical-aerodynamic-torque decay model (§3.1.7.1 calibration term vs §3.1.7.2 textbook torque). L-2: `CheckBoundaries` XML doc records the corner-region precedence Stage-0 simplification (touchline check wins on simultaneous goal+touch crossing) and points at the `BallStateMachine.IsOutOfBounds` z-gate alignment. L-3: `ExportEvents` XML doc warns that it allocates a `List<BallEvent>` per call and MUST be invoked off the hot path; new `Count` property exposes the size without copying. L-4: `BodyPartCoefficients.Get` and all four `SurfaceProperties.Get…` switch expressions throw `ArgumentOutOfRangeException` for unknown enum values instead of silently returning a default tuple / `GrassDry` — fails fast on cast-from-int callers. L-5: `BallState` XML doc records the cross-spec consumers (`HeaderExecutedEvent` in heading-mechanics, `SaveAttemptedEvent` in goalkeeper-mechanics) that embed it via `MemoryMarshal.Write`. L-6: `BallPhysicsCore.UpdateBallPhysics` default-branch comment now states explicitly that `ValidatePhysicsState` is skipped on the Stationary / Controlled / OutOfPlay branches and points at the caller-side validation responsibility. Files: BallEventLogger.cs v1.2, BallCollision.cs v1.2, BallStateMachine.cs v1.2, BallPhysicsCore.cs v1.2, BallState.cs v1.3, SurfaceProperties.cs v1.3, BallGroundInteraction.cs v1.2, BallPhysicsConstants.cs v1.3, tests/BallStateMachineTests.cs v1.3, tests/BallPhysicsCoreTests.cs v1.3, tests/BallIntegrationTests.cs v1.3. |
| 1.32 | 2026-06-02 | — | Testing Strategy (#19) AR-5 fix pass: 2L all fixed against the AR-4 v1.4/v1.3 state. L-1: `DeterminismGate.RunTiers` order snapshot promoted from heap-allocated `DeterminismTierKind[]` to `stackalloc Span<DeterminismTierKind>`, eliminating the per-call heap allocation while preserving the AR-4 L-1 dispatch-elimination benefit; matches the deterministic-sim `CanonicalSerializer` Span pattern (src/CLAUDE.md v1.21 AR-1 H-3). `using System;` added for the `Span<T>` ref-struct; no `unsafe` block required (Span form, C# 7.2+). L-2: `PerfGateReport` XML doc on the constructor split the empty-string rationale per parameter — empty `loopTag` is the existing "SHOULD" advisory (runner does not enforce the `LOOP_TAG_*` allowlist), empty `scenarioManifestId` is the missing-manifest sentinel produced by `PerfGateRunner.Run` when `current.Manifest` is null. Doc-only. Files: DeterminismGate.cs v1.5, PerfGateReport.cs v1.2. |
| 1.31 | 2026-06-02 | — | Testing Strategy (#19) AR-4 fix pass: 1M + 1L all fixed against the AR-3 v1.3 state. M-1: `DeterminismSuiteResult` drops the AR-3 L-4 fast-path bypass (skip wrap if already `ReadOnlyCollection<T>`) — that path did NOT achieve defensive copy because `ReadOnlyCollection<T>` wraps an `IList<T>` that a caller can mutate via a retained reference. Constructor now always copies inputs into fresh `DeterminismTierResult[]` / `GoldenVectorResult[]` arrays before wrapping. Pass-loop iterates the copied arrays so `AllPassed` and the property surface are computed against the same snapshot. Supersedes the AR-2 L-1 producer-side wrap in `DeterminismGate.RunTiers` (reverted to plain array pass-through — the suite's defensive copy is now the authoritative read-only-contract boundary; the producer wrap would have been a double-copy). L-1: `DeterminismGate.RunTiers` snapshots `s_canonicalTierOrder` into a local `DeterminismTierKind[] order` once so the result-construction loop reads plain array elements instead of re-dispatching `IReadOnlyList<T>.this[int]` four times per call. Files: DeterminismSuiteResult.cs v1.3, DeterminismGate.cs v1.4. |
| 1.30 | 2026-06-02 | — | Testing Strategy (#19) AR-3 fix pass: 1M + 4L all fixed against the AR-2 v1.2 state. M-1: `PerfGateReport` constructor null-checks `loopTag` + `scenarioManifestId` with explicit `ArgumentNullException` (parallel to AR-1/AR-2 invariant push on the other result/entry types); empty strings remain valid — `PerfGateRunner.Run` uses `string.Empty` as the missing-manifest sentinel. L-1: null-vs-empty string checks split across `GoldenVectorEntry` / `GoldenVectorResult` / `DeterminismTierResult` — `ArgumentNullException` for null, `ArgumentException` for empty (idiomatic .NET BCL convention; parallels the AR-2 L-3 range-vs-relation split). L-2: `DeterminismGate.RunTiers` caches `s_canonicalTierOrder.Count` into a local `tierCount` once (interface-property dispatch after the AR-2 retype). L-3: `DeterminismTierKind.cs` VersionHistory header row widened to match body-row column widths (was shorter, breaking markdown table alignment). L-4: `DeterminismSuiteResult` constructor re-wraps input lists in `ReadOnlyCollection<T>` unless already wrapped, closing the AR-2 L-1 contract leak for external direct constructions that bypass `DeterminismGate.RunTiers`. Files: PerfGateReport.cs v1.1, GoldenVectorEntry.cs v1.3, GoldenVectorResult.cs v1.3, DeterminismTierResult.cs v1.3, DeterminismGate.cs v1.3, DeterminismTierKind.cs v1.2, DeterminismSuiteResult.cs v1.2. |
| 1.29 | 2026-06-02 | — | Testing Strategy (#19) AR-2 fix pass: 1M + 5L all fixed against the AR-1 v1.1 state. M-1: `GoldenVectorEntry` constructor now enforces non-empty `name` / `sourcePath` / `citation` via `ArgumentException` (parallel to AR-1 L-2/L-3 invariant guards already on `GoldenVectorResult` + `DeterminismTierResult`). L-1: `GoldenVectorRunner.Catalogue()` + `RunAll()` wrap their backing arrays in `ReadOnlyCollection<T>` so callers cannot cast `IReadOnlyList<T>` back to mutable `T[]`; `DeterminismGate.RunTiers` applies the same wrap to the local `tierResults` array before passing to `DeterminismSuiteResult`. L-2: `DeterminismGate.s_canonicalTierOrder` retyped `private static readonly IReadOnlyList<DeterminismTierKind>` backed by `ReadOnlyCollection<T>` (elements no longer reassignable through the field reference); loop in `RunTiers` switched to `.Count`. L-3: range-style violations now throw `ArgumentOutOfRangeException` with explicit `actualValue` argument — `vectorsExecuted < 0`, `vectorsFailed < 0 \|\| vectorsFailed > vectorsExecuted`, `testsExecuted < 0`, `testsFailed < 0 \|\| testsFailed > testsExecuted`; pass invariant + null/empty diagnostic stay `ArgumentException`. L-4: KD-5 "Stage-gated" annotation extended to remaining GT rows (UnitWallTimeBoundMs, PreCommitWallTimeBoundSeconds, QuarantineExpiryDays, EvictionQuarantineCount, EvictionWindowDays) — §3.7 preamble explicitly tags Stage-gated per KD-5. L-5: maintainer notes added to `DeterminismTierKind` enum and `DeterminismGate.s_canonicalTierOrder` reminding contribs to extend atomically when a new tier is added (FR-TS-018 currently forbids new tier categories without a #16 §5 revision, but a missed entry would silently exclude the new tier from `RunTiers`). L-6: XML doc on `GoldenVectorEntry` / `GoldenVectorResult` / `DeterminismTierResult` notes default-value bypass — C# struct default-value semantics skip constructor invariant checks; consumers SHOULD treat default-valued instances as malformed. Files: GoldenVectorEntry.cs v1.2, GoldenVectorResult.cs v1.2, GoldenVectorRunner.cs v1.2, DeterminismTierKind.cs v1.1, DeterminismTierResult.cs v1.2, DeterminismGate.cs v1.2, TestingStrategyConstants.cs v1.2. |
| 1.28 | 2026-06-02 | — | Testing Strategy (#19) AR-1 fix pass: 1H + 3M + 6L all fixed. H-1: `PerfGateRunner.Run` misleading null-conditional removed — `current?.` chain dropped (RegressionGate.Evaluate NPEs first on null current); explicit `ArgumentNullException` guards added on `baseline` + `current` with inner `Manifest?.` kept for the malformed-record diagnostic path only. M-1: `GoldenVectorRunner` private const `GoldenVectorRootRelPath` + `Stage0DeferredDiagnostic` gained XML `<summary>` docs per FR-CS-061. M-2: `DeterminismGate.Stage0DeferredDiagnostic` private const gained XML `<summary>`. M-3: `DeterminismSuiteResult.AllPassed` now fails closed on empty tier or golden-vector lists (previously returned true via empty early-exit loops, which would silently green-light FR-DS-009-GATE for a degenerate caller). L-1: `GoldenVectorEntry` property declaration order reordered to `Kind / Name / SourcePath / Citation` matching the constructor parameter order. L-2: `GoldenVectorResult` + `DeterminismTierResult` constructors now enforce non-empty diagnostic via `ArgumentException`. L-3: same constructors enforce `0 ≤ failed ≤ executed` and `passed == (executed > 0 && failed == 0)` so callers cannot publish "passed with failures", "failed with no failures", or "passed with zero executions" records; the `(passed=false, executed=0, failed=0)` deferred-status shape remains valid. L-4: `DeterminismSuiteResult` constructor null-checks both list arguments. L-5: `TestingStrategyConstants` pyramid-bound + per-tier coverage XML docs gained "Stage-gated per KD-5" annotation. L-6: `DeterminismGate.RunTiers` tier-order array promoted from per-call local to `private static readonly s_canonicalTierOrder`. Files: PerfGateRunner.cs v1.1, GoldenVectorRunner.cs v1.1, DeterminismGate.cs v1.1, DeterminismSuiteResult.cs v1.1, GoldenVectorEntry.cs v1.1, GoldenVectorResult.cs v1.1, DeterminismTierResult.cs v1.1, TestingStrategyConstants.cs v1.1. |
| 1.27 | 2026-06-02 | — | Testing Strategy (#19) scaffold initiated. 14 new files under `src/testing-strategy/`: (1) `testing-strategy.asmdef` (autoReferenced false; references TacticalDirector.DeterministicSim + TacticalDirector.PerformanceOptimization). (2) `TestingStrategyConstants.cs` — §3.10 governance: Fixed `MATCH_LENGTH_MINUTES=90` + GT pyramid bounds / coverage thresholds (Tier A/B line+branch) / `UnitWallTimeBoundMs=1.0` / `PreCommitWallTimeBoundSeconds=60.0` / quarantine + eviction windows. (3) `TestTier.cs` enum (TierA/B/C mirrors #16 §1.1.1 per KD-1). (4) `TestLayer.cs` enum (Unit/Integration/Simulation/Determinism/EndToEndSoak per §3.1.1 / FR-TS-001). Golden-vector harness (4 files): `GoldenVectorKind` discriminates #16 §9.5 #4 a/b/c corpora; `GoldenVectorEntry` carries kind/name/source-path/citation; `GoldenVectorResult` carries pass/fail + vectors-executed + diagnostic; `GoldenVectorRunner.Catalogue() / Run(in entry) / RunAll()` — Stage 0 deferred-status (D1 test-runner pin); Stage 0+1 parses corpus and invokes DeterministicRngService / CanonicalSerializer. Determinism gate (4 files): `DeterminismTierKind` enum (canonical #16 §5 tier order: Unit/Integration/Scenario/Soak per FR-TS-011 / FR-TS-018); `DeterminismTierResult` + `DeterminismSuiteResult`; `DeterminismGate.RunTiers()` — single integration point per FR-TS-016, aggregates §5 tiers + golden-vector corpus into one FR-DS-009-GATE signal. Perf-gate runner (2 files): `PerfGateRunner.Run(specId, loopTag, baseline, current, milestoneMs)` delegates verdict to #18 `RegressionGate.Evaluate` (KD-3 boundary, §3.9.2 pointer) and wraps the result in `PerfGateReport` carrying spec/loop/scenario context for FR-PO-036 message formatting. |
| 1.26 | 2026-06-02 | — | Performance Optimization (#18) AR-2 fix pass (2M+2L). M-1: RegressionGate helper/orchestrator semantic divergence on NaN milestone — PassesAbsoluteDriftCheck now returns true for float.NaN milestone (skip-drift signal), aligning with Evaluate's documented semantics; Evaluate delegates drift verdict entirely to the helper. M-2: TraceChannelDescriptor public constructor gained XML <summary> doc (FR-CS-060 — every public method requires a summary; missed in AR-1 H-1 split). L-1: SessionManifest.IsComplete() now also validates HardwareCounters.CpuModel and HardwareCounters.ThermalState — default-constructed HardwareCounterSnapshot struct (null strings) previously slipped through despite §3.3.2 listing hardware counters as required. L-2: TraceChannelDescriptor constructor enforces the symmetric F.0 invariant InsideTickPipeline ⇒ SignOffLogRef non-empty (AR-1 L-1 fixed the SamplingRule/SampleN side; this completes the descriptor structural-invariant set). Files: RegressionGate.cs v1.2, TraceChannelDescriptor.cs v1.2, SessionManifest.cs v1.1. |
| 1.25 | 2026-06-02 | — | Performance Optimization (#18) AR-1 fix pass (2H+3M+3L). H-1: TraceChannel.cs (5 public types) split into 5 single-type files — ChannelVerbosity.cs / ChannelSamplingRule.cs / ChannelDeterminismClass.cs / TraceChannelDescriptor.cs / TraceChannelRegistry.cs — per CLAUDE.md FILE NAMING. H-2: PerformanceOptimizationConstants.HotPathAllocBudgetBytes → HOT_PATH_ALLOC_BUDGET_BYTES ([FIXED] constants are ALL_CAPS per FR-CS-001). M-1: RegressionGate.Evaluate dedupes via PassesPerPrCheck/PassesAbsoluteDriftCheck rather than reimplementing the formula. M-2: RegressionGate degenerate baseline or milestone (≤0 or NaN) now fails closed instead of silently passing the gate; float.NaN milestone still skips the drift check (explicit opt-in). M-3: BaselineReproducibilityAuditor degenerate origP50 (≤0 or NaN) fails closed instead of silently returning isReproducible=true. L-1: TraceChannelDescriptor constructor enforces the documented SamplingRule=PerNTicks ⇒ SampleN>0 invariant (throws ArgumentException). L-2: BaselineReproducibilityAuditor sealed class → static class (stateless validator; matches RegressionGate style). L-3: IBudgetSource.GetEntries() return type changed from BudgetRollupEntry[] to IReadOnlyList<BudgetRollupEntry> to match the documented "callers must not mutate" contract. L-4: PerformanceOptimizationConstants.PromotionToleranceFraction XML doc — removed stale "also used as the reproducibility confidence-interval tolerance" sentence; auditor consumes ReproducibilityToleranceFraction. |
| 1.44 | 2026-06-04 | — | Agent Movement (#2) regression-test roster initiated. (1) New `docs/specs/agent-movement/test-plan.md` v0.1 catalogues T-AM-NNN test IDs grouped by file under test (dwell formula / pipeline collision / safety override / OscillationGuard), each row anchored to an AR finding; documents that Spec #2 §5 is *performance* (not a test plan) and tombstones the fictitious "§5.1 / 85 tests" anchor in the prior placeholder. (2) `src/agent-movement/Tests/AgentMovementTests.cs` placeholder → 18 NUnit `[Test]`s across 4 `[TestFixture]` classes: `AgentStateMachineDwellTests` (T-AM-001..006), `AgentMovementSystemCollisionTests` (T-AM-010..018), `AgentMovementSystemSafetyOverrideTests` (T-AM-030..033), `OscillationGuardTests` (T-AM-040..043). Primary regression locks: T-AM-011 (AR-9 M-1 — GROUNDED dwell uses cached entry-force, not incoming this-frame force), T-AM-013 (AR-5 M-2 second-hit refresh), T-AM-016 (AR-6 M-2 OscillationGuard collision bypass), T-AM-030 (AR-5 M-1 safety-override `LastValid*` preservation). (3) New `src/agent-movement/AssemblyInfo.cs` adds `[InternalsVisibleTo("TacticalDirector.AgentMovement.Tests")]` so T-AM-030..032 can call the internal `MovementCommand.ToolingOverrideOnly_NaNInjection` factory (added in MovementCommand.cs v1.2; production game logic MUST NOT call this factory — it disables Step 10 safety validation). Files: AgentMovementTests.cs v2.0 (replacement); new: AssemblyInfo.cs v1.0; MovementCommand.cs v1.2; new: docs/specs/agent-movement/test-plan.md v0.1. Tree row updated for the new AssemblyInfo + Tests/ folder casing. |
| 1.45 | 2026-06-04 | — | Agent Movement (#2) pure-function unit coverage expansion. New `src/agent-movement/Tests/AgentMovementUnitTests.cs` v1.0 adds 59 NUnit `[Test]`s across 7 `[TestFixture]` classes: `AgentStateMachineStumbleTests` (T-AM-007..009 dwell + T-AM-019..023 `ShouldStumble`), `AgentSafetySystemUnitTests` (T-AM-034..039 `HasInvalidValues` / `ClampVelocity` / `ClampToPitch` / `Validate`), `OscillationGuardEdgeTests` (T-AM-044..047 window-expiry / buffer-wrap / sparse-burst / `Initialize`-after-lock), `PerformanceContextUnitTests` (T-AM-050..052 neutral / partial-modifier / out-of-range clamp), `AgentLocomotionUnitTests` (T-AM-070..083 — endpoints + clamping for `CalculateBaseTopSpeed` / `CalculateBaseAccelK` / `ApplyAcceleration` / `ApplyDeceleration` / `CalculateStoppingDistance` / `CalculateAerobicModifier`), `AgentDirectionalMovementUnitTests` (T-AM-084..099 — endpoints + zone-blend + degenerate guards for `LateralMultiplier` / `BackwardMultiplier` / `CalculateDirectionalMultiplier` / `ApplyDirectionalToAccelK` / `MovementAngleDeg` / `RotateFacingToward`), `AgentTurningUnitTests` (T-AM-100..107 — speed-dependence + clamps for `CalculateMaxTurnRate` / `MinimumTurnRadius` / `CalculateLeanAngle`). Note T-AM-021: at peak stress (speed=MAX_SPEED, turn=180°) the formula deliberately produces `stumbleRisk = 1.5 > max resistance = 1.0`; the test discriminates at moderate stress (8 m/s, 120°) where the attribute axis actually decides. `docs/specs/agent-movement/test-plan.md` bumped to v0.2 — non-coverage section rewritten (locomotion / turning / directional / safety dropped; `EvaluateFromX` private-branch carve-out, fatigue table, UpdateAllAgents asserts, and the `RotateVelocityToward` both-degenerate `Debug.Assert(false)` branch remain explicitly non-covered). Files: new AgentMovementUnitTests.cs v1.0; test-plan.md v0.2; tree row updated. |
| 1.46 | 2026-06-07 | — | AR-hardening sweep — Ball Physics #1 AR-1..AR-6 complete (final AR-6: 0M+4L); Collision System #3 AR-1..AR-6 (final 3L); First Touch #4 AR-5..AR-6 (final 3L); Pass Mechanics #5 AR-2..AR-5 (final 1M+3L cycle-stop); Shot Mechanics #6 AR-2..AR-4 (final 3L). |
| 1.47 | 2026-06-07 | — | Event System #17 AR-8..AR-11. AR-8 (2M+1L): OnTickBoundary resets CurrentPhase to (PhaseId)0xFF + CurrentTick to uint.MaxValue so stale Publish between OnTickBoundary and next BeginTick/BeginPhase trips IORE rather than silently corrupting digest; EventRegistry.RegisterRowRaw throws ERR_EVT_ORDINAL_COLLISION on already-fully-initialised row; RegisterRow asserts sizeof(T) >= EventHeaderBytes for Tier A/B. AR-9 (1M+1L): DrainTick + SerializeLedger stackallocs switched to compile-time MAX_QUEUE_SORT_INTS=2048 (8 KB cap) + static-ctor invariant; CosmeticChannel.ResetForTests added. AR-10 (1M+2L): EventBus.PublishAuthoritative phase validity guard now precedes QueueCount++ reservation (AR-8 M-2 sentinel previously fired via IORE after slot reservation); EventLedger.SerializeLedger dst.Length >= 5 bound check; version-history rows reordered. AR-11: whole-surface sweep clean — no findings. |
| 1.48 | 2026-06-07 | — | Agent Movement #2 AR-10..AR-11. AR-10 (1M+2L): SanitiseCollisionForce maps NaN/±Inf → 0 before Clamp01 (Unity's Clamp01 does not filter NaN); currentTime assert tightened to IsFinite; dead MIN_TURN_RATE_DIVISOR constant removed. AR-11 (2L): EvaluateState collisionForce/groundedReason defaults dropped parallel to AR-9 L-2 on CalculateGroundedDwell; OscillationGuard.WriteTime gains Debug.Assert(false) default arm parallel to ReadTime's NegativeInfinity default. |
| 1.49 | 2026-06-07 | — | Performance Optimization #18 AR-3..AR-4. AR-3 (3M+5L) + AR-3 full-surface (3M+3L): RegressionGate.Evaluate now distinguishes skipped-drift (NaN milestone) from degenerate-milestone (+Infinity) in MilestoneDriftFraction; PassesPerPrCheck + PassesAbsoluteDriftCheck fail-closed on non-finite/negative currentMs; RECORD_FORMAT_VERSION_STAGE0_PENDING [FIXED] sentinel constant added (replaces 3 identical placeholders in TraceChannelRegistry). AR-4 (2L): enum ordinal-stability + registry placeholder doc rows. All tracking docs (root CLAUDE.md, this file, PROGRESS.md, file-manifest.md, README.md) refreshed in the same commit. |
| 1.58 | 2026-06-10 | — | Collision System (#3) AR-9 sweep (2L doc-only: Agility unconsumed-field pointer — AgentPhysicalProperties.cs v1.3; AGENT_BALL ContactPoint Z claim — CollisionEvent.cs v1.3) + AR-10 sweep (2L: MaxIterations doc-noted declared-but-unconsumed at Stage 0 — CollisionSystemConstants.cs v1.6; v1.57 history-row AR-8 tally corrected 1H+1M → 1H+1M+1L, the third L being the stray namespace brace in CollisionSystemTests.cs). AR-10 mechanical verification: pair-bitfield triangular index proven unique over all 253 (agent, agent/ball) pairs; 3×3 broad-phase query window proven to always contain an overlapping neighbour (cell 1.0 m ≥ max combined radius); all four Debug emits confirmed FR-CS-031-gated. No functional findings. |
| 1.57 | 2026-06-10 | — | Collision System (#3) AR-7 fix pass (1H+3M+3L) + AR-8 sweep (1H+1M+1L). AR-7 H-1 (ERR-003-001): F = j × 60 Hz → F = j / ContactDurationS [GT] 0.15 s — old conversion put the whole stochastic fall/stumble band below walking pace; PHYSICS_TICK_HZ removed. AR-8 H-1 (ERR-003-005): impulse approach gate un-inverted (vRel=(v1−v2)·n>0 IS approaching for the a1→a2 normal); old gate gave real collisions separation-only (no impulse, no falls) and reversed separating pairs inward; impulse application signs corrected, j>0 invariant preserved. M-1 (ERR-003-002 + ERR-003-006): FROM_BEHIND — behindDot sign corrected, instigator→victim normal flipped at the CollisionSystem call site (also fixes ContactForceData.ForceDirection), and the test hoisted above the velocity-only SHOULDER predicate that shadowed chase-down geometry. M-2 (ERR-003-003): same-team force > fallThreshold no longer escapes both branches (stumble prob Clamp01). M-3 (ERR-003-004): MaxCollisionPairs valve counts narrow-phase confirmed collisions, not broad-phase candidates. L: non-finite-velocity sanitise gate; both-grounded event spam stopped; RecordEvent drop warning; CellX/CellY FloorToInt. Spec §3.3/§3.4 pseudocode patched same commit; spec-error-log.md v1.24. Test geometries flipped to approaching and re-derived; verified by numerical mirror with the xorshift128+ RNG. Files: CollisionResponse.cs v1.6, CollisionSystem.cs v1.6, ContactTypeClassifier.cs v1.3, CollisionSystemConstants.cs v1.5, SpatialHashGrid.cs v1.4, CollisionEvent.cs v1.2, ContactForceData.cs v1.1, AgentAgentCollisionResult.cs v1.2, CollisionSystemTests.cs v1.3. |
| 1.56 | 2026-06-09 | — | Agent Movement (#2) AR-12 fix pass (3H+1M+3L) + AR-13 sweep (2M). H-1: IDLE launch deadlock — IDLE locomotion branch now accelerates toward the command-capped topSpeed when commandSpeed > 0 (agents at rest could never start moving). H-2: `topSpeed = min(topSpeed, commandSpeed)` in Step 4–5 + ApplyAcceleration asymptote ceiling — jog commands no longer auto-promote to SPRINTING; walk commands no longer flap. H-3: ApplyDeceleration MinDecelerationFloor [GT] terminates the Zeno braking tail (was ~78 s / ~32 m from 6 m/s); §3.2.5 constant-rate spec-deviation note added. M-1: LeanAngle from Step 8 velocity-direction rotation (new RotateVelocityToward out-param overload). AR-13 M-1: commandSpeed clamped to JogEnter when AerobicPool < AerobicJogFloor (exhaustion flap); AR-13 M-2: IDLE launch gated on non-degenerate target offset (DT HOLD = StrafeWhileWatching at own position would trip the both-degenerate assert from rest). L: GroundedReason producer note; PreviousState doc; AUTO_ALIGN lag note. New MovementCommand.WalkTo factory. Tests: T-AM-108..109 + closed-loop T-AM-110..115; T-AM-079 re-derived. Files: AgentMovementSystem.cs v1.15, AgentLocomotion.cs v1.5, AgentDirectionalMovement.cs v1.7, AgentMovementConstants.cs v1.9, AgentState.cs v1.5, GroundedReason.cs v1.1, MovementCommand.cs v1.3, AgentMovementTests.cs v2.2, AgentMovementUnitTests.cs v1.1, test-plan.md v0.3; file-manifest.md agent-movement section reconciled (missing asmdef/AssemblyInfo/UnitTests rows + broken table). |
| 1.55 | 2026-06-09 | — | Ball Physics (#1) AR-7 fix pass (2H+4M+3L) + AR-8 (2L). H-1: ApplyBounce normal Vector3.up → (0,0,1) Z-up (ERR-001-001; spec §3.1.8.1 patched). H-2: ValidatePhysicsState preserves negative Airborne Velocity.z at the ground clamp (hover-deadlock fix) + new NoHoverDeadlock regression test. M-1: stick impulse / StickImpulseCouplingDivisor [DERIVED] (ERR-001-002). M-2: spin-sign convention fixed in 4 tests. M-3: gravity in Bouncing branch. M-4: LongBall loop + windows re-derived from model. L: MinContactSpeed + MinForceDirectionSqMagnitude constants (FR-CS-016); MomentOfInertia [EST]→[DERIVED]; HasInvalidValues(in); ApplyBounce precondition doc; ERR-001-003 [EST] inventory (FR-CS-020). Files: BallGroundInteraction.cs v1.3.1, BallPhysicsCore.cs v1.4, BallPhysicsConstants.cs v1.8, BallPhysicsCoreTests.cs v1.5, BallIntegrationTests.cs v1.4. |
| 1.54 | 2026-06-08 | — | Event System (#17) Stage 1 boot-wiring smoke-test landed + tracking-doc sync. New `src/event-system/tests/EventBusWiringSmokeTests.cs` v0.4 — SMOKE-EVT-WIRING-001 drives boot → publish-one-per-spec → DrainTick → SerializeLedger and asserts SHA-256 digest stability across the 6 currently-wired EventBusRegistrar.Initialize() call sites (Pass / Shot / Perception / Decision / Heading / Goalkeeper); Agent Movement (#2) carved out as `[CROSS-PENDING]` slot pending the AM-side EventBusRegistrar (then 7 total). Golden digest pinned via `Assert.Inconclusive` carrying the observed hex so an unfilled pin surfaces in CI as yellow instead of false-positive green. Test publishes one Tier A/B event per spec from the correct producer phase (Physics for Goalkeeper Save/BallClaimed + Heading.HeaderExecuted; Resolve for Pass Attempt/Cancelled + Shot Executed/Cancelled + Goalkeeper Distribution); Tier-C-only specs (Perception, Decision Tree) bypass the ledger via immediate-dispatch CosmeticChannel and are exercised by `Initialize()` alone. Default-constructed events keep the digest a pure function of registration metadata + publish order + canonical byte layout. AR-1 (1H+1M+4L) + AR-2 (0H+1M+4L) + AR-3 (0H+0M+2L cycle-stop) adversarial review cycles complete. `event-system-tests.asmdef` extended with 6 production spec references (Editor-only, no production layering impact). file-manifest.md updated with the new test file row + asmdef-ref annotation update. |
| 1.53 | 2026-06-08 | — | Pass Mechanics (#5) AR-8 fix pass: 0M + 3L on a fresh-eyes full-surface sweep over all 24 files in `src/pass-mechanics/` after the AR-7 changes landed. L-1: AR-7 M-1 wrapped the `CrossSubType`-ignore warning at `PassExecutor.cs:163` in braces to host the `#if UNITY_EDITOR || DEVELOPMENT_BUILD` gate. The other 7 AR-7-gated emits MUST keep the body-gate form because their if-bodies contain functional `_lastResult` + `return` follow-up, but this one is a pure-diagnostic if — in production the if-body became empty (`if (cond) { }`). Gate hoisted to wrap the entire if-statement: `#if … if (cond) Debug.LogWarning(…); #endif`. L-2: `PassExecutor.ExecuteContact` state transition (`_state = PassExecutionState.FollowThrough`) hoisted above Step 8 `EventBusStub.Publish(new PassAttemptEvent { … })`. The previous order was Step 6 `ApplyKick` → Step 7 populate `_lastResult` → Step 8 `Publish` → state transition. If `Publish` throws (queue overflow / registry mismatch), the ball is already kicked (Step 6) and the executor stays in `Contact` — next frame's `Update` re-enters `ExecuteContact` and calls `ApplyKick` a second time. The FM-08 possession recheck at line 358 currently saves the double-kick because the first kick released possession, but defensive ordering removes the dependence on that recovery seam: state is now committed before Publish runs, so a Publish exception leaves the executor in a recoverable `FollowThrough` state. Explanatory comment added at the new transition site naming the AR-8 L-2 invariant. L-3: forward-reference notes inserted next to the AR-2 M-2 history rows in both `PassExecutor.cs` v1.6 row and `PassTargetResolver.cs` v1.3 row. Those rows describe the AR-2-era contract as "[-1, +1]" — accurate at the time but superseded by AR-6 L-1 to "[-1, +1)" (24-bit mantissa quantisation never produces +1.0f). Historical rows preserved verbatim per the project convention of not rewriting history; forward-reference notes give readers chasing the bound through history a pointer to the current contract. Files: PassExecutor.cs v1.11, PassTargetResolver.cs v1.7. Root CLAUDE.md AR-by-spec row updated `#5 AR-7 → #5 AR-8 (3L)`. |
| 1.52 | 2026-06-08 | — | Pass Mechanics (#5) AR-7 fix pass: 1M+3L all resolved on a fresh-eyes full-surface sweep over all 24 files in `src/pass-mechanics/`. M-1: FR-CS-031 gating drift across sibling files. `PassMechanicsConstants` v1.2 (AR-2 L-13) gated its FM-01 `Debug.LogError` emits behind `#if UNITY_EDITOR || DEVELOPMENT_BUILD` per FR-CS-031, but the 12 parallel cold-path emits in three sibling files never got the same treatment — 8 in `PassExecutor.cs` (Idle-guard / FM-01 possession / FM-01 PassType / FM-07 distance / CrossSubType-ignore / FM-11 player-targeted / FM-04 NaN / FM-08 lost possession), 2 in `PassTypeProfiles.cs` (FM-01 default arms in `GetProfile` + `GetCrossProfile`), 2 in `PassVelocityCalculator.cs` (`ComputeSpinVector` + `GetApexHeight` defaults). All 12 now gated. L-1: `[-1, +1]` → `[-1, +1)` propagation from AR-6 producer-side correction in `PassErrorCalculator.ComputeErrorDirection` to its consumer-side surfaces — `PassTargetResolver.ApplyErrorToDirection` `<summary>` and `<param>` for `errorDirectionFraction` (deflection range mirrored to `[-errorAngleDeg, +errorAngleDeg)`), plus the `PassExecutor.ExecuteContact` Step 3 callsite comment. L-2: `EventBusRegistrar.cs` v1.3 (AR-3) row claimed "internal but unreachable (no `InternalsVisibleTo` on this assembly)" as part of the rationale for dropping the `ResetForTesting` seam, but that premise was already factually wrong at AR-3 time — `AssemblyInfo.cs` was created 2026-06-01 with `[InternalsVisibleTo("TacticalDirector.PassMechanics.Tests")]`. The seam-drop decision still stands on the boundary-mocking sub-rationale ("tests should mock at the EventRegistry boundary, not poke this guard"); the stale reachability claim corrected without code change. L-3: `CrossSubType` and `PassType` enums gained the ORDINAL STABILITY paragraph parallel to `CancelReason` v1.4. `CrossSubType` is embedded in `PassAttemptEvent` (Tier A 0x0C) payload; `PassType` is embedded in BOTH `PassAttemptEvent` and `PassCancelledEvent` (Tier A 0x0D) payloads AND is the third hash input to `PassErrorCalculator.ComputeErrorDirection` (`(int)_request.PassType` in `PassExecutor.ExecuteContact`) — reordering would change every error-direction draw for the same logical pass and break replay parity even before the event digest catches the drift. PassType.cs doc explicitly calls out the stronger two-surface contract. Files: PassExecutor.cs v1.10, PassTypeProfiles.cs v1.4, PassVelocityCalculator.cs v1.4, PassTargetResolver.cs v1.6, EventBusRegistrar.cs v1.4, CrossSubType.cs v1.1, PassType.cs v1.2. Root CLAUDE.md AR-by-spec row updated `#5 AR-6 → #5 AR-7 (1M+3L, full-surface sweep)`. |
| 1.51 | 2026-06-08 | — | Pass Mechanics (#5) AR-6 fix pass: 1M+3L all resolved, converts the AR-5 cycle-stop. M-1: input-mix primes for `frameNumber` / `passTypeIndex` in `PassErrorCalculator.ComputeErrorDirection` replaced with xxHash64 PRIME64_3 (`0x165667B19E3779F9`) and PRIME64_5 (`0x27D4EB2F165667C5`). AR-5 M-1 fixed agentId by spreading it through `0xC2B2AE3D27D4EB4F` (xxHash64 PRIME64_2) — explicitly NOT a Mix13 finalizer constant — but left the other two axes multiplying through `0xBF58476D1CE4E5B9` and `0x94D049BB133111EB`, the very Stafford Mix13 multipliers the finalizer then re-applies on lines 5 and 7 of the mixer. For adjacent (frame, passIdx) tuples at a fixed agentId, the prime reuse let the finalizer partially undo input-mix structure it was designed against uniform input. AR-6 closes the invariant on all three axes — input-mix primes are now disjoint from finalizer primes. L-1: `<returns>` and `<summary>` upper bound corrected `[-1, +1]` → `[-1, +1)`; the implementation produces `top24 < 2^24` so `normalised < 1.0f`, and EC-010 already enforces `Assert.Less(dir, 1.0f)`. L-2: inline comment block rewritten — stale "Each input is multiplied by a DISTINCT prime before XOR-folding" wording dropped (agentId is folded ADDITIVELY into the seed, not XOR-folded; the asymmetry is intentional AR-5 M-1), and the AR-6 input-mix / finalizer disjointness invariant recorded next to the prime annotations. L-3: bit-extraction literals `0x00FFFFFFu` and `0x01000000u` promoted to named local consts `Mantissa24Mask` and `Mantissa24Scale` with a comment noting the 24-bit window matches float mantissa precision (the divisor is exactly representable so the [0, 1) → [-1, +1) mapping introduces no rounding bias). Files: PassErrorCalculator.cs v1.7. Root CLAUDE.md AR-by-spec table row updated from `#5 AR-5 (1M+3L cycle-stop) ⚠` to `#5 AR-6 (1M+3L, converts AR-5 cycle-stop) ✓`. |
| 1.50 | 2026-06-08 | — | Cross-spec routing close-out: `Possession.ControlHeight` ↔ `GroundControlHeight` resolved (closes the June 3, 2026 root-CLAUDE.md OPEN ISSUE). Ball Physics #1 §3.1.11 `BallPhysicsConstants.Possession.ControlHeight` declared the AUTHORITY for ground-control height; First Touch #4 `FirstTouchConstants.GroundControlHeight` relocated from `#region GT` to `#region Cross`, retagged `[CROSS]`, RHS now `BallPhysicsConstants.Possession.ControlHeight` (single-consumer mirror per Spec #20 §4.2). Rationale: ControlHeight is physical possession geometry, not a First-Touch-specific knob; lives next to the three sibling thresholds (ControlRadius / ControlVelocity / ChallengeRadius). Ball Physics XML drift warning on Possession.ControlHeight replaced with an AUTHORITY / consumer pointer naming the mirror. Sibling-hazard sweep cleared — grep across `docs/specs/` confirms no other spec re-declares `ControlRadius` / `ControlVelocity` / `ChallengeRadius`; those remain single-spec `[GT]` in Ball Physics #1 §3.1.11 with no parallel decls. Heading Mechanics #10 forward note: when #10's eligibility gate begins consuming the value at Stage 0+1, mirror from `BallPhysicsConstants.Possession.ControlHeight` (do NOT re-declare locally). Files: FirstTouchConstants.cs v1.7, BallPhysicsConstants.cs v1.7, docs/specs/first-touch/section-4-4-to-4-6.md §3.4.3 constant block, root CLAUDE.md OPEN ISSUES entry flipped OPEN → RESOLVED. |
